# NEEDED — owner actions

The repository implementation is complete for the current web brief. This file lists only work that requires your accounts, credentials, legal decisions, production data, or external services. Importance uses `[imp:1–5]`; `[imp:5]` blocks a safe public launch.

Production database migrations through `supabase/supabase-schema-031.sql` were applied and verified — 001–024 on 2026-07-28, and 026–031 on 2026-09-09. The two production Vercel projects, product scopes, canonical cross-links, Supabase credentials, and session secrets are configured; both `/api/health` probes report `database: ok`, `serviceRole: ok`, and `rateLimiter: configured` (Upstash Redis distributed rate limiting is live). Sentry error monitoring and PostHog analytics (EU cloud, reverse-proxied) are configured with privacy-forward defaults; admin ACL (`ADMIN_EMAILS`, `OWNER_EMAIL`) is set on both projects; Supabase leaked-password protection is on.

## Before production launch

- [ ] **Run the signed-in production smoke test** in English/Czech and desktop/mobile: Learn completion, Quiz replay rejection, Daily/Challenge idempotency, Flashcards, two-session Play/Classroom, leaderboards, `/dev`, deletion, and the daily-habit additions — the Today queue and its once-per-day Shark Card pack grant, a level going cleared→mastered over separate days, a streak freeze bridging a missed school day, the read-only advisor, adaptive placement (including the "I don't know yet" option), and the devShark typing racer. `[imp:4]` `[owner:me]` `[time:1h]` `[kind:deploy]`
- [ ] **Check the two Profile/Learn changes of 2026-09-04 signed in.** Neither can be rendered in the build container: the responsive sweep only ever sees the signed-out `/profile` redirect, and no Supabase project is reachable to play a Learn level. (1) The identity banner now carries the language, appearance and sound switches — confirm the row wraps cleanly at 390 px, that each button reads its action to a screen reader, and that all three still persist (language to the account, the other two to the device). (2) Spend all three hearts in a Learn level before its last question and confirm the lesson ends on the out-of-hearts screen instead of "Answer every question before completing this lesson". `[imp:4]` `[owner:me]` `[time:20m]` `[kind:deploy]`

## Learning paths (FDE and DSA Foundations)

The two devShark learning paths are implemented and validated in the repository. Three owner steps stand between that and learners seeing them, and they are deliberately separate so a half-finished one cannot open a path by accident.

- [x] **Apply `supabase/supabase-schema-026.sql`** — applied 2026-09-09 and verified: the five `learning_path_*` tables exist with row-level security on, `learning_path_attempts` has no policy and no grant for `authenticated` (it holds grading material, and owning a row is not a reason to read it), and the five new functions are service-role only. `[imp:4]` `[owner:me]` `[time:30m]` `[kind:deploy]`
- [ ] **Enable the paths on the devShark project only, one at a time.** Set `LEARNING_PATH_DSA_ENABLED=true` first and watch it for a few days; add `LEARNING_PATH_FDE_ENABLED=true` when you are ready for the longer path. The switches are independent on purpose. StudyShark must never get either variable — learning paths are devShark-only, and the API refuses them outside the `webdev` scope regardless. `[imp:3]` `[owner:me]` `[time:20m]` `[kind:deploy]`
- [ ] **Check the learning-path workspace signed in.** The responsive sweep ran clean over the new routes — 133 probes across 360–1440 px, light and dark, English and Czech, zero overflow or containment issues — but it can only see the signed-out state, so what it actually rendered was the guest preview, not the graded workspace. Confirm signed in that the two-column brief-beside-editor layout collapses cleanly at 390 px with the controls reachable without covering the editor, that the trace player steps with the keyboard and reads its step aloud, and that verified-pass and self-reviewed stay visibly distinct without relying on colour. `[imp:3]` `[owner:me]` `[time:30m]` `[kind:deploy]`
- [ ] **Run the signed-in learning-path smoke test** on preview before enabling production: read the outline signed out, sign in, start a path, submit a wrong answer and a right one, close the tab mid-draft and reopen it, and send the same idempotency key twice to confirm the second call replays the stored result instead of grading again. Then pause the path and confirm the evidence survives and it leaves the Today queue. `[imp:4]` `[owner:me]` `[time:45m]` `[kind:deploy]`
- [ ] **Pilot with 6–10 practising engineers across the three base tracks**, as the curriculum plan proposes. Record the sample size and the limitations alongside anything you conclude: with a cohort this small the numbers describe those people, not the population, and the thresholds (80% on checks, 80% of weighted non-critical criteria) are pilot assumptions to calibrate, not findings. Do not report a hiring or salary outcome — the path does not measure one. `[imp:2]` `[owner:me]` `[time:4h]` `[kind:content]`
- [ ] **Re-review the cited sources each quarter**, and immediately if a learner reports a broken API example or a grading defect. Every lesson carries the date its sources were last checked; the `/dev` → Learning paths screen lists them, and the content validator warns once a path's review date passes 400 days. `[imp:2]` `[owner:me]` `[time:1h]` `[kind:content]`

## Modernization epic (2026-09-08)

The epic in [#150](https://github.com/lukaskourilcz/react-express-app/issues/150) is implemented: the learner profile and server progression graph, the Edabit-inspired practice work, the merchandise wallet, the curriculum retirements and rescopes, the curation methodology, and the learning-science scheduling. `docs/release-acceptance.md` records every gate with its actual result and, just as importantly, every check that could not run here. These are the ones that need you.

- [x] **Apply migrations 027 to 031 in order** — applied 2026-09-09 and verified. 027 adds the coding bookmarks, collections, skips, puzzle results and practice sessions; 028 the token ledger, balances, merchandise stock, orders and cosmetic entitlements; 029 one nullable column on `question_reports`; 030 the concept review table and its recording routine; 031 the policy and index work below. All eighteen new tables have row-level security on, `merch_stock` and `learning_path_attempts` have no policy at all (service-role only), and every new function is service-role only. `[imp:4]` `[owner:me]` `[time:45m]` `[kind:deploy]`

  Two things were fixed while applying them, and both are now in the migration files so a fresh database comes up the same way. 027 and 028 granted `SELECT` without revoking Supabase's default grants first, which left `INSERT`, `UPDATE`, `DELETE` and `TRUNCATE` on twelve tables — the policies masked the first three, but `TRUNCATE` touches no rows and so passes no policy. And eighteen owner-read policies from earlier migrations evaluated `auth.uid()` once per row rather than once per query.
- [ ] **Leave the shop unconfigured until you decide to sell.** It ships off: no price is set, an incomplete quote is dropped on read, `testMode` defaults on, and payment only ever applies through a signed webhook. `docs/rewards-launch.md` lists what a real launch needs — a supplier, real prices, the payment provider, and the consumer-law wording that a physical sale to EU buyers requires. Free learning does not wait on any of it. `[imp:2]` `[owner:me]` `[time:2h]` `[kind:legal]`
- [ ] **Smoke-test spaced review and interleaving signed in**, on a preview deployment with migration 030 applied: answer a level with a hint open and one without, confirm the hinted concept does not climb the ladder, wait out the shortest interval and confirm the concept appears in Today's "Due for review" card with a sensible size and time. No Supabase project is reachable from the build container, so the policy is unit-tested and the persistence is not exercised. `[imp:3]` `[owner:me]` `[time:40m]` `[kind:deploy]`
- [ ] **Check the new surfaces signed in.** The responsive sweep renders signed out, so what it saw of the review card, the "Why this question?" note and the lesson figures was the guest state. Confirm at 390 px that the figure controls (Previous, Next, Reset) stay reachable and that the "This figure in words" text wraps without pushing the page sideways; that the "Why this question?" panel opens without covering the answer options; and that both read sensibly in Czech. `[imp:3]` `[owner:me]` `[time:30m]` `[kind:deploy]`
- [ ] **Decide whether to run the content audit ([#176](https://github.com/lukaskourilcz/react-express-app/issues/176)).** Until it runs, the product says so: the review registry is empty, `/curation` states that the item-by-item review has not started, and no item claims to have been reviewed. That is honest but it is not the same as having audited the bank. `docs/curation-claims.md` describes the contract the audit would write into. `[imp:3]` `[owner:me]` `[time:1h]` `[kind:decision]`

  One number to decide with: `npm run audit:devshark-content -- --details` flags **158 of the 2,423 devShark questions** (6.5%) where the correct answer is markedly longer and more detailed than its distractors — most in General (36), Security (20), Databases (18), DevOps (18) and Git (17). That is the oldest tell in multiple choice: a learner who knows nothing can pick the long one and be right more often than chance. It is a rewriting job, not a bug, and it is the audit's to do.

- [ ] **Check the touch targets and the pre-paint theme on a real phone.** Both were measured in a headless browser and both need a real device to confirm. (1) Every interactive control now clears 44×44 on a coarse pointer — measured across twenty-four routes at 390 px in English and Czech, zero below the floor. Confirm on a phone that the header menu button, the segmented controls and the footer links feel right rather than merely measuring right, and that nothing became cramped. (2) With dark mode chosen and Czech stored, a cold load should paint dark and set `lang="cs"` before anything renders — no white flash, and VoiceOver or TalkBack should read Czech in a Czech voice. `[imp:3]` `[owner:me]` `[time:20m]` `[kind:deploy]`

- [ ] **Apply migrations 032 to 035.** None of the four is in production: this session lost its Supabase connection partway through, so all four were applied and exercised against a local Postgres 16 instead — the whole 001–035 chain from an empty database, then re-applied to prove idempotency. 032 is the streak shield and the end of off-days (see the weekend decision below); 033 is friends; 034 makes answering a Learn question one database round trip instead of three; 035 is the bought protection and the earned merch package. Every path degrades until its migration lands — the profile shows no shield, the friends tab says it is unavailable, and the answer handler falls back to the two-step path — so nothing breaks by waiting; the features are simply dark. `HANDOFF.md` has the detail. `[imp:4]` `[owner:me]` `[time:40m]` `[kind:deploy]`

- [x] **Decided 2026-09-09.** A streak protection costs 250 tokens — roughly a fortnight of steady learning, against the crown's 1200, because it is consumable and capped at two. The legacy rings and flairs were already unsellable; they stay ownable and equipped, and the shop now lists the crown, streak protection and merchandise. Adjust the price with `streakProtectionTokenPrice` in game settings. `[imp:3]` `[owner:me]` `[time:15m]` `[kind:decision]`

- [x] ~~**Decide the two things `HANDOFF.md` cannot decide.**~~ (1) The token price of a streak protection — the crown is 1200 tokens and tokens accrue at 10% of verified XP, so a protection wants to be far cheaper than that, being consumable and capped at two. (2) Whether the shop's three rings and three flairs stop being sold, stop being *ownable*, or both — they must keep rendering for the learners who already own them. `[imp:3]` `[owner:me]` `[time:15m]` `[kind:decision]`

- [ ] **Apply `supabase/supabase-schema-032.sql`** — the streak shield, and the end of off-days. Additive and idempotent. It was applied and exercised against a local Postgres 16 (the full 001–032 chain from scratch, then re-run for idempotency), so the SQL itself is known to run and the behaviour is known to be right: a first click spends one protection and opens a 48-hour window, a second returns the same window and charges nothing, a shielded gap keeps a streak where the same gap unshielded resets it, and the retroactive safety net still covers a learner who never raised a shield. It has **not** been applied to production — this session lost its Supabase connection partway through. Until you apply it the profile simply shows no shield control, because the API reports whether the column exists and the client refuses to offer what would fail. `[imp:4]` `[owner:me]` `[time:20m]` `[kind:deploy]`

- [x] **Weekends are not free — decided 2026-09-09.** Every calendar day counts against a streak, and the two monthly protections are the whole story. There are no live accounts yet, so nothing is lost applying migration 032. `[imp:4]` `[owner:me]` `[time:5m]` `[kind:decision]`

- [x] ~~**Decide whether weekends should still be free.**~~ Dropping "days off" as asked removed the rule that a learner's configured off-days (weekends by default) never counted against a streak. Every calendar day counts now, and the two monthly protections are the whole story — which is the point, since two rules for the same thing meant nobody could tell which one had saved their streak. But it is a real change for existing learners: someone who practises on weekdays and has a long streak built under the old rule can lose it the first weekend after this deploys. The alternative is to keep Saturday and Sunday free as a fixed constant with no setting, which is one line in `record_verified_quiz_result_v2`. Say which you want before 032 goes to production. `[imp:4]` `[owner:me]` `[time:5m]` `[kind:decision]`

## Production reliability

- [ ] **Monitor `/api/health` externally** and alert on non-2xx. The probe now verifies public DB access, service-role access to migration 023, and reports whether distributed rate limiting is configured. `[imp:4]` `[owner:me]` `[time:1h]` `[kind:deploy]`
- [ ] **Configure verified backups and a restore drill** using `docs/backup-restore.md`; record the recovery point/time objectives you accept. `[imp:4]` `[owner:me]` `[time:1h]` `[kind:deploy]`
- [ ] **Confirm Sentry + PostHog capture no PII in production.** Both are configured with EU regions, `sendDefaultPii: false` (Sentry), and `respect_dnt` + `identified_only` + reverse-proxied `/ingest` (PostHog). Reproduce a client error and a routine pageview signed in, then inspect the payloads in each dashboard — no submitted answer, answer proof, token, or email should be present. `[imp:3]` `[owner:me]` `[time:1h]` `[kind:legal]`

## Brand and launch assets

- [ ] **Attach your preferred custom StudyShark domain, if wanted.** StudyShark is live at `https://studyshark-app.vercel.app`; devShark remains at `https://devshark.app`. After adding a custom domain, update `VITE_STUDYSHARK_URL` on both projects plus OAuth origins/callbacks, then redeploy both. `[imp:3]` `[owner:me]` `[time:1h]` `[kind:deploy]`
- [ ] **Replace placeholder icons and social artwork** (`client/public/icon.svg`, Apple/PWA icons, and `og-image.png`) with final licensed Shark-family assets and verify metadata on both domains. `[imp:3]` `[owner:me]` `[time:2h]` `[kind:legal]`
- [ ] **Investigate the desktop CLS of ~0.96 on both products.** Lighthouse baseline (2026-07-28, `docs/perf/lighthouse-baseline-2026-07-28/`) scored a 45/33 desktop Performance because Cumulative Layout Shift on the landing route is far outside the ≤ 0.1 target. Mobile CLS is 0. The likely culprit is a hero/animation element sized only after its resource loads at desktop widths. Not launch-blocking (mobile is fine), but the desktop score will hurt PageSpeed once you promote the domain publicly. `[imp:3]` `[owner:me]` `[time:2h]` `[kind:deploy]`
- [ ] **Improve mobile LCP (~5.2–5.7 s) on the landing route** — same baseline. The largest paint arrives well after the "poor" threshold (4 s). Common wins: preload the hero image, defer the non-critical scripts, and shrink the above-the-fold bundle. `[imp:3]` `[owner:me]` `[time:2h]` `[kind:deploy]`

## Portfolio thumbnail (2026-07-27)

The devShark thumbnail on lukaskouril.dev was re-recorded from the redesigned
landing page. It had to be captured from a locally-run client, because the
deployed site was unreachable from the session that produced it.

- [ ] **Compare the committed preview against the live site** — open `media/preview-poster.png` next to devshark.app and confirm the local build matched production. devShark is a locked deployment of the shared client, so a capture taken without `VITE_LOCK_SUBJECT=webdev` silently renders StudyShark's landing page under devShark branding; the procedure and the check are in `.claude/skills/preview-video/SKILL.md`. `[imp:3]` `[owner:me]` `[time:20m]` `[kind:content]`

## Optional features

- [ ] **Voluntary support:** set `SUPPORT_ENABLED=true` only after legal/accounting review, then enter truthful provider URLs, target, amount covered, cost breakdown, update date, and public-thanks policy through `/dev`. Support remains unrelated to access or ranking. `[imp:3]` `[owner:me]` `[time:2h]` `[kind:legal]`

AI-driven features (post-answer explanations, Sharkira Socratic hints) are intentionally not listed. The client wiring stays in the repo so they can be enabled later, but no paid model API is provisioned now.

## Repository housekeeping

- [ ] **Decide what happens to the badge feature.** The Profile badge section was removed on 2026-09-04 at your request, so nothing in the UI reads `/api/user/badges` any more. The unused `client/src/lib/badges.ts` wrapper was removed in the 2026-09-07 production cleanup. The `sync_user_badges` RPC and `user_badges` table remain; `shared/badges.ts` stays either way, because the devShark coding badges use it. Say whether badges come back somewhere else or the dormant endpoint should go. `[imp:2]` `[owner:me]` `[time:10m]` `[kind:decision]`
- [x] **Resolve the `fflate` advisory** — production audit 2026-09-07 updated the client lockfile (`posthog-js` and compatible `fflate`). Both production dependency audits report zero vulnerabilities; client TypeScript and both Vite builds pass. [#126](https://github.com/lukaskourilcz/react-express-app/issues/126).
- [ ] **Delete four merged branches on GitHub:** `claude/remove-forms-unlock-system-design-br6l74` (merged as `2333bd0`), `claude/profile-settings-restructure-zk2yi9` (fast-forwarded into `main` as `c386fcd` on 2026-09-04), `claude/dsa-fde-learning-paths-x9vo8q` (merged as `4d88dcb` on 2026-09-08) and `claude/resolve-open-issues-xnr0lv` (fast-forwarded into `main` as `ccee58b` on 2026-09-08). Every local copy is gone, but the git proxy in these sessions rejects delete-ref pushes with a 403 and the GitHub API surface here has no branch-delete call, so the remote branches are still listed. Four clicks in the GitHub branches view. `[imp:1]` `[owner:me]` `[time:5m]` `[kind:setup]`
- [ ] **Decide what to do with the documentation branch `docs/fde-learning-path-plan-2026-09-08` and its PR #149.** Its five planning documents are now on `main` (they were cherry-picked into the implementation branch so the specification and the code landed together), so the PR has nothing left to merge. Close it and delete the branch — but note that issues #128–#148 link to the plan through `blob/docs/fde-learning-path-plan-2026-09-08/...` URLs, which break when the branch goes. The same files are readable at `blob/main/docs/...`. `[imp:2]` `[owner:me]` `[time:10m]` `[kind:setup]`

## Coding integration (2026-09-03)

The plan for folding interview-prepper into devShark is in `docs/coding-integration-plan.md` (section 13 records the implementation status); the implementation issues live on GitHub (epic #105 in this repository, three issues in `interview-prepper`). Every issue on the epic is implemented and closed on `claude/interview-prepper-integration-ogh4ph`, including server-side React grading, so all four tracks record verified verdicts. Decisions recorded on 2026-09-03: the system-design track follows, the mock mode and the AI coach are retired, devShark carries no AI feature, React tasks behave like the other tracks, and the import reads Firestore only. These items need you.

- [x] **Apply `supabase/supabase-schema-025.sql` in production** — done on 2026-09-03. Confirm `/api/health` still reports `database: ok` and `serviceRole: ok` after the next deploy. `[imp:4]` `[owner:me]` `[time:5m]` `[kind:deploy]`
- [ ] **Export your interview-prepper Firestore data and run the one-time import** locally: `npm run import:interview-prepper -- --input export.json --user-id <your Supabase user id>` (dry run), then add `--apply` with `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` set. Export the `progress/{uid}` document and its `attempts` subcollection from the Firebase console (or `firebase firestore:export`) into one JSON file; no browser storage export is needed. Imported passes carry no XP and no scheduled review. `[imp:2]` `[owner:me]` `[time:30m]` `[kind:setup]`
- [ ] **Retire interview-prepper after the import is verified:** archive the repository, delete its Vercel project and Firebase project, and revoke its OpenAI key. `[imp:2]` `[owner:me]` `[time:30m]` `[kind:deploy]`
- [ ] **Merge the implementation branch `claude/interview-prepper-integration-ogh4ph`** in this repository once you have reviewed it, then delete the branch. `[imp:3]` `[owner:me]` `[time:20m]` `[kind:setup]`
- [ ] **Register the devShark GitHub App for the garden feature.** Name it for learners (for example "devShark garden"); permissions: Repository contents read and write (metadata read is implied); installable by any user; no webhook needed; Setup URL `https://devshark.app/settings/github` with "Redirect on update" on; "Request user authorization (OAuth) during installation" off. Generate a private key and set `GITHUB_APP_ID`, `GITHUB_APP_SLUG`, and `GITHUB_APP_PRIVATE_KEY` (PEM, base64) on the devShark Vercel project. The feature stays hidden until these exist. `[imp:3]` `[owner:me]` `[time:30m]` `[kind:setup]`
- [x] **Update the privacy notice for the GitHub garden** — done on 2026-09-03 in `client/src/components/PublicInfoPages.tsx` (EN and CS): what is stored, that passed solutions are committed to the learner's own repository under their GitHub identity, and how to disconnect and uninstall. Review the wording with the rest of the legal text before launch. `[imp:3]` `[owner:me]` `[time:10m]` `[kind:legal]`
- [x] **Implement the coding integration issues in the order the epic lists** — #106 to #117 done on 2026-09-03; see the epic for the follow-up issues. `[imp:3]` `[owner:ai]` `[time:12h]` `[kind:content]`
- [ ] **Smoke-test the coding section signed in on a preview deployment:** pass one JavaScript, one TypeScript, one React, and one system-design task, finish a JavaScript Learn level with its coding phase, and open the review queue after four hours. No Supabase project is reachable from the build container, so these flows are contract-tested and type-checked but never driven with a real account. `[imp:4]` `[owner:me]` `[time:45m]` `[kind:deploy]`
- [x] **Bump the two client production dependencies the audit flagged** — done on 2026-09-03; both audits are clean. `[imp:3]` `[owner:ai]` `[time:30m]` `[kind:deploy]`

## When usage grows

- [ ] Review [scaling.md](./scaling.md) monthly once traffic is meaningful. Upgrade Supabase compute before sustained saturation, add retention/partitioning for event tables, and redesign multiplayer fan-out before large classrooms or high concurrent-room counts. `[imp:1]` `[owner:me]` `[time:20m]` `[kind:setup]`

Native iOS/Android work remains intentionally deferred; it is not a missing item for this web release.

## Recorded decisions (2026-07-28)

- **`claude/favicon-sharkfin-q3z4ot` deleted** — Expo-only branch; mobile app was shut down. PR #91 closed, remote branch removed.
- **`react-router-dom` 7.18.1 kept** — advisories GHSA-qwww-vcr4-c8h2 and RSC-mode CSRF bypass are RSC-mode only. This app is a Vite SPA with no server components (no `renderToPipeableStream`, `react-server-dom`, `createStaticHandler`, or `@react-router/server` usage). Advisories do not apply; no downgrade needed.
- **Upstash Redis provisioned** — `calm-spider-187203.upstash.io` (EU). `UPSTASH_REDIS_REST_URL` + `UPSTASH_REDIS_REST_TOKEN` set on both projects × Production + Preview. Distributed rate limiting active on next deploy.
- **Admin ACL wired** — `ADMIN_EMAILS=kouril.lukas@gmail.com` and `OWNER_EMAIL=kouril.lukas@gmail.com` set on both projects × Production + Preview. Owner still needs to log in and verify `/dev` gates (moved to the launch-prerequisites list).
- **Sentry live** — DSN set on both projects × Production + Preview. EU region (Germany). SDK is DSN-gated + lazy-loaded, `sendDefaultPii: false`, 10% trace sampling, no Session Replay.
- **PostHog live** — `phc_…` project key set on both projects × Production + Preview. EU Cloud, reverse-proxied through the same-origin `/ingest` path (`vercel.json` rewrites), `respect_dnt: true`, `person_profiles: 'identified_only'`, no autocapture of sensitive fields.
- **Supabase leaked-password protection enabled** — Auth advisor no longer flags the WARN.
- **`studyshark-app` Preview-env parity fixed** — added the 4 shared credentials (`SESSION_SECRET`, `SUPABASE_SERVICE_ROLE_KEY`, `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`) that were previously Production-only, so preview deploys of StudyShark now work.
- **Sharkira hint UI hidden and AI features shelved** — `false &&` gate in `client/src/components/Quiz.tsx` `sharkiraEligible` hides the entry point; no paid model API key is configured. Both AI explanations and Sharkira hints are intentionally not on the open todo list. Backend wiring, translations, request path, and `question_hints` cache table stay in place so they can be reactivated later without new plumbing.
- **`.claude/settings.local.json` untracked and gitignored** — per-machine Bash-permission allowlist stays local; the tracked copy was removed from history-going-forward.
- **35 stale `claude/*` branches cleaned up** — 24 fully-merged branches deleted directly; 11 with unique commits reviewed as draft PRs #92–#102, all closed by the owner as superseded and their branches deleted. Zero `claude/*` branches remain on the remote.
- **Repo audited for public visibility** — no committed secrets in HEAD or history (no service-role key, session secret, Upstash token, OpenAI key, or Sentry auth token has ever been in the repo). Personal email and Supabase project ref are intentionally left in tracked files: the email is already the public git-author, and the project ref is already exposed in the deployed JS bundle by design.
- **Migration 024 applied** — `schema_024_daily_habit_freeze_badges_cards` recorded at `20260728211046`. Six new tables live (`user_streak_config`, `user_streak_freezes`, `user_badges`, `user_cards`, `daily_queue_completions`, `question_hints`), the freeze-aware `record_verified_quiz_result_v2` and spaced-mastery `complete_verified_roadmap_attempt` bodies replaced, `delete_user_data` extended to cover the new tables. Both `/api/health` return HTTP 200 with `rateLimiter: configured`.
- **Supabase Auth redirect allowlist fixed** — Site URL set to `https://devshark.app`; Redirect URLs list includes both product domains and `http://localhost:5173/**`. Signing in on either product now lands the user back on the same domain (the client's `redirectTo: window.location.origin` is respected).
- **Google OAuth enabled** — provider is configured in Supabase Authentication → Providers and sign-in works end-to-end. The Google consent screen currently reads "to continue to rvlybcjdpafwyeuojvhl.supabase.co" because Google shows the domain of the redirect_uri, which is Supabase's callback host. Changing that string to `devshark.app` needs a Supabase Custom Domain (paid tier), documented separately below.
- **Admin ACL verification dropped from the tracked list** — env vars are wired (`ADMIN_EMAILS=kouril.lukas@gmail.com`, `OWNER_EMAIL=kouril.lukas@gmail.com`); owner will confirm end-to-end during normal use rather than as a launch gate.
- **Legal / privacy review dropped from the tracked list** — owner chose not to gate launch on it. Left as an implicit obligation: the terms/privacy page must eventually name Supabase, Vercel, Upstash, Sentry (EU), and PostHog (EU) with their retention, and reflect any consent mode chosen. Not enforced by this file going forward.
- **Daily retention purge scheduled** — `pg_cron` extension enabled; job `purge_expired_learning_data_daily` runs `SELECT public.purge_expired_learning_data();` at `07 03 * * *` UTC. Uses the function's default retention (delete `roadmap_attempts` with `expires_at` older than 90 days and `quiz_submissions` older than 90 days). Reschedule or change retention via `cron.alter_job` / `cron.unschedule` when the policy changes.
- **Lighthouse baseline captured** — `docs/perf/lighthouse-baseline-2026-07-28/` holds HTML + JSON for both products at mobile + desktop preset plus a `README.md` summary. Category scores: A11y 96, Best Practices 96, SEO 91 across the board. Performance: 66–68 mobile, 33–45 desktop. The two follow-up items above (desktop CLS ≈ 0.96, mobile LCP > 5 s) are the concrete regressions found and are now tracked in the launch-assets list.

## Developer tooling

- [ ] **Install and initialize RTK (`rtk-ai/rtk`)** — RTK could not be set up from the Claude Code web session because its GitHub download host is outside the session's network allowlist (`github.com/rtk-ai/rtk` and its release binaries return HTTP 403). Set it up locally at home with the commands below, then enable it for this repository following `rtk --help` / the RTK docs (the exact per-repo command isn't documented here because the tool wouldn't install in the sandbox). `[imp:2]` `[owner:me]` `[time:20m]` `[kind:setup]`
- [ ] **Enable Vercel Web Analytics for this project** — turn on Web Analytics in the Vercel project so OwnDashboard's project Overview shows visitors and page views (it reads them via the Vercel API, matched by this repository). `[imp:2]` `[owner:me]` `[time:15m]` `[kind:setup]`

```sh
curl -fsSL https://raw.githubusercontent.com/rtk-ai/rtk/refs/heads/master/install.sh | sh
rtk init --global
```
