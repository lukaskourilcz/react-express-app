# NEEDED — owner actions

The repository implementation is complete for the current web brief. This file lists only work that requires your accounts, credentials, legal decisions, production data, or external services. Importance uses `[imp:1–5]`; `[imp:5]` blocks a safe public launch.

Production database migrations through `supabase/supabase-schema-038.sql` are applied and verified: 001–024 on 2026-07-28, 025 on 2026-09-03, 026–036 on 2026-09-09, 037 on 2026-09-18 and 038 on 2026-09-21. 039 to 044 are not applied yet; the items below carry them. The devShark Vercel project, its product scope, Supabase credentials, and session secret are configured; `/api/health` reports `database: ok`, `serviceRole: ok`, and `rateLimiter: configured` (Upstash Redis distributed rate limiting is live). Sentry error monitoring and PostHog analytics (EU cloud, reverse-proxied) are configured with privacy-forward defaults; admin ACL (`ADMIN_EMAILS`, `OWNER_EMAIL`) is set; Supabase leaked-password protection is on. StudyShark, which this repository also built until 2026-09-24, moved to `lukaskourilcz/studyshark` and is not deployed; its Vercel project still has to go (#214).

## Second handoff, 25 September 2026: the freemium launch

`SECOND-HANDOFF-25-9-2026.md` records the decision that devShark becomes freemium (free tier, Premium at 3.99 a month, merchandise through Spreadshop, coins, a 30-day leaderboard, relabelled and doubled coding challenges) and the design behind it. Its section 11 lists what only you can do; the implementation issues are #219 to #230 (label `second-handoff-25-9-2026`). The items that block the paid launch:

- [ ] **Create the Stripe account, enable Managed Payments, create the Product and two Prices with `tax_behavior: inclusive`, configure the Customer Portal and the webhook, set the environment variables** — handoff section 3; the exact steps are in the D2 block below. [imp:5] [owner:me] [time:1h] [kind:setup]
- [ ] **Have the Terms, the checkout consent text and the order-button wording checked by a lawyer** (§ 1826a OZ, § 1837 l) OZ, § 312k BGB). [imp:4] [owner:me] [time:1h] [kind:legal]
- [ ] **Register the EU Spreadshop, upload designs, enable a t-shirt, a hoodie, a mug and two sticker sizes, set margins, order one sample, export product mockups** — handoff section 8. [imp:4] [owner:me] [time:3h] [kind:setup]
- [ ] **Decide the currency, the voluntary 14-day refund and the social click-through grant** — handoff sections 3 and 7. The Support page took the default in #222: `/support` redirects to `/premium`; say so if you would rather keep one paragraph there. [imp:3] [owner:me] [time:30m] [kind:decision]
- [ ] **Create the devShark LinkedIn Page, Instagram professional account and Threads profile and record the URLs in `client/product-catalog.ts`** — the BoardlessAI handoff has the checklist. [imp:4] [owner:me] [time:1h] [kind:setup]

<!-- D1 (#220): tiers. Added by the D1 lane; keep this block together. -->
- [ ] **Apply `supabase/supabase-schema-039.sql`** — the entitlement tables and routines for the free tier and Premium (#220). Proven on a local Postgres 16 from the 001–038 chain: applied twice, then a rolled-back exercise of grants, revokes, the seven-day grace window, a sticky revoke, duplicate events, owner-only reads and refused writes. In production, run the same exercise inside a transaction that rolls back, then apply it through the Supabase connector and check the security advisor. Until it is applied every signed-in account reads as free. [imp:5] [owner:me] [time:20m] [kind:deploy]
- [ ] **Know that D1 locks Premium content as soon as it deploys** — once #220 reaches `main`, every signed-in account without a grant is refused TypeScript and the other Premium topics, React from level 13, most coding challenges and every stage after the first with the upgrade sheet, before billing (#221) can sell Premium. Keep anyone who should not lose access open with a manual grant: `POST /api/admin/entitlements` with `{ "action": "grant", "userId": "…", "validUntil": null, "note": "who and why" }` from an admin session, or merge D1 and D2 together. [imp:5] [owner:me] [time:10m] [kind:decision]
- [ ] **Check the Premium locks signed in on production** — the locks, the upgrade sheet and the plan line were checked in a browser against the real handlers and a local database, not against production. Sign in with a free account, open React level 13, a Premium coding challenge and stage 2 of a project (each opens the upgrade sheet), grant yourself Premium through the admin route and confirm they open without a deploy, then revoke it. [imp:4] [owner:me] [time:15m] [kind:deploy]
<!-- D2 (#221): billing. Added by the D1/D2 lane; keep this block together. -->
- [ ] **Set up Stripe for devShark Premium** — (1) create the account and apply for Managed Payments (product tax code `txcd_20060058`); (2) create the Product "devShark Premium" with two Prices, 3.99 EUR a month and 39.99 EUR a year, both `tax_behavior: inclusive`; (3) in Checkout settings add the Terms URL `https://devshark.app/terms`, which the required consent box needs; (4) set failed payments to end in "cancel" after the retries; (5) configure the Customer Portal: cancel at period end, switch monthly and annual within the one Product, card update, invoice history; (6) add the webhook endpoint `https://devshark.app/api/user/billing-webhook` on API version `2026-08-26.dahlia` for `checkout.session.completed`, `customer.subscription.created`, `customer.subscription.updated`, `customer.subscription.deleted`, `invoice.paid`, `invoice.payment_failed`, `charge.refunded`, `charge.dispute.created` and `radar.early_fraud_warning.created`; (7) set `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_PRICE_PREMIUM_MONTHLY`, `STRIPE_PRICE_PREMIUM_ANNUAL`, `STRIPE_MANAGED_PAYMENTS` (`true` once Managed Payments is approved, `false` for plain Stripe with Stripe Tax) and `PUBLIC_ORIGIN=https://devshark.app` in Vercel; (8) only then set `BILLING_ENABLED=true`. Until step 8 nobody can buy, and the locks still apply. [imp:5] [owner:me] [time:1h] [kind:setup]
- [ ] **Re-apply `supabase/supabase-schema-039.sql` if its D1 copy is already in production** — step D2 added section 5 (the consent table, two `billing_events` columns, `release_billing_event`, `billing_account`, `billing_customer_owner`, `record_checkout_consent`) before 039 reached production. The file stays additive and idempotent: on a local Postgres 16 it applied twice from scratch and twice over the D1 copy, and the D1 and D2 rolled-back exercises both pass. Applying the current file once covers both steps. [imp:5] [owner:me] [time:10m] [kind:deploy]
- [ ] **Run one Stripe test-clock simulation and capture the fixtures** — in the Stripe sandbox, subscribe a test account through `/premium`, advance a test clock past a failed renewal and confirm the plan line shows "Payment failed, update your card" inside the seven days and Free after it, then that the subscription ends canceled; record it in `docs/release-acceptance.md`. While there, run `stripe trigger` for the nine event types and replace the authored payloads in `scripts/fixtures/billing/` with the captures (`npm run test:billing` must still pass). [imp:4] [owner:me] [time:45m] [kind:deploy]
- [ ] **Have the checkout wording checked with the Terms** — Checkout shows the consent text "I want Premium to start now and I understand that I lose my 14-day right of withdrawal for digital content. I accept the Terms." and, beside Stripe's "Pay" button, "Pay starts a subscription at the price shown, VAT included, renewed every month until you cancel. Cancel any time from your profile or at devshark.app/premium/cancel; Premium stays open until the end of the period you paid for." Both live in `lib/billing/handlers.ts` (`checkoutText`). Ask the lawyer to confirm them against § 1826a OZ and the public cancel page (`/premium/cancel`) against § 312k BGB: anyone who knows a subscriber's email can cancel it at period end, and a withdrawal within 14 days refunds and ends it at once; the page never reveals whether an email has a subscription. [imp:4] [owner:me] [time:30m] [kind:legal]
- [ ] **Optional: set `RESEND_API_KEY` and `RESEND_FROM` for cancellation emails** — without them the cancel page shows its confirmation on screen and Stripe's own emails carry the receipts and refunds. `RESEND_FROM` needs a domain verified in Resend (default `devShark <billing@devshark.app>`). [imp:2] [owner:me] [time:20m] [kind:setup]
<!-- D3 (#222): public copy, /premium and the legal pages. Added by the D1/D2/D3 lane; keep this block together. -->
- [ ] **Fill in the trader details before Premium goes on sale** — set `name`, `companyId` (IČO), `registeredAddress` and `email` in `TRADER` in `client/product-catalog.ts`. The repository knows none of them, so `/terms` says they are missing; each field appears as soon as it is set, and the model withdrawal form, the complaints section and the privacy contact point to them. EU consumer law requires the trader's name, address and email before a sale, so do this before `BILLING_ENABLED=true`. [imp:5] [owner:me] [time:10m] [kind:legal]
- [ ] **Have the new Terms and privacy policy checked with the checkout wording** — `/terms` and `/privacy` (the `legal.*` keys in `client/src/i18n/translations.ts`) now carry the plans, the price with VAT, renewal and cancellation, the waiver and its consequence, the model withdrawal form, the voluntary 14-day refund, revocable grants, notice of changes, Czech law with the consumer's mandatory protection, ČOI as the out-of-court body and a 30-day complaint promise. Ask the lawyer about four points in particular: whether Premium counts as digital content (the waiver) or a digital service; the sentence that a part-used period is not refunded; the joint controllership with sprd.net AG for shop data, which the handoff assumes and Spreadshop's own policy does not mention; and the email notice before a price change. There is no EU ODR link on purpose: that platform closed on 20 July 2025. [imp:4] [owner:me] [time:1h] [kind:legal]
- [ ] **Add the privacy policy URL to Stripe's Checkout settings** — next to the Terms URL from the D2 steps: `https://devshark.app/privacy`. [imp:3] [owner:me] [time:5m] [kind:setup]
- [ ] **Check `/premium` in production after the deploy** — run Google's Rich Results test on `https://devshark.app/premium` (LearningResource with two offers, `isAccessibleForFree: false`) and resubmit the sitemap, which now lists 13 URLs, in Search Console. [imp:2] [owner:me] [time:15m] [kind:deploy]
<!-- D8 (#227): coins. Added by the D1/D2/D3/D8 lane; keep this block together. -->
- [ ] **Apply `supabase/supabase-schema-041.sql`** — coins (#227). Apply it after 039; it does not need 040, and it settles no month until 040's table exists. Proven on a local Postgres 16 from the 001–038 chain plus 039: applied twice, then a rolled-back exercise of the 10 % rate, the Premium doubling, the 400-a-day cap after the doubling, replays that credit nothing, every milestone once per account, a month settled once with a free rank paid nothing, the social grant at 0, owner-only reads and refused writes (recorded in `docs/release-acceptance.md`). It also restates `record_coding_verdict` so a coding challenge's XP goes to every account's first pass: until now `coding:<task>` was one award for the whole product, so only the first learner to pass a task ever got its XP. The code deploys safely before the migration: the XP credit falls back to the old flat rate and the wallet answers without milestones. [imp:5] [owner:me] [time:20m] [kind:deploy]
- [ ] **Decide the social click-through grant in `/dev` → Settings → Coins** — `socialVisitGrant` ships at 0, and the note beside the field explains why (Meta's spam rules, coins buy merchandise, no platform confirms a follow). "Find devShark elsewhere" shows nothing until the profile URLs are in `SOCIAL_PROFILES` in `client/product-catalog.ts`. [imp:3] [owner:me] [time:10m] [kind:decision]
- [ ] **Check Rewards signed in on production after 041** — a new account sees "+200 coins · welcome to devShark" once; a quiz, a first Learn level pass and a first coding pass each add a ledger line; How to earn shows "Streak 7 days: n of 7" and the topics in progress; a free account's Redeem opens the upgrade sheet and `op=orders` answers 402; with a manual Premium grant the next quiz credits double. [imp:4] [owner:me] [time:20m] [kind:deploy]
- [x] **Add a Merchandise editor and the monthly stock caps to `/dev` (D9, #229)** — built on 2026-09-25 as its own tab, `/dev` → Merchandise: quotes and coin prices, this month's caps (`set_merch_stock`, migration 043) and the fulfilment queue. No SQL needed once 043 is applied. [imp:3] [owner:ai] [time:2h] [kind:setup]
<!-- D8b (#228): invitations. Added by the D1/D2/D3/D8/D8b lane; keep this block together. -->
- [ ] **Apply `supabase/supabase-schema-042.sql`** — invitations (#228). Apply it after 041, whose `token_account_key` it uses; it widens the ledger reason check again and keeps 041's `milestone` and `social`. Proven on a local Postgres 16 from the 001–038 chain plus 039 and 041: applied twice, then a rolled-back exercise of the code, a bind inside and outside the 48-hour window, self and mutual referrals refused, nothing paid before a passed Learn level, 100 to each side once, the cap of 20 with the 21st friend still paid, deletion, owner-only reads and refused calls, and two sessions settling at the same moment (recorded in `docs/release-acceptance.md`). The code deploys safely before the migration: the Rewards screen hides the invite section and a first Learn pass credits no referral. [imp:4] [owner:me] [time:15m] [kind:deploy]
- [ ] **Decide whether invitations stay on** — they ship on at 100 coins to each side and 20 paid friends per inviter. `/dev` → Settings → Coins carries both numbers; 0 coins hides the invite section and binds no new code. Each paid invitation costs nothing but coins, and coins only buy merchandise through a Premium account. [imp:2] [owner:me] [time:5m] [kind:decision]
- [ ] **Check an invitation end to end on production after 042** — sign in as account A and copy the link from Rewards → Invite a friend; open it in a private window, sign up as a new Google account B and see "Invitation accepted"; pass HTML level 1 as B; both ledgers show a referral line of +100, A's reads "Referral: a friend finished their first level", and A's invite section says "1 of 20". [imp:3] [owner:me] [time:15m] [kind:deploy]
<!-- D9 (#229): merchandise through Spreadshop. Added by the D1/D2/D3/D8/D8b/D9 lane; keep this block together. -->
- [ ] **Apply `supabase/supabase-schema-043.sql`** — merchandise through Spreadshop (#229). Apply it after 028 and 035; it needs none of 039 to 042. It adds the hoodie to both SKU checks, adds `set_merch_stock` for the monthly caps, and restates `advance_merch_order` and `cancel_merch_order` so a claimed learning-path package (which 035 leaves awaiting payment at zero) can be sent without touching the reservations of coin redemptions. Proven on a local Postgres 16 from the 001–038 chain, alone and after 039, 041 and 042: applied twice, then a rolled-back exercise of the caps, a hoodie redemption, the cap refusing to drop below a paid order, a package shipped and cancelled beside a redemption, and service-role-only routines (recorded in `docs/release-acceptance.md`). Until it is applied `/dev` → Merchandise cannot save a cap and a hoodie cannot be redeemed; nothing else changes. [imp:4] [owner:me] [time:15m] [kind:deploy]
- [ ] **Link the devShark shop and add the mockups** — once the Spreadshop is open, copy the shop URL and each item's URL into `MERCH_SHOP` in `client/product-catalog.ts` (https, one per item; leave `cap` null unless Spreadshop sells one), and export one square mockup per item as `client/public/merch/<sku>.webp` (`sticker-set`, `mug`, `t-shirt`, `hoodie`). A tile shows a link and an image only when they exist. [imp:4] [owner:me] [time:30m] [kind:content]
- [ ] **Enter the quotes, coin prices and this month's caps in `/dev` → Merchandise, then open redemption** — base price as Spreadshop charges your sample order, print and packaging 0, shipping as its checkout shows, vendor `sprd.net AG`; coin prices with the calibration in `docs/rewards-launch.md`; caps per item and size. Walk one test redemption through "Ordered at Spreadshop" and "Mark shipped" with a real sample order before turning test mode off. [imp:3] [owner:me] [time:45m] [kind:content]
- [ ] **Check the fulfilment queue every week** — `/dev` → Merchandise lists paid redemptions and claimed packages with the address. Order each from the Spreadshop preview (Order product samples), mark it ordered, and enter the carrier and tracking number when it ships. Decide who does this before opening redemption. [imp:3] [owner:me] [time:15m] [kind:decision]
- [ ] **Optional: set `SPREADSHOP_API_KEY`, `SPREADSHOP_SHOP_ID` and `SPREADSHOP_CONTACT_EMAIL`** — request the key at partner.spreadshirt.net/apiKey (EU platform). The Rewards screen then shows Spreadshop's own monthly promotion, read on the server and kept 30 minutes, with a note that coins never buy a discount. Without the key nothing is fetched. [imp:1] [owner:me] [time:10m] [kind:setup]
<!-- INT (#219–#230): one erasure routine and the importance check. Added by the integration step; keep this block together. -->
- [ ] **Apply `supabase/supabase-schema-044.sql` after 039 to 043** — it restates `delete_user_data` so one call erases every table that holds an account id, including those of 035 and 039 to 042 (the package claims of 035 were never erased before), and adds the `question_edits_importance_check` constraint that migration 014 meant to add and production lacks. A read-only check on 2026-09-25 found 485 `question_edits` rows, none with an importance, so nothing stored can fail it. Proven on a local Postgres 16 from the 001–038 chain: 039 to 044 applied in order with 044 twice, once as the chain leaves it and once without the constraint (production's shape), then a rolled-back exercise and the real handler against four database shapes (recorded in `docs/release-acceptance.md`). [imp:4] [owner:me] [time:15m] [kind:deploy]
- [ ] **Drop the transitional erasure loop once 044 is in production** — `deleteAccount` in `api/user/[op].ts` still calls `delete_user_activity_days`, `delete_entitlement_data`, `delete_coin_data` and `delete_referral_data` after `delete_user_data` (`LATER_ERASURE_ROUTINES`), because a database with 039 to 043 and without 044 needs them. After 044 is applied they delete nothing: remove the loop and its contract lines, then drop the four routines in a later migration. [imp:2] [owner:ai] [time:20m] [kind:deploy]

## Kickoff 2026-09-25 · devShark as marketingShark's only brand

`KICKOFF-25-9-2026.md` at the repository root is the order; issues #217 and #218 are the steps.

- [ ] **Rename `lukaskourilcz/react-express-app` to `lukaskourilcz/devShark`** — the first of the three renames (order in quorum's kickoff), after own-dashboard #75 is live; confirm the `devShark` Vercel project still deploys from the renamed repository. #217 does the repository side. [imp:3] [owner:me] [time:15m] [kind:setup]

## Before production launch

- [ ] **Delete the paused StudyShark Vercel project, after removing its URL from Supabase Auth** — the `studyshark-app` project was paused on 2026-09-25 and answers 503, but the connector cannot delete a project. First remove `https://studyshark-app.vercel.app/**`, and any StudyShark preview pattern, from Supabase → Authentication → URL Configuration, and the StudyShark origin from the Google OAuth client: once the project is deleted anyone can claim that `vercel.app` subdomain, and a leftover redirect entry would hand them sign-in tokens. Then delete the project (Settings → Advanced → Delete Project) and remove `VITE_STUDYSHARK_URL` from the devShark project. Steps in [#214](https://github.com/lukaskourilcz/react-express-app/issues/214). `[imp:4]` `[owner:me]` `[time:15m]` `[kind:deploy]`
- [x] **Decide what happens to StudyShark's rows in the production database** — deleted on 2026-09-25 at your request, from production and from the `devshark-recovery-20260915` copy: three July test matches that used StudyShark questions and eight anonymous geography attempts. No account held StudyShark rows. Details in [#215](https://github.com/lukaskourilcz/react-express-app/issues/215). `[imp:2]` `[owner:me]` `[time:15m]` `[kind:decision]`
- [ ] **Delete the stale branches on origin** — tracked in [#232](https://github.com/lukaskourilcz/react-express-app/issues/232), which re-derives the merged status of every origin branch and supersedes #187. `claude/elegant-cori-h9cdgb` holds unmerged features ([#208](https://github.com/lukaskourilcz/react-express-app/issues/208)), among them the Classroom rate-limit fix `fa884b7`, which no longer applies cleanly to `main`; keep a copy first if any of it might come back. `[imp:2]` `[owner:me]` `[time:10m]` `[kind:setup]`
- [x] **Apply `supabase/supabase-schema-037.sql`** — applied to production (`rvlybcjdpafwyeuojvhl`) on 2026-09-18 through the Supabase connector and verified: `order_mode`, `task_count` and `scheduled_for` exist on `practice_sessions`, the status check now admits `scheduled` and is the only status check, both new routines are SECURITY DEFINER with an empty `search_path` and executable by `service_role` alone, and a rolled-back exercise confirmed a run planned for tomorrow is stored as scheduled, starts exactly once, is retired by the next run, and that an unknown order mode is refused. Re-applying the SQL changed nothing, no test rows were left behind, and the security advisor reports only the pre-existing notices. The isolated recovery copy (`devshark-recovery-20260915`) was deliberately not touched. `[imp:3]` `[owner:me]` `[time:15m]` `[kind:deploy]`
- [x] **Apply `supabase/supabase-schema-038.sql`** — the Algorithms track. Applied to production (`rvlybcjdpafwyeuojvhl`) on 2026-09-21 through the Supabase connector and verified: both `track` checks on `coding_progress` and `coding_attempts` now list `algorithms` and each table carries exactly one of them, `record_coding_verdict` and `record_coding_reveal` are still SECURITY DEFINER with an empty `search_path` and executable by `service_role` alone, and a rolled-back exercise recorded an `algorithms` verdict and reveal, confirmed both rows carried the new track, and confirmed an unknown track is still refused. No probe rows were left behind and the security advisor reports only the pre-existing RLS notices. The isolated recovery copy (`devshark-recovery-20260915`) was deliberately not touched. `[imp:4]` `[owner:me]` `[time:15m]` `[kind:deploy]`
- [ ] **Apply `supabase/supabase-schema-040.sql`** — the 30-day leaderboard (#223): the `user_activity_days` table, the three verified answer routines restated to date their answers, `record_challenge_completion`, `window_leaderboard`, `window_leaderboard_rank`, `delete_user_activity_days`, and `friend_list` ordered by correct answers instead of the streak. Proven on a local Postgres 16 built from the 001–038 chain: applied twice, then exercised inside a rolled-back transaction (each receipt counts once, a daily retry adds nothing, anonymous Learn answers add nothing, the window edge and the five-answer minimum hold, ties share a rank, RLS lets a learner read only their own rows, every routine is SECURITY DEFINER with an empty `search_path` and `service_role`-only execute). It does not depend on 039. Until it is applied the leaderboard falls back to the all-time board with a note, and a finished challenge run still earns its XP. The window starts empty: nothing is backfilled. `[imp:4]` `[owner:me]` `[time:15m]` `[kind:deploy]`
- [ ] **Decide whether the public leaderboard shows a country flag.** The handoff asked for one on each row, but the Friends tab promises that friends are the only people who see a learner's country, so the rebuilt board shows none. Showing it needs that promise changed first. `[imp:1]` `[owner:me]` `[time:5m]` `[kind:decision]`
- [ ] **Decide what happens to the prerendered Czech topic guides.** The app now ships English only (`ENABLED_LANGS`), but `/cs/topics/:slug` still renders and is still indexed. Those are marketing pages, not app UI, so retiring them is an SEO call with redirects attached rather than a switch: either keep them as a Czech entry point, or 301 each to its English twin and drop the `cs` hreflang alternate in `client/src/App.tsx`. Leaving them indexed while the app is English-only means a Czech visitor lands on Czech copy and then meets an English product. `[imp:2]` `[owner:me]` `[time:20m]` `[kind:decision]`
- [ ] **Check the Coding changes of 2026-09-18 signed in.** The screenshots in this session came from a mocked API, so the account-bound parts were not exercised. (1) Plan a run for later on the Coding home, confirm it appears on Today with its time, that "Start now" opens the first queued task and that a pass on it moves the run to the next one. (2) Pass any challenge and confirm the Solution tab appears beside Resources with a junior and a senior version, and that reopening a passed task shows it again. (3) Try Log it right, level 1, under Debugging paths on the Coding home (it replaced the Debugging path on 2026-09-25), open the Console after Run and confirm the scratch-pad log is there. (4) On a phone, open a task with a puzzle and confirm arranging still submits. `[imp:3]` `[owner:me]` `[time:30m]` `[kind:deploy]`
- [ ] **Check the Coding changes of 2026-09-24 signed in** — the fixes were verified against a mocked API, so the account side was not exercised. (1) Submit a correct answer to any Algorithms challenge and confirm it records a pass instead of "This session expired". Every Algorithms submit failed that way from 2026-09-21 until this deploy. (2) Edit a task, press Run, leave and come back: the code from that Run should be back. Edit again without running, leave, and confirm those edits are gone, which is the new rule. Nothing should ever say the code was saved. (3) On a phone, open a quiz hint and close it with the ×. `[imp:3]` `[owner:me]` `[time:15m]` `[kind:deploy]`
- [ ] **Check the Coding changes of 2026-09-25 signed in** — the card and the code pane were checked with a mocked API and unit tests, so no account was behind them. (1) Reload `/coding` a few times: the next-challenge card should show a grey bar and a dimmed Continue for a moment, then your real next challenge, and never flash "Digit sum" first. (2) Open a challenge and confirm the save star sits at the right end of the action bar beside the report flag, and that saving still works. `[imp:3]` `[owner:me]` `[time:10m]` `[kind:deploy]`
- [ ] **Check the debugging paths signed in (#225).** The three paths were checked against a mocked API and the local graders, not an account. (1) Open Log it right, level 2, add `console.table(basket)`, press Run, then Submit, and confirm the Console shows the same table both times. (2) Pass Trace the state, level 1, and confirm level 2 opens with your code and the Coding home counts 1 of 5 levels. (3) If your account has a pass on the old Debugging path, open `/coding/javascript/js-evolving-debug-1` and confirm it still shows as passed; a bookmarked stage should still be listed in Collection. `[imp:2]` `[owner:me]` `[time:15m]` `[kind:deploy]`
- [ ] **Decide how the Easy band meets the tier ladder (#226).** Tier 3 of a coding track opens after ten JavaScript Learn levels, or after a pass on every tier 1 and tier 2 challenge of that track. Each Easy wave adds challenges to that sweep. The three JavaScript waves added 90, the two TypeScript waves 50 and the two React waves 40, so a learner who opened tier 3 of any of these tracks through the sweep alone, without the Learn levels, finds it locked again until they pass the new ones. The Algorithms wave added 20, but that track opens every tier from the start, so it changes nothing there. The loop and method badges read the same set; a badge already earned stays earned. Keep the rule, or let the sweep count only the challenges that existed before #226. `[imp:2]` `[owner:me]` `[time:10m]` `[kind:decision]`
- [x] **Re-authorize the Supabase and Vercel connectors** — in claude.ai → Settings → Connectors. Both asked for authentication again on 2026-09-24. Both answered on 2026-09-25 (a Supabase read of the migration history, the Vercel project read). `[imp:2]` `[owner:me]` `[time:5m]` `[kind:setup]`

- [ ] **Run the signed-in production smoke test** on desktop and mobile: Learn completion, Quiz replay rejection, Daily/Challenge idempotency, Flashcards, leaderboards, the Today queue, a level going cleared→mastered over separate days, a streak protection bridging a missed day and the 48-hour shield, the read-only advisor, adaptive placement (including the "I don't know yet" option), and the typing racer. Two-session Play/Classroom, non-admin `/dev` denial and account deletion passed on 2026-09-16 ([#190](https://github.com/lukaskourilcz/react-express-app/issues/190)). `[imp:4]` `[owner:me]` `[time:45m]` `[kind:deploy]`
- [ ] **Check the two Profile/Learn changes of 2026-09-04 signed in.** Neither can be rendered in the build container: the responsive sweep only ever sees the signed-out `/profile` redirect, and no Supabase project is reachable to play a Learn level. (1) The identity banner carries the appearance and sound switches: confirm the row wraps cleanly at 390 px, that each button reads its action to a screen reader, and that both persist on the device. (2) Spend all three hearts in a Learn level before its last question and confirm the lesson ends on the out-of-hearts screen instead of "Answer every question before completing this lesson". `[imp:4]` `[owner:me]` `[time:20m]` `[kind:deploy]`

## Learning paths (FDE and DSA Foundations)

The two devShark learning paths are implemented and validated in the repository. Three owner steps stand between that and learners seeing them, and they are deliberately separate so a half-finished one cannot open a path by accident.

- [x] **Apply `supabase/supabase-schema-026.sql`** — applied 2026-09-09 and verified: the five `learning_path_*` tables exist with row-level security on, `learning_path_attempts` has no policy and no grant for `authenticated` (it holds grading material, and owning a row is not a reason to read it), and the five new functions are service-role only. `[imp:4]` `[owner:me]` `[time:30m]` `[kind:deploy]`
- [ ] **Enable the paths one at a time.** Set `LEARNING_PATH_DSA_ENABLED=true` on the devShark project first and watch it for a few days; add `LEARNING_PATH_FDE_ENABLED=true` when you are ready for the longer path. The switches are independent on purpose. `[imp:3]` `[owner:me]` `[time:20m]` `[kind:deploy]`
- [ ] **Check the learning-path workspace signed in.** The responsive sweep ran clean over the new routes — 133 probes across 360–1440 px, light and dark, English and Czech, zero overflow or containment issues — but it can only see the signed-out state, so what it actually rendered was the guest preview, not the graded workspace. Confirm signed in that the two-column brief-beside-editor layout collapses cleanly at 390 px with the controls reachable without covering the editor, that the trace player steps with the keyboard and reads its step aloud, and that verified-pass and self-reviewed stay visibly distinct without relying on colour. `[imp:3]` `[owner:me]` `[time:30m]` `[kind:deploy]`
- [ ] **Run the signed-in learning-path smoke test** on preview before enabling production: read the outline signed out, sign in, start a path, submit a wrong answer and a right one, close the tab mid-draft and reopen it, and send the same idempotency key twice to confirm the second call replays the stored result instead of grading again. Then pause the path and confirm the evidence survives and it leaves the Today queue. `[imp:4]` `[owner:me]` `[time:45m]` `[kind:deploy]`
- [ ] **Pilot with 6–10 practising engineers across the three base tracks**, as the curriculum plan proposes. Record the sample size and the limitations alongside anything you conclude: with a cohort this small the numbers describe those people, not the population, and the thresholds (80% on checks, 80% of weighted non-critical criteria) are pilot assumptions to calibrate, not findings. Do not report a hiring or salary outcome — the path does not measure one. `[imp:2]` `[owner:me]` `[time:4h]` `[kind:content]`
- [ ] **Re-review the cited sources each quarter**, and immediately if a learner reports a broken API example or a grading defect. Every lesson carries the date its sources were last checked; the `/dev` → Learning paths screen lists them, and the content validator warns once a path's review date passes 400 days. `[imp:2]` `[owner:me]` `[time:1h]` `[kind:content]`

## Modernization epic (2026-09-08)

The epic in [#150](https://github.com/lukaskourilcz/react-express-app/issues/150) is implemented: the learner profile and server progression graph, the Edabit-inspired practice work, the merchandise wallet, the curriculum retirements and rescopes, the curation methodology, and the learning-science scheduling. `docs/release-acceptance.md` records every gate with its actual result and, just as importantly, every check that could not run here. These are the ones that need you.

- [x] **Apply migrations 027 to 031 in order** — applied 2026-09-09 and verified. 027 adds the coding bookmarks, collections, skips, puzzle results and practice sessions; 028 the token ledger, balances, merchandise stock, orders and cosmetic entitlements; 029 one nullable column on `question_reports`; 030 the concept review table and its recording routine; 031 the policy and index work below. All eighteen new tables have row-level security on, `merch_stock` and `learning_path_attempts` have no policy at all (service-role only), and every new function is service-role only. `[imp:4]` `[owner:me]` `[time:45m]` `[kind:deploy]`

  Two things were fixed while applying them, and both are now in the migration files so a fresh database comes up the same way. 027 and 028 granted `SELECT` without revoking Supabase's default grants first, which left `INSERT`, `UPDATE`, `DELETE` and `TRUNCATE` on twelve tables — the policies masked the first three, but `TRUNCATE` touches no rows and so passes no policy. And eighteen owner-read policies from earlier migrations evaluated `auth.uid()` once per row rather than once per query.
- [ ] **Leave the shop unconfigured until you decide to sell.** It ships off: no price is set, an incomplete quote is dropped on read, `testMode` defaults on, and payment only ever applies through a signed webhook. `docs/rewards-launch.md` lists what a real launch needs — a supplier, real prices, the payment provider, and the consumer-law wording that a physical sale to EU buyers requires. Free learning does not wait on any of it. `[imp:2]` `[owner:me]` `[time:2h]` `[kind:legal]`
- [ ] **Smoke-test spaced review and interleaving signed in**, on a preview deployment with migration 030 applied: answer a level with a hint open and one without, confirm the hinted concept does not climb the ladder, wait out the shortest interval and confirm the concept appears in Today's "Due for review" card with a sensible size and time. No Supabase project is reachable from the build container, so the policy is unit-tested and the persistence is not exercised. `[imp:3]` `[owner:me]` `[time:40m]` `[kind:deploy]`
- [ ] **Check the new surfaces signed in.** The responsive sweep renders signed out, so what it saw of the review card and the lesson figures was the guest state. Confirm at 390 px that the figure controls (Previous, Next, Reset) stay reachable and that the "This figure in words" text wraps without pushing the page sideways. `[imp:3]` `[owner:me]` `[time:30m]` `[kind:deploy]`
- [x] **Merge the content audit ([#176](https://github.com/lukaskourilcz/react-express-app/issues/176)).** Merged: its commits reached `main` on 2026-09-11 (the branch head `33fe234` is in `main`'s history). It was finished on branch `claude/qa-hints-audit-i0kh9t`, with every repository gate passing. All 2,128 served devShark questions have been read twice, rewritten or retired, translated from the final English, and recorded in the ledger; all sixteen categories are in the gate's scope, 152 items are retired and 2 quarantined. `docs/audit/devshark-content-audit.md` is the report. `[imp:4]` `[owner:me]` `[time:1h]` `[kind:decision]`

  The tell that prompted this is measurably gone: `npm run audit:devshark-content -- --details` flagged **158 questions** where the correct answer was markedly longer and more detailed than its distractors, and now flags **25**. The larger finding was elsewhere — 2,126 of the 2,128 items failed the quality gate on the same dimension, the hint, because that field was auto-populated with one of six generic sentences and nobody read the result.

- [ ] **Read the audit's retirements and corrected keys.** The audit is already live, so this is now a check after the fact. The review is model-performed with recorded evidence, and the product says so rather than calling it a human review. 152 items are retired, 2 quarantined and 3 wrong keys corrected; `docs/audit/devshark-content-ledger.json` holds the rationale and the evidence for each. A human pass over those 157 decisions is the cheapest check on the whole exercise. React alone accounts for 42 retirements, because its later levels carried a code version and a prose version of the same fact. `[imp:4]` `[owner:me]` `[time:1h]` `[kind:content]`

- [ ] **Decide the 46 retired items that want a home.** Items in the retired sections whose review judged the objective worth keeping and named a live level that does not already teach it. Nothing is applied — moving an item into a live level is a content decision, not a rewrite — so they sit in `docs/audit/devshark-redistribution-candidates.md` with the reviewer's reasoning beside each. Say yes, no, or yes-to-some. `[imp:2]` `[owner:me]` `[time:45m]` `[kind:content]`

If Czech ships again, two typography conventions in the retained Czech copy need a decision first: the decimal comma, and whether a space goes before `%` (46 occurrences are spaced and 16 tight, and Czech writes `100% využití` tight when adjectival). The audit already regrouped the thousands separators. No task while `ENABLED_LANGS` ships English only.

- [ ] **Check the touch targets and the pre-paint theme on a real phone.** Both were measured in a headless browser and both need a real device to confirm. (1) Every interactive control now clears 44×44 on a coarse pointer, measured across twenty-four routes at 390 px with zero below the floor. Confirm on a phone that the header menu button, the segmented controls and the footer links feel right rather than merely measuring right, and that nothing became cramped. (2) With dark mode chosen, a cold load should paint dark before anything renders, with no white flash. `[imp:3]` `[owner:me]` `[time:20m]` `[kind:deploy]`

- [x] **Apply migrations 032 to 035** — applied to production 2026-09-09 and verified there. 032 is the streak shield and the end of off-days; 033 is friends; 034 makes answering a Learn question one database round trip instead of three; 035 is the bought protection and the earned merch package. All four were exercised first against a local Postgres 16 (the whole 001–035 chain from an empty database, then re-applied for idempotency) and then again against production inside transactions that were rolled back, so no rows were left behind. What that confirmed: a handle is claimed, found case-insensitively, requested, answered, listed and removed, and an unknown handle returns no row rather than an error; the month grants two protections, raising the shield spends one and opens a 48-hour window, and raising a second while the first is live is refused and spends nothing; buying is refused on an empty wallet, debits 250 with tokens, and at the cap refuses **and takes no tokens**; the path reward is refused before completion, on a bad size and on an empty address field, grants once, and reports `already` on a second claim. Row-level security is on for all three new tables with one owner-scoped SELECT policy each, `anon` holds nothing, and all eighteen routines are SECURITY DEFINER with an empty `search_path`, executable by `service_role` alone. The security advisor reports no errors and no warnings. The full handoff of 2026-09-09 is in git at `e05640d:HANDOFF.md`. `[imp:4]` `[owner:me]` `[time:40m]` `[kind:deploy]`

- [ ] **Decide how long the ten test friends stay.** `seed-friend-pablo-r` … `seed-friend-amara-o` are in the production database, befriended with your account, spread from twenty answers to a 45-day streak. They hold no email, no name and no auth user — only progress rows and a handle — but they are ordinary accounts in every other respect, so **they appear in the public leaderboard**, which is what you asked for. Regenerate them with `npm run seed:test-friends -- --owner <your user_id>`; the generated SQL ends with the seven DELETEs that remove them again. Worth removing before the product is public, or the first real learner arrives to find ten strangers above them. The 30-day board, the default since #223, does not list them: they have no dated answers. The all-time board still does. `[imp:3]` `[owner:me]` `[time:10m]` `[kind:decision]`

- [ ] **Check the four new surfaces signed in.** Their migrations are live, so the features are no longer dark — but the responsive sweep renders signed out, so none of them has been seen with an account behind it. (1) Claim a handle on the Profile's Friends tab, add a second account, accept from that side, and confirm each sees the other's streak and accuracy and that a crown owner shows the crown. (2) Raise the shield from the Current streak container and confirm it shows hours and minutes remaining after a reload, and that a second click does not spend a second protection. (3) Buy a protection in the shop with a wallet below the cap, then try again at the cap and confirm the balance does not move. (4) Finish a learning path and confirm the claim form appears, takes a size and an address, and claims once. Check all four at 390 px. `[imp:4]` `[owner:me]` `[time:45m]` `[kind:deploy]`

- [x] **Decided 2026-09-09.** A streak protection costs 250 tokens — roughly a fortnight of steady learning, against the crown's 1200, because it is consumable and capped at two. The legacy rings and flairs were already unsellable; they stay ownable and equipped, and the shop now lists the crown, streak protection and merchandise. Adjust the price with `streakProtectionTokenPrice` in game settings. `[imp:3]` `[owner:me]` `[time:15m]` `[kind:decision]`

- [x] **Weekends are not free — decided 2026-09-09.** Every calendar day counts against a streak, and the two monthly protections are the whole story. There were no live accounts, so nothing was lost when migration 032 landed. `[imp:4]` `[owner:me]` `[time:5m]` `[kind:decision]`

## Production reliability

- [ ] **Monitor `/api/health` externally** and alert on non-2xx. The probe now verifies public DB access, service-role access to migration 023, and reports whether distributed rate limiting is configured. Tracked with its evidence in [#190](https://github.com/lukaskourilcz/react-express-app/issues/190). `[imp:4]` `[owner:me]` `[time:1h]` `[kind:deploy]`

  **Set the alert to require two consecutive failures**, or the first page you get will be a false one. Observed on production 2026-09-09: a cold lambda answered `503 degraded` with `serviceRole: unavailable`, and the next three probes answered `200` with `serviceRole: ok` in 0.9-1.7s. Nothing was wrong — `quiz_submissions` still grants the service role every privilege and `grade_key` is still there. The probe gives each readiness query 1500 ms, and on a cold start the first Supabase round trip includes connection setup, so it exceeds that and is reported as `down`. Widening the budget would hide a real outage for longer, so it is a decision rather than an obvious fix: either require two failures at the monitor, or keep a warm ping against the probe. `[imp:3]` `[owner:me]` `[time:10m]` `[kind:decision]`
- [ ] **Configure verified backups and a restore drill** using `docs/backup-restore.md`. You chose RPO 24 hours and RTO 1 hour on 2026-09-16; the isolated restore itself is still open. Tracked with its evidence in [#190](https://github.com/lukaskourilcz/react-express-app/issues/190). `[imp:4]` `[owner:me]` `[time:1h]` `[kind:deploy]`
- [ ] **Confirm Sentry + PostHog capture no PII in production.** Both are configured with EU regions, `sendDefaultPii: false` (Sentry), and `respect_dnt` + `identified_only` + reverse-proxied `/ingest` (PostHog). Reproduce a client error and a routine pageview signed in, then inspect the payloads in each dashboard — no submitted answer, answer proof, token, or email should be present. `[imp:3]` `[owner:me]` `[time:1h]` `[kind:legal]`

## Brand and launch assets

- [ ] **Replace placeholder icons and social artwork** (`client/public/icon.svg`, Apple/PWA icons, and `og-image.png`) with final licensed devShark assets and verify metadata on devshark.app. `[imp:3]` `[owner:me]` `[time:2h]` `[kind:legal]`
- [ ] **Investigate the desktop CLS of ~0.96 on the landing route.** Lighthouse baseline (2026-07-28, `docs/perf/lighthouse-baseline-2026-07-28/`) scored devShark 33 for desktop Performance because Cumulative Layout Shift on the landing route is far outside the ≤ 0.1 target. Mobile CLS is 0. The likely culprit is a hero/animation element sized only after its resource loads at desktop widths. Not launch-blocking (mobile is fine), but the desktop score will hurt PageSpeed once you promote the domain publicly. `[imp:3]` `[owner:me]` `[time:2h]` `[kind:deploy]`
- [ ] **Brand the Google OAuth consent screen** — Google Cloud Console → APIs & Services → OAuth consent screen: app name devShark, support email, logo, home page https://devshark.app, privacy and terms links, authorized domain devshark.app. The "to continue to rvlybcjdpafwyeuojvhl.supabase.co" line changes only with a Supabase Custom Domain (Pro tier: `auth.devshark.app` as a CNAME, then `https://auth.devshark.app/auth/v1/callback` as a Google redirect URI), which is not worth it before launch. `[imp:2]` `[owner:me]` `[time:15m]` `[kind:setup]`
- [ ] **Improve mobile LCP (~5.7 s) on the landing route** — same baseline. The largest paint arrives well after the "poor" threshold (4 s). Common wins: preload the hero image, defer the non-critical scripts, and shrink the above-the-fold bundle. `[imp:3]` `[owner:me]` `[time:2h]` `[kind:deploy]`

## Portfolio thumbnail (2026-07-27)

The devShark thumbnail on lukaskouril.dev was re-recorded from the redesigned
landing page. It had to be captured from a locally-run client, because the
deployed site was unreachable from the session that produced it.

- [ ] **Compare the committed preview against the live site** — open `media/preview-poster.png` next to devshark.app and confirm the local build matched production. The procedure and the check are in `.claude/skills/preview-video/SKILL.md`. `[imp:3]` `[owner:me]` `[time:20m]` `[kind:content]`

## Optional features

- [x] **Voluntary support:** retired by #222. Premium replaced it: the support page is gone, `/support` redirects to `/premium`, and `SUPPORT_ENABLED` stays off. `[imp:3]` `[owner:me]` `[time:2h]` `[kind:legal]`

devShark ships no AI feature. The dormant AI wiring (post-answer explanations, Sharkira hints) left this repository with StudyShark on 2026-09-24.

## Repository housekeeping

- [ ] **Decide what happens to the badge feature.** The Profile badge section was removed on 2026-09-04 at your request, so nothing in the UI reads `/api/user/badges` any more. The unused `client/src/lib/badges.ts` wrapper was removed in the 2026-09-07 production cleanup. The `sync_user_badges` RPC and `user_badges` table remain; `shared/badges.ts` stays either way, because the devShark coding badges use it. Say whether badges come back somewhere else or the dormant endpoint should go. Nothing in `client/src` calls `/api/user/badges`, production `user_badges` holds 0 rows (2026-09-25), and `client/src/lib/achievements.ts` still records perfect quizzes that nothing reads. `[imp:2]` `[owner:me]` `[time:10m]` `[kind:decision]`
- [x] **Resolve the `fflate` advisory** — production audit 2026-09-07 updated the client lockfile (`posthog-js` and compatible `fflate`). Both production dependency audits report zero vulnerabilities; client TypeScript and both Vite builds pass. [#126](https://github.com/lukaskourilcz/react-express-app/issues/126). `[imp:3]` `[owner:ai]` `[time:30m]` `[kind:deploy]`
- [ ] **Delete the merged branches listed in [#232](https://github.com/lukaskourilcz/react-express-app/issues/232)** — including `claude/remove-forms-unlock-system-design-br6l74`, `claude/profile-settings-restructure-zk2yi9`, `claude/dsa-fde-learning-paths-x9vo8q`, `claude/resolve-open-issues-xnr0lv` and `docs/fde-learning-path-plan-2026-09-08`, all still on origin on 2026-09-25. PR #149 is already closed. Issues #128–#148 link to the plan through `blob/docs/fde-learning-path-plan-2026-09-08/...` URLs, which break when that branch goes; the same files are at `blob/main/docs/...`. `[imp:2]` `[owner:me]` `[time:10m]` `[kind:setup]`

## Coding integration (2026-09-03)

The plan for folding interview-prepper into devShark is in `docs/coding-integration-plan.md` (section 13 records the implementation status); the implementation issues live on GitHub (epic #105 in this repository, three issues in `interview-prepper`). Every issue on the epic is implemented and merged into `main`, including server-side React grading, so all four tracks record verified verdicts. Decisions recorded on 2026-09-03: the system-design track follows, the mock mode and the AI coach are retired, devShark carries no AI feature, React tasks behave like the other tracks, and the import reads Firestore only. These items need you.

- [x] **Apply `supabase/supabase-schema-025.sql` in production** — done on 2026-09-03. Confirm `/api/health` still reports `database: ok` and `serviceRole: ok` after the next deploy. `[imp:4]` `[owner:me]` `[time:5m]` `[kind:deploy]`
- [ ] **Export your interview-prepper Firestore data and run the one-time import** locally: `npm run import:interview-prepper -- --input export.json --user-id <your Supabase user id>` (dry run), then add `--apply` with `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` set. Export the `progress/{uid}` document and its `attempts` subcollection from the Firebase console (or `firebase firestore:export`) into one JSON file; no browser storage export is needed. Imported passes carry no XP and no scheduled review. `[imp:2]` `[owner:me]` `[time:30m]` `[kind:setup]`
- [ ] **Retire interview-prepper after the import is verified:** archive the repository, delete its Vercel project and Firebase project, and revoke its OpenAI key. `[imp:2]` `[owner:me]` `[time:30m]` `[kind:deploy]`
- [x] **Merge the implementation branch `claude/interview-prepper-integration-ogh4ph`** in this repository once you have reviewed it, then delete the branch. Merged into `main`; its branch is deleted with #232. `[imp:3]` `[owner:me]` `[time:20m]` `[kind:setup]`
- [ ] **Register the devShark GitHub App for the garden feature.** Name it for learners (for example "devShark garden"); permissions: Repository contents read and write (metadata read is implied); installable by any user; no webhook needed; Setup URL `https://devshark.app/settings/github` with "Redirect on update" on; "Request user authorization (OAuth) during installation" off. Generate a private key and set `GITHUB_APP_ID`, `GITHUB_APP_SLUG`, and `GITHUB_APP_PRIVATE_KEY` (PEM, base64) on the devShark Vercel project. The feature stays hidden until these exist. `[imp:3]` `[owner:me]` `[time:30m]` `[kind:setup]`
- [x] **Update the privacy notice for the GitHub garden** — done on 2026-09-03 in `client/src/components/PublicInfoPages.tsx` (EN and CS): what is stored, that passed solutions are committed to the learner's own repository under their GitHub identity, and how to disconnect and uninstall. Review the wording with the rest of the legal text before launch. `[imp:3]` `[owner:me]` `[time:10m]` `[kind:legal]`
- [x] **Implement the coding integration issues in the order the epic lists** — #106 to #117 done on 2026-09-03; see the epic for the follow-up issues. `[imp:3]` `[owner:ai]` `[time:12h]` `[kind:content]`
- [ ] **Smoke-test the coding section signed in on a preview deployment:** pass one JavaScript, one TypeScript, one React, and one system-design task, and finish a JavaScript Learn level with its coding phase. No Supabase project is reachable from the build container, so these flows are contract-tested and type-checked but never driven with a real account. A signed-in JavaScript stage already passed on production on 2026-09-15 (`docs/launch-audit-2026-09-15.md`, "Live account and database"). `[imp:4]` `[owner:me]` `[time:45m]` `[kind:deploy]`
- [x] **Bump the two client production dependencies the audit flagged** — done on 2026-09-03; both audits are clean. `[imp:3]` `[owner:ai]` `[time:30m]` `[kind:deploy]`

## When usage grows

- [ ] **Review [scaling.md](./scaling.md) monthly once traffic is meaningful** — upgrade Supabase compute before sustained saturation, add retention/partitioning for event tables, and redesign multiplayer fan-out before large classrooms or high concurrent-room counts. `[imp:1]` `[owner:me]` `[time:20m]` `[kind:setup]`

Native iOS/Android work remains intentionally deferred; it is not a missing item for this web release.

## Recorded decisions (2026-07-28)

- **`claude/favicon-sharkfin-q3z4ot` deleted** — Expo-only branch; mobile app was shut down. PR #91 closed, remote branch removed.
- **`react-router-dom` 7.18.1 kept** — advisories GHSA-qwww-vcr4-c8h2 and RSC-mode CSRF bypass are RSC-mode only. This app is a Vite SPA with no server components (no `renderToPipeableStream`, `react-server-dom`, `createStaticHandler`, or `@react-router/server` usage). Advisories do not apply; no downgrade needed.
- **Upstash Redis provisioned** — `calm-spider-187203.upstash.io` (EU). `UPSTASH_REDIS_REST_URL` + `UPSTASH_REDIS_REST_TOKEN` set on both projects × Production + Preview. Distributed rate limiting active on next deploy.
- **Admin ACL wired** — `ADMIN_EMAILS=kouril.lukas@gmail.com` and `OWNER_EMAIL=kouril.lukas@gmail.com` set on both projects × Production + Preview. Owner still needs to log in and verify `/dev` gates (moved to the launch-prerequisites list).
- **Sentry live** — DSN set on both projects × Production + Preview. EU region (Germany). SDK is DSN-gated + lazy-loaded, `sendDefaultPii: false`, 10% trace sampling, no Session Replay.
- **PostHog live** — `phc_…` project key set on both projects × Production + Preview. EU Cloud, reverse-proxied through the same-origin `/ingest` path (`vercel.json` rewrites), `respect_dnt: true`, `person_profiles: 'identified_only'`, no autocapture of sensitive fields.
- **Supabase leaked-password protection enabled** — Auth advisor no longer flags the WARN.
- **`studyshark-app` Preview-env parity fixed** — added the 4 shared credentials (`SESSION_SECRET`, `SUPABASE_SERVICE_ROLE_KEY`, `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`) that were previously Production-only, so preview deploys of StudyShark now work.
- **AI features shelved, then removed** — the Sharkira hint was hidden on 2026-07-28; its wiring, translations and request path left this repository on 2026-09-24 (see "devShark ships no AI feature" above). The empty `question_hints`, `question_explanations` and `ai_generation_budget` tables remain.
- **`.claude/settings.local.json` untracked and gitignored** — per-machine Bash-permission allowlist stays local; the tracked copy was removed from history-going-forward.
- **35 stale `claude/*` branches cleaned up** — 24 fully-merged branches deleted directly; 11 with unique commits reviewed as draft PRs #92–#102, all closed by the owner as superseded and their branches deleted. Zero `claude/*` branches remain on the remote.
- **Repo audited for public visibility** — no committed secrets in HEAD or history (no service-role key, session secret, Upstash token, OpenAI key, or Sentry auth token has ever been in the repo). Personal email and Supabase project ref are intentionally left in tracked files: the email is already the public git-author, and the project ref is already exposed in the deployed JS bundle by design.
- **Migration 024 applied** — `schema_024_daily_habit_freeze_badges_cards` recorded at `20260728211046`. Six new tables live (`user_streak_config`, `user_streak_freezes`, `user_badges`, `user_cards`, `daily_queue_completions`, `question_hints`), the freeze-aware `record_verified_quiz_result_v2` and spaced-mastery `complete_verified_roadmap_attempt` bodies replaced, `delete_user_data` extended to cover the new tables. Both `/api/health` return HTTP 200 with `rateLimiter: configured`.
- **Supabase Auth redirect allowlist fixed** — Site URL set to `https://devshark.app`; Redirect URLs list includes both product domains and `http://localhost:5173/**`. Signing in on either product now lands the user back on the same domain (the client's `redirectTo: window.location.origin` is respected).
- **Google OAuth enabled** — provider is configured in Supabase Authentication → Providers and sign-in works end-to-end. The Google consent screen currently reads "to continue to rvlybcjdpafwyeuojvhl.supabase.co" because Google shows the domain of the redirect_uri, which is Supabase's callback host. Changing that string to `devshark.app` needs a Supabase Custom Domain (paid tier); see "Brand the Google OAuth consent screen" under Brand and launch assets.
- **Admin ACL verification dropped from the tracked list** — env vars are wired (`ADMIN_EMAILS=kouril.lukas@gmail.com`, `OWNER_EMAIL=kouril.lukas@gmail.com`); owner will confirm end-to-end during normal use rather than as a launch gate.
- **Legal / privacy review dropped from the tracked list** — owner chose not to gate launch on it. Left as an implicit obligation: the terms/privacy page must eventually name Supabase, Vercel, Upstash, Sentry (EU), and PostHog (EU) with their retention, and reflect any consent mode chosen. Not enforced by this file going forward.
- **Daily retention purge scheduled** — `pg_cron` extension enabled; job `purge_expired_learning_data_daily` runs `SELECT public.purge_expired_learning_data();` at `07 03 * * *` UTC. Uses the function's default retention (delete `roadmap_attempts` with `expires_at` older than 90 days and `quiz_submissions` older than 90 days). Reschedule or change retention via `cron.alter_job` / `cron.unschedule` when the policy changes.
- **Lighthouse baseline captured** — `docs/perf/lighthouse-baseline-2026-07-28/` holds HTML + JSON for devShark at mobile + desktop preset plus a `README.md` summary (the run also measured StudyShark; those reports left with it on 2026-09-24). Category scores: A11y 96, Best Practices 96, SEO 91 across the board. Performance: 66 mobile, 33 desktop. The two follow-up items above (desktop CLS ≈ 0.96, mobile LCP > 5 s) are the concrete regressions found and are now tracked in the launch-assets list.

## Developer tooling

- [ ] **Install and initialize RTK (`rtk-ai/rtk`)** — RTK could not be set up from the Claude Code web session because its GitHub download host is outside the session's network allowlist (`github.com/rtk-ai/rtk` and its release binaries return HTTP 403). Set it up locally at home with the commands below, then enable it for this repository following `rtk --help` / the RTK docs (the exact per-repo command isn't documented here because the tool wouldn't install in the sandbox). `[imp:2]` `[owner:me]` `[time:20m]` `[kind:setup]`
- [ ] **Enable Vercel Web Analytics for this project** — turn on Web Analytics in the Vercel project so OwnDashboard's project Overview shows visitors and page views (it reads them via the Vercel API, matched by this repository). `[imp:2]` `[owner:me]` `[time:15m]` `[kind:setup]`

```sh
curl -fsSL https://raw.githubusercontent.com/rtk-ai/rtk/refs/heads/master/install.sh | sh
rtk init --global
```


### Free-tool implementation — 2026-09-11

- [x] **Add the developer-quality tools** — MSW client failure/retry tests, Knip reviewed inventory, Astryx Storybook, bilingual static topic guides and metadata, activation instrumentation, fluid public typography, performance/bundle checks and CSP regression checks. See `docs/quality/README.md`. `[imp:3]` `[owner:ai]` `[time:4h]` `[kind:setup]`
- [ ] **Verify the activation events in PostHog and save the funnel** — see `docs/quality/activation.md`; account access was not available during implementation. `[imp:2]` `[owner:me]` `[time:20m]` `[kind:setup]`
- [ ] **Submit /sitemap.xml to Search Console** — in the relevant properties, when you are ready to request indexing. `[imp:2]` `[owner:me]` `[time:10m]` `[kind:setup]`
