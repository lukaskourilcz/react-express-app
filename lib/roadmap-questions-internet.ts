// The Internet roadmap questions (80): 10 levels × 8, easiest → hardest. Covers
// how the internet actually works under the hood: what it is, IP addresses, DNS,
// packets & routing, TCP/UDP, the protocol stack, the web over HTTP, ports/NAT/
// firewalls, ISPs & physical connections, and security/HTTPS/encryption.

import type { Seed } from './roadmap-build';

export const internetSeeds: Seed[] = [
  // ── Level 1 — What Is the Internet? ────────────────────────────────────
  { q: 'In one sentence, what is the internet?', opts: ['A single giant computer in the cloud', 'A company that owns all websites', 'A global network of networks that connect and exchange data', 'Another name for the web browser'], a: 2, e: 'The internet is a worldwide collection of independent networks that interconnect so any device can reach any other.', tags: ['Internet', 'Basics'] },
  { q: 'Is "the internet" the same thing as "the web"?', opts: ['Yes, they are identical', 'No, the web is one service that runs on top of the internet', 'No, the web is older than the internet', 'Yes, but only on phones'], a: 1, e: 'The internet is the underlying network; the web (websites over HTTP) is just one of many things that ride on it.', tags: ['Internet', 'Basics'] },
  { q: 'What is the relationship between the internet and the web?', opts: ['They are two names for the same thing', 'The web is one service running on the internet, alongside email, video calls, and app traffic', 'The internet is a service that runs on top of the web', 'The web is the hardware and the internet is the software'], a: 1, e: 'The internet is the underlying network; the web (HTTP sites) is just one of many services on it, together with email, streaming, games, and app data.', tags: ['Internet', 'Basics'] },
  { q: 'Does the internet have a single central computer that controls everything?', opts: ['Yes, one master server runs it', 'No, it is decentralized with no single point of control', 'Yes, owned by one company', 'Yes, located in one country'], a: 1, e: 'The internet is decentralized; it is built from many independently operated networks with no single controller.', tags: ['Internet', 'Basics'] },
  { q: 'What basic idea lets two distant computers talk to each other?', opts: ['They share the same hard drive', 'They follow shared rules called protocols', 'They must be the same brand', 'They use the same password'], a: 1, e: 'Protocols are agreed-upon rules for formatting and exchanging data so different machines can understand each other.', tags: ['Internet', 'Basics'] },
  { q: 'When you load a website, your computer is acting mainly as a:', opts: ['Client that requests data', 'Server', 'Router', 'Database'], a: 0, e: 'The device asking for data is the client; the machine that answers with the data is the server.', tags: ['Internet', 'Basics'] },
  { q: 'Roughly how is most data sent across the internet?', opts: ['As one huge continuous stream', 'Broken into small pieces called packets', 'As printed pages', 'Only as plain text'], a: 1, e: 'Data is split into small packets that travel independently and are reassembled at the destination.', tags: ['Internet', 'Basics'] },
  { q: 'Why was the internet designed to be decentralized?', opts: ['To make it slower', 'To save electricity', 'So it keeps working even if some parts fail', 'To require fewer cables'], a: 2, e: 'With no single central point, traffic can reroute around failures, making the network more resilient.', tags: ['Internet', 'Basics'] },

  // ── Level 2 — IP Addresses ─────────────────────────────────────────────
  { q: 'What is an IP address used for?', opts: ['Uniquely identifying a device on a network', 'Naming a website in words', 'Encrypting passwords', 'Storing browser history'], a: 0, e: 'An IP address is a numeric label that identifies a device so data can be delivered to it.', tags: ['Internet', 'IP'] },
  { q: 'Which of these looks like an IPv4 address?', opts: ['example.com', 'https://', '192.168.1.10', 'user@mail.com'], a: 2, e: 'IPv4 addresses are four numbers (0 to 255) separated by dots, like 192.168.1.10.', tags: ['Internet', 'IP'] },
  { q: 'Why was IPv6 created?', opts: ['To make addresses shorter', 'Because the world was running out of IPv4 addresses', 'To replace DNS', 'To block hackers'], a: 1, e: 'IPv4 has about 4 billion addresses, which ran out, so IPv6 provides a far larger pool.', tags: ['Internet', 'IP'] },
  { q: 'What is the difference between a public and a private IP address?', opts: ['Public ones are encrypted', 'Public IPs are reachable across the internet; private IPs are used inside local networks', 'Private IPs are faster', 'There is no difference'], a: 1, e: 'Private IPs (like 192.168.x.x) work only within a local network; public IPs are routable on the open internet.', tags: ['Internet', 'IP'] },
  { q: 'A device on your home Wi-Fi typically has what kind of IP address?', opts: ['A public IP unique worldwide', 'A private IP assigned by your router', 'No IP address at all', 'The same IP as the website it visits'], a: 1, e: 'Your router hands out private IPs to your devices and shares one public IP with the wider internet.', tags: ['Internet', 'IP'] },
  { q: 'What does the loopback address 127.0.0.1 (localhost) refer to?', opts: ['Your internet provider', 'The device itself', 'A default search engine', 'The nearest server'], a: 1, e: '127.0.0.1, known as localhost, always points back to the same machine you are using.', tags: ['Internet', 'IP'] },
  { q: 'Roughly how many addresses does IPv4 provide?', opts: ['About 256', 'About 4.3 billion', 'Unlimited', 'Exactly one million'], a: 1, e: 'IPv4 uses 32-bit addresses, giving roughly 4.3 billion possible values, which is no longer enough.', tags: ['Internet', 'IP'] },
  { q: 'How does a device usually get its IP address on a typical network?', opts: ['The user types it from memory', 'It is printed on the device', 'It never changes from the factory', 'Automatically via DHCP'], a: 3, e: 'DHCP (Dynamic Host Configuration Protocol) automatically assigns IP addresses to devices joining a network.', tags: ['Internet', 'IP'] },

  // ── Level 3 — DNS — The Internet's Phonebook ───────────────────────────
  { q: 'What problem does DNS solve?', opts: ['It translates human-friendly names into IP addresses', 'It speeds up downloads', 'It encrypts your traffic', 'It blocks ads'], a: 0, e: 'DNS maps names like example.com to the numeric IP addresses computers actually use to connect.', tags: ['Internet', 'DNS'] },
  { q: 'DNS is often compared to what everyday tool?', opts: ['A calculator', 'A phonebook', 'A camera', 'A clock'], a: 1, e: 'Like a phonebook maps names to phone numbers, DNS maps domain names to IP addresses.', tags: ['Internet', 'DNS'] },
  { q: 'When you visit a site, when does the DNS lookup happen?', opts: ['After the page fully loads', 'Before connecting, to find the server\'s IP address', 'Only if the site is offline', 'Never, the browser already knows'], a: 1, e: 'The browser must resolve the domain name to an IP before it can open a connection to the server.', tags: ['Internet', 'DNS'] },
  { q: 'Why do computers cache DNS results?', opts: ['To use more memory on purpose', 'To hide the website', 'To avoid repeating the same lookup and respond faster', 'To change the IP address'], a: 2, e: 'Caching stores recent lookups so repeat visits skip the full resolution process and load faster.', tags: ['Internet', 'DNS'] },
  { q: 'In the name "shop.example.com", which part is the top-level domain (TLD)?', opts: ['shop', 'example', 'com', 'www'], a: 2, e: 'The TLD is the rightmost label, like com, org, or net; example is the domain and shop is a subdomain.', tags: ['Internet', 'DNS'] },
  { q: 'What is a "subdomain"?', opts: ['A backup website', 'A prefix that creates a section under a domain, like blog.example.com', 'A type of IP address', 'A browser setting'], a: 1, e: 'A subdomain such as blog.example.com is a named division within the main example.com domain.', tags: ['Internet', 'DNS'] },
  { q: 'Roughly, how is the DNS system organized?', opts: ['As one giant flat list', 'As a distributed hierarchy of name servers', 'As a single file on your computer', 'As a printed book updated yearly'], a: 1, e: 'DNS is a distributed, hierarchical system: root servers, TLD servers, and authoritative servers each handle part of a name.', tags: ['Internet', 'DNS'] },
  { q: 'What does an "A record" in DNS do?', opts: ['Stores a password', 'Maps a domain name to an IPv4 address', 'Lists email rules', 'Blocks a domain'], a: 1, e: 'An A record points a hostname to its IPv4 address (an AAAA record does the same for IPv6).', tags: ['Internet', 'DNS'] },

  // ── Level 4 — Packets & Routing ────────────────────────────────────────
  { q: 'What is a packet?', opts: ['A whole file sent at once', 'A type of cable', 'A small chunk of data with addressing information', 'A web browser'], a: 2, e: 'A packet is a small unit of data wrapped with source and destination info so it can be routed.', tags: ['Internet', 'Routing'] },
  { q: 'Why is large data split into many packets?', opts: ['To make it harder to read', 'So pieces can travel flexibly and be re-sent if lost', 'To use more bandwidth', 'Because cables are short'], a: 1, e: 'Splitting data lets packets take different routes, recover from loss one at a time, and share the network fairly.', tags: ['Internet', 'Routing'] },
  { q: 'What is the main job of a router?', opts: ['To forward packets toward their destination', 'To store websites', 'To encrypt all traffic', 'To assign domain names'], a: 0, e: 'Routers read each packet\'s destination and forward it along the best available path toward that target.', tags: ['Internet', 'Routing'] },
  { q: 'Do all packets in one message have to take the same path?', opts: ['Yes, always the same path', 'No, they can take different routes and still arrive', 'Yes, or they are lost', 'Only if encrypted'], a: 1, e: 'Packets are routed independently and may follow different paths, getting reassembled in order at the destination.', tags: ['Internet', 'Routing'] },
  { q: 'What does each packet carry to help it reach the right place?', opts: ['A photo of the sender', 'Source and destination addresses', 'The user\'s password', 'A copy of the website'], a: 1, e: 'Packet headers include source and destination IP addresses so routers know where to send them.', tags: ['Internet', 'Routing'] },
  { q: 'What happens if a packet gets lost on the way?', opts: ['The whole internet stops', 'It can be detected and re-sent depending on the protocol', 'It is gone forever with no recovery', 'It turns into a virus'], a: 1, e: 'Reliable protocols like TCP detect missing packets and request that they be re-sent.', tags: ['Internet', 'Routing'] },
  { q: 'What does the "ping" command roughly measure?', opts: ['The bandwidth of your connection', 'Round-trip time for a packet to reach a host and return', 'The number of router hops to a host', 'How long a DNS lookup takes'], a: 1, e: 'Ping sends a small packet and times the round trip, indicating latency to a host. Bandwidth needs a throughput test, and hop counts come from traceroute.', tags: ['Internet', 'Routing'] },
  { q: 'What is "latency" on a network?', opts: ['The amount of data per second', 'The delay before data starts arriving', 'The number of devices online', 'The size of a file'], a: 1, e: 'Latency is the time it takes for data to travel from source to destination; bandwidth is how much can flow.', tags: ['Internet', 'Routing'] },

  // ── Level 5 — TCP & UDP ────────────────────────────────────────────────
  { q: 'What is TCP mainly designed to provide?', opts: ['Reliable, ordered delivery of data', 'The fastest possible speed with no checks', 'Encryption of all traffic', 'Domain name lookups'], a: 0, e: 'TCP ensures data arrives complete and in the right order, re-sending anything lost along the way.', tags: ['Internet', 'TCP/UDP'] },
  { q: 'How does UDP differ from TCP?', opts: ['UDP is slower and more reliable', 'UDP is faster but does not guarantee delivery or order', 'UDP encrypts data; TCP does not', 'They are exactly the same'], a: 1, e: 'UDP skips delivery guarantees and ordering to be faster and lower-overhead, accepting some loss.', tags: ['Internet', 'TCP/UDP'] },
  { q: 'Which protocol is usually better for live video calls or online games?', opts: ['TCP, because order matters most', 'UDP, because speed matters more than perfect delivery', 'Neither works for video', 'DNS'], a: 1, e: 'Real-time apps prefer UDP since a slightly dropped frame is better than waiting to re-send old data.', tags: ['Internet', 'TCP/UDP'] },
  { q: 'Which protocol would you expect a file download to use?', opts: ['UDP, to allow missing bytes', 'TCP, so every byte arrives correctly', 'Neither', 'Only DNS'], a: 1, e: 'Downloads need every byte intact and in order, which is exactly what TCP guarantees.', tags: ['Internet', 'TCP/UDP'] },
  { q: 'What is the TCP "three-way handshake"?', opts: ['A way to encrypt data', 'The SYN, SYN-ACK, ACK exchange that sets up a connection', 'A method of compressing files', 'A DNS lookup step'], a: 1, e: 'TCP opens a connection with a three-step handshake (SYN, SYN-ACK, ACK) before data flows.', tags: ['Internet', 'TCP/UDP'] },
  { q: 'How does TCP keep data in the correct order?', opts: ['It sends only one packet at a time', 'It relies on the user to sort them', 'It numbers packets so the receiver can reassemble them', 'It cannot keep order'], a: 2, e: 'TCP assigns sequence numbers so the receiver can reorder packets and detect any that are missing.', tags: ['Internet', 'TCP/UDP'] },
  { q: 'Why might UDP have lower overhead than TCP?', opts: ['It uses bigger packets', 'It does not track acknowledgements or re-send lost data', 'It always encrypts', 'It needs more handshakes'], a: 1, e: 'UDP is connectionless and skips acknowledgements and retransmission, reducing overhead and delay.', tags: ['Internet', 'TCP/UDP'] },
  { q: 'TCP and UDP are both examples of what kind of protocol?', opts: ['Application-layer protocols', 'Physical cables', 'Encryption standards', 'Transport-layer protocols'], a: 3, e: 'TCP and UDP operate at the transport layer, moving data between applications on different hosts.', tags: ['Internet', 'TCP/UDP'] },

  // ── Level 6 — The Protocol Stack (TCP/IP) ──────────────────────────────
  { q: 'Why is the internet organized into layers?', opts: ['So each layer handles one job and builds on the one below', 'To make it more confusing', 'To slow data down', 'Because of legal rules'], a: 0, e: 'Layering separates concerns so each layer (links, IP, transport, application) can work independently.', tags: ['Internet', 'Stack'] },
  { q: 'Which layer is responsible for addressing and routing packets between networks?', opts: ['Application layer', 'Internet (IP) layer', 'Physical layer', 'Presentation layer'], a: 1, e: 'The internet layer, using IP, handles logical addressing and routing packets across networks.', tags: ['Internet', 'Stack'] },
  { q: 'At which layer do TCP and UDP operate?', opts: ['Link layer', 'Transport layer', 'Application layer', 'Internet layer'], a: 1, e: 'TCP and UDP are transport-layer protocols that carry data between applications on hosts.', tags: ['Internet', 'Stack'] },
  { q: 'Where does HTTP sit in the TCP/IP model?', opts: ['The application layer', 'The transport layer', 'The link layer', 'The internet layer'], a: 0, e: 'HTTP is an application-layer protocol that relies on TCP beneath it to actually move the bytes.', tags: ['Internet', 'Stack'] },
  { q: 'What does "encapsulation" mean in networking?', opts: ['Deleting old data', 'Encrypting passwords', 'Each layer wrapping data with its own header before passing it down', 'Renaming a domain'], a: 2, e: 'As data moves down the stack, each layer adds a header, wrapping the data from the layer above.', tags: ['Internet', 'Stack'] },
  { q: 'Roughly, what does the link layer handle?', opts: ['Choosing which website to load', 'Moving data over the local physical connection, like Ethernet or Wi-Fi', 'Translating domain names', 'Encrypting HTTPS'], a: 1, e: 'The link layer handles data transfer over the immediate physical medium between directly connected devices.', tags: ['Internet', 'Stack'] },
  { q: 'Why can the web work the same over Wi-Fi, fiber, or mobile data?', opts: ['Because all cables are identical', 'Because upper layers do not care about the physical medium underneath', 'Because Wi-Fi copies fiber exactly', 'It actually only works over fiber'], a: 1, e: 'Layering hides the physical details, so the same IP and HTTP work regardless of the underlying connection.', tags: ['Internet', 'Stack'] },
  { q: 'In what order does data typically pass through the layers when being sent?', opts: ['Physical to application', 'Application down to physical, then back up at the receiver', 'Transport only', 'It skips all layers'], a: 1, e: 'Outgoing data moves from the application layer down to physical, then back up the stack at the receiver.', tags: ['Internet', 'Stack'] },

  // ── Level 7 — The Web over HTTP ────────────────────────────────────────
  { q: 'What is HTTP?', opts: ['The protocol browsers use to request and receive web pages', 'A type of cable', 'A programming language', 'A search engine'], a: 0, e: 'HTTP is the application-layer protocol that defines how clients request resources and servers respond.', tags: ['Internet', 'HTTP'] },
  { q: 'In a web request, who is the client and who is the server?', opts: ['The server asks; the browser answers', 'The browser asks; the web server answers', 'Both are servers', 'Neither has a role'], a: 1, e: 'The browser is the client sending requests; the web server responds with the requested content.', tags: ['Internet', 'HTTP'] },
  { q: 'What does an HTTP GET request do?', opts: ['Deletes a page', 'Restarts the server', 'Asks the server to send a resource', 'Encrypts the site'], a: 2, e: 'GET requests retrieve a resource, such as an HTML page or image, from the server.', tags: ['Internet', 'HTTP'] },
  { q: 'What does the HTTP status code 404 mean?', opts: ['Success', 'The requested resource was not found', 'The server is redirecting you', 'Access forbidden'], a: 1, e: '404 means the server could not find the requested resource at that URL.', tags: ['Internet', 'HTTP'] },
  { q: 'What does the "S" in HTTPS add to HTTP?', opts: ['Speed', 'Security through TLS encryption', 'Storage', 'Streaming'], a: 1, e: 'HTTPS layers TLS encryption on HTTP so traffic is protected from eavesdropping and tampering.', tags: ['Internet', 'HTTP'] },
  { q: 'Why is plain HTTP often described as "stateless"?', opts: ['It cannot send data', 'Each request is independent and the server does not remember previous ones by default', 'It only works once', 'It stores everything forever'], a: 1, e: 'HTTP itself keeps no memory between requests; state is added with cookies or tokens on top.', tags: ['Internet', 'HTTP'] },
  { q: 'What is in a URL like https://example.com/page?', opts: ['Only the IP address', 'A scheme, a host (domain), and a path to a resource', 'A password', 'Just random text'], a: 1, e: 'A URL combines the scheme (https), the host (example.com), and the path (/page) to locate a resource.', tags: ['Internet', 'HTTP'] },
  { q: 'What role do HTTP headers play in a request or response?', opts: ['They are the page content', 'They store the database', 'They replace the URL', 'They carry metadata like content type, length, and caching info'], a: 3, e: 'Headers carry metadata about the message, such as content type, length, cookies, and caching directives.', tags: ['Internet', 'HTTP'] },

  // ── Level 8 — Ports, NAT & Firewalls ───────────────────────────────────
  { q: 'What is a network port used for?', opts: ['Identifying which application or service traffic is meant for', 'Plugging in a monitor', 'Storing files', 'Encrypting data'], a: 0, e: 'A port number tells a device which application or service should receive a given piece of traffic.', tags: ['Internet', 'Ports/NAT'] },
  { q: 'Which port does HTTPS use by default?', opts: ['21', '80', '443', '25'], a: 2, e: 'HTTPS uses port 443 by default, while plain HTTP uses port 80.', tags: ['Internet', 'Ports/NAT'] },
  { q: 'What does NAT (Network Address Translation) do?', opts: ['Encrypts all packets', 'Speeds up DNS', 'Lets many private devices share one public IP address', 'Blocks all websites'], a: 2, e: 'NAT maps many internal private IPs to a single public IP, sharing one address across a network.', tags: ['Internet', 'Ports/NAT'] },
  { q: 'Thanks to NAT, how many public IP addresses does a typical home usually need?', opts: ['One per device', 'Often just one for the whole household', 'Zero', 'Exactly ten'], a: 1, e: 'NAT lets all home devices share a single public IP provided by the ISP, conserving addresses.', tags: ['Internet', 'Ports/NAT'] },
  { q: 'What is the main purpose of a firewall?', opts: ['To speed up the connection', 'To assign IP addresses', 'To store passwords', 'To filter traffic and block unwanted or unsafe connections'], a: 3, e: 'A firewall enforces rules about which traffic is allowed in or out, blocking unwanted connections.', tags: ['Internet', 'Ports/NAT'] },
  { q: 'How does a server know to send a web page back to the right program on your device?', opts: ['It guesses randomly', 'It uses the combination of your IP address and port number', 'It uses your username', 'It uses the screen size'], a: 1, e: 'The IP address plus port number identifies the exact application connection that should receive the response.', tags: ['Internet', 'Ports/NAT'] },
  { q: 'Why are ports 0 to 1023 called "well-known" ports?', opts: ['They are always blocked', 'They are reserved for common standard services like HTTP and SSH', 'They are the fastest', 'They are only for games'], a: 1, e: 'Well-known ports are reserved by convention for standard services, such as 80 for HTTP and 22 for SSH.', tags: ['Internet', 'Ports/NAT'] },
  { q: 'What does "port forwarding" on a router do?', opts: ['Closes all ports', 'Directs incoming traffic on a port to a specific internal device', 'Changes your public IP', 'Encrypts the network'], a: 1, e: 'Port forwarding routes traffic arriving on a chosen port to a particular device behind the router.', tags: ['Internet', 'Ports/NAT'] },

  // ── Level 9 — ISPs & Physical Connections ──────────────────────────────
  { q: 'What is an ISP?', opts: ['A company that provides your access to the internet', 'A web browser', 'A type of website', 'A security protocol'], a: 0, e: 'An Internet Service Provider connects your home or device to the rest of the internet.', tags: ['Internet', 'ISP'] },
  { q: 'How does most international internet traffic physically travel between continents?', opts: ['Only by satellite', 'Through the air as radio waves', 'Through undersea fiber-optic cables', 'By copper phone lines only'], a: 2, e: 'The vast majority of intercontinental data flows through undersea fiber-optic cables on the ocean floor.', tags: ['Internet', 'ISP'] },
  { q: 'What carries data fastest over long distances?', opts: ['Copper wire', 'Fiber-optic cable using light', 'Wi-Fi', 'Bluetooth'], a: 1, e: 'Fiber-optic cables transmit data as pulses of light, offering very high speed and bandwidth over long distances.', tags: ['Internet', 'ISP'] },
  { q: 'What is "bandwidth"?', opts: ['The delay before data arrives', 'The maximum amount of data that can flow per second', 'The number of cables', 'The age of a network'], a: 1, e: 'Bandwidth is the capacity of a connection, how much data it can carry per second, distinct from latency.', tags: ['Internet', 'ISP'] },
  { q: 'What is the role of an Internet Exchange Point (IXP)?', opts: ['It hosts all websites', 'It is where different networks connect to exchange traffic', 'It assigns domain names', 'It encrypts the internet'], a: 1, e: 'IXPs are physical locations where ISPs and networks interconnect to exchange traffic efficiently.', tags: ['Internet', 'ISP'] },
  { q: 'When you send data to a faraway website, your traffic usually:', opts: ['Travels in a single hop straight to the destination server', 'Passes through many routers and networks along the way', 'Stays inside your ISP\'s network the whole way', 'Uses a dedicated cable reserved just for your connection'], a: 1, e: 'Data typically hops across multiple routers and provider networks (visible with traceroute) before reaching a distant destination.', tags: ['Internet', 'ISP'] },
  { q: 'Why might satellite internet have higher latency than fiber?', opts: ['Satellites are slower computers', 'Signals must travel a long distance up to orbit and back', 'Fiber uses radio waves', 'Satellites have no antennas'], a: 1, e: 'Sending signals up to a satellite and back adds distance, increasing the round-trip delay.', tags: ['Internet', 'ISP'] },
  { q: 'What does the "last mile" refer to in internet connections?', opts: ['The final website you visit', 'The slowest server', 'The end of a cable\'s life', 'The final link connecting the provider network to your home or building'], a: 3, e: 'The last mile is the final stretch of infrastructure connecting the broader network to the end user.', tags: ['Internet', 'ISP'] },

  // ── Level 10 — Security, HTTPS & Encryption ────────────────────────────
  { q: 'What does encryption do to your data?', opts: ['Scrambles it so only the intended party can read it', 'Deletes it', 'Makes it load faster', 'Translates it to another language'], a: 0, e: 'Encryption transforms data into an unreadable form that only someone with the key can decode.', tags: ['Internet', 'Security'] },
  { q: 'What does HTTPS protect against?', opts: ['Slow websites', 'Eavesdropping and tampering with data in transit', 'Running out of storage', 'Typing mistakes'], a: 1, e: 'HTTPS encrypts traffic so attackers between you and the server cannot read or alter it.', tags: ['Internet', 'Security'] },
  { q: 'What protocol provides the encryption used by HTTPS?', opts: ['DNS', 'FTP', 'TLS (formerly SSL)', 'DHCP'], a: 2, e: 'HTTPS relies on TLS (the successor to SSL) to encrypt and secure the connection.', tags: ['Internet', 'Security'] },
  { q: 'What is the main purpose of a website\'s TLS certificate?', opts: ['To make the page colorful', 'To prove the server\'s identity and enable encryption', 'To store user passwords', 'To speed up loading'], a: 1, e: 'A certificate verifies that the server really is who it claims to be and enables a secure encrypted session.', tags: ['Internet', 'Security'] },
  { q: 'In public-key cryptography, how are the keys used?', opts: ['One secret key shared by everyone', 'A public key encrypts and a matching private key decrypts', 'No keys are needed', 'The same key is posted publicly'], a: 1, e: 'A public key (shared openly) and a private key (kept secret) form a pair; what one locks, the other unlocks.', tags: ['Internet', 'Security'] },
  { q: 'What is a "man-in-the-middle" attack?', opts: ['A slow server', 'An attacker secretly intercepting traffic between two parties', 'A type of firewall', 'A backup system'], a: 1, e: 'In a man-in-the-middle attack, someone sits between you and the server to read or alter the traffic.', tags: ['Internet', 'Security'] },
  { q: 'Why is a VPN sometimes used?', opts: ['To delete cookies', 'To increase bandwidth limits', 'To replace your browser', 'To encrypt your traffic and route it through another server'], a: 3, e: 'A VPN encrypts your connection and tunnels it through a remote server, hiding traffic from local observers.', tags: ['Internet', 'Security'] },
  { q: 'When you see a padlock icon in the browser address bar, what does it mainly indicate?', opts: ['The site is owned by Google', 'The connection is encrypted with HTTPS', 'The site has no ads', 'The page loaded quickly'], a: 1, e: 'The padlock shows the connection uses HTTPS encryption; it does not by itself guarantee the site is trustworthy.', tags: ['Internet', 'Security'] },
];
