// CSS roadmap questions (72): 9 levels × 8, easiest → hardest. Deliberately
// understanding-first — the goal is to grasp how CSS works (the cascade,
// applying classes, the box model, layout, custom properties) rather than
// memorising every property. The last three levels are a Tailwind section
// (how utility-first differs from writing CSS) that also covers why component
// frameworks reach for CSS-in-JS. See lib/roadmap.ts for the level titles.

import type { Seed } from './roadmap-build';

export const cssSeeds: Seed[] = [
  // ── Level 1 — How CSS Works ─────────────────────────────────────────────
  { q: 'What is the main job of CSS?', opts: ['Structuring content', 'Controlling how content looks and is laid out', 'Adding interactivity', 'Storing data'], a: 1, e: 'HTML is structure, JavaScript is behaviour, and CSS is presentation — colours, spacing, and layout.', tags: ['CSS', 'Basics'] },
  { q: 'Why keep CSS separate from HTML instead of styling inline everywhere?', opts: ['It loads faster only', 'Separation of concerns: reuse styles across the page and restyle without touching markup', 'It is required by browsers', 'It makes HTML invalid otherwise'], a: 1, e: 'Separating style from structure lets one stylesheet style many elements and pages, and you can change the look without editing the HTML.', tags: ['CSS', 'Basics'] },
  { q: 'What does the "cascade" actually decide?', opts: ['The animation order', 'Which rule wins when several rules target the same element', 'How files download', 'The HTML nesting'], a: 1, e: 'The cascade resolves conflicts — when multiple rules apply, it picks the winner.', tags: ['CSS', 'Basics'] },
  { q: 'Which factors decide which conflicting rule wins?', opts: ['File size and name', 'Origin/importance, then specificity, then source order', 'Alphabetical order', 'Random choice'], a: 1, e: 'The browser weighs importance first, then how specific the selector is, then which rule came last.', tags: ['CSS', 'Basics'] },
  { q: 'Some properties (like `color`) are inherited. What does that mean?', opts: ['They cannot be changed', 'Children take the value from their parent unless overridden', 'They only work on the body', 'They need JavaScript'], a: 1, e: 'Inherited properties flow down the tree, so setting text colour on a container affects its children by default.', tags: ['CSS', 'Basics'] },
  { q: 'You want the same look on 20 buttons. The maintainable way is to:', opts: ['Style each one inline', 'Give them a shared class and write one rule for that class', 'Copy the CSS 20 times', 'Use 20 ids'], a: 1, e: 'A shared class means one rule styles them all — change it once, every button updates.', tags: ['CSS', 'Basics'] },
  { q: 'Why are inline `style="..."` attributes usually discouraged for app styling?', opts: ['They are slower to parse', 'They can’t be reused, mix concerns, and are hard to override', 'They are not supported', 'They break HTML'], a: 1, e: 'Inline styles apply to one element only, duplicate intent, and have high precedence that’s awkward to override later.', tags: ['CSS', 'Basics'] },
  { q: 'Two rules set the same property with equal specificity. Which wins?', opts: ['The first one', 'The one written later in the source', 'Neither applies', 'The shorter selector'], a: 1, e: 'With equal specificity, later declarations override earlier ones — order matters.', tags: ['CSS', 'Basics'] },

  // ── Level 2 — Selectors & Applying Classes ──────────────────────────────
  { q: 'How do you apply a reusable style to an element?', opts: ['Write it in JavaScript', 'Give the element a class and target `.class` in CSS', 'Use a comment', 'Rename the tag'], a: 1, e: 'Classes are the everyday way to attach styles: add class="x" and write a `.x { }` rule.', tags: ['CSS', 'Selectors'] },
  { q: 'What does the selector `.btn` match?', opts: ['The <btn> tag', 'Elements with id "btn"', 'Elements with class "btn"', 'The first button only'], a: 2, e: 'A leading dot is a class selector — it matches every element with that class.', tags: ['CSS', 'Selectors'] },
  { q: 'Why prefer classes over ids for styling?', opts: ['Ids are deprecated', 'Classes are reusable on many elements; ids must be unique and are very specific', 'Ids can’t be styled', 'Classes load faster'], a: 1, e: 'Classes can be shared and are easy to override; ids are unique and their high specificity causes override headaches.', tags: ['CSS', 'Selectors'] },
  { q: 'An element with `class="card featured"` lets you:', opts: ['Only use one class', 'Compose styles from several class rules at once', 'Break the cascade', 'Override JavaScript'], a: 1, e: 'Multiple classes combine — a base "card" plus a "featured" modifier is a common, composable pattern.', tags: ['CSS', 'Selectors'] },
  { q: 'Roughly, specificity from lowest to highest is:', opts: ['id < class < type', 'type < class < id < inline', 'class < type < id', 'they are all equal'], a: 1, e: 'A tag selector is weakest, then classes, then ids, then inline styles — knowing the order explains override surprises.', tags: ['CSS', 'Selectors'] },
  { q: 'Why avoid reaching for `!important`?', opts: ['It’s invalid CSS', 'It overrides the normal cascade and is itself hard to override later', 'It only works once', 'It slows the page'], a: 1, e: '!important wins regardless of specificity, so it tends to start an escalating war of !importants. Use it sparingly.', tags: ['CSS', 'Selectors'] },
  { q: 'The descendant selector `.card p` targets:', opts: ['A <p> next to .card', 'Any <p> inside an element with class "card"', '.card inside a <p>', 'Paragraphs with class "card"'], a: 1, e: 'A space means "descendant", so the rule is scoped to paragraphs that live inside .card.', tags: ['CSS', 'Selectors'] },
  { q: 'Your button rule "should" apply but the button looks wrong. The most likely cause is:', opts: ['A browser bug', 'A more specific (or later) rule is overriding it', 'CSS is disabled', 'The class name is too long'], a: 1, e: 'Most "my CSS isn’t working" cases are the cascade — another rule with higher specificity or later order wins.', tags: ['CSS', 'Selectors'] },

  // ── Level 3 — The Box Model & Spacing ───────────────────────────────────
  { q: 'What is the conceptual idea of the box model?', opts: ['Everything is a circle', 'Every element is a rectangular box: content + padding + border + margin', 'Only divs have boxes', 'Boxes are just for images'], a: 1, e: 'Layout is boxes — each element’s footprint is its content wrapped by padding, then border, then margin.', tags: ['CSS', 'Box Model'] },
  { q: 'Padding vs margin:', opts: ['They are identical', 'Padding is space inside the border; margin is space outside, between elements', 'Margin is inside, padding is outside', 'Both are borders'], a: 1, e: 'Padding pushes the content away from the border; margin pushes other elements away.', tags: ['CSS', 'Box Model'] },
  { q: 'Why do people set `box-sizing: border-box`?', opts: ['To remove borders', 'So an element’s `width` includes its padding and border, making sizing predictable', 'To center everything', 'To enable flexbox'], a: 1, e: 'With border-box, a width of 200px stays 200px even with padding/border — far easier to reason about.', tags: ['CSS', 'Box Model'] },
  { q: 'With the default box model, why can an element end up wider than the `width` you set?', opts: ['A browser bug', 'Padding and border are added on top of the content width', 'Margins are doubled', 'Width is ignored'], a: 1, e: 'In the default content-box, width sizes only the content; padding and border add to the visible size.', tags: ['CSS', 'Box Model'] },
  { q: 'What is margin collapsing?', opts: ['Margins disappear on scroll', 'Adjacent vertical margins merge into the larger of the two', 'Margins turn into padding', 'Margins become negative'], a: 1, e: 'Stacked block margins don’t add up — they collapse to the larger value, which surprises many beginners.', tags: ['CSS', 'Box Model'] },
  { q: 'A classic way to horizontally center a fixed-width block is:', opts: ['text-align: center', 'margin: 0 auto', 'padding: auto', 'float: center'], a: 1, e: 'auto left/right margins split the leftover space, centering the block.', tags: ['CSS', 'Box Model'] },
  { q: 'To separate two elements, the idiomatic tool is:', opts: ['An empty spacer element', 'Margin (or `gap` in flex/grid)', 'Many <br> tags', 'A transparent image'], a: 1, e: 'Spacing is a styling concern — use margin or gap, not extra markup.', tags: ['CSS', 'Box Model'] },
  { q: 'Why is understanding the box model so important?', opts: ['It controls colours', 'Almost all layout and spacing bugs come from how these boxes size and stack', 'It’s needed for animations', 'It defines fonts'], a: 1, e: 'Sizing, alignment, and spacing all flow from the box model — it’s the foundation of layout.', tags: ['CSS', 'Box Model'] },

  // ── Level 4 — Layout with Flexbox ───────────────────────────────────────
  { q: 'What problem does Flexbox solve?', opts: ['Storing data', 'Arranging items in a single direction and distributing space between them', 'Animating colours', 'Loading fonts'], a: 1, e: 'Flexbox is a one-dimensional layout model: lay items out in a row or a column and share space.', tags: ['CSS', 'Layout'] },
  { q: '`display: flex` on a container affects:', opts: ['Its parent', 'Its direct children (the flex items)', 'The whole page', 'Only text'], a: 1, e: 'The container becomes a flex context and its immediate children become flex items.', tags: ['CSS', 'Layout'] },
  { q: 'What does `justify-content` control in flexbox?', opts: ['Cross-axis alignment', 'Distribution of items along the main axis', 'Font weight', 'Z-order'], a: 1, e: 'justify-content spaces items along the main axis (e.g. space-between, center).', tags: ['CSS', 'Layout'] },
  { q: 'What does `align-items` control?', opts: ['Main-axis spacing', 'Alignment of items on the cross axis', 'The number of columns', 'Text colour'], a: 1, e: 'align-items aligns items across the axis perpendicular to the main one (e.g. center vertically in a row).', tags: ['CSS', 'Layout'] },
  { q: '`flex-direction` lets you:', opts: ['Change colours', 'Switch the main axis between a row and a column', 'Set the gap', 'Hide items'], a: 1, e: 'flex-direction: row | column flips the layout direction, which also swaps what the main/cross axes mean.', tags: ['CSS', 'Layout'] },
  { q: 'The cleanest way to put even spacing between flex items is:', opts: ['Margins on every item', 'The `gap` property', 'Extra spacer divs', '&nbsp; entities'], a: 1, e: 'gap adds consistent spacing between items without fiddly per-item margins.', tags: ['CSS', 'Layout'] },
  { q: '`flex: 1` on items roughly means:', opts: ['Hide them', 'Grow to share the available space equally', 'Fix their width to 1px', 'Make them bold'], a: 1, e: 'flex: 1 lets items grow and split leftover space — handy for equal columns or a stretchy element.', tags: ['CSS', 'Layout'] },
  { q: 'When is Flexbox the natural choice?', opts: ['For a full 2D page grid', 'For laying items out in a single row or column (a navbar, a toolbar)', 'For storing state', 'For database joins'], a: 1, e: 'Flexbox shines for one-dimensional arrangements; reach for Grid when you need rows AND columns together.', tags: ['CSS', 'Layout'] },

  // ── Level 5 — Layout with Grid & Responsive Design ──────────────────────
  { q: 'CSS Grid is best suited for:', opts: ['One-dimensional lines only', 'Two-dimensional layouts with rows and columns', 'Styling text', 'Network requests'], a: 1, e: 'Grid lays content out in two dimensions at once — ideal for page and card layouts.', tags: ['CSS', 'Layout'] },
  { q: 'Flexbox vs Grid, in one line:', opts: ['They are the same', 'Flex is 1D (a line); Grid is 2D (rows and columns)', 'Grid is older', 'Flex only works on text'], a: 1, e: 'Pick Flex for a single direction, Grid when you’re placing things across both axes.', tags: ['CSS', 'Layout'] },
  { q: 'What is responsive design?', opts: ['A fast-loading site', 'A layout that adapts to different screen sizes', 'Server-side rendering', 'A CSS framework'], a: 1, e: 'Responsive design means the same page reflows to look good on phones, tablets, and desktops.', tags: ['CSS', 'Responsive'] },
  { q: 'A media query (`@media`) lets you:', opts: ['Play audio', 'Apply styles conditionally based on viewport size or features', 'Import fonts', 'Define variables'], a: 1, e: 'Media queries gate CSS on conditions like screen width, enabling breakpoints.', tags: ['CSS', 'Responsive'] },
  { q: '"Mobile-first" CSS means:', opts: ['Only support phones', 'Write base styles for small screens, then enhance upward with min-width queries', 'Hide content on mobile', 'Use bigger fonts'], a: 1, e: 'Mobile-first starts simple for small screens and layers on complexity as space grows.', tags: ['CSS', 'Responsive'] },
  { q: 'Why favour relative units (%, rem, fr) over fixed px for responsive layouts?', opts: ['They are shorter', 'They scale with their context instead of being locked to one size', 'They are required', 'px doesn’t work on mobile'], a: 1, e: 'Relative units flex with the screen and root font-size, so the layout adapts instead of breaking.', tags: ['CSS', 'Responsive'] },
  { q: 'How can a grid become responsive WITHOUT writing media queries?', opts: ['It can’t', 'With `repeat(auto-fit, minmax(...))` so columns wrap to fit', 'By using ids', 'By disabling CSS'], a: 1, e: 'auto-fit/auto-fill with minmax lets the grid add or drop columns based on available width automatically.', tags: ['CSS', 'Responsive'] },
  { q: 'Which approach scales best across many unknown screen sizes?', opts: ['Hard-coded pixel widths per device', 'Fluid/relative sizing plus a few breakpoints', 'One fixed desktop layout', 'Inline styles'], a: 1, e: 'Fluid layouts handle the in-between sizes; breakpoints make bigger structural changes where needed.', tags: ['CSS', 'Responsive'] },

  // ── Level 6 — Custom Properties (--variables) & Reuse ───────────────────
  { q: 'How do you declare a CSS custom property (variable)?', opts: ['var name = value', '--name: value;', '$name: value;', '@name: value;'], a: 1, e: 'Custom properties use a double-dash name, e.g. `--brand: #2d7a2d;`.', tags: ['CSS', 'Variables'] },
  { q: 'How do you use a CSS variable’s value?', opts: ['use(--name)', 'var(--name)', '$name', '{name}'], a: 1, e: 'You read a custom property with the var() function: `color: var(--brand);`.', tags: ['CSS', 'Variables'] },
  { q: 'The main reason to use CSS variables is:', opts: ['They’re faster', 'Define a value once and reuse it everywhere, changing it in one place', 'They reduce HTML', 'They’re required for flexbox'], a: 1, e: 'Variables centralise values (colours, spacing) so a single edit updates the whole UI.', tags: ['CSS', 'Variables'] },
  { q: 'Defining variables on `:root` is common because:', opts: ['It’s the only place allowed', 'They cascade/inherit, so they’re available globally to all descendants', 'It disables the cascade', 'It hides them'], a: 1, e: ':root holds document-wide tokens; because custom properties inherit, everything below can use them.', tags: ['CSS', 'Variables'] },
  { q: 'A very practical use of CSS variables is:', opts: ['Sorting data', 'Theming — e.g. swapping light/dark by changing variable values', 'Routing', 'Validation'], a: 1, e: 'Flip a handful of variables (or toggle a class on :root) and the whole theme changes.', tags: ['CSS', 'Variables'] },
  { q: 'Unlike Sass/preprocessor variables, CSS custom properties:', opts: ['Are compiled away', 'Are live in the browser, cascade, and can be read/changed at runtime', 'Only work in Chrome', 'Can’t hold colours'], a: 1, e: 'Preprocessor variables vanish at build time; CSS variables exist at runtime and respond to the cascade and JS.', tags: ['CSS', 'Variables'] },
  { q: 'Can JavaScript change a CSS variable at runtime?', opts: ['No, never', 'Yes — setting it (e.g. on :root) instantly updates everything using it', 'Only with a plugin', 'Only on load'], a: 1, e: 'Because they’re live, JS can update a custom property and every var() reference re-renders — great for dynamic theming.', tags: ['CSS', 'Variables'] },
  { q: 'Why centralise "design tokens" (colours, spacing) in variables?', opts: ['To write more CSS', 'Consistency across the UI and easy global changes', 'To avoid classes', 'To slow rendering'], a: 1, e: 'Shared tokens keep the design coherent and let you re-skin the app by editing a small set of values.', tags: ['CSS', 'Variables'] },

  // ── Level 7 — Tailwind: Utility-First Basics ────────────────────────────
  { q: 'What is Tailwind CSS?', opts: ['A JavaScript framework', 'A utility-first CSS framework — small single-purpose classes you compose in markup', 'A database', 'A browser'], a: 1, e: 'Tailwind ships tiny utility classes (like `p-4`, `flex`, `text-center`) that you combine directly on elements.', tags: ['CSS', 'Tailwind'] },
  { q: 'How does Tailwind differ from writing your own CSS?', opts: ['It uses a different language', 'You style by adding utility classes in the HTML instead of writing custom rule sets in a stylesheet', 'It removes the cascade', 'It only works with React'], a: 1, e: 'Instead of naming things and writing rules, you apply prebuilt utilities inline — the styling lives with the markup.', tags: ['CSS', 'Tailwind'] },
  { q: '`class="p-4 text-center"` in Tailwind roughly means:', opts: ['Nothing', 'Apply padding and center the text — each utility maps to a CSS property', 'Define two variables', 'Import a file'], a: 1, e: 'Utilities are thin wrappers over CSS: p-4 → padding, text-center → text-align: center.', tags: ['CSS', 'Tailwind'] },
  { q: 'A commonly cited benefit of utility-first CSS is:', opts: ['No CSS is downloaded', 'No inventing class names or jumping to a CSS file, and a consistent spacing/colour scale', 'It removes HTML', 'It needs no build step ever'], a: 1, e: 'You stop bikeshedding names and context-switching; values come from a shared scale, keeping things consistent.', tags: ['CSS', 'Tailwind'] },
  { q: 'A common criticism of utility-first CSS is:', opts: ['It’s too slow', 'Markup gets verbose, with long lists of classes on elements', 'It can’t do layout', 'It breaks accessibility'], a: 1, e: 'Lots of utilities on one element can look noisy — teams mitigate it by extracting components.', tags: ['CSS', 'Tailwind'] },
  { q: 'Where do Tailwind utility values (spacing, colours) come from?', opts: ['Random', 'A configured design scale, so the UI stays consistent', 'The browser default', 'Inline styles'], a: 1, e: 'Tailwind’s utilities are generated from a theme/config, giving a consistent, constrained set of values.', tags: ['CSS', 'Tailwind'] },
  { q: 'Does using Tailwind mean you don’t need to understand CSS?', opts: ['Yes, it replaces CSS', 'No — utilities are wrappers over CSS properties, so you still need the box model, flexbox, layout, etc.', 'Only for animations', 'Only on mobile'], a: 1, e: 'Tailwind changes how you author CSS, not what CSS does — the underlying concepts still apply.', tags: ['CSS', 'Tailwind'] },
  { q: 'How does Tailwind keep the shipped CSS small?', opts: ['It minifies HTML', 'It generates only the utility classes you actually use (unused ones are purged)', 'It deletes your CSS', 'It uses no classes'], a: 1, e: 'A build step scans your markup and emits just the utilities in use, so the final CSS stays lean.', tags: ['CSS', 'Tailwind'] },

  // ── Level 8 — Tailwind: Responsive, States & Customization ──────────────
  { q: 'A responsive utility like `md:flex` means:', opts: ['Always flex', 'Apply `flex` at the md breakpoint and larger (mobile-first prefixes)', 'Flex only on mobile', 'Disable flex'], a: 1, e: 'Tailwind is mobile-first: unprefixed utilities apply everywhere; a breakpoint prefix applies from that width up.', tags: ['CSS', 'Tailwind'] },
  { q: 'A state variant like `hover:bg-blue-500` does what?', opts: ['Always blue', 'Applies the background on hover (a pseudo-class via prefix)', 'Removes the background', 'Errors out'], a: 1, e: 'Prefixes like hover:, focus:, and disabled: map to pseudo-classes, so you handle states inline.', tags: ['CSS', 'Tailwind'] },
  { q: 'To avoid repeating the same long class list on many elements you can:', opts: ['Nothing, you must repeat it', 'Extract a component in your framework, or use `@apply` to bundle utilities into a class', 'Use !important', 'Inline styles'], a: 1, e: 'Components (React/Vue/etc.) or @apply let you name a reusable bundle instead of duplicating utilities.', tags: ['CSS', 'Tailwind'] },
  { q: 'What does Tailwind’s `@apply` do?', opts: ['Applies JavaScript', 'Inlines utility classes into a custom CSS rule — a bridge back to traditional CSS', 'Imports a font', 'Purges CSS'], a: 1, e: '@apply lets you compose utilities inside a normal CSS rule, useful for repeated patterns.', tags: ['CSS', 'Tailwind'] },
  { q: 'You customise Tailwind’s colours, spacing, and breakpoints in:', opts: ['The HTML', 'The Tailwind config (theme)', 'A database', 'The browser settings'], a: 1, e: 'The config file defines the design system; utilities are generated from it.', tags: ['CSS', 'Tailwind'] },
  { q: 'Why does Tailwind push a fixed spacing scale (1, 2, 4, 8…) instead of arbitrary pixels?', opts: ['To limit you', 'For visual consistency and rhythm across the whole UI', 'Because px is invalid', 'To save memory'], a: 1, e: 'A constrained scale keeps spacing harmonious and decisions fast, instead of one-off magic numbers.', tags: ['CSS', 'Tailwind'] },
  { q: 'In mobile-first Tailwind, an UNprefixed utility applies to:', opts: ['Only large screens', 'All screen sizes (and prefixed variants override from a breakpoint up)', 'Only mobile', 'Nothing'], a: 1, e: 'Base utilities are the default everywhere; `sm:`, `md:`… layer overrides on top as the screen grows.', tags: ['CSS', 'Tailwind'] },
  { q: 'How does Tailwind compare to a component library like MUI?', opts: ['They are identical', 'Tailwind gives low-level utilities to build your own components; component libraries ship ready-made components', 'MUI is utility-first', 'Tailwind ships full components'], a: 1, e: 'Tailwind is unopinionated building blocks; libraries like MUI hand you finished, styled components.', tags: ['CSS', 'Tailwind'] },

  // ── Level 9 — Beyond Plain CSS: Tailwind vs CSS-in-JS ───────────────────
  { q: 'What is CSS-in-JS?', opts: ['A CSS preprocessor', 'Writing styles in JavaScript, colocated with the component (e.g. styled-components, Emotion)', 'A linter', 'A bundler'], a: 1, e: 'CSS-in-JS keeps a component’s styles in its JS/TS file rather than a separate stylesheet.', tags: ['CSS', 'CSS-in-JS'] },
  { q: 'A key reason component frameworks reach for CSS-in-JS is:', opts: ['Smaller files', 'Scoped styles — each component’s styles are isolated, avoiding global name clashes', 'Faster networks', 'Fewer components'], a: 1, e: 'Styles are scoped to the component, so you don’t fight global class-name collisions as the app grows.', tags: ['CSS', 'CSS-in-JS'] },
  { q: 'Another motivation for CSS-in-JS is:', opts: ['It disables the cascade', 'Dynamic styling — computing styles from props/state in JavaScript', 'Avoiding HTML', 'Removing classes'], a: 1, e: 'Because styles are JS, they can react to props/state directly (e.g. a colour driven by a variant prop).', tags: ['CSS', 'CSS-in-JS'] },
  { q: 'Which classic problem of large global CSS does scoping solve?', opts: ['Slow downloads', 'Class-name collisions and specificity wars across a big codebase', 'Missing fonts', 'Browser support'], a: 1, e: 'In big apps, global class names clash and specificity escalates; scoping (CSS-in-JS or CSS Modules) contains styles.', tags: ['CSS', 'CSS-in-JS'] },
  { q: 'What do CSS Modules do?', opts: ['Run JS on the server', 'Compile locally-scoped class names from a regular CSS file', 'Replace HTML', 'Add animations'], a: 1, e: 'CSS Modules keep you in .css files but generate unique, scoped class names — scoping without writing CSS in JS.', tags: ['CSS', 'CSS-in-JS'] },
  { q: 'A tradeoff of (some) CSS-in-JS libraries is:', opts: ['No tradeoffs', 'Possible runtime cost and added build complexity', 'You can’t use props', 'It can’t scope styles'], a: 1, e: 'Runtime CSS-in-JS can add overhead and tooling complexity; zero-runtime options exist to mitigate it.', tags: ['CSS', 'CSS-in-JS'] },
  { q: 'How do Tailwind and CSS-in-JS each avoid name collisions?', opts: ['They don’t', 'Tailwind uses shared utility classes (no custom names); CSS-in-JS generates unique scoped class names', 'Both use ids', 'Both use !important'], a: 1, e: 'Tailwind sidesteps naming entirely with utilities; CSS-in-JS auto-generates unique class names per component.', tags: ['CSS', 'CSS-in-JS'] },
  { q: 'The big-picture takeaway across plain CSS, Tailwind, CSS Modules, and CSS-in-JS is:', opts: ['Only one is valid', 'They all compile down to the same CSS the browser applies — they differ in authoring and scoping ergonomics', 'They replace the browser engine', 'Only CSS-in-JS works'], a: 1, e: 'Underneath, it’s all CSS. The approaches differ in how you write and scope styles, not in what the browser does.', tags: ['CSS', 'CSS-in-JS'] },
];
