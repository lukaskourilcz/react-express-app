// Generated by scripts/port-interview-prepper.mjs from the interview-prepper catalogue.
// Task bodies only: prompts, starters, visible tests, hints. No solutions.
// English is the source of truth; Czech copy lives in the sibling *.cs.ts overlay.

import type { CodingTaskSource } from '../types';

export const SYSTEM_DESIGN_TASKS: CodingTaskSource[] = [
  {
    id: "sd-url-shortener",
    legacyId: "s1",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 2,
    focus: ["scoping", "data-model", "request-flow", "failure", "scale"],
    title: "URL shortener",
    prompt: "Your team wants short links for the company newsletter, so that a long tracking URL becomes something like ex.co/spring. You are building the service that hands out those short codes and sends people on to the real address. There are only two things this has to do: make a short code for a long address, and send whoever follows that code to the right place. Nearly all the traffic is the second one, because a link is created once and clicked thousands of times. These five questions walk the same path an interviewer walks — what you are building, what you store, what happens on one click, what happens when something is wrong, and what breaks first as it grows.",
    starter: "",
    hints: [
      "A link is created once and followed thousands of times. Design the following, and let the creating be slow.",
    ],
    verify: "guided",
    estimatedMinutes: 10,
    design: {
      scenario: "Your team wants short links for the company newsletter, so that a long tracking URL becomes something like ex.co/spring. You are building the service that hands out those short codes and sends people on to the real address.",
      brief: "There are only two things this has to do: make a short code for a long address, and send whoever follows that code to the right place. Nearly all the traffic is the second one, because a link is created once and clicked thousands of times. These five questions walk the same path an interviewer walks — what you are building, what you store, what happens on one click, what happens when something is wrong, and what breaks first as it grows.",
      steps: [
        {
          key: "requirements",
          title: "What are we building",
          prompt: "Before designing anything, what is the most useful thing to find out about how this service gets used?",
          options: [
            "That links are followed far more often than they are created, so the redirect is the part that matters",
            "Which programming language the team prefers",
            "How many people work at the company",
            "Whether the short codes should be uppercase or lowercase",
          ],
          correct: 0,
          explanation: "Almost every design decision follows from the balance of reads and writes. One link created and thousands of clicks means the click path is the product: you keep it simple, you put a cache in front of it, and you happily accept that creating a link is slower. If it were the other way round you would design something completely different. Language and team size do not change the shape of the system, and the case of the codes is a detail you settle in a sentence.",
        },
        {
          key: "data",
          title: "What we store",
          prompt: "What does one row in the links table need to hold?",
          options: [
            "The short code, the full destination address, who created it, and when",
            "Only the short code and the destination",
            "The short code, the destination, and a copy of the destination page",
            "A new table for every link, named after its code",
          ],
          correct: 0,
          explanation: "The code and the destination are what the redirect needs. The owner and the creation date are what everything else needs — showing somebody their links, deleting them, or working out what is old. Adding those two columns costs nothing now and saves a migration later. Storing a copy of the destination page is a different product. A table per link is not a data model at all: you cannot ask a question across links, and databases are not designed for thousands of tables.",
        },
        {
          key: "design",
          title: "How a request flows",
          prompt: "Somebody clicks a short link. What happens on the server?",
          options: [
            "Look the code up and reply with a redirect telling the browser the real address",
            "Fetch the destination page and send its contents back to the browser",
            "Put the click on a queue and wait for a worker to answer",
            "Look the code up and reply with a page containing a link to click again",
          ],
          correct: 0,
          explanation: "A redirect is an HTTP response that says \"what you want is over there\", and the browser goes there by itself. It is one lookup by the code, which should be the primary key, and one response — nothing else belongs on this path. Fetching the destination yourself makes you a proxy for the whole internet, paying for the bandwidth of every page. A queue adds waiting to something a person is sitting in front of. An extra page to click is a redirect done badly.",
        },
        {
          key: "deepdive",
          title: "When things go wrong",
          prompt: "Someone follows a code that does not exist, because it was mistyped or deleted. What should happen?",
          options: [
            "Answer 404 with a short page explaining the link is not valid",
            "Answer 500, because the lookup did not find anything",
            "Redirect them to the homepage as if nothing happened",
            "Answer 200 with an empty page",
          ],
          correct: 0,
          explanation: "A 404 means \"there is nothing here\", which is exactly what happened, and a human-readable page tells the person what to do next. A 500 says your server broke and will wake somebody up for a mistyped link. Silently redirecting to the homepage is confusing — people think the link worked and wonder why the content is wrong. An empty 200 tells both the browser and your monitoring that everything is fine, which will hide a broken campaign until somebody complains.",
        },
        {
          key: "scale",
          title: "What breaks first",
          prompt: "The newsletter goes out and thousands of people click the same link within a minute. What is the first thing you would do?",
          options: [
            "Keep the popular codes in a cache, so most clicks never reach the database",
            "Split the links table across several databases",
            "Add more storage to the database",
            "Rewrite the service in a faster language",
          ],
          correct: 0,
          explanation: "Thousands of people following the same handful of links is the easiest case a cache ever gets: a few entries in memory serve nearly all the traffic and the database barely notices. Reach for that first — it is a small change with a large effect. Splitting the table across machines is a big, expensive move that answers a different problem, namely one machine no longer holding the data. Storage is not what is under pressure, and the language is almost never the reason something is slow.",
        },
      ],
      reference: `What we are building
Two operations: create a short code for a long address, and follow a code to that address. The second happens thousands of times more often, so the whole design bends around keeping it fast and simple. I would say out loud that editing links, folders and per-user dashboards are out of scope for a first version.

What we store
One table. links(code, destination, owner_id, created_at), with code as the primary key, because every single read arrives as a code and asks for a destination. Owner and creation date are not needed for the redirect but are needed for everything else, and adding them now is free. Codes are never reused once handed out, or an old printed link would start pointing somewhere new.

How a request flows

  [browser] -> [web server] -> [cache] -> [database]
                    |
                    v
              302 redirect

The browser asks for /spring. The server looks up "spring", first in the cache and only then in the database, and answers with a redirect carrying the real address. The browser follows it. That is the whole hot path, and there is nothing else on it.

When things go wrong
A code that does not exist gets a 404 and a short page saying the link is not valid. I would not answer 500, because nothing is broken, and I would not quietly send people to the homepage, because then a broken campaign looks like a working one. If we want click counts, the count is recorded after the redirect has already been sent, so nobody waits for it.

What breaks first
Not the redirects. A newsletter drives thousands of clicks to a handful of codes, and a handful of entries in a cache absorbs that easily. If reads did outgrow one database I would add a read replica before splitting anything, since almost all the traffic is reads. The honest trade-off is between a permanent redirect, which browsers remember and which means we stop seeing most clicks, and a temporary one, which comes back to us every time and keeps the click counts real.`,
      passMark: 4,
    },
  },
  {
    id: "sd-to-do-list-api",
    legacyId: "s2",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 2,
    focus: ["scoping", "data-model", "request-flow", "failure", "scale"],
    title: "To-do list API",
    prompt: "A small team wants a shared to-do app. You are building the API behind it: people sign in, add tasks, tick them off, and see only their own list. The web and mobile clients will both talk to it. This is the most ordinary system there is, which is exactly why it is worth walking through carefully. Everything an interviewer wants to see lives here in miniature: what the endpoints are, what a row looks like, how one request is served, what happens when somebody asks for something that is not theirs, and what starts to hurt as lists grow. Get this shape right and most CRUD systems are the same conversation with different nouns.",
    starter: "",
    hints: [
      "Every task belongs to somebody. Decide how the server knows who is asking before you design anything else.",
    ],
    verify: "guided",
    estimatedMinutes: 10,
    design: {
      scenario: "A small team wants a shared to-do app. You are building the API behind it: people sign in, add tasks, tick them off, and see only their own list. The web and mobile clients will both talk to it.",
      brief: "This is the most ordinary system there is, which is exactly why it is worth walking through carefully. Everything an interviewer wants to see lives here in miniature: what the endpoints are, what a row looks like, how one request is served, what happens when somebody asks for something that is not theirs, and what starts to hurt as lists grow. Get this shape right and most CRUD systems are the same conversation with different nouns.",
      steps: [
        {
          key: "requirements",
          title: "What are we building",
          prompt: "What is the most important thing to agree on before designing the API?",
          options: [
            "That every task belongs to one person, and nobody may see anybody else’s",
            "Whether the buttons should be blue or green",
            "How the mobile app will animate the tick",
            "Which cloud provider it will be hosted on",
          ],
          correct: 0,
          explanation: "Ownership is the rule the whole design has to enforce, and it shows up everywhere: a column on the table, a condition on every query, a check on every endpoint. Establishing it early is what stops you building something where changing a number in a URL shows you somebody else’s tasks — one of the most common real security holes there is. Colours and animations belong to the client, and the hosting choice does not change the design.",
        },
        {
          key: "data",
          title: "What we store",
          prompt: "What should the tasks table look like?",
          options: [
            "tasks(id, user_id, title, done, created_at), with an index on user_id",
            "tasks(id, title, done) with the user’s tasks joined by matching titles",
            "One row per user, with all their tasks in a single text field",
            "tasks(id, title, done), and a separate table for each user",
          ],
          correct: 0,
          explanation: "Each task is a row, and user_id is what makes it somebody’s. The index on user_id matters because the query you run constantly is \"all tasks for this user\", and without one the database reads every row in the table to find them. Matching by title is not a relationship — two people can write \"buy milk\". Cramming a list into one text field means you cannot tick off a single task without rewriting the lot, and a table per user cannot be queried across.",
        },
        {
          key: "design",
          title: "How a request flows",
          prompt: "The app asks for the signed-in person’s tasks. What does the server do?",
          options: [
            "Work out who is asking from their session, then query tasks where user_id matches",
            "Query every task, and let the app show only the ones belonging to that user",
            "Query tasks where user_id matches the id sent in the URL by the client",
            "Query every task and filter them by user in the server code",
          ],
          correct: 0,
          explanation: "The server decides who you are from the session, not from anything the client sends — otherwise changing the id in the URL hands over somebody else’s data. The filter belongs in the query so the database returns only what is needed. Sending everything to the app and filtering there means the data has already left your server, which is a leak rather than a design. Filtering in server code is safe but wasteful: you read the whole table to throw nearly all of it away.",
        },
        {
          key: "deepdive",
          title: "When things go wrong",
          prompt: "Someone requests a task id that exists but belongs to another person. What should the API answer?",
          options: [
            "A 404, as if the task did not exist",
            "A 403, saying it belongs to someone else",
            "A 200 with the task, since they knew the id",
            "A 500, because the ownership check failed",
          ],
          correct: 0,
          explanation: "Answering 403 confirms the task exists, which lets somebody probe for valid ids and learn about data they cannot see. Answering 404 tells them nothing they did not already know. Both are defensible and you should say why you chose one — 403 is friendlier inside a trusted system where a colleague might genuinely have mistyped. Returning the task is the security hole the ownership rule exists to prevent, and 500 means \"we broke\", when in fact everything worked correctly.",
        },
        {
          key: "scale",
          title: "What breaks first",
          prompt: "Some people now have thousands of tasks, and the list endpoint has become slow. What is the first fix?",
          options: [
            "Return a page at a time and add an index on the columns being sorted and filtered",
            "Add a second application server",
            "Cache each user’s full task list",
            "Move the tasks table to its own database",
          ],
          correct: 0,
          explanation: "The cost here grows with how much one person has, not with how many people there are, so more servers do not help — each one runs the same slow query. Returning a fixed page keeps the work constant whatever the history, and the index stops the database sorting a large set from scratch every time. Caching a list that the owner is constantly changing means fighting to keep it correct for very little gain, and a separate database is a large move for something two ordinary fixes solve.",
        },
      ],
      reference: `What we are building
Sign in, add a task, tick it off, delete it, and see your own list and nobody else’s. The endpoints are POST /tasks, GET /tasks, PATCH /tasks/:id and DELETE /tasks/:id. Ownership is the rule that runs through everything, so I want it enforced in the query and not only in the interface.

What we store
tasks(id, user_id, title, done, created_at) with an index on user_id, since "all tasks for this user" is the query that runs constantly and without the index the database reads the whole table every time. Completion is a boolean here; if we ever want to know when something was finished, it becomes a completed_at timestamp instead. Users live in their own table and tasks point at it.

How a request flows

  [app] -> [API server] -> session lookup -> [database]
                               |
                               v
                        tasks WHERE user_id = me

The server takes the session cookie, works out who is asking, and queries tasks for that user id. The id never comes from the URL or the request body, because anything the client sends can be changed by the client.

When things go wrong
A task id belonging to somebody else gets a 404 rather than a 403, so the API does not confirm which ids exist. Bad input gets a 400 with a message naming the field. If the database is unreachable we return 503 rather than pretending an empty list is the answer, because an empty list looks to the app like "you have no tasks" and could get somebody's screen cleared.

What breaks first
Not the number of users, which is the intuitive answer — the app servers scale by adding more. It is the person with four thousand tasks, because the list endpoint's cost grows with their history. So the list is paged, twenty at a time with a way to ask for the next, and the index covers the sort. Beyond that, reads outnumber writes heavily in this kind of app, so a read replica is the next step long before anything more exotic.`,
      passMark: 4,
    },
  },
  {
    id: "sd-photo-sharing",
    legacyId: "s3",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 2,
    focus: ["scoping", "data-model", "request-flow", "failure", "scale"],
    title: "Photo sharing",
    prompt: "A hobby club wants members to upload photos from their events and browse them afterwards. Photos come off phones at several megabytes each, and members want to scroll a gallery page without waiting. The whole lesson here is that a photo is two different things: a large lump of bytes, and a small set of facts about it — who took it, when, which event. Those two want completely different homes, and almost every mistake in a system like this comes from putting them in the same one. The five questions follow that split through the design, from what you agree up front to what falls over once the club has ten thousand photos.",
    starter: "",
    hints: ["A photo is two things: a big lump of bytes and a few small facts. They want different homes."],
    verify: "guided",
    estimatedMinutes: 10,
    design: {
      scenario: "A hobby club wants members to upload photos from their events and browse them afterwards. Photos come off phones at several megabytes each, and members want to scroll a gallery page without waiting.",
      brief: "The whole lesson here is that a photo is two different things: a large lump of bytes, and a small set of facts about it — who took it, when, which event. Those two want completely different homes, and almost every mistake in a system like this comes from putting them in the same one. The five questions follow that split through the design, from what you agree up front to what falls over once the club has ten thousand photos.",
      steps: [
        {
          key: "requirements",
          title: "What are we building",
          prompt: "What should you find out first about the photos themselves?",
          options: [
            "How large a typical photo is, because that decides where the bytes can live",
            "Which camera brands members use",
            "What the gallery page should be called",
            "Whether photos should have rounded corners",
          ],
          correct: 0,
          explanation: "Size decides the design. A few kilobytes each and you could reasonably keep them anywhere; several megabytes each and they have to go somewhere built for files, served directly to browsers, never passing through your application on the way out. Asking for the number early is what separates a considered answer from a guess. Camera brands, page names and corner radius are real product questions but none of them changes a single box in the diagram.",
        },
        {
          key: "data",
          title: "What we store",
          prompt: "Where should the image file and the facts about it each be kept?",
          options: [
            "The file in object storage, and a database row holding its location, owner and date",
            "Both the file and the facts in the database",
            "The file on the web server’s disk, and the facts in the database",
            "The file in object storage, with owner and date written into the file name",
          ],
          correct: 0,
          explanation: "Object storage is a service built to hold files cheaply and hand them straight to browsers. The database keeps the small facts you actually search and sort on, plus a pointer to where the bytes went. Putting megabytes into database rows makes every query and every backup drag them around. The web server’s own disk breaks as soon as there is a second server with a different disk. Facts in file names cannot be queried — you could not list this month’s photos without inspecting every file.",
        },
        {
          key: "design",
          title: "How a request flows",
          prompt: "A member opens the gallery. How does the page get thirty photos onto the screen?",
          options: [
            "The API returns thirty rows with image URLs, and the browser fetches each image from storage",
            "The API reads all thirty files and sends the image data inside its response",
            "The API returns thirty rows, and the browser asks the API for each image in turn",
            "The API sends every photo the club has, and the browser shows thirty",
          ],
          correct: 0,
          explanation: "Your API answers with small JSON: thirty rows, each carrying a URL. The browser then fetches those images itself, straight from storage or a CDN — in parallel, cached by the browser afterwards, and never touching your servers. Reading the files and embedding them makes your API carry every megabyte. Routing each image through the API does the same thing more slowly. Sending every photo in the club is the mistake that only shows up once there are a lot of them.",
        },
        {
          key: "deepdive",
          title: "When things go wrong",
          prompt: "An upload fails halfway: the file never arrives, but a row was already saved for it. How do you stop that happening?",
          options: [
            "Store the file first, and only write the row once storage has confirmed it",
            "Write the row first, so the photo has an id before it uploads",
            "Write the row first and delete it later if nobody complains",
            "Allow rows without files, and hide the broken ones in the gallery",
          ],
          correct: 0,
          explanation: "Order the two writes so that a failure leaves the harmless state. A file with no row is invisible and wasted space, cleaned up by a periodic sweep. A row with no file is a broken image on the page, which people see. So the bytes go first and the row follows. Writing the row first to get an id is a real pattern, but then the row needs a status column saying it is not ready yet — which is fine, as long as you say so rather than leaving the gap unhandled.",
        },
        {
          key: "scale",
          title: "What breaks first",
          prompt: "Uploads grow, and the gallery is now slow to load. Photos are resized into thumbnails during the upload request. What breaks first?",
          options: [
            "Resizing uses up the servers’ processing power, slowing down every other request too",
            "Object storage runs out of room",
            "The database cannot store the rows fast enough",
            "Browsers refuse to display that many images",
          ],
          correct: 0,
          explanation: "Resizing an image is heavy processor work, and doing it inside the request means it competes with everything else those servers are meant to be answering — so pages with no photos on them get slow too, which makes the cause hard to spot. Move it to a background worker: accept the upload, store the original, reply, and let a separate pool make the thumbnails and update the row when they are ready. Object storage grows without you planning for it, and the rows are tiny.",
        },
      ],
      reference: `What we are building
Members upload photos from club events and browse them. Files come off phones at three to five megabytes, so the first thing I want is that number, because it rules out keeping the bytes anywhere near the database. Out of scope for a first version: albums, comments, and editing.

What we store
Two homes for two kinds of thing. The file goes to object storage under a key. The row is photos(id, owner_id, event_id, storage_key, width, height, uploaded_at) with an index on event_id, because browsing by event is the query that runs. Thumbnails get their own keys recorded on the same row, so the gallery never asks for full-size images.

How a request flows

  [browser] -> [API] -> [database]   (thirty rows, each with URLs)
       |
       +------> [object storage / CDN]   (the images themselves)

The API answers with small JSON and the browser fetches the images directly. That keeps megabytes off our servers entirely, lets the browser fetch in parallel and cache afterwards, and means a CDN can serve them from somewhere close by.

When things go wrong
Bytes are stored before the row is written, so a failed upload leaves an orphaned file rather than a broken image on the page; a periodic job deletes keys with no row. Type and size are checked before anything is stored, or we have paid to accept a file we were going to reject. If storage is unreachable the upload fails loudly rather than recording a photo that is not there.

What breaks first
Resizing, if it is done inside the request. It is processor-heavy and it starves every unrelated endpoint on the same machine, which makes the symptom appear everywhere except where the cause is. So uploads are accepted, stored, and handed to a background worker that makes the thumbnails and updates the row. After that the next pressure is bandwidth rather than storage, which is the argument for a CDN: the same bytes leave a machine near the user instead of leaving us.`,
      passMark: 4,
    },
  },
  {
    id: "sd-accounts-and-login",
    legacyId: "s4",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 2,
    focus: ["scoping", "data-model", "request-flow", "failure", "scale"],
    title: "Accounts and login",
    prompt: "A site currently has no accounts and needs them: register with an email and password, sign in, stay signed in, and sign out. It runs on two web servers behind a load balancer. Nearly every system needs this, and nearly every junior interview asks about some corner of it. The ideas are small and specific: passwords are never stored, being signed in is a fact the server holds rather than something the browser can claim, and two servers means anything kept in one machine’s memory is invisible to the other. These five questions cover the parts you are expected to know without hesitating.",
    starter: "",
    hints: [
      "The password is never stored. Everything else in this design follows from taking that seriously.",
    ],
    verify: "guided",
    estimatedMinutes: 10,
    design: {
      scenario: "A site currently has no accounts and needs them: register with an email and password, sign in, stay signed in, and sign out. It runs on two web servers behind a load balancer.",
      brief: "Nearly every system needs this, and nearly every junior interview asks about some corner of it. The ideas are small and specific: passwords are never stored, being signed in is a fact the server holds rather than something the browser can claim, and two servers means anything kept in one machine’s memory is invisible to the other. These five questions cover the parts you are expected to know without hesitating.",
      steps: [
        {
          key: "requirements",
          title: "What are we building",
          prompt: "What is the one rule to agree on before anything else?",
          options: [
            "The password is never stored or logged in a form anyone can read",
            "People should pick a memorable password",
            "The login form should be on its own page",
            "Sign-in should work on mobile as well as desktop",
          ],
          correct: 0,
          explanation: "It is the rule that shapes the table, the code and the logging. You store a hash — a one-way transformation you can check against but never reverse — so a leaked backup exposes nobody, and you make sure the raw password never reaches a log file on its way past. The other three are real considerations, but none of them changes what you build, and none of them is what an interviewer is checking when they ask how you handle passwords.",
        },
        {
          key: "data",
          title: "What we store",
          prompt: "What goes in the users table?",
          options: [
            "An id, the email with a unique constraint, and a hash of the password made with bcrypt",
            "An id, the email, and the password encrypted so it can be recovered",
            "An id, the email, and the password as typed",
            "An id, the email, and a fast hash of the password such as MD5",
          ],
          correct: 0,
          explanation: "A hash lets you check a password without ever being able to read it, and bcrypt is deliberately slow so that guessing millions of candidates is impractical. The unique constraint on email is what actually stops two accounts existing for one address, since two people can register at the same instant and a check in your code will not catch that. Encryption is reversible, so whoever holds the key holds every password. Storing them as typed, or hashing them quickly, both end the same way when the database leaks.",
        },
        {
          key: "design",
          title: "How a request flows",
          prompt: "Someone signs in successfully. How does the next request know who they are?",
          options: [
            "A session id in a cookie, looked up in a store both servers can read",
            "The user id in a cookie, read straight from the cookie on each request",
            "A session kept in the memory of the server that handled the login",
            "The email and password sent again with every request",
          ],
          correct: 0,
          explanation: "The cookie carries a meaningless identifier; the server looks it up to find out who that is. Because both servers read the same store, it does not matter which one the load balancer picks. Putting the user id in the cookie means anybody can edit it and become somebody else. Keeping the session in one server’s memory signs people out at random as soon as there are two. Resending the password on every request means it travels constantly and has to be checked constantly.",
        },
        {
          key: "deepdive",
          title: "When things go wrong",
          prompt: "Somebody tries to sign in with an email that has no account. What should the response say?",
          options: [
            "The same \"email or password is incorrect\" used for a wrong password",
            "\"No account with that email\" — it is more helpful",
            "\"Wrong password\" regardless, to keep it simple",
            "Nothing, with the form silently doing nothing",
          ],
          correct: 0,
          explanation: "Different messages let anybody with the form discover which addresses have accounts here, which is worth something on its own and worth more on a site people would rather not be known to use. One message for both cases gives that away for nothing. The friendlier wording is a real cost and you should say so, since people do genuinely forget which address they used — the usual answer is a good password reset flow, not a more revealing error. Silence just looks broken.",
        },
        {
          key: "scale",
          title: "What breaks first",
          prompt: "Someone starts trying thousands of passwords against one account. What is the first thing to do about it?",
          options: [
            "Slow down and then block repeated failed attempts for that account and that address",
            "Make everybody choose a longer password",
            "Add more web servers so the traffic is spread out",
            "Switch to a faster hashing algorithm so the checks cost less",
          ],
          correct: 0,
          explanation: "Rate limiting is what turns an attack that would take hours into one that would take years, and it belongs on both the account and the source address, since an attacker will try one password against many accounts as readily as many against one. Longer passwords help but you cannot make people choose them retroactively. More servers give the attacker more capacity. A faster hash is exactly backwards: the slowness of bcrypt is the feature, and speeding it up helps the attacker far more than it helps you.",
        },
      ],
      reference: `What we are building
Register, sign in, stay signed in across requests, sign out. Two web servers behind a load balancer, so nothing important can live in one machine's memory. The rule I would state first is that we never store a password in a readable form, and never log one.

What we store
users(id, email UNIQUE, password_hash, created_at). The hash comes from bcrypt, which is slow on purpose so guessing at volume is impractical. The unique constraint on email is the real guard against duplicate accounts, because a check in application code loses the race when two registrations arrive together. Sessions live in their own store: a session id, the user it belongs to, and an expiry.

How a request flows

  [browser] -> [load balancer] -> [either web server] -> [session store]
                                          |                    |
                                          v                    v
                                    [database]           who is this?

Signing in checks the submitted password against the stored hash and creates a session; the browser gets the session id in a cookie marked HttpOnly and Secure so scripts cannot read it and it never travels unencrypted. Every later request looks that id up.

When things go wrong
A wrong password and an unknown email get the same message, so the form cannot be used to find out who has an account. Signing out deletes the session record, which is why a server-side session is worth having: it can actually be revoked, unlike a self-contained token that stays valid until it expires. A password reset issues a single-use token with a short expiry, stores only a hash of it, and signs out existing sessions when it completes.

What breaks first
Not capacity — this is a small amount of work per request. It is somebody guessing passwords, which costs us nothing to allow and everything to ignore. So repeated failures are slowed and then blocked, counted against both the account and the source address. The honest trade-off is bcrypt's cost: it is deliberately slow, which protects the hashes and also means a burst of real sign-ins is more expensive than it looks.`,
      passMark: 4,
    },
  },
  {
    id: "sd-small-online-shop",
    legacyId: "s5",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 2,
    focus: ["scoping", "data-model", "request-flow", "failure", "scale"],
    title: "Small online shop",
    prompt: "A bakery sells a few dozen products online. Customers browse, add things to a basket, and pay by card through an outside payment provider. Stock is limited and they must not sell what they do not have. A shop pulls together nearly everything the other walkthroughs cover — reading a catalogue constantly, writing an order rarely, calling somebody else’s service, and one number that must not go wrong. It is also where the difference between \"reads that can be a little stale\" and \"writes that cannot be wrong\" becomes obvious. Five questions: what it does, what it stores, what happens when somebody pays, what happens when the payment provider misbehaves, and what breaks first on a busy morning.",
    starter: "",
    hints: [
      "Almost all of it is browsing, which is forgiving. One number is not, and that is the stock count.",
    ],
    verify: "guided",
    estimatedMinutes: 10,
    design: {
      scenario: "A bakery sells a few dozen products online. Customers browse, add things to a basket, and pay by card through an outside payment provider. Stock is limited and they must not sell what they do not have.",
      brief: "A shop pulls together nearly everything the other walkthroughs cover — reading a catalogue constantly, writing an order rarely, calling somebody else’s service, and one number that must not go wrong. It is also where the difference between \"reads that can be a little stale\" and \"writes that cannot be wrong\" becomes obvious. Five questions: what it does, what it stores, what happens when somebody pays, what happens when the payment provider misbehaves, and what breaks first on a busy morning.",
      steps: [
        {
          key: "requirements",
          title: "What are we building",
          prompt: "Which part of this shop deserves the most care in the design?",
          options: [
            "The stock count, because selling something twice cannot be undone by the software",
            "The product photographs, because they take the most storage",
            "The basket, because people change it most often",
            "The category menu, because it appears on every page",
          ],
          correct: 0,
          explanation: "Most of a shop is forgiving. A slightly stale product description or a basket that loses an item is annoying and fixable. Selling the last loaf to two people is a phone call and a refund, and no amount of retrying fixes it afterwards — so that is the number the design has to protect, and saying so early shows you can tell the risky part from the busy part. Photos are just storage, and the menu is the easiest thing in the system to cache.",
        },
        {
          key: "data",
          title: "What we store",
          prompt: "How should an order record the price of what was bought?",
          options: [
            "Copy the price onto each order line at the time of purchase",
            "Store only the product id, and read the current price when showing the order",
            "Store the total on the order and nothing per line",
            "Store a link to the product page as it looked that day",
          ],
          correct: 0,
          explanation: "This is one of the few places where copying data is right. An order is a record of what happened, and what happened included a price. If you read the current price later, last year’s receipts silently change every time the bakery puts its prices up — which is wrong, and in most places illegal. Storing only a total loses what was actually bought. Products and prices stay in their own tables for browsing; the order keeps its own copy because it is history, not a view of the present.",
        },
        {
          key: "design",
          title: "How a request flows",
          prompt: "A customer clicks pay. What order do things happen in?",
          options: [
            "Check stock is still available, take the payment, then save the order and reduce the stock",
            "Save the order, reduce the stock, then take the payment",
            "Take the payment, then check whether the stock is still there",
            "Reduce the stock, then take the payment, then save the order",
          ],
          correct: 0,
          explanation: "Check the thing you cannot undo before doing the thing you cannot undo. Confirming stock first means you rarely take money for something unavailable; taking payment before writing the order means you never record a sale that was not paid for. The database should also enforce that stock cannot go below zero, because two customers can pass the check at the same instant. Taking money first and checking after guarantees refunds, and reducing stock before payment loses inventory to abandoned checkouts.",
        },
        {
          key: "deepdive",
          title: "When things go wrong",
          prompt: "The payment provider is slow, so the customer’s browser times out and they press pay again. How do you avoid charging twice?",
          options: [
            "Send a unique key with the payment, so the provider treats a repeat as the same charge",
            "Check whether an order with the same total was created in the last minute",
            "Disable the button after the first click",
            "Refund any duplicate charges when a customer complains",
          ],
          correct: 0,
          explanation: "A key generated for that one checkout lets the provider recognise the second request as a repeat of the first and return the original result instead of charging again. This is called idempotency, and it is the standard answer whenever a request can be retried. Matching on the amount and time rejects a customer legitimately buying the same thing twice. Disabling the button helps with double-clicks but does nothing for a refresh or a flaky connection. Refunding on complaint means only the customers who notice get their money back.",
        },
        {
          key: "scale",
          title: "What breaks first",
          prompt: "On a busy Saturday morning the site slows down. Browsing is most of the traffic. What would you do first?",
          options: [
            "Cache the product pages and the menu, since they are read constantly and change rarely",
            "Cache the stock counts as well, since they are read on the same pages",
            "Split the orders table across several databases",
            "Ask customers to check out at quieter times",
          ],
          correct: 0,
          explanation: "Browsing is nearly all of the traffic and almost none of the risk, so it is exactly what should be served from a cache — the catalogue changes when the bakery updates it, not when somebody looks at it. The stock count is the one thing on those pages you should not cache for long, since a stale number is how you oversell; showing it fresh, or with a very short expiry, is the compromise. Splitting the orders table is a large move for a shop with a few dozen products.",
        },
      ],
      reference: `What we are building
Browse a few dozen products, add to a basket, pay through an outside provider, and never sell stock we do not have. Browsing is nearly all the traffic; ordering is rare and is the part that must not go wrong. That split is the design.

What we store
products(id, name, price, stock) for the catalogue. orders(id, customer_id, total, status, placed_at) and order_lines(id, order_id, product_id, quantity, unit_price). The unit price is copied onto the line at purchase time, because an order is a record of what happened and a later price rise must not rewrite old receipts. Stock has a constraint keeping it at or above zero, so the database refuses to oversell even if two checkouts arrive together.

How a request flows

  [browser] -> [web server] -> [cache]        (browsing)
                    |
                    +--------> [database]     (checkout, in one transaction)
                    |
                    +--------> [payment provider]

Browsing reads from a cache. Checkout confirms stock, charges the card, then writes the order and decrements stock together in one transaction so we never have half a sale.

When things go wrong
The payment call carries a key unique to that checkout, so a customer who refreshes after a timeout is not charged twice — the provider recognises the repeat and returns the first result. If the provider is unreachable we fail the checkout rather than recording an unpaid order. If the charge succeeds and our write then fails, that is the case worth naming out loud: we record it for a human, because money moved and the customer has nothing.

What breaks first
The database, under reads, long before anything to do with orders — a few dozen products read thousands of times is a caching problem and nothing more. So the catalogue and menu are cached and the stock number is not, or is cached for seconds at most. The honest trade-off is there: cache the stock and the pages get faster and the overselling gets worse.`,
      passMark: 4,
    },
  },
  {
    id: "dd-requests-per-second",
    legacyId: "d1",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["estimation"],
    title: "Requests per second",
    prompt: "An app is used by about 1 million people on a given day, and each of them makes roughly 10 requests while they are using it. Spread evenly across the day, how many requests per second does the server handle? (A day is 86,400 seconds.)",
    starter: "",
    hints: ["A rough number is fine — you are not expected to be exact."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "estimate",
      scenario: "An app is used by about 1 million people on a given day, and each of them makes roughly 10 requests while they are using it.",
      prompt: "Spread evenly across the day, how many requests per second does the server handle? (A day is 86,400 seconds.)",
      explanation: "A million people times ten requests is ten million requests a day, and ten million divided by 86,400 seconds is about 116 a second. That is a small number, and knowing it is small is the point: one ordinary server handles this without breaking a sweat. Being able to do this sum in your head is what stops you designing for a scale you do not have. Real traffic is not spread evenly, so the busiest hour usually runs two or three times the average — size for that, not for the mean.",
      unit: "requests per second",
      answer: 116,
      min: 50,
      max: 300,
    },
  },
  {
    id: "dd-a-year-of-photos",
    legacyId: "d2",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["estimation"],
    title: "A year of photos",
    prompt: "A small photo app takes about 1,000 uploads a day. A photo comes to roughly 2 megabytes once it is stored. How many gigabytes does one year of uploads take? (A thousand megabytes is a gigabyte, and a year is about 365 days.)",
    starter: "",
    hints: ["A rough number is fine — you are not expected to be exact."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "estimate",
      scenario: "A small photo app takes about 1,000 uploads a day. A photo comes to roughly 2 megabytes once it is stored.",
      prompt: "How many gigabytes does one year of uploads take? (A thousand megabytes is a gigabyte, and a year is about 365 days.)",
      explanation: "A thousand photos at two megabytes is two thousand megabytes, or two gigabytes a day, so a year is about 730 gigabytes. Under a terabyte a year is worth recognising, because it means ordinary cloud storage handles this and there is nothing clever to design. The sum is also the first thing an interviewer wants when you say \"we will store the images\" — the answer decides whether storage is a footnote or the whole problem.",
      unit: "gigabytes",
      answer: 730,
      min: 350,
      max: 1500,
    },
  },
  {
    id: "dd-how-much-cache",
    legacyId: "d3",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["estimation"],
    title: "How much cache",
    prompt: "A site keeps its 100,000 most-visited pages in memory so it does not have to build them from the database every time. One page takes about 1 kilobyte to hold. How many megabytes of memory does that need? (A thousand kilobytes is a megabyte.)",
    starter: "",
    hints: ["A rough number is fine — you are not expected to be exact."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "estimate",
      scenario: "A site keeps its 100,000 most-visited pages in memory so it does not have to build them from the database every time. One page takes about 1 kilobyte to hold.",
      prompt: "How many megabytes of memory does that need? (A thousand kilobytes is a megabyte.)",
      explanation: "A hundred thousand entries at a kilobyte each is a hundred thousand kilobytes, which is about 100 megabytes. That fits in memory on any machine, which is the useful conclusion: caching the popular pages is cheap. Doing the sum stops the conversation from stalling on whether a cache is affordable, and it is nearly always this answer — the popular slice of anything is small enough to hold, which is exactly why caching works at all.",
      unit: "megabytes",
      answer: 100,
      min: 40,
      max: 250,
    },
  },
  {
    id: "dd-rows-in-two-years",
    legacyId: "d4",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["estimation"],
    title: "Rows in two years",
    prompt: "A shop takes about 500 orders a day, and each order is one row in a table. The team wants to know how big that table gets before they worry about it. Roughly how many rows does the orders table hold after two years? (A year is about 365 days.)",
    starter: "",
    hints: ["A rough number is fine — you are not expected to be exact."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "estimate",
      scenario: "A shop takes about 500 orders a day, and each order is one row in a table. The team wants to know how big that table gets before they worry about it.",
      prompt: "Roughly how many rows does the orders table hold after two years? (A year is about 365 days.)",
      explanation: "Five hundred a day times 365 is about 180,000 a year, so two years is roughly 365,000 rows. That is a small table. A single database handles millions of rows comfortably as long as queries use an index, so the honest answer to \"how will you scale this\" here is that you do not need to yet. Knowing where the real thresholds are is what keeps you from proposing sharding for a shop that takes five hundred orders a day.",
      unit: "rows",
      answer: 365000,
      min: 150000,
      max: 800000,
    },
  },
  {
    id: "dd-people-online-at-once",
    legacyId: "d5",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["estimation"],
    title: "People online at once",
    prompt: "A service has 100,000 people using it across a day. Measurements show about 5 per cent of them have it open at any given moment. How many people are using it at the same time?",
    starter: "",
    hints: ["A rough number is fine — you are not expected to be exact."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "estimate",
      scenario: "A service has 100,000 people using it across a day. Measurements show about 5 per cent of them have it open at any given moment.",
      prompt: "How many people are using it at the same time?",
      explanation: "Five per cent of a hundred thousand is five thousand at once. The gap between \"users\" and \"users right now\" is one of the most common mix-ups in a design conversation: a hundred thousand sounds like a lot and five thousand concurrent is what your servers actually have to hold. Always ask which number somebody means. The ratio varies — a chat app people leave open runs far higher than a site people visit once a week.",
      unit: "people at once",
      answer: 5000,
      min: 2000,
      max: 12000,
    },
  },
  {
    id: "dd-a-day-of-messages",
    legacyId: "d6",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["estimation"],
    title: "A day of messages",
    prompt: "A messaging feature carries about 1 million messages a day. A stored message is short — roughly 200 bytes with the sender and timestamp included. How many megabytes of new messages is that per day? (A million bytes is a megabyte.)",
    starter: "",
    hints: ["A rough number is fine — you are not expected to be exact."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "estimate",
      scenario: "A messaging feature carries about 1 million messages a day. A stored message is short — roughly 200 bytes with the sender and timestamp included.",
      prompt: "How many megabytes of new messages is that per day? (A million bytes is a megabyte.)",
      explanation: "A million messages at 200 bytes is 200 million bytes, or about 200 megabytes a day. Text is tiny — that is the lesson. A year of it is well under a hundred gigabytes, which no database minds. Anything with pictures or video in it is a completely different problem, and separating \"the text\" from \"the files\" in your head early is what keeps a design honest about where the real weight is.",
      unit: "megabytes",
      answer: 200,
      min: 80,
      max: 500,
    },
  },
  {
    id: "dd-a-month-of-backups",
    legacyId: "d7",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["estimation"],
    title: "A month of backups",
    prompt: "A database holds about 50 gigabytes. A full copy is taken every night and the last 30 nights are kept. How many gigabytes do the backups take altogether?",
    starter: "",
    hints: ["A rough number is fine — you are not expected to be exact."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "estimate",
      scenario: "A database holds about 50 gigabytes. A full copy is taken every night and the last 30 nights are kept.",
      prompt: "How many gigabytes do the backups take altogether?",
      explanation: "Fifty gigabytes times thirty copies is 1,500 gigabytes, so the backups are thirty times the database. That surprises people, and it is why backups usually keep fewer full copies and more incremental ones, which store only what changed since the last. The number worth carrying is that a backup policy is a storage decision, and \"keep everything forever\" is a bill, not a plan.",
      unit: "gigabytes",
      answer: 1500,
      min: 700,
      max: 3000,
    },
  },
  {
    id: "dd-sizing-for-the-peak",
    legacyId: "d8",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["estimation"],
    title: "Sizing for the peak",
    prompt: "A site averages 100 requests a second across the whole day. Traffic follows waking hours, and the busiest hour runs about three times the average. How many requests a second should the servers be able to handle?",
    starter: "",
    hints: ["A rough number is fine — you are not expected to be exact."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "estimate",
      scenario: "A site averages 100 requests a second across the whole day. Traffic follows waking hours, and the busiest hour runs about three times the average.",
      prompt: "How many requests a second should the servers be able to handle?",
      explanation: "Three times a hundred is three hundred a second at the peak, and the peak is what you have to survive — an average nobody ever experiences is no use for sizing. This is the difference between a system that stays up at lunchtime and one that falls over. Most teams add headroom on top of that, because a machine at 100 per cent of its capacity has nothing left for a spike or a deploy.",
      unit: "requests per second",
      answer: 300,
      min: 200,
      max: 600,
    },
  },
  {
    id: "dd-the-size-of-an-index",
    legacyId: "d9",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["estimation"],
    title: "The size of an index",
    prompt: "A table has 1 million rows. An index is a separate sorted structure the database keeps so it can find rows by a column without reading them all; each entry here is about 20 bytes. How many megabytes does that index take? (A million bytes is a megabyte.)",
    starter: "",
    hints: ["A rough number is fine — you are not expected to be exact."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "estimate",
      scenario: "A table has 1 million rows. An index is a separate sorted structure the database keeps so it can find rows by a column without reading them all; each entry here is about 20 bytes.",
      prompt: "How many megabytes does that index take? (A million bytes is a megabyte.)",
      explanation: "A million entries at twenty bytes is about twenty megabytes. Tiny, which is why adding an index to a slow query is nearly always worth it at this size. Indexes are not free, though: the database has to update every one of them on every write, and each one takes space in memory and in every backup. That is the real trade — fast reads, slightly slower writes — and it is worth saying out loud rather than adding indexes to everything.",
      unit: "megabytes",
      answer: 20,
      min: 8,
      max: 50,
    },
  },
  {
    id: "dd-data-leaving-per-day",
    legacyId: "d10",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["estimation"],
    title: "Data leaving per day",
    prompt: "A site serves 10,000 page views a day. Each page pulls about 1 megabyte of images, scripts and styles. How many gigabytes leave the servers each day? (A thousand megabytes is a gigabyte.)",
    starter: "",
    hints: ["A rough number is fine — you are not expected to be exact."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "estimate",
      scenario: "A site serves 10,000 page views a day. Each page pulls about 1 megabyte of images, scripts and styles.",
      prompt: "How many gigabytes leave the servers each day? (A thousand megabytes is a gigabyte.)",
      explanation: "Ten thousand views at a megabyte each is 10,000 megabytes, or about 10 gigabytes a day. Cloud providers charge for data leaving their network, so this is the number that turns into a bill, and it grows with visitors rather than with the size of your database. It is also the argument for a CDN: those servers keep copies close to users, so most of those bytes leave the CDN instead of leaving you.",
      unit: "gigabytes",
      answer: 10,
      min: 4,
      max: 25,
    },
  },
  {
    id: "dd-which-kind-of-database",
    legacyId: "d11",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["tradeoff", "data-model"],
    title: "Which kind of database",
    prompt: "A booking app stores customers, bookings and payments. Every booking belongs to a customer, every payment belongs to a booking, and the fields are the same for all of them. Which kind of database should they use?",
    starter: "",
    hints: ["Pick the option that fits this situation best."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "tradeoff",
      scenario: "A booking app stores customers, bookings and payments. Every booking belongs to a customer, every payment belongs to a booking, and the fields are the same for all of them.",
      prompt: "Which kind of database should they use?",
      explanation: "Relational databases exist for exactly this: fixed fields, and records that point at each other. You get one place per fact, links the database itself enforces, and the ability to ask questions across tables. A document database is better when records vary wildly in shape or are always read whole. A key-value store is better when you only ever fetch by id and never filter. Plain files give you no querying, no safety when two writes land at once, and no way to ask anything across bookings.",
      options: [
        "A relational (SQL) database, with a table each and links between them",
        "A document database, storing each customer as one big nested document",
        "A key-value store, where the only way in is by id",
        "Plain files on the server, one per booking",
      ],
      correct: 0,
    },
  },
  {
    id: "dd-where-uploaded-files-go",
    legacyId: "d12",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["tradeoff", "data-model"],
    title: "Where uploaded files go",
    prompt: "Users upload profile pictures of two or three megabytes each. The app also needs to know who each picture belongs to and when it was uploaded. Where should the picture itself and the details about it each live?",
    starter: "",
    hints: ["Pick the option that fits this situation best."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "tradeoff",
      scenario: "Users upload profile pictures of two or three megabytes each. The app also needs to know who each picture belongs to and when it was uploaded.",
      prompt: "Where should the picture itself and the details about it each live?",
      explanation: "Object storage is a cloud service built to hold files cheaply and serve them straight to browsers, so the picture never travels through your app. The database keeps the small facts you need to search on. Putting the bytes in the database makes every query and backup drag megabytes around. The server’s own drive breaks the moment there is a second server, since it has a different drive. Encoding facts into file names means you cannot ask \"everything uploaded this week\" without listing every file.",
      options: [
        "The picture in object storage, the owner and date in a database row that points to it",
        "Both the picture and its details in the database",
        "The picture on the server’s own hard drive, its details in the database",
        "The picture in object storage, with the owner and date built into the file name",
      ],
      correct: 0,
    },
  },
  {
    id: "dd-where-a-login-lives",
    legacyId: "d13",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["tradeoff", "auth"],
    title: "Where a login lives",
    prompt: "A site remembers who is signed in by keeping a session in the memory of the server that handled the login. The team is about to add a second server. Where should sessions live once there is more than one server?",
    starter: "",
    hints: ["Pick the option that fits this situation best."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "tradeoff",
      scenario: "A site remembers who is signed in by keeping a session in the memory of the server that handled the login. The team is about to add a second server.",
      prompt: "Where should sessions live once there is more than one server?",
      explanation: "Each server only knows what is in its own memory, so a request landing on the other one looks like nobody is signed in. Moving sessions somewhere both can read fixes it, and lets you add a third server whenever you like. Keeping them in memory is what breaks. A plain cookie holding a user id is worse than broken — anyone can edit it and become somebody else. A file has the same problem as memory, since the other server cannot see it either.",
      options: [
        "In a shared store both servers can read, such as Redis or a database table",
        "In each server’s own memory, as now",
        "In the browser, with the user id in a plain cookie",
        "In a file on whichever server created the session",
      ],
      correct: 0,
    },
  },
  {
    id: "dd-storing-passwords",
    legacyId: "d14",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["tradeoff", "auth"],
    title: "Storing passwords",
    prompt: "A new sign-up form takes an email and a password. The team has to decide what actually goes into the users table when somebody registers. What should be stored in the password column?",
    starter: "",
    hints: ["Pick the option that fits this situation best."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "tradeoff",
      scenario: "A new sign-up form takes an email and a password. The team has to decide what actually goes into the users table when somebody registers.",
      prompt: "What should be stored in the password column?",
      explanation: "A hash is one-way: you can check a password by hashing what was typed and comparing, but you can never turn the stored value back into the password. Slow-by-design algorithms like bcrypt make guessing millions of candidates impractical. Encryption is the wrong tool because it is reversible — whoever holds the key holds every password. Storing them as typed means one leaked backup exposes every account, on your site and on every other site where somebody reused it. Fast hashes like MD5 are guessed at enormous speed.",
      options: [
        "A hash of the password made with a slow algorithm built for passwords, such as bcrypt",
        "The password encrypted, so it can be decrypted when needed",
        "The password as typed, since the database is private anyway",
        "A fast hash of the password, such as MD5 or SHA-1",
      ],
      correct: 0,
    },
  },
  {
    id: "dd-where-to-check-input",
    legacyId: "d15",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["tradeoff", "request-flow"],
    title: "Where to check input",
    prompt: "A form already refuses to submit when the email looks wrong or the age is negative. The team is deciding whether the server needs to check the same things. Where does the checking have to happen for it to be safe?",
    starter: "",
    hints: ["Pick the option that fits this situation best."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "tradeoff",
      scenario: "A form already refuses to submit when the email looks wrong or the age is negative. The team is deciding whether the server needs to check the same things.",
      prompt: "Where does the checking have to happen for it to be safe?",
      explanation: "Anyone can send a request straight to your API without ever loading your page, so a browser check stops honest mistakes and nothing else. The server is the only place that can actually refuse bad data. That does not make the browser check pointless — telling somebody about a typo instantly is far better than a round trip — it just means it is there for the user, not for safety. Dropping it makes the form worse for everyone to prevent nothing.",
      options: [
        "On the server, always — the browser check is a convenience on top",
        "In the browser only, since it catches mistakes before they are sent",
        "On the server only, and drop the browser check",
        "Whichever is easier for the particular field",
      ],
      correct: 0,
    },
  },
  {
    id: "dd-the-slow-email",
    legacyId: "d16",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["tradeoff", "queues"],
    title: "The slow email",
    prompt: "Signing up sends a welcome email. The email provider sometimes takes several seconds, and while it does, the person is staring at a spinner. What should the sign-up request do about the email?",
    starter: "",
    hints: ["Pick the option that fits this situation best."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "tradeoff",
      scenario: "Signing up sends a welcome email. The email provider sometimes takes several seconds, and while it does, the person is staring at a spinner.",
      prompt: "What should the sign-up request do about the email?",
      explanation: "The account exists whether or not the email has gone out, so waiting for it makes the user pay for work they do not care about. Handing it to a background worker — a separate process picking jobs off a list — means the reply is instant and the worker can keep retrying through an outage with nobody watching. Doing it in the request is only right when the answer changes the response, like a card payment. Retrying inline makes the worst case longer. Overnight is fine for a digest, not for a message people expect now.",
      options: [
        "Save the account, hand the email to a background worker, and reply straight away",
        "Send the email during the request, as now, but with a shorter timeout",
        "Send the email during the request, and retry if it fails",
        "Skip it, and send the day’s welcome emails overnight",
      ],
      correct: 0,
    },
  },
  {
    id: "dd-a-query-that-got-slow",
    legacyId: "d17",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["tradeoff", "data-model"],
    title: "A query that got slow",
    prompt: "A page that lists a customer’s orders has become slow. The orders table has grown to a few hundred thousand rows, and the query filters by customer id. What should you do first to make the page fast again?",
    starter: "",
    hints: ["Pick the option that fits this situation best."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "tradeoff",
      scenario: "A page that lists a customer’s orders has become slow. The orders table has grown to a few hundred thousand rows, and the query filters by customer id.",
      prompt: "What should you do first to make the page fast again?",
      explanation: "Without an index the database reads every row to find the ones matching, and that gets slower as the table grows. An index is a sorted structure that lets it jump straight to the matching rows — usually turning seconds into milliseconds, for a few lines of migration. Always check for the missing index first. A cache hides the problem while adding staleness. Splitting the table or buying a bigger machine are large, expensive moves for something one index fixes.",
      options: [
        "Add an index on the customer id column",
        "Add a cache in front of the query",
        "Move the orders table to its own database",
        "Buy a bigger database machine",
      ],
      correct: 0,
    },
  },
  {
    id: "dd-answering-for-a-missing-thing",
    legacyId: "d18",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["tradeoff", "request-flow"],
    title: "Answering for a missing thing",
    prompt: "An API endpoint returns a product by its id. Sometimes the id does not exist, because the product was deleted or the link is old. What should the API answer?",
    starter: "",
    hints: ["Pick the option that fits this situation best."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "tradeoff",
      scenario: "An API endpoint returns a product by its id. Sometimes the id does not exist, because the product was deleted or the link is old.",
      prompt: "What should the API answer?",
      explanation: "The status code is how a machine understands the answer without reading the body, so it has to tell the truth. 404 means \"there is nothing here\", which is exactly what happened. Returning 200 says everything is fine and forces every caller to parse the body to discover otherwise — and caches and monitoring will believe the 200. A 500 means your server broke, which will page somebody at three in the morning for a link that was simply old. An empty 200 is the same lie with less information.",
      options: [
        "Status 404, with a short message saying the product was not found",
        "Status 200, with a body saying the product was not found",
        "Status 500, since the lookup did not produce a product",
        "Status 200 with an empty body, and let the caller work it out",
      ],
      correct: 0,
    },
  },
  {
    id: "dd-plain-http-or-https",
    legacyId: "d19",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["tradeoff", "auth"],
    title: "Plain HTTP or HTTPS",
    prompt: "An internal admin tool is only reachable inside the office network. Someone suggests skipping certificates because it is not on the public internet. Should it be served over HTTPS?",
    starter: "",
    hints: ["Pick the option that fits this situation best."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "tradeoff",
      scenario: "An internal admin tool is only reachable inside the office network. Someone suggests skipping certificates because it is not on the public internet.",
      prompt: "Should it be served over HTTPS?",
      explanation: "HTTPS encrypts the traffic, so anyone able to watch the network cannot read passwords, session cookies or personal data as they go past. Office networks are not magically safe: guest wifi, a compromised laptop or a misconfigured switch all put somebody on the path. Protecting only the login page is the classic mistake, because the session cookie sent on every later request is as good as the password. Certificates are free and automated now, so the trade is not really a trade.",
      options: [
        "Yes — anything carrying a login or personal data uses HTTPS, internal or not",
        "No, since nothing on an internal network can be intercepted",
        "Only for the login page, and plain HTTP once signed in",
        "Only if it is later opened up to the public internet",
      ],
      correct: 0,
    },
  },
  {
    id: "dd-a-list-that-keeps-growing",
    legacyId: "d20",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["tradeoff", "request-flow"],
    title: "A list that keeps growing",
    prompt: "An endpoint returns every notification a user has ever had. It was fine at launch; some accounts now have tens of thousands. What should the endpoint return instead?",
    starter: "",
    hints: ["Pick the option that fits this situation best."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "tradeoff",
      scenario: "An endpoint returns every notification a user has ever had. It was fine at launch; some accounts now have tens of thousands.",
      prompt: "What should the endpoint return instead?",
      explanation: "Returning everything costs the database, your bandwidth and the browser’s memory, and it grows with the heaviest user rather than the typical one. Sending a fixed page and a way to ask for the next keeps the cost the same however much history there is. Filtering in the browser means all that work already happened. Compression shrinks bytes but not the query. Deleting data to avoid pagination is throwing away the product to avoid the fix.",
      options: [
        "Return a page at a time — a fixed number, with a way to ask for the next",
        "Return everything, and let the browser show only the first twenty",
        "Return everything, but compress the response",
        "Delete notifications older than a week so the list stays short",
      ],
      correct: 0,
    },
  },
  {
    id: "dd-what-is-worth-caching",
    legacyId: "d21",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["tradeoff", "caching"],
    title: "What is worth caching",
    prompt: "A shop page shows the site-wide list of categories, which changes a few times a year, and the stock count for the product, which changes constantly. Which of those two should be cached?",
    starter: "",
    hints: ["Pick the option that fits this situation best."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "tradeoff",
      scenario: "A shop page shows the site-wide list of categories, which changes a few times a year, and the stock count for the product, which changes constantly.",
      prompt: "Which of those two should be cached?",
      explanation: "Caching pays off when something is read far more often than it changes, and it costs you when a stale answer matters. Categories are read on every page and change twice a year — nearly free, nearly never wrong. A stock count that is a minute out of date sells something you do not have. The general rule is worth carrying: cache what is stable and popular, read the volatile and important things fresh, and one page can happily do both.",
      options: [
        "The category list, because it is read constantly and almost never changes",
        "The stock count, because it is the most expensive query",
        "Both, with the same short expiry",
        "Neither, because the page mixes the two",
      ],
      correct: 0,
    },
  },
  {
    id: "dd-one-big-server-or-several",
    legacyId: "d22",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["tradeoff", "scale"],
    title: "One big server or several",
    prompt: "A single web server is running near its limit at busy times. The team can move to a much larger machine, or put a load balancer in front of several ordinary ones. Which should the team choose?",
    starter: "",
    hints: ["Pick the option that fits this situation best."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "tradeoff",
      scenario: "A single web server is running near its limit at busy times. The team can move to a much larger machine, or put a load balancer in front of several ordinary ones.",
      prompt: "Which should the team choose?",
      explanation: "A bigger machine is genuinely simpler and is often the right first move, but it runs out twice: there is a largest machine you can buy, and while it reboots you are down. Several ordinary servers behind a load balancer keep going when one dies and let you add more as needed. The cost is that your app must stop keeping anything important in its own memory — sessions and uploads have to move somewhere shared. That is the real trade, and it is the reason the change is worth making before you are desperate.",
      options: [
        "Several servers behind a load balancer, because you can keep adding them and one failing is survivable",
        "One much larger machine, because it is simpler and needs no changes",
        "Several servers, because each one is cheaper than a large machine",
        "One larger machine, because load balancers are a single point of failure",
      ],
      correct: 0,
    },
  },
  {
    id: "dd-images-loading-slowly",
    legacyId: "d23",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["tradeoff", "caching"],
    title: "Images loading slowly",
    prompt: "A site serves its images and scripts from the same servers that run the application. Users far from the data centre complain that pages load slowly. What is the best fix for the slow images?",
    starter: "",
    hints: ["Pick the option that fits this situation best."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "tradeoff",
      scenario: "A site serves its images and scripts from the same servers that run the application. Users far from the data centre complain that pages load slowly.",
      prompt: "What is the best fix for the slow images?",
      explanation: "The delay is distance: every file makes a round trip across the world. A CDN keeps copies at locations near users, so the bytes travel a short way and never trouble your servers at all. A faster application server does not change how far the data travels. Compression helps a little and is worth doing anyway, but it does not fix the distance. Moving the whole application closer helps those users and hurts everyone else, and brings all the difficulty of running in two places.",
      options: [
        "A CDN — a network of servers worldwide that keep copies of your files near users",
        "A faster application server",
        "Compressing the images further and leaving everything else alone",
        "Moving the whole application closer to those users",
      ],
      correct: 0,
    },
  },
  {
    id: "dd-when-a-call-fails",
    legacyId: "d24",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["tradeoff", "queues"],
    title: "When a call fails",
    prompt: "A background worker calls another service, which has started returning errors because it is overloaded. How should the worker retry?",
    starter: "",
    hints: ["Pick the option that fits this situation best."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "tradeoff",
      scenario: "A background worker calls another service, which has started returning errors because it is overloaded.",
      prompt: "How should the worker retry?",
      explanation: "Retrying instantly piles more requests onto a service that is already struggling, and can hold it down long after it would otherwise have recovered. Waiting longer each time — a second, then two, then four — gives it room to breathe, and a limit stops one broken job retrying for ever. Three instant retries is the same pile-on, just smaller. Never retrying throws away the easy win, since most failures of this kind are temporary and the next attempt succeeds.",
      options: [
        "Wait a little longer before each attempt, and give up after a few tries",
        "Retry immediately, and keep retrying until it works",
        "Retry immediately, but only three times",
        "Do not retry at all — report the failure and move on",
      ],
      correct: 0,
    },
  },
  {
    id: "dd-store-the-total-or-add-it-up",
    legacyId: "d25",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["tradeoff", "data-model"],
    title: "Store the total or add it up",
    prompt: "An order has a handful of line items. The page shows the order total, which is just the sum of those lines. Should the total be stored on the order, or worked out when it is shown?",
    starter: "",
    hints: ["Pick the option that fits this situation best."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "tradeoff",
      scenario: "An order has a handful of line items. The page shows the order total, which is just the sum of those lines.",
      prompt: "Should the total be stored on the order, or worked out when it is shown?",
      explanation: "Adding up five numbers costs nothing, and keeping one source of truth means the total can never disagree with the lines it came from. Storing it creates a second copy that must be updated everywhere a line changes, and the bug where they drift apart is quiet and horrible. Storing a computed value is a real technique, but it earns its keep when the calculation is genuinely expensive — millions of rows, not five — and then it comes with the job of keeping it correct. A nightly repair is a confession that it drifts.",
      options: [
        "Worked out when shown, since there are only a few lines and one copy of the truth cannot disagree",
        "Stored on the order, since adding numbers on every page load is wasteful",
        "Stored, and recalculated by a nightly job in case it drifts",
        "Both — store it, and check it against the sum on every read",
      ],
      correct: 0,
    },
  },
  {
    id: "dd-a-table-with-no-index",
    legacyId: "d26",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["bottleneck", "data-model"],
    title: "A table with no index",
    prompt: "A support tool looks up tickets by their reference number. There is no index on that column, and the table has grown from a few thousand rows to a few million. What breaks first as the table grows?",
    starter: "",
    hints: ["Name the first thing that breaks."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "bottleneck",
      scenario: "A support tool looks up tickets by their reference number. There is no index on that column, and the table has grown from a few thousand rows to a few million.",
      prompt: "What breaks first as the table grows?",
      explanation: "Without an index the database has no way to find a reference number except to look at every row, so the work grows exactly as the table does — fine at a few thousand, painful at a few million. Storage is not the issue; tickets are small. Neither is memory, since only one row comes back. The fix is one index on the column being searched, and the reason to recognise this quickly is that it is the single most common cause of a query that \"suddenly\" got slow.",
      options: [
        "Every lookup reads the whole table, so searches get slower in step with its size",
        "The database runs out of storage",
        "The tool runs out of memory holding one ticket",
        "The network between the app and the database runs out of capacity",
      ],
      correct: 0,
    },
  },
  {
    id: "dd-sessions-and-a-second-server",
    legacyId: "d27",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["bottleneck", "auth"],
    title: "Sessions and a second server",
    prompt: "Logins are kept in the memory of whichever server handled them. A second server is added behind a load balancer that sends each request to whichever is free. What breaks first once there are two servers?",
    starter: "",
    hints: ["Name the first thing that breaks."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "bottleneck",
      scenario: "Logins are kept in the memory of whichever server handled them. A second server is added behind a load balancer that sends each request to whichever is free.",
      prompt: "What breaks first once there are two servers?",
      explanation: "Only the server that created a session knows it exists, so roughly half the requests arrive somewhere that has never heard of you and look signed out. This is not slowness, it is wrongness, and it shows up the instant the second server is added rather than at any particular scale. Memory is nowhere near the limit. The fix is to keep sessions somewhere both servers can read, which is also what lets you add a third.",
      options: [
        "People are signed out at random, whenever a request lands on the other server",
        "Both servers run short of memory",
        "The load balancer cannot keep up with the traffic",
        "The login page becomes slow",
      ],
      correct: 0,
    },
  },
  {
    id: "dd-emailing-inside-a-request",
    legacyId: "d28",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["bottleneck", "queues"],
    title: "Emailing inside a request",
    prompt: "An admin page emails every member of a group by looping through them and calling the email provider once per person. Groups have grown from ten people to two thousand. What breaks first on a large group?",
    starter: "",
    hints: ["Name the first thing that breaks."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "bottleneck",
      scenario: "An admin page emails every member of a group by looping through them and calling the email provider once per person. Groups have grown from ten people to two thousand.",
      prompt: "What breaks first on a large group?",
      explanation: "Two thousand calls one after another take far longer than a web request is allowed to run, and because the only record of progress is the loop itself, the timeout leaves the job half done with no safe way to resume — run it again and the first half get a second email. The fix is to hand the work to a background worker, record what happened per person, and make each step safe to repeat. Two thousand names is nothing to hold in memory.",
      options: [
        "The request hits its time limit partway through, with no record of who was already emailed",
        "The email provider rejects the messages as spam",
        "The database runs out of connections",
        "The group list stops fitting in memory",
      ],
      correct: 0,
    },
  },
  {
    id: "dd-serving-images-yourself",
    legacyId: "d29",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["bottleneck", "data-model"],
    title: "Serving images yourself",
    prompt: "Uploaded images are read from disk and sent out by the same application servers that handle every other request. A campaign is about to bring ten times the traffic. What breaks first when the traffic arrives?",
    starter: "",
    hints: ["Name the first thing that breaks."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "bottleneck",
      scenario: "Uploaded images are read from disk and sent out by the same application servers that handle every other request. A campaign is about to bring ten times the traffic.",
      prompt: "What breaks first when the traffic arrives?",
      explanation: "An image is thousands of times larger than a typical API response, so shipping them out consumes the connections, bandwidth and attention of servers meant to be answering everything else. The symptom is confusing, because pages with no images get slow too. The fix is to let object storage or a CDN serve the files directly, leaving your servers to do the part only they can do. Disk fills eventually, but far more slowly than capacity runs out.",
      options: [
        "Pushing image bytes uses up the servers’ capacity, so ordinary requests slow down too",
        "The images stop fitting on the disk",
        "The database cannot keep up with the image lookups",
        "Browsers refuse to load that many images",
      ],
      correct: 0,
    },
  },
  {
    id: "dd-everything-on-one-database",
    legacyId: "d30",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["bottleneck", "scale"],
    title: "Everything on one database",
    prompt: "Four application servers sit behind a load balancer, and all four talk to a single database. Traffic is growing steadily and most of it is reading. What breaks first as traffic keeps growing?",
    starter: "",
    hints: ["Name the first thing that breaks."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "bottleneck",
      scenario: "Four application servers sit behind a load balancer, and all four talk to a single database. Traffic is growing steadily and most of it is reading.",
      prompt: "What breaks first as traffic keeps growing?",
      explanation: "Application servers are easy to add because they hold nothing of their own — but every one you add points at the same database, so scaling the easy half only pushes harder on the half you have not scaled. Since most of the traffic is reads, the usual next step is a read replica: a synchronised copy that serves reads while writes still go to the original. Recognising which part of a diagram is the shared one is most of what \"where does this break\" means.",
      options: [
        "The database, since adding application servers only sends it more work",
        "The load balancer, which has to route more requests",
        "The application servers, which each handle a quarter of the traffic",
        "The network between the load balancer and the servers",
      ],
      correct: 0,
    },
  },
  {
    id: "dd-filtering-in-the-app",
    legacyId: "d31",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["bottleneck", "data-model"],
    title: "Filtering in the app",
    prompt: "A page needs last month’s orders. The code loads every order from the database and then filters by date in the application. What breaks first as the orders table grows?",
    starter: "",
    hints: ["Name the first thing that breaks."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "bottleneck",
      scenario: "A page needs last month’s orders. The code loads every order from the database and then filters by date in the application.",
      prompt: "What breaks first as the orders table grows?",
      explanation: "The database is very good at filtering, and it is sitting right next to the data. Doing it in the application means every row is read, serialised, sent across the network and held in memory before nearly all of them are discarded — work that grows with the whole table rather than with the answer. Moving the condition into the query, with an index on the date, makes the database return only what you asked for. Comparing dates is trivial; it is the volume that hurts.",
      options: [
        "The app pulls the entire table over the network and into memory just to throw most of it away",
        "The date comparison becomes too slow to compute",
        "The database runs out of connections",
        "The browser cannot render the results",
      ],
      correct: 0,
    },
  },
  {
    id: "dd-querying-in-a-loop",
    legacyId: "d32",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["bottleneck", "data-model"],
    title: "Querying in a loop",
    prompt: "A page lists fifty blog posts. The code fetches the posts with one query, then loops over them and fetches each post’s author with another. What breaks first as the page shows more posts?",
    starter: "",
    hints: ["Name the first thing that breaks."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "bottleneck",
      scenario: "A page lists fifty blog posts. The code fetches the posts with one query, then loops over them and fetches each post’s author with another.",
      prompt: "What breaks first as the page shows more posts?",
      explanation: "This is the N plus one problem: one query for the list, then one more for every item in it. Each round trip is small but they add up, and the count grows with the page size, so it degrades exactly when the page gets useful. The fix is to ask for all the authors at once — one query with the fifty ids, or a join — turning fifty-one trips into two. Knowing the name is worth it, because it is one of the first things a reviewer looks for.",
      options: [
        "One page turns into fifty-one separate queries, each paying its own round trip",
        "The posts stop fitting in memory",
        "The authors table needs an index",
        "The template takes too long to render",
      ],
      correct: 0,
    },
  },
  {
    id: "dd-a-call-with-no-timeout",
    legacyId: "d33",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["bottleneck", "queues"],
    title: "A call with no timeout",
    prompt: "A page calls an external weather service and waits for the answer. No timeout is set. The weather service has started hanging instead of replying. What breaks first once the other service hangs?",
    starter: "",
    hints: ["Name the first thing that breaks."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "bottleneck",
      scenario: "A page calls an external weather service and waits for the answer. No timeout is set. The weather service has started hanging instead of replying.",
      prompt: "What breaks first once the other service hangs?",
      explanation: "A request that never finishes still occupies whatever is handling it, so every new visitor takes another slot and nothing is ever released. Before long the whole site is down because of one slow dependency — and the pages that never call the weather service go down with it. Always set a timeout on a call to something you do not control, decide what to show when it fires, and consider stopping the calls entirely for a while once failures pass a threshold.",
      options: [
        "Requests pile up waiting, using up the server’s capacity until it can serve nothing at all",
        "The weather service starts rejecting the calls",
        "The database connection pool is exhausted first",
        "Users see an error page immediately",
      ],
      correct: 0,
    },
  },
  {
    id: "dd-resizing-during-upload",
    legacyId: "d34",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["bottleneck", "data-model"],
    title: "Resizing during upload",
    prompt: "When a photo is uploaded, the same server that handles all other requests resizes it into four sizes before replying. Uploads are about to grow tenfold. What breaks first when uploads grow?",
    starter: "",
    hints: ["Name the first thing that breaks."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "bottleneck",
      scenario: "When a photo is uploaded, the same server that handles all other requests resizes it into four sizes before replying. Uploads are about to grow tenfold.",
      prompt: "What breaks first when uploads grow?",
      explanation: "Resizing is heavy processor work happening inside the process that answers ordinary requests, so a burst of uploads starves endpoints that have nothing to do with photos. The symptom appears everywhere except where the work is, which makes it hard to diagnose. The fix is to accept the upload, store the original, reply, and let a separate pool of workers do the resizing — scaled on its own and able to retry with nobody waiting.",
      options: [
        "Resizing uses up the processor, so every unrelated request on that server slows down",
        "Object storage runs out of capacity",
        "The upload form stops validating correctly",
        "The database cannot record the uploads fast enough",
      ],
      correct: 0,
    },
  },
  {
    id: "dd-returning-everything",
    legacyId: "d35",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["bottleneck", "request-flow"],
    title: "Returning everything",
    prompt: "A mobile app asks the API for a user’s full message history on every launch. Long-standing users now have tens of thousands of messages. What breaks first for those users?",
    starter: "",
    hints: ["Name the first thing that breaks."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "bottleneck",
      scenario: "A mobile app asks the API for a user’s full message history on every launch. Long-standing users now have tens of thousands of messages.",
      prompt: "What breaks first for those users?",
      explanation: "The cost is tied to how much history somebody has rather than to what they need to see, so the app gets slower the longer someone has used it — punishing your best users. Everything in the chain suffers together: a big query, a big transfer, and a phone parsing all of it to show the last twenty. The fix is to send a page at a time with a way to ask for the next, which keeps the cost of a launch the same for everybody.",
      options: [
        "The response grows without limit, so the query, the transfer and the phone all struggle at once",
        "The API runs out of endpoints",
        "The database index on messages stops being used",
        "The phone runs out of storage",
      ],
      correct: 0,
    },
  },
  {
    id: "dd-typing-a-web-address",
    legacyId: "d36",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["sequence", "networking"],
    title: "Typing a web address",
    prompt: "Somebody types an address into their browser and presses enter. Several things happen before anything appears on screen. Put these steps into the order they happen.",
    starter: "",
    hints: ["Put the steps in the order they happen."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "sequence",
      scenario: "Somebody types an address into their browser and presses enter. Several things happen before anything appears on screen.",
      prompt: "Put these steps into the order they happen.",
      explanation: "This is the question behind a great many interview questions, and each step is somewhere things can go wrong or be sped up. The name lookup is why DNS problems look like the site being down. The connection setup is why a far-away server feels slow before it has done any work. The status code is how the browser knows what it got. And the page arriving first, with images fetched afterwards, is why a page can appear instantly and then fill in.",
      steps: [
        "Browser looks up the domain name to find the server’s IP address",
        "Browser opens a connection to that address and agrees encryption for HTTPS",
        "Browser sends an HTTP request asking for the page",
        "Server works out the answer and sends back a response with a status code",
        "Browser renders the page, then fetches the images and scripts it refers to",
      ],
    },
  },
  {
    id: "dd-one-request-through-the-system",
    legacyId: "d37",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["sequence", "scale"],
    title: "One request through the system",
    prompt: "A request reaches a site running several application servers behind a load balancer, with one database behind them. Put the path of one request into the order it happens.",
    starter: "",
    hints: ["Put the steps in the order they happen."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "sequence",
      scenario: "A request reaches a site running several application servers behind a load balancer, with one database behind them.",
      prompt: "Put the path of one request into the order it happens.",
      explanation: "Being able to walk this path out loud is most of what a system design interview is asking for. Each box does one job: the load balancer decides where, the application server decides what, the database holds the truth. It also shows why the application servers must not keep anything important in their own memory — the next request from the same person may well be handled by a different one.",
      steps: [
        "Request arrives at the load balancer",
        "Load balancer picks one of the application servers and passes it on",
        "That server checks who is asking and what they are allowed to do",
        "Server queries the database for the data it needs",
        "Server builds the response and sends it back to the browser",
      ],
    },
  },
  {
    id: "dd-signing-somebody-up",
    legacyId: "d38",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["sequence", "auth"],
    title: "Signing somebody up",
    prompt: "A new user submits the sign-up form with an email address and a password. Put the sign-up steps into the order they happen.",
    starter: "",
    hints: ["Put the steps in the order they happen."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "sequence",
      scenario: "A new user submits the sign-up form with an email address and a password.",
      prompt: "Put the sign-up steps into the order they happen.",
      explanation: "The order carries the reasoning. Cheap checks come first, so bad input never reaches the database. The duplicate check comes before the write, though the email column should also be unique in the database, since two people can submit at the same instant. The password is hashed before it is stored, never after. And the welcome email is last and off to one side, because the account exists whether or not it ever sends.",
      steps: [
        "Server checks the email looks valid and the password is long enough",
        "Server checks no account already exists with that email",
        "Server hashes the password and saves the new account",
        "Server creates a session so the person is signed in straight away",
        "Server replies, and a background worker sends the welcome email",
      ],
    },
  },
  {
    id: "dd-reading-through-a-cache",
    legacyId: "d39",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["sequence", "caching"],
    title: "Reading through a cache",
    prompt: "A page reads a record that is asked for constantly, using a cache in front of the database. Put the steps of one cached read into order.",
    starter: "",
    hints: ["Put the steps in the order they happen."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "sequence",
      scenario: "A page reads a record that is asked for constantly, using a cache in front of the database.",
      prompt: "Put the steps of one cached read into order.",
      explanation: "The cache is asked first and is never the source of truth, so if it disappears you get slower rather than wrong. The expiry is the safety net for the invalidation you forgot — it puts a limit on how long a stale value can survive. Removing the entry when the record changes is safer than updating it, because two writes at once can otherwise leave the older value sitting in the cache indefinitely.",
      steps: [
        "Application asks the cache for the record",
        "The cache does not have it, so the application reads the database",
        "Application puts the record in the cache with an expiry time",
        "Application returns the record",
        "Later, when that record changes, the application removes it from the cache",
      ],
    },
  },
  {
    id: "dd-uploading-a-file",
    legacyId: "d40",
    track: "system-design",
    topic: "system-design",
    level: 0,
    tier: 1,
    focus: ["sequence", "data-model"],
    title: "Uploading a file",
    prompt: "Somebody attaches a document to a form and submits it. The file goes to object storage rather than onto the server. Put the upload steps into the order they happen.",
    starter: "",
    hints: ["Put the steps in the order they happen."],
    verify: "drill",
    estimatedMinutes: 4,
    drill: {
      format: "sequence",
      scenario: "Somebody attaches a document to a form and submits it. The file goes to object storage rather than onto the server.",
      prompt: "Put the upload steps into the order they happen.",
      explanation: "The two halves are the thing to hold on to: the bytes go to storage built for bytes, and a small row in the database records where they went and who they belong to. Checking type and size comes before storing anything, or you have already paid for a file you were going to reject. Recording the location afterwards means a file with no row is just wasted space, while a row with no file is a broken link — which is why the row is written last.",
      steps: [
        "Browser sends the file to the server",
        "Server checks the file type and size are allowed",
        "Server stores the file in object storage and gets back its location",
        "Server saves a database row with the location, owner and upload time",
        "Server replies with a link the browser can use to show the file",
      ],
    },
  },
];
