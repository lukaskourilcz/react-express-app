// Git roadmap questions (120): 15 levels × 8, easiest → hardest. Unlike the
// other roadmap topics (which interleave two authored sets), Git is authored as
// a single 8-per-level list and built directly. See lib/roadmap.ts for titles.

import type { Seed } from './roadmap-build';

export const gitSeeds: Seed[] = [
  // ── Level 1 — Version Control Basics ───────────────────────────────────
  { q: 'Developers need local history, branches, and the ability to exchange commits without a central server holding the only complete copy. Which tool model fits?', opts: ['A text editor with one undo history per file', 'Git, a distributed version control system', 'A package registry that stores published builds', 'A cloud hosting service that owns the version history format'], a: 1, e: 'Git is distributed version control: each clone normally contains repository history and can create commits and branches locally. Hosting services such as GitHub build collaboration around Git.', tags: ['Git', 'Basics'] },
  { q: 'What does a single commit represent?', opts: ['A backup server', 'A snapshot of your files at a point in time', 'A branch', 'A remote'], a: 1, e: 'A commit is a snapshot of the tracked files at the moment you committed, plus metadata (author, message, parent).', tags: ['Git', 'Basics'] },
  { q: 'How does Git model project history?', opts: ['As a single file', 'As a series of commits, each linked to its parent', 'As a database table', 'As a zip archive'], a: 1, e: 'History is a graph of commits; each commit points back to the commit(s) it was based on.', tags: ['Git', 'Basics'] },
  { q: '"Distributed" version control means:', opts: ['Only the server has the history', 'Every clone contains the full history', 'History lives only in the cloud', 'There is no history'], a: 1, e: 'Every clone is a full repository with the complete history, so you can work and commit offline.', tags: ['Git', 'Basics'] },
  { q: 'Which hidden folder holds a Git repository’s data?', opts: ['`.vcs`', '`.git`', '`.repo`', '`git/`'], a: 1, e: 'Git stores all objects, refs and config in the `.git` directory at the repository root.', tags: ['Git', 'Basics'] },
  { q: 'A tool requires Git 2.40 or newer. Which command checks the installed Git release without changing the repository?', opts: ['git update', 'git --version', 'git init --versioned', 'git log --git'], a: 1, e: 'git --version reports the installed Git version and does not read or modify repository history.', tags: ['Git', 'Basics'] },
  { q: 'How is a commit identified?', opts: ['A sequential number', 'A SHA hash', 'A timestamp only', 'Its filename'], a: 1, e: 'Each commit has a unique SHA-1 (or SHA-256) hash computed from its content and metadata.', tags: ['Git', 'Basics'] },
  { q: 'What does Git primarily track?', opts: ['CPU usage', 'Changes to files over time', 'Open browser tabs', 'Network traffic'], a: 1, e: 'Git records how your tracked files change across commits.', tags: ['Git', 'Basics'] },

  // ── Level 2 — Repositories ─────────────────────────────────────────────
  { q: 'What does this do?\n\n```bash\n$ git init\n```', opts: ['Clones a repository', 'Creates a new empty repository in the current folder', 'Deletes a repository', 'Pushes your code'], a: 1, e: 'git init creates a new, empty repository (a `.git` folder) in the current directory.', tags: ['Git', 'Repositories'] },
  { q: 'What does this do?\n\n```bash\n$ git clone https://example.com/repo.git\n```', opts: ['Creates an empty repo', 'Copies a remote repository locally, with its history', 'Uploads your code', 'Renames a repo'], a: 1, e: 'git clone downloads a full copy of a remote repository, including all branches and history.', tags: ['Git', 'Repositories'] },
  { q: 'After cloning, what is the default remote called?', opts: ['`main`', '`origin`', '`remote`', '`upstream`'], a: 1, e: 'Git names the remote you cloned from `origin` by convention.', tags: ['Git', 'Remotes'] },
  { q: 'What is a "bare" repository?', opts: ['One with no commits', 'One with no working directory, used as a remote/server', 'One that cannot be cloned', 'A read-only repo'], a: 1, e: 'A bare repo (often `.git`-only) has no working tree; it is meant to be pushed to and cloned from, like a server.', tags: ['Git', 'Repositories'] },
  { q: 'When you clone, which branch is checked out?', opts: ['No branch', 'The repository’s default branch (e.g. main)', 'All branches at once', 'A random branch'], a: 1, e: 'Clone checks out the remote’s default branch (commonly main).', tags: ['Git', 'Repositories'] },
  { q: 'What does this do?\n\n```bash\n$ git clone <url> app\n```', opts: ['Fails', 'Clones into a folder named `app`', 'Renames the remote to app', 'Creates a branch app'], a: 1, e: 'A second argument sets the target directory; the repo is cloned into `app`.', tags: ['Git', 'Repositories'] },
  { q: 'What state does this command create?\n\n```bash\n$ git init -b main\n```', opts: ['An empty repository with no branch name until the first push', 'A new repository whose initial branch is named main', 'A clone of a remote repository’s main branch', 'A new main branch inside the nearest parent repository'], a: 1, e: 'git init creates a repository in the current directory, and -b main chooses the initial branch name. It does not clone a remote.', tags: ['Git', 'Repositories'] },
  { q: 'Is copying a project folder (including `.git`) enough to copy the repo?', opts: ['No, history is server-side', 'Yes — the whole repository lives in `.git`', 'Only with a remote', 'Only the latest commit copies'], a: 1, e: 'The entire repository (objects, refs, history) is inside `.git`, so copying it copies everything.', tags: ['Git', 'Repositories'] },

  // ── Level 3 — Staging & Status ─────────────────────────────────────────
  { q: 'What does this do?\n\n```bash\n$ git add file.txt\n```', opts: ['Commits the file', 'Stages file.txt for the next commit', 'Deletes the file', 'Pushes the file'], a: 1, e: 'git add moves changes into the staging area (index) so they’ll be included in the next commit.', tags: ['Git', 'Staging'] },
  { q: 'What does `git status` show?', opts: ['The commit history', 'Staged, unstaged and untracked changes', 'Remote branches', 'A full diff'], a: 1, e: 'git status summarizes what’s staged, what’s modified but unstaged, and what’s untracked.', tags: ['Git', 'Staging'] },
  { q: 'The staging area is also known as the:', opts: ['Cache', 'Index', 'Stash', 'Tree'], a: 1, e: 'The staging area is called the "index" — it holds the snapshot for your next commit.', tags: ['Git', 'Staging'] },
  { q: 'From the repository root, what does this add to the index?\n\n```bash\n$ git add .\n```', opts: ['Only untracked files, leaving edits and deletions unstaged', 'New, modified, and deleted paths under the current directory', 'Every change in every Git worktree on the machine', 'Only deletions already recorded in the last commit'], a: 1, e: 'git add . stages changes under the current path, including new, modified, and deleted files. Its scope depends on the current directory.', tags: ['Git', 'Staging'] },
  { q: 'A file Git has never been told about is:', opts: ['Staged', 'Modified', 'Untracked', 'Ignored'], a: 2, e: 'A new file Git isn’t tracking yet is "untracked" until you `git add` it.', tags: ['Git', 'Staging'] },
  { q: 'What does this do?\n\n```bash\n$ git restore --staged file.txt\n```', opts: ['Deletes the file', 'Unstages it, keeping your changes', 'Commits it', 'Ignores it'], a: 1, e: 'It removes the file from the staging area but leaves your working-tree changes intact.', tags: ['Git', 'Staging'] },
  { q: 'What does this do?\n\n```bash\n$ git add -p\n```', opts: ['Stages whole files only', 'Lets you stage changes interactively, hunk by hunk', 'Prints a patch', 'Pushes patches'], a: 1, e: 'The -p (patch) flag lets you review and stage individual chunks of a file.', tags: ['Git', 'Staging'] },
  { q: 'You staged a file, then edited it again. What must you do to include the new edits?', opts: ['Nothing, it’s already staged', '`git add` it again to stage the new edits', 'Commit twice', 'Stash it'], a: 1, e: 'Staging captures a snapshot; later edits are unstaged until you `git add` again.', tags: ['Git', 'Staging'] },

  // ── Level 4 — Committing ───────────────────────────────────────────────
  { q: 'What does this do?\n\n```bash\n$ git commit -m "Add login"\n```', opts: ['Stages changes', 'Records the staged changes as a commit with that message', 'Pushes to the remote', 'Creates a branch'], a: 1, e: 'git commit -m records everything currently staged as a new commit, using the inline message.', tags: ['Git', 'Committing'] },
  { q: 'What does the -a flag do here?\n\n```bash\n$ git commit -am "Fix"\n```', opts: ['Commits untracked files too', 'Stages already-tracked modified files, then commits', 'Only commits', 'Amends the last commit'], a: 1, e: '-a auto-stages modifications to tracked files (not new/untracked files) before committing.', tags: ['Git', 'Committing'] },
  { q: 'What does this do?\n\n```bash\n$ git commit --amend\n```', opts: ['Adds a new commit', 'Replaces the previous commit (its message and/or content)', 'Undoes a commit', 'Pushes'], a: 1, e: '--amend rewrites the most recent commit; use it before sharing, since it changes the commit’s hash.', tags: ['Git', 'Committing'] },
  { q: 'What happens if you commit with nothing staged?', opts: ['It commits an empty change', 'Git reports there’s nothing to commit', 'It corrupts the repo', 'It deletes history'], a: 1, e: 'With nothing staged, git commit reports "nothing to commit" (unless you pass --allow-empty).', tags: ['Git', 'Committing'] },
  { q: 'What makes a good commit message?', opts: ['Exactly one word', 'A concise description of what changed and why', 'It must include a date', 'It’s always optional'], a: 1, e: 'Good messages summarize the change (and often the reason) so history is readable later.', tags: ['Git', 'Committing'] },
  { q: 'What identity metadata does a commit store?', opts: ['Just a username', 'Author/committer name, email and timestamp', 'An IP address', 'Only a SHA'], a: 1, e: 'A commit records author and committer name + email + timestamps, alongside the message.', tags: ['Git', 'Committing'] },
  { q: 'What does `HEAD` refer to?', opts: ['The first commit', 'The current commit (the tip of the branch you’re on)', 'The remote', 'The staging area'], a: 1, e: 'HEAD is a pointer to your current location — usually the latest commit of the checked-out branch.', tags: ['Git', 'Committing'] },
  { q: 'What does this local Git setting affect?\n\n```bash\n$ git config user.email "me@x.com"\n```', opts: ['The credentials used to authenticate every GitHub request', 'The author email recorded on commits created in this repository', 'The SSH key selected when pushing to a remote', 'The address that receives repository notifications'], a: 1, e: 'user.email is commit identity metadata. It does not log in to a hosting provider or choose an SSH key.', tags: ['Git', 'Committing'] },

  // ── Level 5 — History & Diffs ──────────────────────────────────────────
  { q: 'You need the ordered commits reachable from the current branch, including author, date, and message. Which command starts that investigation?', opts: ['git diff', 'git log', 'git branch --list', 'git remote -v'], a: 1, e: 'git log walks commit history reachable from the selected revision. Flags can then change its format or path scope.', tags: ['Git', 'History'] },
  { q: 'What does `git log --oneline` do?', opts: ['Shows full details', 'Shows one compact line per commit', 'Shows only the latest commit', 'Shows a graph only'], a: 1, e: '--oneline condenses each commit to a short hash plus its summary line.', tags: ['Git', 'History'] },
  { q: 'With no arguments, `git diff` shows:', opts: ['Staged changes', 'Unstaged changes (working tree vs index)', 'The last commit', 'A remote diff'], a: 1, e: 'Plain git diff compares your working tree to the staging area — i.e. unstaged edits.', tags: ['Git', 'History'] },
  { q: 'Before committing, which comparison does `git diff --staged` display?', opts: ['The working tree against the index', 'The index against the current commit', 'Every pair of commits on the branch', 'The local branch against its remote without fetching'], a: 1, e: '--staged, also called --cached, shows what the index would contribute to the next commit compared with HEAD.', tags: ['Git', 'History'] },
  { q: 'What does `git show <commit>` display?', opts: ['Only the message', 'A commit’s metadata and its diff', 'Only the file list', 'Branches'], a: 1, e: 'git show prints the commit’s message plus the patch (diff) it introduced.', tags: ['Git', 'History'] },
  { q: 'What does `git log --graph` add?', opts: ['It hides merges', 'An ASCII graph of branches and merges', 'An exported image', 'Sorting by size'], a: 1, e: '--graph draws the branch/merge topology as text alongside the log.', tags: ['Git', 'History'] },
  { q: 'What does `git blame file.txt` show?', opts: ['It deletes a file', 'Who last changed each line and in which commit', 'It blames a commit', 'It reverts lines'], a: 1, e: 'git blame annotates every line with the commit and author that last modified it.', tags: ['Git', 'History'] },
  { q: 'What does this show?\n\n```bash\n$ git log -- file.txt\n```', opts: ['All commits', 'Only commits that changed file.txt', 'Only merges', 'File size history'], a: 1, e: 'Passing a path limits the log to commits that touched that file.', tags: ['Git', 'History'] },

  // ── Level 6 — Branches ─────────────────────────────────────────────────
  { q: 'With no arguments, what does `git branch` do?', opts: ['Creates a branch', 'Lists local branches', 'Deletes a branch', 'Switches branch'], a: 1, e: 'git branch lists local branches and marks the current one with a `*`.', tags: ['Git', 'Branches'] },
  { q: 'What does this do?\n\n```bash\n$ git branch feature\n```', opts: ['Switches to feature', 'Creates a branch named feature without switching to it', 'Deletes feature', 'Merges feature'], a: 1, e: 'git branch <name> creates the branch but leaves you on your current branch.', tags: ['Git', 'Branches'] },
  { q: 'What does this do?\n\n```bash\n$ git switch feature\n```', opts: ['Creates it', 'Switches to the feature branch', 'Deletes it', 'Renames it'], a: 1, e: 'git switch moves you to an existing branch (the modern alternative to git checkout <branch>).', tags: ['Git', 'Branches'] },
  { q: 'What does this do?\n\n```bash\n$ git switch -c feature\n```', opts: ['Only creates it', 'Creates feature and switches to it', 'Only switches', 'Deletes it'], a: 1, e: '-c creates a new branch and checks it out in one step.', tags: ['Git', 'Branches'] },
  { q: 'A branch in Git is:', opts: ['A copy of all the files', 'A movable pointer to a commit', 'A remote server', 'A tag'], a: 1, e: 'A branch is just a lightweight, movable reference to a commit — it advances as you commit.', tags: ['Git', 'Branches'] },
  { q: 'What does this do?\n\n```bash\n$ git branch -d feature\n```', opts: ['Force-deletes always', 'Deletes feature if it’s already merged', 'Renames it', 'Archives it'], a: 1, e: '-d deletes a branch but refuses if it has unmerged commits (use -D to force).', tags: ['Git', 'Branches'] },
  { q: 'What is the most common name for the default branch?', opts: ['root', 'main', 'trunk', 'head'], a: 1, e: 'Modern repositories default to `main` (older ones used `master`).', tags: ['Git', 'Branches'] },
  { q: 'What does this do?\n\n```bash\n$ git branch -m old new\n```', opts: ['Moves a commit', 'Renames branch `old` to `new`', 'Merges them', 'Deletes old'], a: 1, e: 'git branch -m renames a branch.', tags: ['Git', 'Branches'] },

  // ── Level 7 — Merging ──────────────────────────────────────────────────
  { q: 'You’re on main. What does `git merge feature` do?', opts: ['Rebases feature', 'Merges feature into main (the current branch)', 'Deletes feature', 'Pushes feature'], a: 1, e: 'git merge integrates the named branch into your current branch.', tags: ['Git', 'Merging'] },
  { q: 'When does a fast-forward merge happen?', opts: ['When there are conflicts', 'When the current branch hasn’t diverged, so its pointer just moves forward', 'When you force it', 'Never'], a: 1, e: 'If the target branch has no new commits since the branch point, Git just moves the pointer forward — no merge commit.', tags: ['Git', 'Merging'] },
  { q: 'How many parents does a merge commit have?', opts: ['Zero', 'One', 'Two', 'Three'], a: 2, e: 'A merge commit ties two histories together, so it has two parent commits.', tags: ['Git', 'Merging'] },
  { q: 'What does `git merge --no-ff feature` guarantee?', opts: ['It avoids merge commits', 'It always creates a merge commit, even if a fast-forward was possible', 'It rebases', 'It squashes'], a: 1, e: '--no-ff forces a merge commit, preserving the fact that a branch existed.', tags: ['Git', 'Merging'] },
  { q: 'To merge feature into main, which branch should you be on?', opts: ['feature', 'main — the branch that will receive the changes', 'a detached HEAD', 'the remote'], a: 1, e: 'You check out the receiving branch first, then merge the other branch into it.', tags: ['Git', 'Merging'] },
  { q: 'What does `git merge --abort` do?', opts: ['Commits the merge', 'Cancels an in-progress merge and restores the pre-merge state', 'Deletes the branch', 'Pushes'], a: 1, e: 'During a conflicted merge, --abort rolls back to how things were before you started.', tags: ['Git', 'Merging'] },
  { q: 'What does `git merge --squash feature` do?', opts: ['Keeps all of feature’s commits', 'Stages feature’s combined changes as one commit to make', 'Rebases', 'Tags the merge'], a: 1, e: 'A squash merge collapses the branch’s changes into your working tree/index so you commit them as a single commit.', tags: ['Git', 'Merging'] },
  { q: 'After a successful merge, the feature branch:', opts: ['Is deleted automatically', 'Still exists until you delete it', 'Becomes the remote', 'Is archived'], a: 1, e: 'Merging doesn’t delete the source branch; you remove it yourself when you’re done.', tags: ['Git', 'Merging'] },

  // ── Level 8 — Remotes ──────────────────────────────────────────────────
  { q: 'What does `git remote -v` show?', opts: ['Branches', 'The configured remotes and their URLs', 'The Git version', 'It adds a remote'], a: 1, e: 'git remote -v lists each remote name with its fetch and push URLs.', tags: ['Git', 'Remotes'] },
  { q: 'What does this do?\n\n```bash\n$ git remote add origin <url>\n```', opts: ['Removes origin', 'Adds a remote named origin pointing at the URL', 'Renames a remote', 'Clones'], a: 1, e: 'git remote add registers a named connection to another copy of the repo.', tags: ['Git', 'Remotes'] },
  { q: 'A remote is:', opts: ['A local branch', 'A named reference to another copy of the repository (e.g. on a server)', 'A commit', 'A tag'], a: 1, e: 'A remote is a named URL for a repository you exchange commits with, like one on GitHub.', tags: ['Git', 'Remotes'] },
  { q: 'What is `origin/main`?', opts: ['Your local main', 'A remote-tracking branch — your last-known state of main on origin', 'A tag', 'The staging area'], a: 1, e: 'Remote-tracking branches like origin/main record where the remote’s branch was at your last fetch.', tags: ['Git', 'Remotes'] },
  { q: 'What does `git fetch origin` do?', opts: ['Merges changes', 'Downloads new commits from origin without changing your branches', 'Pushes commits', 'Deletes origin'], a: 1, e: 'fetch updates your remote-tracking branches but does not modify your working branches.', tags: ['Git', 'Remotes'] },
  { q: 'What does `git remote remove origin` do?', opts: ['Deletes the repository', 'Removes the remote named origin', 'Renames origin', 'Clones origin'], a: 1, e: 'It deletes the remote configuration; your local commits are untouched.', tags: ['Git', 'Remotes'] },
  { q: 'What is the conventional name for the primary remote?', opts: ['main', 'origin', 'remote', 'server'], a: 1, e: 'The repo you cloned from (your primary remote) is conventionally named origin.', tags: ['Git', 'Remotes'] },
  { q: 'What does this do?\n\n```bash\n$ git remote rename origin upstream\n```', opts: ['Deletes origin', 'Renames the remote origin to upstream', 'Adds upstream', 'Fetches'], a: 1, e: 'It changes the local name of the remote from origin to upstream.', tags: ['Git', 'Remotes'] },

  // ── Level 9 — Push & Pull ──────────────────────────────────────────────
  { q: 'What does this do?\n\n```bash\n$ git push origin main\n```', opts: ['Downloads main', 'Uploads your local main commits to origin', 'Deletes main', 'Merges main'], a: 1, e: 'git push sends your local commits on main to the origin remote.', tags: ['Git', 'Push/Pull'] },
  { q: 'What does `git pull` do?', opts: ['Only fetches', 'Fetches remote changes and merges them into the current branch', 'Only pushes', 'Always rebases'], a: 1, e: 'git pull = git fetch followed by an integration (merge by default) into your branch.', tags: ['Git', 'Push/Pull'] },
  { q: 'What does the -u flag do here?\n\n```bash\n$ git push -u origin main\n```', opts: ['Pushes once', 'Pushes and sets upstream tracking so later `git push` needs no arguments', 'Deletes upstream', 'Pulls'], a: 1, e: '-u (--set-upstream) links your local branch to origin/main so future push/pull can omit arguments.', tags: ['Git', 'Push/Pull'] },
  { q: 'Your branch has two local commits while its upstream gained three commits. What does `git pull --rebase` try to do?', opts: ['Fetch and create a merge commit joining both histories', 'Fetch, then replay the two local commits on the updated upstream', 'Replace the remote branch with the two local commits', 'Discard the two local commits and reset to upstream'], a: 1, e: 'pull --rebase fetches the upstream changes and rebases local commits onto them instead of creating a merge commit.', tags: ['Git', 'Push/Pull'] },
  { q: 'Why might a push be rejected (non-fast-forward)?', opts: ['Your branch is behind nothing', 'The remote has commits you don’t have locally', 'You have no commits', 'It always is'], a: 1, e: 'Git rejects the push to avoid overwriting remote commits you haven’t merged; pull first, then push.', tags: ['Git', 'Push/Pull'] },
  { q: 'What does `git push --force-with-lease` do?', opts: ['Always overwrites the remote', 'Force-pushes, but refuses if the remote moved unexpectedly since your last fetch', 'Pulls first', 'Tags'], a: 1, e: 'It’s the safer force-push: it only overwrites if no one else pushed in the meantime.', tags: ['Git', 'Push/Pull'] },
  { q: 'How do `git fetch` and `git pull` differ?', opts: ['They’re identical', 'fetch only downloads; pull downloads and integrates into your branch', 'pull is read-only', 'fetch merges'], a: 1, e: 'fetch updates remote-tracking branches; pull additionally merges (or rebases) those changes in.', tags: ['Git', 'Push/Pull'] },
  { q: 'What does this do for a brand-new local branch?\n\n```bash\n$ git push -u origin feature\n```', opts: ['Fails', 'Creates `feature` on origin and sets it to track', 'Deletes feature', 'Renames it'], a: 1, e: 'It publishes the new branch to origin and establishes tracking for future pushes/pulls.', tags: ['Git', 'Push/Pull'] },

  // ── Level 10 — Undoing Changes ─────────────────────────────────────────
  { q: 'What does this do?\n\n```bash\n$ git restore file.txt\n```', opts: ['Stages it', 'Discards unstaged changes in file.txt, restoring it from the index', 'Deletes it', 'Commits it'], a: 1, e: 'git restore overwrites the working-tree file with the staged/committed version, dropping unstaged edits.', tags: ['Git', 'Undoing'] },
  { q: 'What does `git revert <commit>` do?', opts: ['Deletes the commit from history', 'Creates a new commit that undoes that commit’s changes', 'Resets history', 'Amends'], a: 1, e: 'revert is safe for shared history: it adds an inverse commit rather than rewriting the past.', tags: ['Git', 'Undoing'] },
  { q: 'What does this do?\n\n```bash\n$ git reset --soft HEAD~1\n```', opts: ['Discards changes', 'Moves HEAD back one commit but keeps the changes staged', 'Deletes a branch', 'Pushes'], a: 1, e: '--soft undoes the commit while leaving its changes in the staging area, ready to re-commit.', tags: ['Git', 'Undoing'] },
  { q: 'What does this do?\n\n```bash\n$ git reset --hard HEAD~1\n```', opts: ['Keeps changes staged', 'Removes the last commit AND discards its changes', 'Reverts safely', 'Only moves a tag'], a: 1, e: '--hard resets the branch and the working tree, permanently discarding the changes (use with care).', tags: ['Git', 'Undoing'] },
  { q: 'What does this do?\n\n```bash\n$ git restore --staged file.txt\n```', opts: ['Deletes the file', 'Unstages it while keeping your edits', 'Commits it', 'Reverts it'], a: 1, e: 'It removes the file from the index but keeps your working-tree changes.', tags: ['Git', 'Undoing'] },
  { q: 'Which is safe to undo a commit that others have already pulled?', opts: ['`git reset --hard`', '`git revert`', '`git reset --soft`', 'Both rewrite history'], a: 1, e: 'revert adds a new inverse commit, so it doesn’t rewrite shared history the way reset does.', tags: ['Git', 'Undoing'] },
  { q: 'What does this older command do?\n\n```bash\n$ git checkout -- file.txt\n```', opts: ['Stages the file', 'Discards working-tree changes to file.txt', 'Switches branch', 'Deletes the file'], a: 1, e: 'The older checkout -- form discards unstaged changes; `git restore` is the modern equivalent.', tags: ['Git', 'Undoing'] },
  { q: 'What does this do?\n\n```bash\n$ git clean -fd\n```', opts: ['Unstages files', 'Deletes untracked files and directories', 'Reverts commits', 'Resets branches'], a: 1, e: 'git clean removes untracked files; -f forces it and -d includes untracked directories.', tags: ['Git', 'Undoing'] },

  // ── Level 11 — Stashing ────────────────────────────────────────────────
  { q: 'What does `git stash` do?', opts: ['Commits your changes', 'Shelves your uncommitted changes and cleans the working tree', 'Deletes your changes', 'Pushes them'], a: 1, e: 'git stash saves your modifications away and reverts the working tree to a clean state.', tags: ['Git', 'Stash'] },
  { q: 'What does `git stash pop` do?', opts: ['Saves a new stash', 'Reapplies the latest stash and removes it from the stash list', 'Lists stashes', 'Clears all stashes'], a: 1, e: 'pop applies the most recent stash and then drops it.', tags: ['Git', 'Stash'] },
  { q: 'What does `git stash list` show?', opts: ['It applies a stash', 'The saved stashes', 'It deletes stashes', 'It creates one'], a: 1, e: 'git stash list shows your stack of saved stashes (stash@{0}, stash@{1}, …).', tags: ['Git', 'Stash'] },
  { q: 'How do `git stash apply` and `git stash pop` differ?', opts: ['They’re identical', 'apply keeps the stash; pop applies and then removes it', 'apply deletes it', 'Both keep it'], a: 1, e: 'apply leaves the stash in the list so you can reuse it; pop removes it after applying.', tags: ['Git', 'Stash'] },
  { q: 'What does the -u flag add here?\n\n```bash\n$ git stash -u\n```', opts: ['Ignores untracked files', 'Also stashes untracked files', 'Stashes only untracked files', 'Unstages everything'], a: 1, e: '-u (--include-untracked) stashes untracked files in addition to tracked changes.', tags: ['Git', 'Stash'] },
  { q: 'When is stashing most useful?', opts: ['When you want to delete code', 'When you must switch branches without committing work-in-progress', 'When you push code', 'When you tag a release'], a: 1, e: 'Stash lets you set aside half-done work to switch context, then restore it later.', tags: ['Git', 'Stash'] },
  { q: 'What does `git stash drop` do?', opts: ['Applies a stash', 'Deletes a specific stash entry', 'Lists stashes', 'Pops one'], a: 1, e: 'drop removes a stash entry without applying it.', tags: ['Git', 'Stash'] },
  { q: 'Where do stashes live?', opts: ['On the remote', 'In your local repository only (they’re not pushed)', 'In a branch', 'In a tag'], a: 1, e: 'Stashes are local; they aren’t shared with remotes.', tags: ['Git', 'Stash'] },

  // ── Level 12 — Rebasing ────────────────────────────────────────────────
  { q: 'On feature, what does `git rebase main` do?', opts: ['Merges main into feature', 'Replays feature’s commits on top of the latest main', 'Deletes feature', 'Pushes feature'], a: 1, e: 'Rebase rewrites feature’s commits so they start from the tip of main, giving a linear history.', tags: ['Git', 'Rebase'] },
  { q: 'How does rebase differ from merge?', opts: ['It adds a merge commit', 'It produces a linear history with no merge commit', 'It keeps two parents', 'They’re identical'], a: 1, e: 'Rebase moves commits to create a straight line; merge records a merge commit joining histories.', tags: ['Git', 'Rebase'] },
  { q: 'What does this let you do?\n\n```bash\n$ git rebase -i HEAD~3\n```', opts: ['Deletes 3 commits', 'Interactively reorder, squash, edit or drop the last 3 commits', 'Pushes 3 commits', 'Reverts 3 commits'], a: 1, e: 'Interactive rebase opens an editor to rewrite a range of commits (pick/squash/reword/drop/reorder).', tags: ['Git', 'Rebase'] },
  { q: 'What is the golden rule of rebasing?', opts: ['Always rebase main', 'Don’t rebase commits others have already pulled (public history)', 'Rebase before every commit', 'Never rebase locally'], a: 1, e: 'Rebasing rewrites history; doing it to shared commits forces painful re-syncs for everyone else.', tags: ['Git', 'Rebase'] },
  { q: 'After resolving a conflict mid-rebase, which command continues it?', opts: ['`git commit`', '`git rebase --continue`', '`git merge`', '`git push`'], a: 1, e: 'You stage the resolved files, then run git rebase --continue to proceed.', tags: ['Git', 'Rebase'] },
  { q: 'What does `git rebase --abort` do?', opts: ['Finishes the rebase', 'Cancels it and restores the original branch state', 'Squashes commits', 'Force-pushes'], a: 1, e: '--abort throws away the in-progress rebase and returns to where you started.', tags: ['Git', 'Rebase'] },
  { q: 'In interactive rebase, what does `squash` (s) do to a commit?', opts: ['Drops it', 'Combines it into the previous commit', 'Reorders it', 'Edits only its message'], a: 1, e: 'squash merges a commit into the one before it, letting you write a combined message.', tags: ['Git', 'Rebase'] },
  { q: 'After rebasing a branch you had already pushed, you usually must:', opts: ['Push normally', 'Force-push (the history was rewritten)', 'Only pull', 'Do nothing'], a: 1, e: 'Because rebase changes commit hashes, the remote rejects a normal push; force-push (with lease).', tags: ['Git', 'Rebase'] },

  // ── Level 13 — Tags & .gitignore ───────────────────────────────────────
  { q: 'What does this do?\n\n```bash\n$ git tag v1.0\n```', opts: ['Creates a branch', 'Creates a lightweight tag at the current commit', 'Pushes a release', 'Deletes a tag'], a: 1, e: 'git tag <name> marks the current commit with a lightweight tag.', tags: ['Git', 'Tags'] },
  { q: 'What does this create?\n\n```bash\n$ git tag -a v1.0 -m "Release 1.0"\n```', opts: ['A lightweight tag', 'An annotated tag (a full object with message and author)', 'A branch', 'A commit'], a: 1, e: 'Annotated tags (-a) store a message, tagger and date — preferred for releases.', tags: ['Git', 'Tags'] },
  { q: 'When are tags sent to a remote?', opts: ['Automatically with every push', 'Only when you explicitly push them (e.g. git push --tags)', 'Never', 'During fetch'], a: 1, e: 'Tags aren’t pushed by default; push them with `git push <remote> <tag>` or `--tags`.', tags: ['Git', 'Tags'] },
  { q: 'What is a `.gitignore` file for?', opts: ['Listing files to delete', 'Listing patterns of files Git should not track', 'Storing commit messages', 'Configuring remotes'], a: 1, e: '.gitignore tells Git which untracked files/patterns to leave alone (build output, secrets, etc.).', tags: ['Git', 'gitignore'] },
  { q: 'You add an already-tracked file to `.gitignore`. What happens?', opts: ['It’s removed automatically', 'It stays tracked until you untrack it (e.g. git rm --cached)', 'It’s deleted', 'It’s stashed'], a: 1, e: '.gitignore only affects untracked files; already-tracked files keep being tracked until removed from the index.', tags: ['Git', 'gitignore'] },
  { q: 'What does this do?\n\n```bash\n$ git rm --cached secrets.env\n```', opts: ['Deletes the file from disk', 'Stops tracking it but keeps it in your working directory', 'Ignores it', 'Stashes it'], a: 1, e: '--cached removes the file from the index (untracks it) while leaving the local file in place.', tags: ['Git', 'gitignore'] },
  { q: 'A release pipeline needs a stable name such as v2.3.0 for one exact commit. Which Git reference fits best?', opts: ['A short-lived work branch', 'A tag pointing to the release commit', 'A .gitignore pattern', 'A stash entry on one developer’s machine'], a: 1, e: 'Tags name fixed points in history and are commonly used for releases. Branches are expected to move as new commits are added.', tags: ['Git', 'Tags'] },
  { q: 'What does this do?\n\n```bash\n$ git push origin v1.0\n```', opts: ['Pushes a branch', 'Pushes the v1.0 tag to origin', 'Deletes the tag', 'Creates a release page'], a: 1, e: 'It uploads the specific tag to the remote.', tags: ['Git', 'Tags'] },

  // ── Level 14 — Collaboration & PRs ─────────────────────────────────────
  { q: 'What is a pull request (PR)?', opts: ['A Git command', 'A proposal to merge a branch, with review and discussion', 'A type of clone', 'A stash'], a: 1, e: 'A PR (or merge request) is a hosting-platform feature to review and merge a branch’s changes.', tags: ['Git', 'Collaboration'] },
  { q: 'Is `git fork` a core Git command?', opts: ['Yes, it’s built in', 'No — forking is a hosting (e.g. GitHub) feature that copies a repo to your account', 'It’s the same as clone', 'It’s a branch'], a: 1, e: 'Fork is a platform concept (server-side copy); Git itself has no fork command.', tags: ['Git', 'Collaboration'] },
  { q: 'What’s the typical contribution flow?', opts: ['Commit to main directly', 'Branch → commit → push → open a PR', 'Push then branch', 'Open a PR then commit'], a: 1, e: 'You create a branch, commit your work, push it, then open a PR for review and merge.', tags: ['Git', 'Collaboration'] },
  { q: 'In a fork workflow, the `upstream` remote usually points to:', opts: ['Your fork', 'The original repository you forked from', 'A tag', 'Your local repo'], a: 1, e: 'upstream is conventionally the source project, so you can pull in its latest changes.', tags: ['Git', 'Collaboration'] },
  { q: 'How do you keep a fork up to date with the original?', opts: ['Delete and re-fork', 'Fetch from upstream and merge/rebase it into your branch', 'Push to upstream', 'Ignore it'], a: 1, e: 'You fetch upstream and integrate its changes into your local branch, then push to your fork.', tags: ['Git', 'Collaboration'] },
  { q: 'What does code review on a PR enable?', opts: ['Running the code', 'Comments and approvals/requested changes before merging', 'Automatic deployment', 'Tagging a release'], a: 1, e: 'Reviewers can comment, approve, or request changes, improving quality before the merge.', tags: ['Git', 'Collaboration'] },
  { q: 'What does marking a PR as "draft" signal?', opts: ['It auto-merges', 'It’s a work in progress, not ready to merge', 'It deletes the branch', 'It’s private'], a: 1, e: 'Draft PRs share progress and invite early feedback while indicating it isn’t ready to merge.', tags: ['Git', 'Collaboration'] },
  { q: 'After a PR is merged, the feature branch:', opts: ['Must stay forever', 'Can usually be deleted', 'Becomes main', 'Must be force-pushed'], a: 1, e: 'Once merged, the branch has served its purpose and is typically deleted.', tags: ['Git', 'Collaboration'] },

  // ── Level 15 — Advanced Git ────────────────────────────────────────────
  { q: 'What does `git cherry-pick <commit>` do?', opts: ['Merges a whole branch', 'Applies a single specific commit onto the current branch', 'Deletes a commit', 'Rebases everything'], a: 1, e: 'cherry-pick copies the changes from one commit and applies them as a new commit on your branch.', tags: ['Git', 'Advanced'] },
  { q: 'What does `git reflog` show?', opts: ['The normal commit log', 'A history of where HEAD has pointed (useful for recovery)', 'Remote refs', 'Tags'], a: 1, e: 'reflog records every move of HEAD, so you can recover commits that seem lost (e.g. after a reset).', tags: ['Git', 'Advanced'] },
  { q: 'What does `git bisect` help you do?', opts: ['Split a repository', 'Binary-search the history to find the commit that introduced a bug', 'Bisect a single file', 'Merge two halves'], a: 1, e: 'bisect checks out midpoints between a good and bad commit so you can quickly locate the breaking commit.', tags: ['Git', 'Advanced'] },
  { q: 'What is a "detached HEAD"?', opts: ['A broken repo', 'HEAD pointing directly at a commit instead of a branch', 'A repo with no commits', 'A missing remote'], a: 1, e: 'When you check out a commit (not a branch), HEAD is detached; new commits won’t belong to any branch unless you make one.', tags: ['Git', 'Advanced'] },
  { q: 'What is a Git submodule?', opts: ['A subfolder', 'A reference to another repository nested at a path inside yours', 'A branch', 'A tag'], a: 1, e: 'A submodule pins another repo at a specific commit inside your repository.', tags: ['Git', 'Advanced'] },
  { q: 'What are Git hooks?', opts: ['Remote configs', 'Scripts that run automatically on Git events (e.g. pre-commit)', 'Aliases', 'Tags'], a: 1, e: 'Hooks are scripts in `.git/hooks` that fire on events like commit, push, or merge.', tags: ['Git', 'Advanced'] },
  { q: 'You ran `git reset --hard` and lost a commit. How can you recover it?', opts: ['It’s gone forever', 'Use `git reflog` to find it, then reset/checkout to it', 'Run `git pull`', 'Run `git stash`'], a: 1, e: 'The commit usually still exists; reflog reveals its hash so you can restore it before garbage collection.', tags: ['Git', 'Advanced'] },
  { q: 'When is `git cherry-pick` especially handy?', opts: ['Merging everything', 'Porting a single fix to another branch', 'Deleting commits', 'Tagging releases'], a: 1, e: 'It’s ideal for applying one specific commit (like a hotfix) to another branch without merging the whole thing.', tags: ['Git', 'Advanced'] },
];
