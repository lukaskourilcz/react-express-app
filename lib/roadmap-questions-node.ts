// Node.js roadmap questions (200): 25 levels × 8, easiest → hardest. Authored as
// a single 8-per-level list and built directly (like Git/HTML/CSS), so each
// 5-level checkpoint covers a full 40-question pool. See lib/roadmap.ts for the
// level titles.

import type { Seed } from './roadmap-build';

export const nodeSeeds: Seed[] = [
  // ── Level 1 — Node Basics ──────────────────────────────────────────────
  { q: 'What is Node.js?', opts: ['A web browser', 'A JavaScript runtime built on the V8 engine', 'A CSS framework', 'A database server'], a: 1, e: 'Node.js is a JavaScript runtime built on Chrome’s V8 engine that runs JS outside the browser.', tags: ['Node', 'Basics'] },
  { q: 'Where does Node.js let you run JavaScript?', opts: ['Only in the browser', 'Outside the browser, e.g. on a server', 'Only inside HTML', 'Only in mobile apps'], a: 1, e: 'Node.js executes JavaScript on the server / your machine, independent of any browser.', tags: ['Node', 'Basics'] },
  { q: 'Which JavaScript engine powers Node.js?', opts: ['SpiderMonkey', 'Chakra', 'V8', 'JavaScriptCore'], a: 2, e: 'Node.js embeds Google’s V8 engine, the same one used by Chrome.', tags: ['Node', 'Basics'] },
  { q: 'How is JavaScript execution modeled in Node?', opts: ['Multi-threaded by default', 'Single-threaded with an event loop', 'One process per request', 'No concurrency at all'], a: 1, e: 'Your JS runs on a single thread; concurrency comes from the non-blocking event loop.', tags: ['Node', 'Basics'] },
  { q: 'What does this run?\n\n```bash\n$ node app.js\n```', opts: ['Compiles app.js to a binary', 'Executes app.js with Node', 'Opens app.js in an editor', 'Installs app.js'], a: 1, e: 'node <file> runs the given JavaScript file in the Node runtime.', tags: ['Node', 'Basics'] },
  { q: 'Node.js is best known for which I/O model?', opts: ['Blocking, synchronous I/O', 'Non-blocking, event-driven I/O', 'No I/O support', 'Polling-only I/O'], a: 1, e: 'Node uses an event-driven, non-blocking I/O model that scales well for I/O-heavy work.', tags: ['Node', 'Basics'] },
  { q: 'Which browser-only global is NOT available in Node?', opts: ['console', 'setTimeout', 'document', 'Math'], a: 2, e: 'Node has no DOM, so `document` (and `window`) don’t exist; console/setTimeout/Math do.', tags: ['Node', 'Basics'] },
  { q: 'Running `node` with no arguments starts:', opts: ['A web server', 'An interactive REPL', 'A file watcher', 'The package manager'], a: 1, e: 'Plain `node` opens a REPL (Read-Eval-Print Loop) for evaluating JS interactively.', tags: ['Node', 'Basics'] },

  // ── Level 2 — Modules (CommonJS) ───────────────────────────────────────
  { q: 'In CommonJS, how do you import another module?', opts: ['import x from "x"', 'require("x")', 'include("x")', 'load("x")'], a: 1, e: 'CommonJS uses the require() function to load modules.', tags: ['Node', 'Modules'] },
  { q: 'How do you export a value in CommonJS?', opts: ['export default value', 'module.exports = value', 'return value', 'global.value'], a: 1, e: 'Assigning to module.exports sets what require() returns for that module.', tags: ['Node', 'Modules'] },
  { q: 'What does require() return?', opts: ['Always undefined', 'The loaded module’s module.exports', 'A Promise', 'The file path'], a: 1, e: 'require() returns whatever the target module assigned to module.exports.', tags: ['Node', 'Modules'] },
  { q: 'Are CommonJS modules cached after the first require?', opts: ['No, re-read every time', 'Yes, the same instance is reused', 'Only in production', 'Only for core modules'], a: 1, e: 'Node caches modules after first load, so repeated require() calls return the same exports object.', tags: ['Node', 'Modules'] },
  { q: 'What does `__dirname` contain?', opts: ['The file’s contents', 'The absolute directory of the current module', 'The project root URL', 'The OS temp folder'], a: 1, e: '__dirname is the absolute path of the directory containing the current module file.', tags: ['Node', 'Modules'] },
  { q: 'What does `require("./utils")` load?', opts: ['An npm package', 'A local file module named utils', 'A built-in core module', 'A remote URL'], a: 1, e: 'A path starting with ./ resolves to a local file relative to the current module.', tags: ['Node', 'Modules'] },
  { q: 'Why can reassigning `exports = {…}` fail to export?', opts: ['It throws an error', 'It breaks the reference to module.exports', 'exports is read-only', 'It exports twice'], a: 1, e: '`exports` just points at module.exports; reassigning it locally breaks that link — assign to module.exports instead.', tags: ['Node', 'Modules'] },
  { q: 'What does `require("fs")` load?', opts: ['A local file', 'The built-in file-system core module', 'An npm dependency', 'A JSON file'], a: 1, e: 'A bare name like "fs" resolves to a built-in core module.', tags: ['Node', 'Modules'] },

  // ── Level 3 — ES Modules ───────────────────────────────────────────────
  { q: 'What is the ES Modules import syntax?', opts: ['require("./x")', 'import x from "./x.js"', 'include "./x"', 'load("./x")'], a: 1, e: 'ESM uses the static `import` statement to bring in bindings from other modules.', tags: ['Node', 'ESM'] },
  { q: 'How do you make .js files use ES Modules in a package?', opts: ['Set "type": "module" in package.json', 'Rename to .cjs', 'Add "use esm"', 'Nothing is needed'], a: 0, e: 'Setting "type": "module" in package.json makes .js files load as ES Modules.', tags: ['Node', 'ESM'] },
  { q: 'Which file extension is always treated as an ES Module?', opts: ['.cjs', '.mjs', '.node', '.json'], a: 1, e: '.mjs files are always ESM; .cjs is always CommonJS regardless of package.json.', tags: ['Node', 'ESM'] },
  { q: 'What is a named export in ESM?', opts: ['export default fn', 'export const x = 1', 'module.exports.x = 1', 'global.x = 1'], a: 1, e: '`export const x = 1` creates a named export imported with `import { x }`.', tags: ['Node', 'ESM'] },
  { q: 'ESM `import` statements are:', opts: ['Dynamic and runtime-only', 'Static and hoisted', 'Optional', 'Asynchronous functions'], a: 1, e: 'ESM imports are statically analyzable and hoisted to the top of the module.', tags: ['Node', 'ESM'] },
  { q: 'Can you call `require()` directly inside an ES Module?', opts: ['Yes, always', 'No — use import or createRequire', 'Only for JSON', 'Only in tests'], a: 1, e: 'require is not defined in ESM scope; use import, or module.createRequire if you must.', tags: ['Node', 'ESM'] },
  { q: 'What is the default export syntax in ESM?', opts: ['export default value', 'module.exports = value', 'export value', 'default export value'], a: 0, e: '`export default value` defines the module’s default export.', tags: ['Node', 'ESM'] },
  { q: 'Is top-level await allowed in an ES Module?', opts: ['No, never', 'Yes', 'Only in .cjs files', 'Only inside functions'], a: 1, e: 'ES Modules support top-level await; CommonJS modules do not.', tags: ['Node', 'ESM'] },

  // ── Level 4 — Globals & process ────────────────────────────────────────
  { q: 'What is the global object in Node?', opts: ['window', 'global (a.k.a. globalThis)', 'self', 'document'], a: 1, e: 'In Node the top-level global object is `global`; the standard `globalThis` also points to it.', tags: ['Node', 'Globals'] },
  { q: 'What does `process.argv` contain?', opts: ['Environment variables', 'The command-line arguments', 'The exit code', 'The current directory'], a: 1, e: 'process.argv is an array of the command-line arguments (node path, script path, then user args).', tags: ['Node', 'process'] },
  { q: 'What is `process.env`?', opts: ['The event loop', 'An object of environment variables', 'The process id', 'The argument list'], a: 1, e: 'process.env holds the environment variables visible to the process.', tags: ['Node', 'process'] },
  { q: 'What does `process.exit(0)` do?', opts: ['Restarts Node', 'Ends the process with a success code', 'Throws an error', 'Pauses execution'], a: 1, e: 'process.exit(0) terminates the process immediately with exit code 0 (success).', tags: ['Node', 'process'] },
  { q: 'What does `process.cwd()` return?', opts: ['The script’s folder', 'The current working directory', 'The home directory', 'The temp directory'], a: 1, e: 'process.cwd() returns the directory the Node process was launched from.', tags: ['Node', 'process'] },
  { q: 'Where does `console.log` write by default?', opts: ['A log file', 'stdout', 'stderr', 'The network'], a: 1, e: 'console.log writes to the standard output stream (stdout).', tags: ['Node', 'Globals'] },
  { q: 'What does `process.platform` give?', opts: ['The Node version', 'The operating-system platform (e.g. "linux")', 'The CPU model', 'The hostname'], a: 1, e: 'process.platform reports the OS platform such as "linux", "darwin" or "win32".', tags: ['Node', 'process'] },
  { q: 'Which is NOT a Node global?', opts: ['__dirname', 'process', 'window', 'Buffer'], a: 2, e: '`window` is a browser global; Node provides __dirname, process and Buffer instead.', tags: ['Node', 'Globals'] },

  // ── Level 5 — The Event Loop ───────────────────────────────────────────
  { q: 'What does the event loop enable?', opts: ['Multi-threaded JS', 'Non-blocking async work on one thread', 'Faster CPU math', 'Parallel garbage collection'], a: 1, e: 'The event loop lets a single JS thread handle many async operations without blocking.', tags: ['Node', 'EventLoop'] },
  { q: 'A `setTimeout` callback runs in which event-loop phase?', opts: ['The poll phase', 'The timers phase', 'The close phase', 'The idle phase'], a: 1, e: 'Expired setTimeout/setInterval callbacks run in the timers phase.', tags: ['Node', 'EventLoop'] },
  { q: 'When does a `process.nextTick` callback run?', opts: ['After all timers', 'Right after the current operation, before the loop continues', 'Once per second', 'Only on exit'], a: 1, e: 'nextTick callbacks run immediately after the current operation, before the event loop proceeds.', tags: ['Node', 'EventLoop'] },
  { q: 'When do Promise reaction callbacks (microtasks) run?', opts: ['Before the current sync code finishes', 'After the current operation, before macrotasks like timers', 'Only after I/O', 'Never automatically'], a: 1, e: 'Promise microtasks drain after the current operation completes and before the next macrotask.', tags: ['Node', 'EventLoop'] },
  { q: 'In which phase does `setImmediate` fire?', opts: ['The timers phase', 'The check phase (after I/O)', 'Before any I/O', 'The close phase'], a: 1, e: 'setImmediate callbacks run in the check phase, right after the poll/I/O phase.', tags: ['Node', 'EventLoop'] },
  { q: 'Which runs first: `process.nextTick` or a Promise `.then`?', opts: ['The Promise .then', 'process.nextTick', 'They run together', 'Whichever was scheduled first'], a: 1, e: 'Node drains the nextTick queue before the Promise microtask queue.', tags: ['Node', 'EventLoop'] },
  { q: 'What happens if you run a long synchronous loop?', opts: ['Other threads keep working', 'It blocks the event loop, stalling timers and I/O', 'Node spawns a worker', 'Nothing changes'], a: 1, e: 'Heavy synchronous work blocks the single thread, so timers, I/O and callbacks all wait.', tags: ['Node', 'EventLoop'] },
  { q: 'Which library provides Node’s event loop and thread pool?', opts: ['V8', 'libuv', 'OpenSSL', 'zlib'], a: 1, e: 'libuv implements the event loop and the worker thread pool used for some async operations.', tags: ['Node', 'EventLoop'] },

  // ── Level 6 — Path ─────────────────────────────────────────────────────
  { q: 'What does `path.join("a", "b")` do?', opts: ['Returns "ab"', 'Joins segments with the OS separator → "a/b"', 'Resolves to absolute', 'Throws'], a: 1, e: 'path.join concatenates path segments using the platform separator and normalizes them.', tags: ['Node', 'Path'] },
  { q: 'What does `path.resolve` produce?', opts: ['A relative path', 'An absolute path', 'A URL', 'A file’s contents'], a: 1, e: 'path.resolve builds an absolute path, resolving from right to left against the cwd if needed.', tags: ['Node', 'Path'] },
  { q: 'What is `path.basename("/a/b.txt")`?', opts: ['"/a"', '"b.txt"', '".txt"', '"b"'], a: 1, e: 'path.basename returns the last portion of a path — the file name "b.txt".', tags: ['Node', 'Path'] },
  { q: 'What is `path.extname("index.html")`?', opts: ['"index"', '"html"', '".html"', '""'], a: 2, e: 'path.extname returns the extension including the dot: ".html".', tags: ['Node', 'Path'] },
  { q: 'What is `path.dirname("/a/b/c.js")`?', opts: ['"c.js"', '"/a/b"', '"/a"', '"/a/b/c.js"'], a: 1, e: 'path.dirname returns the directory part: "/a/b".', tags: ['Node', 'Path'] },
  { q: 'On Linux/macOS, what is `path.sep`?', opts: ['"\\\\"', '"/"', '":"', '","'], a: 1, e: 'On POSIX systems the path separator is "/"; on Windows it is "\\".', tags: ['Node', 'Path'] },
  { q: 'What does `path.parse(p)` return?', opts: ['A string', 'An object with root, dir, base, ext, name', 'An array', 'A Buffer'], a: 1, e: 'path.parse splits a path into an object: { root, dir, base, ext, name }.', tags: ['Node', 'Path'] },
  { q: 'How does `path.resolve` differ from `path.join`?', opts: ['They are identical', 'resolve yields an absolute path; join just combines segments', 'join is absolute; resolve is relative', 'resolve removes the extension'], a: 1, e: 'join simply combines and normalizes; resolve walks to an absolute path from the cwd.', tags: ['Node', 'Path'] },

  // ── Level 7 — File System ──────────────────────────────────────────────
  { q: 'Which module reads and writes files?', opts: ['os', 'fs', 'path', 'net'], a: 1, e: 'The built-in `fs` module provides file-system operations.', tags: ['Node', 'fs'] },
  { q: 'How does `fs.readFileSync` behave?', opts: ['Asynchronously', 'Synchronously, blocking until done', 'It streams', 'It returns a Promise'], a: 1, e: 'The *Sync variants block the event loop until the operation completes.', tags: ['Node', 'fs'] },
  { q: 'What does `fs.promises.readFile` return?', opts: ['A Buffer immediately', 'A Promise', 'undefined', 'A stream'], a: 1, e: 'fs.promises methods return Promises so you can await them.', tags: ['Node', 'fs'] },
  { q: 'How do you read a file as text instead of a Buffer?', opts: ['Pass an encoding like "utf8"', 'Call .toText()', 'Use path.read', 'It is always text'], a: 0, e: 'Passing an encoding (e.g. "utf8") makes fs return a decoded string instead of a Buffer.', tags: ['Node', 'fs'] },
  { q: 'What does `fs.writeFile` do when the file already exists?', opts: ['Appends to it', 'Overwrites its contents', 'Throws an error', 'Renames it'], a: 1, e: 'writeFile replaces the file’s contents by default.', tags: ['Node', 'fs'] },
  { q: 'What does `fs.appendFile` do?', opts: ['Overwrites the file', 'Adds data to the end of the file', 'Deletes the file', 'Reads the file'], a: 1, e: 'appendFile adds data to the end, creating the file if it doesn’t exist.', tags: ['Node', 'fs'] },
  { q: 'What does `fs.mkdir(dir, { recursive: true })` do?', opts: ['Fails if parents are missing', 'Creates the directory and any missing parents', 'Removes a directory', 'Lists a directory'], a: 1, e: 'recursive: true creates nested directories, making missing parent folders as needed.', tags: ['Node', 'fs'] },
  { q: 'What does `fs.existsSync(p)` return?', opts: ['The file contents', 'A boolean: whether the path exists', 'The file size', 'A Promise'], a: 1, e: 'existsSync synchronously returns true/false for whether the path exists.', tags: ['Node', 'fs'] },

  // ── Level 8 — OS & Util ────────────────────────────────────────────────
  { q: 'What does `os.platform()` return?', opts: ['The Node version', 'The OS platform string', 'The CPU count', 'The hostname'], a: 1, e: 'os.platform() returns the operating-system platform, e.g. "linux" or "darwin".', tags: ['Node', 'os'] },
  { q: 'What does `os.cpus()` return?', opts: ['Total memory', 'An array describing each CPU core', 'A single number', 'The load average'], a: 1, e: 'os.cpus() returns an array with model/speed/times for each logical CPU core.', tags: ['Node', 'os'] },
  { q: 'What does `os.totalmem()` return?', opts: ['Free disk space', 'Total system memory in bytes', 'Used memory percent', 'The page size'], a: 1, e: 'os.totalmem() returns the total amount of system memory in bytes.', tags: ['Node', 'os'] },
  { q: 'What does `os.homedir()` return?', opts: ['The temp folder', 'The current user’s home directory', 'The cwd', 'The root directory'], a: 1, e: 'os.homedir() returns the home directory of the current user.', tags: ['Node', 'os'] },
  { q: 'What does `util.promisify(fn)` do?', opts: ['Caches fn', 'Wraps an error-first callback fn to return a Promise', 'Makes fn synchronous', 'Logs fn calls'], a: 1, e: 'util.promisify converts a function using the (err, result) callback style into one returning a Promise.', tags: ['Node', 'util'] },
  { q: 'What does `os.hostname()` return?', opts: ['The IP address', 'The machine’s hostname', 'The username', 'The OS version'], a: 1, e: 'os.hostname() returns the host name of the operating system.', tags: ['Node', 'os'] },
  { q: 'What is `util.inspect(obj)` useful for?', opts: ['Deleting an object', 'Producing a readable string representation for debugging', 'Validating types', 'Cloning an object'], a: 1, e: 'util.inspect formats objects into a human-readable string, used by console.log internally.', tags: ['Node', 'util'] },
  { q: 'What does `os.EOL` represent?', opts: ['The end of a file', 'The platform’s end-of-line marker', 'A null byte', 'The exit code'], a: 1, e: 'os.EOL is the line-ending string for the platform ("\\n" on POSIX, "\\r\\n" on Windows).', tags: ['Node', 'os'] },

  // ── Level 9 — Events ───────────────────────────────────────────────────
  { q: 'What does `emitter.on("evt", fn)` do?', opts: ['Emits the event', 'Registers fn as a listener for "evt"', 'Removes a listener', 'Runs fn once and removes it'], a: 1, e: '.on() subscribes a listener that runs every time the event is emitted.', tags: ['Node', 'Events'] },
  { q: 'What does `emitter.emit("evt", data)` do?', opts: ['Schedules the event for later', 'Synchronously calls all listeners for "evt"', 'Removes listeners', 'Returns a Promise'], a: 1, e: 'emit() calls each registered listener synchronously, passing along any arguments.', tags: ['Node', 'Events'] },
  { q: 'How does `emitter.once` differ from `.on`?', opts: ['It never fires', 'The listener fires at most once, then is removed', 'It fires twice', 'It is asynchronous'], a: 1, e: '.once() registers a listener that automatically unsubscribes after its first call.', tags: ['Node', 'Events'] },
  { q: 'Which removes a previously added listener?', opts: ['emitter.stop', 'emitter.removeListener / emitter.off', 'emitter.delete', 'emitter.clear'], a: 1, e: 'removeListener (aliased as off) detaches a specific listener function.', tags: ['Node', 'Events'] },
  { q: 'What is special about the "error" event?', opts: ['It is ignored', 'If no listener exists, it throws and can crash the process', 'It retries automatically', 'It pauses the loop'], a: 1, e: 'An emitted "error" with no listener is re-thrown, often crashing the process.', tags: ['Node', 'Events'] },
  { q: 'Which Node objects extend EventEmitter?', opts: ['None', 'Streams, HTTP servers and many core objects', 'Only timers', 'Only Buffers'], a: 1, e: 'Many core APIs (streams, servers, sockets) inherit from EventEmitter.', tags: ['Node', 'Events'] },
  { q: 'What does `emitter.listenerCount("evt")` return?', opts: ['The event data', 'The number of listeners for that event', 'A boolean', 'The last result'], a: 1, e: 'listenerCount returns how many listeners are registered for the given event.', tags: ['Node', 'Events'] },
  { q: 'What is the default max listeners before a warning?', opts: ['1', '10', '100', 'Unlimited'], a: 1, e: 'EventEmitter warns when more than 10 listeners are added for one event (a possible leak).', tags: ['Node', 'Events'] },

  // ── Level 10 — Streams ─────────────────────────────────────────────────
  { q: 'What is the advantage of streams?', opts: ['They load everything into memory', 'They process data in chunks, incrementally', 'They are synchronous', 'They avoid the event loop'], a: 1, e: 'Streams handle data piece by piece, keeping memory low for large inputs.', tags: ['Node', 'Streams'] },
  { q: 'What are the four fundamental stream types?', opts: ['Open, Close, Read, Write', 'Readable, Writable, Duplex, Transform', 'In, Out, Pipe, Buffer', 'Sync, Async, File, Net'], a: 1, e: 'Node streams come in Readable, Writable, Duplex and Transform varieties.', tags: ['Node', 'Streams'] },
  { q: 'What does a readable stream’s "data" event provide?', opts: ['The whole file', 'A chunk of data', 'An error', 'The byte count'], a: 1, e: 'In flowing mode, the "data" event delivers each chunk as it arrives.', tags: ['Node', 'Streams'] },
  { q: 'What does `readable.pipe(writable)` do?', opts: ['Copies a file synchronously', 'Connects a readable to a writable, forwarding data', 'Closes both streams', 'Reverses the stream'], a: 1, e: 'pipe() forwards all data from a readable into a writable, handling backpressure.', tags: ['Node', 'Streams'] },
  { q: 'What does `fs.createReadStream("f")` return?', opts: ['The file contents', 'A Readable stream of the file', 'A Promise', 'A Buffer'], a: 1, e: 'createReadStream returns a Readable stream so you can read a file incrementally.', tags: ['Node', 'Streams'] },
  { q: 'What is "backpressure"?', opts: ['A network error', 'Slowing the producer when the consumer can’t keep up', 'Reversing data flow', 'A type of compression'], a: 1, e: 'Backpressure is the mechanism that pauses a fast producer so a slower consumer isn’t overwhelmed.', tags: ['Node', 'Streams'] },
  { q: 'What does a Transform stream do?', opts: ['Only reads', 'Reads input, transforms it, and writes output (e.g. gzip)', 'Only writes', 'Deletes data'], a: 1, e: 'A Transform is a Duplex stream whose output is computed from its input, like compression.', tags: ['Node', 'Streams'] },
  { q: 'What does the "end" event on a readable mean?', opts: ['An error occurred', 'There is no more data to read', 'The stream paused', 'A new chunk arrived'], a: 1, e: 'The "end" event fires once all data has been consumed from the readable.', tags: ['Node', 'Streams'] },

  // ── Level 11 — Buffers ─────────────────────────────────────────────────
  { q: 'What does a Buffer represent?', opts: ['A string only', 'A fixed-length chunk of raw binary data', 'A JSON object', 'A stream'], a: 1, e: 'A Buffer is a fixed-size sequence of bytes for handling raw binary data.', tags: ['Node', 'Buffers'] },
  { q: 'What does `Buffer.from("hi")` create?', opts: ['A string', 'A Buffer holding those bytes', 'An array of chars', 'A Promise'], a: 1, e: 'Buffer.from builds a Buffer from a string (or array/ArrayBuffer).', tags: ['Node', 'Buffers'] },
  { q: 'What does `buf.toString("utf8")` do?', opts: ['Returns the byte length', 'Decodes the bytes into a UTF-8 string', 'Clears the buffer', 'Encrypts it'], a: 1, e: 'toString decodes a Buffer’s bytes using the given encoding.', tags: ['Node', 'Buffers'] },
  { q: 'When are Buffers most useful?', opts: ['For DOM manipulation', 'For binary data and file/network I/O', 'For CSS', 'For routing'], a: 1, e: 'Buffers shine when handling raw binary data from files, sockets or crypto.', tags: ['Node', 'Buffers'] },
  { q: 'What does `Buffer.alloc(10)` produce?', opts: ['A 10-character string', 'A zero-filled 10-byte buffer', 'An empty array', 'Ten buffers'], a: 1, e: 'Buffer.alloc(n) returns a new buffer of n bytes, initialized to zero.', tags: ['Node', 'Buffers'] },
  { q: 'What is the default encoding for `buf.toString()`?', opts: ['ascii', 'utf8', 'hex', 'base64'], a: 1, e: 'Without an argument, toString() decodes using UTF-8.', tags: ['Node', 'Buffers'] },
  { q: 'A Buffer’s length is measured in:', opts: ['Characters', 'Bytes', 'Bits', 'Words'], a: 1, e: 'buf.length is the number of bytes the Buffer holds.', tags: ['Node', 'Buffers'] },
  { q: 'How does `Buffer.allocUnsafe` differ from `Buffer.alloc`?', opts: ['It is slower', 'It skips zero-filling, so memory may contain old data', 'It throws', 'It returns a string'], a: 1, e: 'allocUnsafe is faster but the memory is uninitialized; you must overwrite it before use.', tags: ['Node', 'Buffers'] },

  // ── Level 12 — HTTP Server ─────────────────────────────────────────────
  { q: 'Which core module creates a basic HTTP server?', opts: ['net', 'http', 'url', 'dns'], a: 1, e: 'The built-in `http` module can create HTTP servers and clients.', tags: ['Node', 'HTTP'] },
  { q: 'What arguments does the `http.createServer` callback receive?', opts: ['(err, data)', '(req, res)', '(socket)', '(next)'], a: 1, e: 'The handler is called with the incoming request and the response object: (req, res).', tags: ['Node', 'HTTP'] },
  { q: 'What does `res.writeHead(200, headers)` set?', opts: ['The body', 'The status code and response headers', 'The URL', 'The method'], a: 1, e: 'writeHead sets the HTTP status code and response headers before the body.', tags: ['Node', 'HTTP'] },
  { q: 'What does `res.end()` do?', opts: ['Starts the server', 'Finishes the response (optionally sending data)', 'Closes the port', 'Throws'], a: 1, e: 'res.end signals the response is complete; you can pass a final chunk to send.', tags: ['Node', 'HTTP'] },
  { q: 'What does `server.listen(3000)` do?', opts: ['Sends a request', 'Binds the server to port 3000', 'Stops the server', 'Reads a file'], a: 1, e: 'listen starts the server accepting connections on the given port.', tags: ['Node', 'HTTP'] },
  { q: 'What does `req.url` give you?', opts: ['The response body', 'The request path/URL', 'The status code', 'The client IP'], a: 1, e: 'req.url is the path (and query string) of the incoming request.', tags: ['Node', 'HTTP'] },
  { q: 'What does `req.method` contain?', opts: ['The body', 'The HTTP method (GET, POST, …)', 'The headers', 'The URL'], a: 1, e: 'req.method is the request’s HTTP verb, such as "GET" or "POST".', tags: ['Node', 'HTTP'] },
  { q: 'To return JSON, which response header should you set?', opts: ['Content-Type: text/html', 'Content-Type: application/json', 'Accept: json', 'Encoding: json'], a: 1, e: 'Set Content-Type to application/json so clients parse the body as JSON.', tags: ['Node', 'HTTP'] },

  // ── Level 13 — Callbacks & Promisify ───────────────────────────────────
  { q: 'What is Node’s conventional callback signature?', opts: ['(data, err)', '(err, data)', '(result)', '(success, fail)'], a: 1, e: 'Node uses error-first callbacks: the first argument is the error (or null).', tags: ['Node', 'Callbacks'] },
  { q: 'Why does Node use error-first callbacks?', opts: ['To make code shorter', 'So errors are surfaced and handled explicitly', 'To improve speed', 'To avoid Promises'], a: 1, e: 'Putting the error first makes it a consistent, hard-to-ignore part of every async call.', tags: ['Node', 'Callbacks'] },
  { q: 'What is "callback hell"?', opts: ['A runtime error', 'Deeply nested callbacks that are hard to read', 'A type of stream', 'A missing callback'], a: 1, e: 'Callback hell is the pyramid of deeply nested callbacks that hurts readability.', tags: ['Node', 'Callbacks'] },
  { q: 'What kind of function can `util.promisify` wrap?', opts: ['Any function', 'One that uses the error-first callback convention', 'Only synchronous functions', 'Only Promises'], a: 1, e: 'promisify expects a function whose last argument is an (err, result) callback.', tags: ['Node', 'Callbacks'] },
  { q: 'After promisifying a function, you can use:', opts: ['require', 'await', 'emit', 'pipe'], a: 1, e: 'Once it returns a Promise, you can await the result instead of nesting callbacks.', tags: ['Node', 'Callbacks'] },
  { q: 'In `fs.readFile(path, cb)`, what is the callback’s first argument?', opts: ['The file data', 'The error (or null)', 'The encoding', 'The path'], a: 1, e: 'The first callback argument is the error, which is null on success.', tags: ['Node', 'Callbacks'] },
  { q: 'Converting nested callbacks to Promises mainly improves:', opts: ['Raw speed', 'Readability and error flow', 'Memory usage', 'Bundle size'], a: 1, e: 'Promises/async-await flatten nesting and centralize error handling.', tags: ['Node', 'Callbacks'] },
  { q: 'A callback that is accidentally invoked twice is:', opts: ['Expected', 'A bug — it should be called exactly once', 'Required for streams', 'How retries work'], a: 1, e: 'Error-first callbacks must be called once; double-calling causes subtle bugs.', tags: ['Node', 'Callbacks'] },

  // ── Level 14 — Async / Await ───────────────────────────────────────────
  { q: 'What does an `async` function always return?', opts: ['undefined', 'A Promise', 'The awaited value directly', 'A callback'], a: 1, e: 'An async function wraps its return value in a Promise.', tags: ['Node', 'Async'] },
  { q: 'What does `await` do?', opts: ['Blocks all threads', 'Pauses the async function until the promise settles', 'Cancels the promise', 'Creates a new thread'], a: 1, e: 'await suspends the async function (not the whole process) until the awaited promise resolves or rejects.', tags: ['Node', 'Async'] },
  { q: 'What happens when you `await` a rejected promise?', opts: ['It returns undefined', 'It throws, which you can catch', 'It retries', 'It is ignored'], a: 1, e: 'Awaiting a rejected promise throws the rejection reason at the await point.', tags: ['Node', 'Async'] },
  { q: 'How do you handle errors with async/await?', opts: ['.catch only', 'try/catch around the await', 'process.exit', 'Ignore them'], a: 1, e: 'Wrap awaited calls in try/catch to handle rejections like synchronous errors.', tags: ['Node', 'Async'] },
  { q: 'How do you run several promises concurrently and wait for all?', opts: ['Await them one by one', 'Promise.all([...])', 'A for-loop with await', 'setTimeout'], a: 1, e: 'Promise.all runs the promises concurrently and resolves when all complete.', tags: ['Node', 'Async'] },
  { q: 'Using `await` inside a normal for-loop runs the iterations:', opts: ['In parallel', 'Sequentially, one after another', 'Randomly', 'Never'], a: 1, e: 'Awaiting inside a loop waits for each iteration before starting the next — they run in series.', tags: ['Node', 'Async'] },
  { q: 'How does `Promise.allSettled` differ from `Promise.all`?', opts: ['It rejects faster', 'It waits for all and never short-circuits on rejection', 'It runs sequentially', 'It only takes two promises'], a: 1, e: 'allSettled resolves once every promise settles, reporting each as fulfilled or rejected.', tags: ['Node', 'Async'] },
  { q: 'async/await is syntactic sugar over:', opts: ['Callbacks', 'Promises', 'Streams', 'Events'], a: 1, e: 'async/await is built on top of Promises, making asynchronous code read synchronously.', tags: ['Node', 'Async'] },

  // ── Level 15 — Environment & CLI ───────────────────────────────────────
  { q: 'How do you read an environment variable named API_KEY?', opts: ['process.get("API_KEY")', 'process.env.API_KEY', 'env("API_KEY")', 'global.API_KEY'], a: 1, e: 'Environment variables are accessed via process.env.<NAME>.', tags: ['Node', 'CLI'] },
  { q: 'What is `process.argv[0]`?', opts: ['The first user argument', 'The path to the node executable', 'The script name', 'The exit code'], a: 1, e: 'argv[0] is the node binary path; argv[1] is the script; argv[2]+ are user args.', tags: ['Node', 'CLI'] },
  { q: 'What is `process.argv[2]`?', opts: ['The node path', 'The script path', 'The first user-supplied argument', 'Always undefined'], a: 2, e: 'The first argument the user passes appears at index 2 of process.argv.', tags: ['Node', 'CLI'] },
  { q: 'How do you set an env var inline on Unix when running a script?', opts: ['node app.js --PORT 3000', 'PORT=3000 node app.js', 'set PORT 3000', 'env.PORT = 3000'], a: 1, e: 'Prefixing the command with NAME=value sets the variable for that process: PORT=3000 node app.js.', tags: ['Node', 'CLI'] },
  { q: 'Are `.env` files loaded automatically by Node by default?', opts: ['Yes, always', 'No — use a loader (dotenv) or --env-file', 'Only in production', 'Only with TypeScript'], a: 1, e: 'Historically Node didn’t read .env on its own; you use dotenv or the newer --env-file flag.', tags: ['Node', 'CLI'] },
  { q: 'An exit code of 0 from a process conventionally means:', opts: ['An error', 'Success', 'A warning', 'A timeout'], a: 1, e: 'By convention exit code 0 indicates success; non-zero indicates failure.', tags: ['Node', 'CLI'] },
  { q: 'Which stream reads input typed by the user?', opts: ['process.stdout', 'process.stdin', 'process.stderr', 'process.env'], a: 1, e: 'process.stdin is the readable stream for standard input.', tags: ['Node', 'CLI'] },
  { q: 'What is `NODE_ENV` commonly set to?', opts: ['"fast" or "slow"', '"production" or "development"', '"on" or "off"', 'A port number'], a: 1, e: 'NODE_ENV typically holds the environment name, e.g. "development" or "production".', tags: ['Node', 'CLI'] },

  // ── Level 16 — npm & package.json ──────────────────────────────────────
  { q: 'What does the package.json "scripts" field define?', opts: ['Dependencies', 'Named commands runnable via npm run', 'The license', 'The author'], a: 1, e: 'scripts maps names to shell commands you invoke with `npm run <name>`.', tags: ['Node', 'npm'] },
  { q: 'How do dependencies differ from devDependencies?', opts: ['No difference', 'devDependencies are only needed for development/build/test', 'devDependencies ship to users', 'dependencies are optional'], a: 1, e: 'devDependencies (tools like test runners/bundlers) aren’t needed at runtime in production.', tags: ['Node', 'npm'] },
  { q: 'What does `npm install jest --save-dev` do?', opts: ['Adds jest to dependencies', 'Adds jest to devDependencies', 'Removes jest', 'Runs jest'], a: 1, e: '--save-dev (or -D) records the package under devDependencies.', tags: ['Node', 'npm'] },
  { q: 'What is package-lock.json for?', opts: ['Storing secrets', 'Pinning the exact resolved dependency versions', 'Listing scripts', 'Configuring Node'], a: 1, e: 'The lockfile records the exact versions of every installed package for reproducible installs.', tags: ['Node', 'npm'] },
  { q: 'What does the "main" field in package.json specify?', opts: ['The test command', 'The package’s entry-point module', 'The author', 'The Node version'], a: 1, e: '"main" is the file loaded when another module requires the package.', tags: ['Node', 'npm'] },
  { q: 'What does the version range `^1.2.3` allow?', opts: ['Only 1.2.3', '>=1.2.3 and <2.0.0', 'Any version', '<1.2.3'], a: 1, e: 'A caret allows compatible updates: the latest 1.x.x that is >=1.2.3 but below 2.0.0.', tags: ['Node', 'npm'] },
  { q: 'What does `npm ci` do?', opts: ['Edits package.json', 'Installs exactly from the lockfile, removing node_modules first', 'Publishes a package', 'Runs continuous integration tests'], a: 1, e: 'npm ci does a clean, lockfile-faithful install — ideal for CI and reproducible builds.', tags: ['Node', 'npm'] },
  { q: 'What lives in the node_modules folder?', opts: ['Your source code', 'Installed dependencies', 'Environment variables', 'Build output'], a: 1, e: 'node_modules holds the installed packages your project depends on.', tags: ['Node', 'npm'] },

  // ── Level 17 — Error Handling ──────────────────────────────────────────
  { q: 'Throwing inside an async function causes the returned promise to:', opts: ['Resolve', 'Reject', 'Hang forever', 'Retry'], a: 1, e: 'A throw in an async function rejects its returned promise with that error.', tags: ['Node', 'Errors'] },
  { q: 'Which event fires on a synchronous uncaught exception?', opts: ['process.on("exit")', 'process.on("uncaughtException")', 'process.on("error")', 'process.on("crash")'], a: 1, e: 'uncaughtException catches errors that bubble up with no handler — typically log and exit.', tags: ['Node', 'Errors'] },
  { q: 'Which event fires for a promise rejection with no .catch?', opts: ['unhandledRejection', 'rejection', 'promiseError', 'asyncError'], a: 0, e: 'process emits "unhandledRejection" when a promise rejects without a handler.', tags: ['Node', 'Errors'] },
  { q: 'What does a try/catch block catch?', opts: ['Only network errors', 'Synchronous throws and awaited rejections', 'Callback errors automatically', 'Nothing async'], a: 1, e: 'try/catch handles synchronous errors and, with await, asynchronous rejections.', tags: ['Node', 'Errors'] },
  { q: 'How do you create a custom error type?', opts: ['return "error"', 'class MyError extends Error', 'throw a string', 'new Object()'], a: 1, e: 'Extending the built-in Error class gives custom errors a name, message and stack.', tags: ['Node', 'Errors'] },
  { q: 'What does `error.stack` contain?', opts: ['The exit code', 'The stack trace where the error occurred', 'The HTTP status', 'The env vars'], a: 1, e: 'The stack property holds a trace of the call stack at the point the error was created.', tags: ['Node', 'Errors'] },
  { q: 'After an `uncaughtException`, the safest practice is to:', opts: ['Ignore it and continue', 'Log it and exit, since state may be corrupt', 'Restart the event loop', 'Swallow it silently'], a: 1, e: 'The process may be in an undefined state, so the recommendation is to log and exit (let a supervisor restart).', tags: ['Node', 'Errors'] },
  { q: 'How do operational and programmer errors differ?', opts: ['They are the same', 'Operational are expected runtime issues (e.g. network); programmer errors are bugs', 'Operational are always fatal', 'Programmer errors never crash'], a: 1, e: 'Operational errors (timeouts, bad input) are expected and handled; programmer errors are bugs to fix.', tags: ['Node', 'Errors'] },

  // ── Level 18 — Timers & Scheduling ─────────────────────────────────────
  { q: 'When does `setTimeout(fn, 0)` run fn?', opts: ['Immediately, synchronously', 'After the current synchronous code, in the timers phase', 'Never', 'Before the current code'], a: 1, e: 'Even with 0ms, the callback is deferred until the call stack clears and the timers phase runs.', tags: ['Node', 'Timers'] },
  { q: 'What does `setInterval` do?', opts: ['Runs once after a delay', 'Runs a callback repeatedly on an interval', 'Cancels a timer', 'Pauses execution'], a: 1, e: 'setInterval schedules a callback to run repeatedly every N milliseconds.', tags: ['Node', 'Timers'] },
  { q: 'What does `clearTimeout(id)` do?', opts: ['Runs the timer now', 'Cancels a pending timeout', 'Creates a timer', 'Pauses all timers'], a: 1, e: 'clearTimeout cancels a timeout that hasn’t fired yet, using its id.', tags: ['Node', 'Timers'] },
  { q: 'How does `setImmediate` differ from `setTimeout(fn, 0)`?', opts: ['They are identical', 'setImmediate runs in the check phase, after I/O', 'setImmediate is synchronous', 'setImmediate never runs'], a: 1, e: 'setImmediate fires in the check phase (after the poll/I/O phase), often after a 0ms timeout within an I/O cycle.', tags: ['Node', 'Timers'] },
  { q: 'Relative to timers and promises, `process.nextTick` callbacks run:', opts: ['Last', 'Before promise microtasks and timers', 'Only on exit', 'At a fixed interval'], a: 1, e: 'The nextTick queue is drained before promise microtasks and before the loop moves on to timers.', tags: ['Node', 'Timers'] },
  { q: 'Do Node timers guarantee an exact delay?', opts: ['Yes, to the millisecond', 'No — they guarantee a minimum delay, not exact timing', 'Only setInterval does', 'Only in production'], a: 1, e: 'Timers fire no earlier than the delay, but a busy event loop can push them later.', tags: ['Node', 'Timers'] },
  { q: 'What does calling `.unref()` on a timer do?', opts: ['Speeds it up', 'Lets the process exit if the timer is the only thing left', 'Cancels it', 'Doubles the delay'], a: 1, e: 'An unref’d timer won’t keep the event loop (and process) alive on its own.', tags: ['Node', 'Timers'] },
  { q: 'What does `queueMicrotask(fn)` schedule?', opts: ['A macrotask', 'A microtask, run after the current operation', 'A timer', 'A new thread'], a: 1, e: 'queueMicrotask enqueues fn onto the microtask queue, alongside promise reactions.', tags: ['Node', 'Timers'] },

  // ── Level 19 — Child Processes ─────────────────────────────────────────
  { q: 'Which module runs other programs or commands?', opts: ['cluster', 'child_process', 'worker_threads', 'net'], a: 1, e: 'The child_process module spawns subprocesses to run commands or other scripts.', tags: ['Node', 'ChildProcess'] },
  { q: 'What does `exec(cmd, cb)` do?', opts: ['Streams output with no shell', 'Runs a command in a shell and buffers the output', 'Forks a Node process', 'Creates a thread'], a: 1, e: 'exec runs the command in a shell and passes the buffered stdout/stderr to the callback.', tags: ['Node', 'ChildProcess'] },
  { q: 'How does `spawn` differ from `exec`?', opts: ['spawn buffers everything', 'spawn streams output and doesn’t use a shell by default', 'spawn is synchronous', 'spawn returns a Promise'], a: 1, e: 'spawn streams stdout/stderr (good for large output) and runs without a shell unless you ask.', tags: ['Node', 'ChildProcess'] },
  { q: 'What does `fork` create?', opts: ['A thread', 'A new Node process with an IPC channel', 'A shell command', 'A cluster'], a: 1, e: 'fork spawns a child Node process and sets up an IPC channel for message passing.', tags: ['Node', 'ChildProcess'] },
  { q: 'How do a parent and a forked child communicate?', opts: ['Shared variables', 'process.send and the "message" event', 'Global state', 'A database'], a: 1, e: 'Forked processes exchange messages via child.send / process.on("message").', tags: ['Node', 'ChildProcess'] },
  { q: 'A risk of `exec` with very large output is:', opts: ['It is too fast', 'Exceeding maxBuffer and erroring', 'Using no memory', 'Running twice'], a: 1, e: 'exec buffers all output in memory; output larger than maxBuffer throws an error.', tags: ['Node', 'ChildProcess'] },
  { q: 'What does `spawn` return?', opts: ['A string of output', 'A ChildProcess with stdout/stderr streams', 'A Buffer', 'undefined'], a: 1, e: 'spawn returns a ChildProcess object whose stdout/stderr are readable streams.', tags: ['Node', 'ChildProcess'] },
  { q: 'What does `execSync` do?', opts: ['Runs asynchronously', 'Runs a command synchronously and returns its output', 'Streams output', 'Forks a process'], a: 1, e: 'execSync blocks until the command finishes and returns its stdout.', tags: ['Node', 'ChildProcess'] },

  // ── Level 20 — Crypto ──────────────────────────────────────────────────
  { q: 'Which module provides cryptographic functions?', opts: ['hash', 'crypto', 'secure', 'cipher'], a: 1, e: 'The built-in `crypto` module offers hashing, HMAC, ciphers and random bytes.', tags: ['Node', 'Crypto'] },
  { q: 'What does `crypto.randomUUID()` return?', opts: ['A timestamp', 'A random RFC 4122 v4 UUID string', 'A hash', 'A Buffer'], a: 1, e: 'randomUUID generates a random version-4 UUID as a string.', tags: ['Node', 'Crypto'] },
  { q: 'What does `crypto.createHash("sha256")` create?', opts: ['A cipher', 'A hash object you update then digest', 'A random key', 'A certificate'], a: 1, e: 'createHash returns a Hash instance; you call update() with data then digest() for the result.', tags: ['Node', 'Crypto'] },
  { q: 'A cryptographic hash function is:', opts: ['Reversible', 'One-way (you can’t recover the input)', 'A form of encryption', 'Random each call'], a: 1, e: 'Hashes are one-way: the same input always yields the same digest, but you can’t invert it.', tags: ['Node', 'Crypto'] },
  { q: 'What does `crypto.randomBytes(16)` give you?', opts: ['A predictable sequence', '16 cryptographically strong random bytes', 'A UUID', 'A hash'], a: 1, e: 'randomBytes produces cryptographically secure random data, e.g. for tokens or salts.', tags: ['Node', 'Crypto'] },
  { q: 'What does an HMAC require that a plain hash does not?', opts: ['A larger input', 'A secret key', 'A salt only', 'A certificate'], a: 1, e: 'HMAC combines a hash with a secret key to authenticate a message.', tags: ['Node', 'Crypto'] },
  { q: 'How should you store user passwords?', opts: ['Plain sha256', 'With a slow password hash like bcrypt/scrypt/pbkdf2', 'Base64', 'In plain text'], a: 1, e: 'Use a deliberately slow, salted KDF (bcrypt, scrypt, argon2, pbkdf2) — not a fast raw hash.', tags: ['Node', 'Crypto'] },
  { q: 'What does `createHash("sha256").update(data).digest("hex")` return?', opts: ['The original data', 'The hash as a hex string', 'A Buffer of the input', 'A random value'], a: 1, e: 'digest("hex") finalizes the hash and returns it encoded as hexadecimal.', tags: ['Node', 'Crypto'] },

  // ── Level 21 — Express: Basics ─────────────────────────────────────────
  { q: 'What is Express?', opts: ['A database', 'A minimal web framework for Node', 'A template engine', 'A bundler'], a: 1, e: 'Express is a small, flexible web framework for building Node HTTP servers and APIs.', tags: ['Node', 'Express'] },
  { q: 'How do you create an Express app?', opts: ['new Express()', 'const app = express()', 'express.app()', 'createApp()'], a: 1, e: 'Calling the imported express() function returns an application object.', tags: ['Node', 'Express'] },
  { q: 'What does `app.get("/", handler)` do?', opts: ['Sends a GET request', 'Registers a handler for GET requests to "/"', 'Starts the server', 'Adds middleware globally'], a: 1, e: 'app.get registers a route handler for GET requests at the given path.', tags: ['Node', 'Express'] },
  { q: 'What is the signature of Express middleware?', opts: ['(req, res)', '(req, res, next)', '(err)', '(app)'], a: 1, e: 'Middleware functions take (req, res, next) and call next() to continue.', tags: ['Node', 'Express'] },
  { q: 'What does `app.listen(3000)` do?', opts: ['Defines a route', 'Starts the server listening on port 3000', 'Parses JSON', 'Adds middleware'], a: 1, e: 'app.listen binds the Express app to a port and starts accepting requests.', tags: ['Node', 'Express'] },
  { q: 'What does `express.json()` do?', opts: ['Sends JSON', 'Parses incoming JSON request bodies into req.body', 'Validates JSON', 'Stringifies responses'], a: 1, e: 'express.json() is built-in middleware that parses JSON bodies and populates req.body.', tags: ['Node', 'Express'] },
  { q: 'What does calling `next()` in middleware do?', opts: ['Ends the response', 'Passes control to the next matching middleware/handler', 'Restarts the request', 'Throws an error'], a: 1, e: 'next() hands off to the next middleware in the chain; without it the request stalls.', tags: ['Node', 'Express'] },
  { q: 'What does `app.use(fn)` do?', opts: ['Defines a GET route', 'Mounts middleware that runs for matching requests', 'Starts the server', 'Sends a response'], a: 1, e: 'app.use registers middleware (optionally path-scoped) in the request pipeline.', tags: ['Node', 'Express'] },

  // ── Level 22 — Express: Req & Res ──────────────────────────────────────
  { q: 'In Express, what is `req.params`?', opts: ['The query string', 'The named route parameters (e.g. :id)', 'The request body', 'The headers'], a: 1, e: 'req.params holds values captured from route segments like /users/:id.', tags: ['Node', 'Express'] },
  { q: 'What is `req.query`?', opts: ['The route params', 'The parsed query-string parameters', 'The body', 'The cookies'], a: 1, e: 'req.query is an object of the URL’s query-string key/value pairs.', tags: ['Node', 'Express'] },
  { q: 'What is `req.body` (with a body parser)?', opts: ['The URL', 'The parsed request payload', 'The status code', 'The route'], a: 1, e: 'With body-parsing middleware, req.body holds the parsed request payload (e.g. JSON).', tags: ['Node', 'Express'] },
  { q: 'What does `res.json(obj)` do?', opts: ['Logs obj', 'Sends obj as JSON and sets the content type', 'Validates obj', 'Redirects'], a: 1, e: 'res.json serializes the object to JSON and sets Content-Type: application/json.', tags: ['Node', 'Express'] },
  { q: 'What does `res.status(404)` do?', opts: ['Sends the body', 'Sets the HTTP status code on the response', 'Ends the response', 'Logs an error'], a: 1, e: 'res.status sets the status code; it’s chainable, e.g. res.status(404).json(...).', tags: ['Node', 'Express'] },
  { q: 'What does `res.send(data)` do?', opts: ['Reads a file', 'Sends a response with appropriate content type', 'Defines a route', 'Starts the server'], a: 1, e: 'res.send sends a response body (string, Buffer or object) and infers the content type.', tags: ['Node', 'Express'] },
  { q: 'What does `res.redirect("/login")` do?', opts: ['Renders a page', 'Sends a redirect response (302 by default)', 'Throws', 'Sets a cookie'], a: 1, e: 'res.redirect sends an HTTP redirect to the given location.', tags: ['Node', 'Express'] },
  { q: 'How do you define a route parameter for a user id?', opts: ['"/users/{id}"', '"/users/:id"', '"/users/[id]"', '"/users/*"'], a: 1, e: 'Express uses the colon syntax: "/users/:id" captures id into req.params.', tags: ['Node', 'Express'] },

  // ── Level 23 — Concurrency ─────────────────────────────────────────────
  { q: 'How does Node achieve concurrency for I/O?', opts: ['One OS thread per request', 'A single thread plus the non-blocking event loop', 'Busy-waiting', 'Web Workers in the browser'], a: 1, e: 'Node overlaps many I/O operations on one thread via the event loop and async callbacks.', tags: ['Node', 'Concurrency'] },
  { q: 'What does CPU-bound work do to the event loop?', opts: ['Nothing', 'Blocks it, delaying all other callbacks', 'Speeds it up', 'Spawns threads automatically'], a: 1, e: 'Long synchronous CPU work blocks the single JS thread, stalling everything else.', tags: ['Node', 'Concurrency'] },
  { q: 'What does the `worker_threads` module enable?', opts: ['Child processes only', 'Running JavaScript in parallel threads', 'Faster timers', 'HTTP routing'], a: 1, e: 'worker_threads lets you run JS on additional threads — useful for CPU-heavy tasks.', tags: ['Node', 'Concurrency'] },
  { q: 'libuv’s thread pool handles some async ops with a default size of:', opts: ['1', '4', '16', 'Unlimited'], a: 1, e: 'The libuv thread pool defaults to 4 threads (used by fs, some crypto, dns.lookup, etc.).', tags: ['Node', 'Concurrency'] },
  { q: 'Which env var changes the libuv thread-pool size?', opts: ['NODE_THREADS', 'UV_THREADPOOL_SIZE', 'THREAD_POOL', 'NODE_POOL_SIZE'], a: 1, e: 'Setting UV_THREADPOOL_SIZE adjusts how many threads libuv’s pool uses.', tags: ['Node', 'Concurrency'] },
  { q: 'What does the `cluster` module do?', opts: ['Runs one thread', 'Forks multiple worker processes that share a server port', 'Encrypts data', 'Parses JSON'], a: 1, e: 'cluster forks worker processes (one per core, typically) that share the same listening port.', tags: ['Node', 'Concurrency'] },
  { q: 'What does a `SharedArrayBuffer` allow?', opts: ['Sharing memory between threads/workers', 'Faster strings', 'Encrypted buffers', 'Streaming files'], a: 0, e: 'A SharedArrayBuffer lets multiple workers access the same memory region.', tags: ['Node', 'Concurrency'] },
  { q: 'To keep the event loop responsive during heavy computation, you should:', opts: ['Use a bigger setTimeout', 'Offload work to worker threads or child processes', 'Add more callbacks', 'Use synchronous fs'], a: 1, e: 'Move CPU-bound work off the main thread (workers/child processes) so I/O stays responsive.', tags: ['Node', 'Concurrency'] },

  // ── Level 24 — Testing & Debugging ─────────────────────────────────────
  { q: 'What is Node’s built-in test runner module?', opts: ['node:assert', 'node:test', 'node:jest', 'node:spec'], a: 1, e: 'Node ships a built-in test runner accessible via node:test and `node --test`.', tags: ['Node', 'Testing'] },
  { q: 'Which built-in module provides assertions?', opts: ['expect', 'assert', 'check', 'verify'], a: 1, e: 'The `assert` module offers assertion helpers like strictEqual and deepEqual.', tags: ['Node', 'Testing'] },
  { q: 'What does running with `node --inspect` enable?', opts: ['Production mode', 'The debugger / DevTools protocol', 'Faster startup', 'Verbose logging only'], a: 1, e: '--inspect starts the Inspector so you can attach Chrome DevTools or an IDE debugger.', tags: ['Node', 'Debugging'] },
  { q: 'Where does `console.error` write?', opts: ['stdout', 'stderr', 'A file', 'The network'], a: 1, e: 'console.error writes to the standard error stream (stderr).', tags: ['Node', 'Debugging'] },
  { q: 'Which is a popular third-party Node test framework?', opts: ['Webpack', 'Jest', 'Express', 'Nodemon'], a: 1, e: 'Jest is a widely used testing framework for JavaScript/Node projects.', tags: ['Node', 'Testing'] },
  { q: 'What do `console.time("t")` and `console.timeEnd("t")` measure?', opts: ['Memory', 'The elapsed duration between the two calls', 'CPU temperature', 'Network latency'], a: 1, e: 'They print how long elapsed between the matching time/timeEnd calls.', tags: ['Node', 'Debugging'] },
  { q: 'What comparison does `assert.strictEqual` use?', opts: ['== (loose)', '=== (strict)', 'Deep equality', 'Reference only'], a: 1, e: 'strictEqual checks strict equality (===) between actual and expected.', tags: ['Node', 'Testing'] },
  { q: 'A good tool for investigating a memory leak is:', opts: ['console.log only', 'Heap snapshots via --inspect', 'Deleting node_modules', 'A bigger timeout'], a: 1, e: 'Capturing heap snapshots with the Inspector helps find what’s retaining memory.', tags: ['Node', 'Debugging'] },

  // ── Level 25 — Mixed Mastery ───────────────────────────────────────────
  { q: 'Which call blocks the event loop?', opts: ['fs.readFile', 'fs.readFileSync', 'fs.promises.readFile', 'fs.createReadStream'], a: 1, e: 'The synchronous *Sync variants block the thread until they finish.', tags: ['Node', 'Mastery'] },
  { q: 'How can you force a module to be re-loaded instead of using the cache?', opts: ['Restart Node only', 'delete require.cache[require.resolve(path)]', 'Call require twice', 'Use import()'], a: 1, e: 'Removing the cache entry makes the next require re-evaluate the module.', tags: ['Node', 'Mastery'] },
  { q: 'Which works in both Node and the browser?', opts: ['process', 'globalThis', '__dirname', 'window'], a: 1, e: 'globalThis is the standard global reference available in every JS environment.', tags: ['Node', 'Mastery'] },
  { q: 'Streaming a large file instead of readFile mainly improves:', opts: ['Code length', 'Memory usage', 'Network speed', 'Type safety'], a: 1, e: 'Streaming processes the file in chunks, so it doesn’t load the whole thing into memory.', tags: ['Node', 'Mastery'] },
  { q: 'Given a `process.nextTick` and a `setImmediate` scheduled together, which runs first?', opts: ['setImmediate', 'process.nextTick', 'They tie', 'Neither runs'], a: 1, e: 'nextTick callbacks run before the loop continues, ahead of setImmediate’s check phase.', tags: ['Node', 'Mastery'] },
  { q: 'Which actually controls a CommonJS module’s exports?', opts: ['exports', 'module.exports', 'export default', 'global'], a: 1, e: 'require returns module.exports; the `exports` shortcut only works while it still references it.', tags: ['Node', 'Mastery'] },
  { q: 'To run independent async tasks together and collect every result, use:', opts: ['A sequential await loop', 'Promise.all', 'setInterval', 'process.nextTick'], a: 1, e: 'Promise.all runs them concurrently and resolves with an array of all results.', tags: ['Node', 'Mastery'] },
  { q: 'An EventEmitter emits "error" with no listener attached. What happens?', opts: ['It is ignored', 'Node throws the error, often crashing the process', 'It retries', 'It logs a warning only'], a: 1, e: 'An unhandled "error" event is thrown by EventEmitter and can crash the process — always handle it.', tags: ['Node', 'Mastery'] },
];
