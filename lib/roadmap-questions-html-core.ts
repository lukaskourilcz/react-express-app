// HTML, condensed: six levels of what the markup actually decides.
//
// The old fifteen-level path asked repeatedly what HTML stands for, what a
// comment looks like, and which of four rarely-used elements exists. Recall of
// a tag list is not the skill; choosing the element whose built-in behaviour
// you want is, and it is the one that keeps paying off in a React codebase
// where the browser is still doing the work underneath.
//
// So the scope is six levels: structure and semantics, links and buttons,
// forms, media and responsive delivery, real content structures, and reading
// and repairing markup. Accessibility is folded through every level rather than
// saved for a vocabulary test at the end, because that is how it is decided in
// practice — by which element you reach for.
//
// Authored for devShark. See docs/curriculum-retirement.md for how this relates
// to the previous bank.

import type { Seed } from './roadmap-build';

export const htmlCoreSeeds: Seed[] = [
  // ── Level 1 — Document structure and semantics ─────────────────────────
  { q: 'A page has a banner, a nav, the article, a related-links sidebar and a footer. Which markup gives assistive technology the most to work with?', opts: ['A `div` per region with a descriptive class name', '`header`, `nav`, `main`, `aside` and `footer`', 'One `div` containing everything, styled with CSS Grid', 'A `section` per region with an `id`'], a: 1, e: 'At page level `header`, `nav`, `main`, `aside` and `footer` are exposed as banner, navigation, main, complementary and contentinfo landmarks, so a screen reader can list them and move between them. Class names and ids are never exposed to assistive technology. A `section` is only exposed as a `region` when it has an accessible name, so a bare `section` per region maps to `generic` and adds nothing.', intro: 'Ask which of these the browser exposes as page regions a screen-reader user can jump to, rather than which ones you can style.', tags: ['HTML', 'Semantics'] },
  { q: 'Why should a page have exactly one `main`?', opts: ['Browsers refuse to render a second one', '`main` marks the page\'s primary content, and two of them means neither is primary', 'CSS cannot target more than one', 'Search engines rank a page with two of them lower'], a: 1, e: '`main` names the one region holding this page\'s own content, so a skip link and a landmark list have a single unambiguous target; two of them make that choice meaningless. Nothing is enforced at runtime — a browser renders the second one and CSS still matches both — but the spec does forbid more than one non-hidden `main`, so a conformance checker reports it.', intro: 'Think about what a skip-to-content link, or a list of landmarks, has to point at when two regions both claim to be the main one.', tags: ['HTML', 'Semantics'] },
  { q: 'A page jumps from `h1` straight to `h4` for its first subsection. What breaks?', opts: ['The visual size of the heading', 'The document outline: a reader navigating by heading level sees two missing levels', 'The `h4` stops being announced as a heading', 'Search engines refuse to index it'], a: 1, e: 'Heading elements carry level, not size, so `h1` followed by `h4` tells anyone navigating by heading that two levels of nesting sit in between when they do not exist. The `h4` is still announced as a heading — only its level is wrong — and the size it renders at is CSS\'s job, not the element\'s.', intro: 'Nothing changes visually here. Ask what the number on a heading claims, and who moves through a page by those numbers.', tags: ['HTML', 'Semantics'] },
  { q: 'What does `<html lang="cs">` actually change?', opts: ['The direction text is written in', 'Which pronunciation and hyphenation a screen reader and the browser apply', 'The character encoding of the file', 'The language of the browser’s own interface'], a: 1, e: '`lang` tags the language of the content, so a screen reader selects the matching voice and pronunciation rules and the browser can hyphenate with the right dictionary — `hyphens: auto` does nothing without it. Direction comes from `dir`, encoding from `meta charset`, and the browser\'s own interface language is a user setting a page cannot change.', intro: 'Ask which of these depends on knowing which language the text is in — not on how the file is encoded or which way it reads.', tags: ['HTML', 'Semantics'] },
  { q: 'A `section` has no accessible name — no `aria-label`, no `aria-labelledby`. What is it doing for a reader?', opts: ['Exposing a named region a screen reader can jump to', 'Very little — without a name it is exposed as a generic container, like a `div`', 'Making the content focusable for keyboard users', 'Grouping the content as a separate document for search engines'], a: 1, e: 'A `section` maps to the `region` landmark only when it has an accessible name; without one it maps to `generic`, which is exactly what a `div` maps to. The fix is to name it — usually `aria-labelledby` pointing at the heading inside it — because a heading sitting in a section does not name the section on its own.', intro: 'Ask what a screen reader can announce about this container, and what would have to be present for it to be a region at all.', tags: ['HTML', 'Semantics'] },
  { q: 'Which of these belongs in `head` rather than `body`?', opts: ['The page’s main heading', 'The character encoding declaration', 'The navigation links', 'The site footer'], a: 1, e: '`head` carries metadata about the document — title, encoding, viewport, stylesheet links — while a heading, navigation and a footer are content the reader sees, so they belong in `body`. Encoding is the urgent one: the declaration has to sit within the first 1024 bytes, because by the time the parser has read further it has already had to guess how to decode the bytes.', intro: 'Ask what the browser needs to know about the document before it can render any of it.', tags: ['HTML', 'Document'] },
  { q: 'A `div` is given `onclick` and styled to look like a button. What has it not got?', opts: ['A background colour', 'Keyboard focus, Enter and Space activation, and a button role', 'A place in the DOM', 'The ability to receive a click'], a: 1, e: 'A button is a bundle of behaviours, most of them invisible. A styled `div` gets the click and loses everything a keyboard or screen-reader user needs — which is why the fix is the element, not more attributes.', intro: 'The mouse path already works. Ask what is missing for someone who never uses a mouse and never sees the styling.', tags: ['HTML', 'Semantics'] },
  { q: 'What does the viewport meta tag do on a phone?', opts: ['Sets the page\'s background colour on mobile', 'Lays the page out at the device\'s width instead of a virtual desktop width', 'Stops the page from scrolling sideways', 'Loads a separate stylesheet written for phones'], a: 1, e: 'Without the tag a mobile browser lays the page out in a virtual viewport wider than the screen and scales the result down, so a responsive layout never gets the chance to respond. `width=device-width` sets the layout width to the screen width. It does not stop sideways scrolling, since content wider than the viewport still overflows, and it loads no separate stylesheet.', intro: 'A phone\'s layout width and its screen width are not the same number by default. Which one does the tag set?', tags: ['HTML', 'Document'] },

  // ── Level 2 — Links, buttons and native interaction ────────────────────
  { q: 'A control takes the user to a different page. Which element is right?', opts: ['`button` with a click handler that navigates', '`a` with an `href`', '`div` with `role="link"`', '`input type="button"`'], a: 1, e: 'A link navigates; a button acts. The `a` brings the middle-click, the open-in-new-tab, the visited state and the status-bar preview — all things a scripted navigation quietly loses.', intro: 'Ask which of these the browser itself treats as a destination — the thing middle-click, open-in-new-tab and the status-bar preview all depend on.', tags: ['HTML', 'Links'] },
  { q: 'A control submits a form on the same page. Which element is right?', opts: ['`a href="#"` with a handler', '`button`', '`span` with a click handler', '`a` with no `href`'], a: 1, e: 'Submitting is an action on the current page, not a navigation, so a link makes a promise the control does not keep — and an `a` with no `href` is not even focusable. A `<button>` inside a form defaults to `type="submit"`, so it submits with no handler at all, and it responds to Enter and Space as a button.', intro: 'Nothing here is a destination. Ask which element the form already knows how to act on when it is pressed.', tags: ['HTML', 'Links'] },
  { q: 'What is the accessible name of `<button><svg …/></button>` with no text?', opts: ['The name of the SVG file', 'Nothing — a screen reader announces an unnamed button', 'The button’s CSS class', 'The nearest heading'], a: 1, e: 'An icon has no text, so there is nothing to announce. Visually-hidden text inside the button, or an `aria-label` on it, gives it a name — and the icon should then be hidden from the tree with `aria-hidden` so it is not read twice.', intro: 'A screen reader builds a control\'s name out of its content. Ask what text content this button actually has.', tags: ['HTML', 'Accessibility'] },
  { q: 'Why is "click here" a poor link text?', opts: ['It is too short for search engines to index', 'In a list of links it says nothing about where it goes', 'It cannot be styled like the other links', 'Screen readers skip over it'], a: 1, e: 'Users navigate by pulling up a list of links. Each one has to make sense alone, which "click here" cannot: the destination lives in the sentence around it.', intro: 'Assistive software can present link text stripped of the sentence around it. Judge these words on their own.', tags: ['HTML', 'Links'] },
  { q: 'What does `target="_blank"` change about a link, besides opening a new tab?', opts: ['Nothing else changes', 'The new tab starts with no history, so Back is no longer the way out', 'The link stops being reachable from the keyboard', 'The visited state is no longer applied to it'], a: 1, e: 'A new tab starts with no session history, so Back no longer returns the user to where they were, which is why MDN advises saying in the link text when a link opens a new tab. It changes neither focusability nor the visited state, and modern browsers already imply `rel="noopener"` for `target="_blank"`, so that is no longer something you add by hand.', intro: 'The new tab is a fresh browsing context. Ask what the user had in the old one that they no longer have.', tags: ['HTML', 'Links'] },
  { q: 'A page has a long navigation before its content. What does a skip link give a keyboard user?', opts: ['A faster page load', 'A first focusable control that jumps past the navigation to the main content', 'A shortcut key for every menu item', 'A visible table of contents'], a: 1, e: 'Without one, every visit to every page starts with tabbing through the same twenty links. The skip link is usually hidden until focused, which is why it is easy to forget it exists.', intro: 'Picture the very first Tab press on a page whose navigation holds twenty links. What would you want that first stop to be?', tags: ['HTML', 'Accessibility'] },
  { q: 'What is the difference between `disabled` and `aria-disabled="true"` on a button?', opts: ['They are identical', '`disabled` removes it from the tab order and blocks events; `aria-disabled` only announces the state', '`aria-disabled` is the modern replacement', '`disabled` works only on form controls inside a form'], a: 1, e: 'A truly disabled button cannot be reached by keyboard, which means the user cannot find out why. `aria-disabled` keeps it focusable so it can explain itself — at the cost of you having to block the action yourself.', intro: 'One of these is an ARIA attribute. Ask what ARIA can and cannot change about how a control actually behaves.', tags: ['HTML', 'Accessibility'] },
  { q: 'A `details`/`summary` pair is used for a FAQ answer. What comes for free?', opts: ['Styling of the open state', 'Toggle behaviour, keyboard operation and the expanded state announced', 'Animation of the reveal', 'Search-engine indexing of the answer'], a: 1, e: 'The element is a disclosure widget with the interaction already built. Reimplementing it with a `div` and a click handler means rebuilding the keyboard behaviour and the announced state, usually incompletely.', intro: 'Ask what you would have to write yourself if you built this out of a `div` and a click handler.', tags: ['HTML', 'Interaction'] },

  // ── Level 3 — Forms ────────────────────────────────────────────────────
  { q: 'What does a correctly associated `label` give an input, besides a caption?', opts: ['Automatic validation of what is typed', 'A larger hit target and a name announced on focus', 'A default value for the field', 'Automatic submission once the field is filled'], a: 1, e: 'Clicking the label focuses the field, which matters on a phone, and the label becomes the field’s accessible name. Placeholder text does neither, and disappears the moment typing starts.', intro: 'Ask what the browser does when you click the caption itself, not what the caption looks like.', tags: ['HTML', 'Forms'] },
  { q: 'Which pairing correctly associates a label with its input?', opts: ['`<label class="email">` and `<input class="email">`', '`<label for="email">` and `<input id="email">`', '`<label name="email">` and `<input name="email">`', 'Placing them adjacent in the markup'], a: 1, e: '`for` points at the input’s `id`. Wrapping the input inside the label also works; adjacency and matching classes do not.', intro: 'Click the caption in your head: which of these pairings gives the browser enough to know which control to focus?', tags: ['HTML', 'Forms'] },
  { q: 'A form field is submitted but arrives empty on the server. Which attribute is most likely missing?', opts: ['`id`', '`name`', '`class`', '`placeholder`'], a: 1, e: 'The form serialises by `name`. An input without one is not submitted at all — a bug that looks like a backend problem and is not.', intro: 'The request did arrive, so the field was on the page. Ask which attribute gives its value a key to be sent under.', tags: ['HTML', 'Forms'] },
  { q: 'Why use `type="email"` rather than `type="text"` for an email field on a phone?', opts: ['It guarantees the address exists', 'It brings the right keyboard and a basic format check', 'It encrypts the value', 'It is required for autofill'], a: 1, e: 'The input type shapes the on-screen keyboard, which is most of the benefit on mobile. The format check is shallow — it is not verification that anyone reads that address.', intro: 'Ask what a phone does differently the moment this field takes focus.', tags: ['HTML', 'Forms'] },
  { q: 'What does `required` actually do?', opts: ['Blocks submission of an empty field and marks it required to assistive technology', 'Guarantees the server receives a value for the field', 'Styles the field red for as long as it is empty', 'Stops the user leaving the field until it is filled'], a: 0, e: 'It is a client-side constraint and an announced state. It is not a guarantee: a request can be made without a browser at all, so the server validates too.', intro: 'Ask where the check runs, and what is still unchecked once the request has left the browser.', tags: ['HTML', 'Forms'] },
  { q: 'Several radio buttons should be mutually exclusive. What makes them one group?', opts: ['Wrapping them in a `div`', 'Giving them the same `name`', 'Giving them the same `class`', 'Placing them in the same `fieldset`'], a: 1, e: 'The shared `name` is what makes the browser treat them as one choice. A `fieldset` and its `legend` label the group for a screen reader, which is worth adding — but it is not what creates the exclusivity.', intro: 'Ask what the browser uses to decide that checking one of them has to uncheck another.', tags: ['HTML', 'Forms'] },
  { q: 'A validation message appears above a field after submission. How does a screen-reader user learn about it?', opts: ['Automatically — errors are always announced', 'By associating it with the field, for example through `aria-describedby`, and marking the field invalid', 'By the message being red', 'By the message appearing first in the DOM'], a: 1, e: 'A screen-reader user hears the field, not the layout, so the message has to be attached to the control: reference it with `aria-describedby` so it is read with the field, and set `aria-invalid="true"` so the field is announced as invalid. `aria-errormessage` is the more specific pairing for validation errors. Being red, or being earlier in the DOM, conveys nothing programmatically.', intro: 'Nothing about where the message sits or what color it is reaches the accessibility tree. Ask what could point the field at it.', tags: ['HTML', 'Forms'] },
  { q: 'What does `autocomplete="new-password"` tell the browser?', opts: ['That the browser should generate a password without being asked', 'That this field sets a password, so the saved one should not be filled', 'That autocomplete should be switched off for this field', 'That the password must differ from the previous one'], a: 1, e: '`new-password` marks a field that creates a password, so the browser should keep the saved one out of it and may offer to generate a strong replacement. Offering is not the same as generating unasked, and the token neither switches autocomplete off nor requires the new password to differ from the old one.', intro: 'Compare this field with the password box on a sign-in page, and ask what the browser would get wrong if it treated them alike.', tags: ['HTML', 'Forms'] },

  // ── Level 4 — Images, media and responsive delivery ────────────────────
  { q: 'A photograph carries information the surrounding text does not. What should its `alt` say?', opts: ['The single word "image"', 'What the image conveys where it appears', 'The image\'s file name', 'Nothing — omit the `alt` attribute'], a: 1, e: 'Alt text replaces the image for someone who cannot see it, so it depends on why the image is there. The same photo has different alt text in a news story and in a gallery of camera samples.', intro: 'Imagine reading the page aloud with the picture removed. What would the listener need to hear in its place?', tags: ['HTML', 'Media'] },
  { q: 'An icon sits beside a label that already says the same thing. What should its `alt` be?', opts: ['A description of the icon', 'Empty: `alt=""`', 'The label repeated', 'Omitted entirely'], a: 1, e: 'An empty `alt` says "decorative, skip me". Omitting the attribute is different: some readers then announce the filename, which is worse than either.', intro: 'The label beside it already says it. Ask what a screen reader should do with the icon, and what it does when the attribute is simply absent.', tags: ['HTML', 'Media'] },
  { q: 'Why set `width` and `height` on an image even when CSS sizes it?', opts: ['It stops CSS overriding the displayed size', 'It gives the browser the aspect ratio before the file arrives', 'It makes the browser render the image at higher quality', 'It is what switches lazy loading on'], a: 1, e: 'Without the ratio the browser reserves nothing, and the page jumps when the image lands. That jump is the commonest cause of a bad layout-shift score, and the fix is two attributes.', intro: 'The browser lays out the page before the file arrives. Ask what it has to guess about the image at that moment.', tags: ['HTML', 'Media'] },
  { q: 'What does `srcset` let the browser do?', opts: ['Choose a file appropriate to the screen’s size and pixel density', 'Load several images at once', 'Convert the image format', 'Defer loading until scrolled into view'], a: 0, e: 'You list candidate files and the browser picks one, because only it knows the viewport, the pixel density and the conditions at load time; with width descriptors you also give it `sizes` so it knows how wide the image will be laid out. It loads one file, not several, and it neither converts formats nor defers the request — that is `loading="lazy"`.', intro: 'The markup is written once but runs on every device. Ask which decision can only be made at load time.', tags: ['HTML', 'Media'] },
  { q: 'When is `picture` the right element rather than `srcset` alone?', opts: ['When the image is decorative rather than informative', 'When the image itself should differ — a different crop or format', 'When the image is a logo rather than a photograph', 'When the image should be loaded lazily'], a: 1, e: '`srcset` picks between renditions of the same picture; `picture` picks between different pictures. A wide banner cropped to a square on a phone is art direction, and that is what `picture` is for.', intro: 'Both offer the browser a set of files. Ask which choice you want to make yourself rather than leave to the browser.', tags: ['HTML', 'Media'] },
  { q: 'What does `loading="lazy"` on an image below the fold buy you?', opts: ['A smaller file for the browser to download', 'The request waits until the image is near the viewport', 'A higher-quality rendering of the same image', 'Automatic selection of the image format'], a: 1, e: 'It removes competition for bandwidth with things the user can actually see. Applied to the hero image it does the opposite, delaying the one image that matters most.', intro: 'Ask what the browser would otherwise spend that bandwidth on while the user is still looking at the top of the page.', tags: ['HTML', 'Media'] },
  { q: 'A video carries spoken information. What does it need beyond a play button?', opts: ['A higher bitrate so the audio is clearer', 'Captions, and a transcript when the content matters outside the video', 'Autoplay so playback starts immediately', 'A poster image shown before playback'], a: 1, e: 'Captions carry the dialogue and the meaningful sounds, delivered with `<track kind="captions">`, and they serve deaf and hard-of-hearing users as well as anyone with the sound off. A transcript adds what no video has: text that can be searched, skimmed and translated. A poster image and a higher bitrate change nothing for a user who cannot hear the audio.', intro: 'Ask how the spoken content reaches someone who cannot hear it.', tags: ['HTML', 'Media'] },
  { q: 'What does `figure` with `figcaption` express that a `div` with a paragraph does not?', opts: ['Better styling defaults', 'That the caption belongs to that content, as one unit', 'Faster rendering', 'That the content is an image'], a: 1, e: 'It is an association, announced as one. A `figure` can hold a code sample, a table or a diagram — the element is about the caption relationship, not about images.', intro: 'A caption after a `div` is just the next paragraph. Ask what the markup itself is claiming when you use `figure` instead.', tags: ['HTML', 'Media'] },

  // ── Level 5 — Real content structures ──────────────────────────────────
  { q: 'A list of navigation links is marked up as `div`s. Beyond how it looks, what does a `ul` add?', opts: ['Bullet markers before each item', 'The count and each item\'s position in it', 'Better ranking in search results', 'Consistent spacing between the items'], a: 1, e: 'The count and the position within it are what the list element is actually for. The bullets are CSS, and usually removed anyway.', intro: 'The bullets are CSS and usually removed anyway. Ask what a screen reader can say about the group that it cannot say about five separate divs.', tags: ['HTML', 'Content'] },
  { q: 'When is `ol` right rather than `ul`?', opts: ['When the list is long', 'When the order carries meaning — steps, rankings, sequence', 'When the list is nested', 'When numbers are wanted visually'], a: 1, e: 'The element states whether order matters. If you would be equally happy with the items shuffled, it is a `ul`, however the numbers look.', intro: 'Ask what the element claims about the items themselves, not what the browser draws in front of them.', tags: ['HTML', 'Content'] },
  { q: 'A term-and-definition pairing — a glossary, a specification list — is best expressed by which element?', opts: ['`ul` with a bold word in each item', '`dl` with `dt` and `dd`', '`table` with two columns', 'Two parallel `ul` elements'], a: 1, e: 'A description list encodes the pairing itself, so a reader can move between term and description. A bold word inside a list item looks the same and says nothing.', intro: 'Each entry here is two parts that belong together. Ask which element pairs them rather than just listing them one after another.', tags: ['HTML', 'Content'] },
  { q: 'A data table has a header row. Which markup makes it navigable?', opts: ['Bold text in the first row', '`th` cells with `scope="col"` inside `thead`', 'A CSS class on the first row', 'An `aria-label` on the table'], a: 1, e: 'A `th` is a header cell, so a screen reader can announce the matching column header with each data cell; `scope="col"` states that direction explicitly, which matters once the table is anything but simple. Bold text, a class, and an `aria-label` on the table create no relationship between a cell and its header, so twenty rows of numbers arrive unlabeled.', intro: 'A sighted reader glances up to the top of the column. Ask what gives a screen reader the same association.', tags: ['HTML', 'Content'] },
  { q: 'What does `caption` do for a table?', opts: ['It applies the table\'s default styling', 'It names the table, announced as part of it', 'It adds a footer row beneath the data', 'It makes the columns sortable'], a: 1, e: 'It is the table’s title, tied to it structurally. A heading above the table looks identical and is not associated with it, so a user landing on the table has no idea what it holds.', intro: 'Ask what a user who lands on the table without reading what came before it would know about what it holds.', tags: ['HTML', 'Content'] },
  { q: 'A layout is built with `table` elements. What is the cost?', opts: ['Slower rendering only', 'Assistive technology announces rows and columns that mean nothing', 'The page cannot be responsive', 'CSS cannot target it'], a: 1, e: 'Table semantics promise that the grid carries meaning, so a screen reader announces rows, columns and header relationships that the content does not have — more confusing than no structure at all. Layout belongs to CSS grid or flexbox; where a layout table cannot be removed yet, `role="presentation"` strips the semantics. Responsiveness and CSS targeting are not the problem here.', intro: 'Ask what a grid claims about the content inside it, and who is left acting on that claim.', tags: ['HTML', 'Content'] },
  { q: 'What does `blockquote` with a `cite` attribute express?', opts: ['Italic text', 'That the content is quoted, with a reference to where it came from', 'A pull quote style', 'An inline citation'], a: 1, e: '`blockquote` marks the content as quoted from elsewhere and the `cite` attribute holds the URL of the source. That attribute is machine-readable only — browsers do not display it — so visible attribution goes beside the quote, usually in a `cite` element outside the blockquote. Italic text is styling and says nothing about where the words came from.', intro: 'An element and an attribute on it are doing different jobs. Ask what each is for, and which a reader ever sees.', tags: ['HTML', 'Content'] },
  { q: 'Some inline text is code. Which element says so?', opts: ['`pre`', '`code`', '`samp`', '`kbd`'], a: 1, e: '`code` marks a fragment of code, `pre` preserves whitespace, `samp` is program output and `kbd` is user input. Wrapping a snippet in `pre` alone preserves the layout and says nothing about what it is.', intro: 'All four render in a monospace font. Ask what each one claims the text actually is.', tags: ['HTML', 'Content'] },

  // ── Level 6 — Inspect and repair markup ────────────────────────────────
  { q: 'A component uses `role="button"` on a `div` and adds `tabindex="0"`. What is still missing?', opts: ['Nothing — that is a complete button', 'Key handling: Enter and Space must be wired up by hand', 'A `type` attribute', 'A `name` attribute'], a: 1, e: 'The role announces it and the tabindex reaches it; neither activates it. This is the standard argument for the native element — every attribute you add is a behaviour you now own.', intro: 'Both additions here are attributes on a `div`. Ask what a native `button` still does once focus is on it, and what would have to supply that.', tags: ['HTML', 'Repair'] },
  { q: 'A page has two `nav` elements — site navigation and footer links. How should each be distinguished?', opts: ['By their position in the DOM', 'By giving each an accessible name, for example with `aria-label`', 'By a CSS class', 'By using `role="navigation"` on both'], a: 1, e: 'Two unnamed navigation landmarks are announced identically, so the user cannot tell "main" from "footer". A one-word label on each fixes it.', intro: 'Think about a user moving through a list of this page\'s landmarks. Both entries there say navigation, so ask what else each one could carry.', tags: ['HTML', 'Repair'] },
  { q: 'A heading is styled small so it fits the design, so `h4` is used for the page title. What is the correct fix?', opts: ['Keep the `h4`; the design decides', 'Use `h1` and style it small', 'Use a `div` with the right class', 'Add `aria-level="1"` to the `h4`'], a: 1, e: 'The level is structure and the size is presentation, and CSS separates them. Reaching for `aria-level` to correct a deliberately wrong element is treating the symptom.', intro: 'Ask what someone listing this page\'s headings would expect the top one to be, and whether the design has any say in that.', tags: ['HTML', 'Repair'] },
  { q: 'When is ARIA the right tool rather than a native element?', opts: ['Whenever accessibility is a requirement for the page', 'When no native element expresses the pattern and you implement its behaviour', 'On every interactive element, as a safety net', 'Never — HTML always has an equivalent element'], a: 1, e: 'ARIA changes what is announced and nothing else. A native element that already behaves correctly beats a described `div` every time; ARIA is for the patterns HTML has no element for.', intro: 'Ask what is left for you to build once a role has been added, and whether a native element would have brought it already.', tags: ['HTML', 'Repair'] },
  { q: 'An `iframe` embeds a third-party widget. What does it need for a screen-reader user?', opts: ['A `name` attribute', 'A `title` describing what it contains', 'An `alt` attribute', 'Nothing — iframes are announced automatically'], a: 1, e: 'The iframe is announced as a frame, and its `title` is the only clue about what is inside. Without one it is an unlabelled region the user has to enter to identify.', intro: 'Ask what a screen-reader user can learn about this widget before moving into the frame, and which attribute could carry that.', tags: ['HTML', 'Repair'] },
  { q: 'Markup arrives from a content editor and is inserted into the page. What is the most serious risk?', opts: ['The page renders more slowly', 'Markup you did not write runs with the page\'s privileges', 'The layout may break in unexpected ways', 'The page may stop validating'], a: 1, e: 'Inserting HTML from elsewhere means running content you did not author. Through `innerHTML` a `<script>` element is inserted but never executed, while an event-handler attribute such as `<img src=x onerror=...>` fires immediately — enough for cross-site scripting. Insert it with `textContent` when the markup is not needed, or sanitise against an allow-list; a broken layout is a nuisance, this one runs code.', intro: 'Ask what can be in that markup besides text, and who wrote it.', tags: ['HTML', 'Repair'] },
  { q: 'The browser’s element inspector shows a `tbody` nobody wrote. Why is it there?', opts: ['A build step added it', 'The parser inserts it: the DOM the browser built is not always the markup you sent', 'The page is invalid', 'A browser extension added it'], a: 1, e: 'The HTML parser inserts an implied `tbody` around rows written straight inside `table`, since the `tbody` start tag is one HTML lets you leave out, so the DOM holds an element the source does not. That is why a selector like `table > tr` does not match rows written that way, and why a structural bug is diagnosed in the inspector rather than by rereading the template: the markup is valid, and no build step or extension is involved.', intro: 'Ask what the browser does with tags an author is allowed to leave out, and where the result of that shows up.', tags: ['HTML', 'Repair'] },
  { q: 'A form works with a mouse and does nothing on Enter. Which cause is most likely?', opts: ['The submit control is not a `button` or `input` of type submit inside the form', 'The form is missing a `method`', 'The inputs are missing `id` attributes', 'The page has no `main`'], a: 0, e: 'Enter in a text field clicks the form\'s default button, the first submit button in the form. Replace that button with a `div` and a click handler and the mouse path keeps working while the keyboard path disappears silently, which is why a keyboard pass finds bugs a mouse pass cannot. The exception is worth knowing: with no submit button at all, Enter still submits a form that has only one field blocking implicit submission, but does nothing once there is more than one.', intro: 'A mouse press and Enter do not reach submission by the same route. Ask what the browser looks for when the gesture comes from the keyboard.', tags: ['HTML', 'Repair'] },
];
