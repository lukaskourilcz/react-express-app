// HTML, condensed: six levels of what the markup actually decides.
//
// The old fifteen-level path asked repeatedly what HTML stands for, what a
// comment looks like, and which of four rarely-used elements exists. Recall of
// a tag list is not the skill; choosing the element whose built-in behaviour
// you want is, and it is the one that keeps paying off in a React codebase
// where the browser is still doing the work underneath.
//
// So the scope is six levels: structure and semantics, links and buttons,
// forms, media and responsive delivery, real content structures, and reading
// and repairing markup. Accessibility is folded through every level rather than
// saved for a vocabulary test at the end, because that is how it is decided in
// practice — by which element you reach for.
//
// Authored for devShark. See docs/curriculum-retirement.md for how this relates
// to the previous bank.

import type { Seed } from './roadmap-build';

export const htmlCoreSeeds: Seed[] = [
  // ── Level 1 — Document structure and semantics ─────────────────────────
  { q: 'A page has a banner, a nav, the article, a related-links sidebar and a footer. Which markup gives assistive technology the most to work with?', opts: ['A `div` per region with a descriptive class name', '`header`, `nav`, `main`, `aside` and `footer`', 'One `div` containing everything, styled with CSS Grid', 'A `section` per region with an `id`'], a: 1, e: 'Landmark elements are announced as landmarks, and a screen-reader user can jump between them. A class name is invisible to the browser: it styles the region without telling anyone what it is.', tags: ['HTML', 'Semantics'] },
  { q: 'Why should a page have exactly one `main`?', opts: ['Browsers refuse to render a second one', '`main` marks the page’s primary content, and two of them means neither is primary', 'CSS cannot target more than one', 'It is required for the page to validate'], a: 1, e: '`main` is a promise about where the content of this page is. A second one makes the skip-to-content affordance ambiguous, which is exactly the user it exists for.', tags: ['HTML', 'Semantics'] },
  { q: 'A page jumps from `h1` straight to `h4` for its first subsection. What breaks?', opts: ['The visual size of the heading', 'The document outline: a reader navigating by heading level sees two missing levels', 'The page fails to validate', 'Search engines refuse to index it'], a: 1, e: 'Heading levels are the outline, not the size. Skipping levels tells a reader navigating by structure that two levels of nesting exist which do not. Size is CSS’s job.', tags: ['HTML', 'Semantics'] },
  { q: 'What does `<html lang="cs">` actually change?', opts: ['The direction text is written in', 'Which pronunciation and hyphenation a screen reader and the browser apply', 'The character encoding of the file', 'The language of the browser’s own interface'], a: 1, e: 'The language attribute tells a speech synthesiser which voice and rules to use. Without it, Czech read in an English voice is close to unintelligible.', tags: ['HTML', 'Semantics'] },
  { q: 'A `section` has no heading. What is it doing for a reader?', opts: ['Grouping content into a named region of the outline', 'Very little — a section without a heading is a `div` with extra ceremony', 'Making the content focusable', 'Improving the page’s validation score'], a: 1, e: '`section` earns its place by contributing a labelled entry to the outline. Unlabelled, it groups nothing anyone can navigate to, and `div` says the same thing more honestly.', tags: ['HTML', 'Semantics'] },
  { q: 'Which of these belongs in `head` rather than `body`?', opts: ['The page’s main heading', 'The character encoding declaration', 'The navigation links', 'The site footer'], a: 1, e: '`head` holds metadata about the document — encoding, title, viewport, links to stylesheets. Encoding especially: declared late, the browser has already begun guessing.', tags: ['HTML', 'Document'] },
  { q: 'A `div` is given `onclick` and styled to look like a button. What has it not got?', opts: ['A background colour', 'Keyboard focus, Enter and Space activation, and a button role', 'A place in the DOM', 'The ability to receive a click'], a: 1, e: 'A button is a bundle of behaviours, most of them invisible. A styled `div` gets the click and loses everything a keyboard or screen-reader user needs — which is why the fix is the element, not more attributes.', tags: ['HTML', 'Semantics'] },
  { q: 'What does the viewport meta tag do on a phone?', opts: ['Sets the page background colour', 'Tells the browser to lay the page out at the device’s width rather than a virtual desktop width', 'Disables horizontal scrolling', 'Loads a mobile-specific stylesheet'], a: 1, e: 'Without it a mobile browser assumes a wide desktop viewport and scales the result down, so a responsive layout never gets the chance to respond.', tags: ['HTML', 'Document'] },

  // ── Level 2 — Links, buttons and native interaction ────────────────────
  { q: 'A control takes the user to a different page. Which element is right?', opts: ['`button` with a click handler that navigates', '`a` with an `href`', '`div` with `role="link"`', '`input type="button"`'], a: 1, e: 'A link navigates; a button acts. The `a` brings the middle-click, the open-in-new-tab, the visited state and the status-bar preview — all things a scripted navigation quietly loses.', tags: ['HTML', 'Links'] },
  { q: 'A control submits a form on the same page. Which element is right?', opts: ['`a href="#"` with a handler', '`button`', '`span` with a click handler', '`a` with no `href`'], a: 1, e: 'Nothing is being navigated to, so a link is the wrong promise. `button` also brings Enter and Space, and inside a form it submits without a handler at all.', tags: ['HTML', 'Links'] },
  { q: 'What is the accessible name of `<button><svg …/></button>` with no text?', opts: ['The name of the SVG file', 'Nothing — a screen reader announces an unnamed button', 'The button’s CSS class', 'The nearest heading'], a: 1, e: 'An icon has no text, so there is nothing to announce. Visually-hidden text inside the button, or an `aria-label` on it, gives it a name — and the icon should then be hidden from the tree with `aria-hidden` so it is not read twice.', tags: ['HTML', 'Accessibility'] },
  { q: 'Why is "click here" a poor link text?', opts: ['It is too short to be indexed', 'Out of context — in a list of links — it says nothing about where it goes', 'It cannot be styled', 'Screen readers skip it'], a: 1, e: 'Users navigate by pulling up a list of links. Each one has to make sense alone, which "click here" cannot: the destination lives in the sentence around it.', tags: ['HTML', 'Links'] },
  { q: 'What does `target="_blank"` change about a link, besides opening a new tab?', opts: ['Nothing else', 'It removes the back button as a way out, so the choice should be the user’s or clearly signalled', 'It makes the link keyboard-inaccessible', 'It disables the visited state'], a: 1, e: 'A new tab breaks the browser’s own undo. It is sometimes right — leaving a form half-filled, opening a reference — but it is a decision to justify, not a default.', tags: ['HTML', 'Links'] },
  { q: 'A page has a long navigation before its content. What does a skip link give a keyboard user?', opts: ['A faster page load', 'A first focusable control that jumps past the navigation to the main content', 'A shortcut key for every menu item', 'A visible table of contents'], a: 1, e: 'Without one, every visit to every page starts with tabbing through the same twenty links. The skip link is usually hidden until focused, which is why it is easy to forget it exists.', tags: ['HTML', 'Accessibility'] },
  { q: 'What is the difference between `disabled` and `aria-disabled="true"` on a button?', opts: ['They are identical', '`disabled` removes it from the tab order and blocks events; `aria-disabled` only announces the state', '`aria-disabled` is the modern replacement', '`disabled` works only on form controls inside a form'], a: 1, e: 'A truly disabled button cannot be reached by keyboard, which means the user cannot find out why. `aria-disabled` keeps it focusable so it can explain itself — at the cost of you having to block the action yourself.', tags: ['HTML', 'Accessibility'] },
  { q: 'A `details`/`summary` pair is used for a FAQ answer. What comes for free?', opts: ['Styling of the open state', 'Toggle behaviour, keyboard operation and the expanded state announced', 'Animation of the reveal', 'Search-engine indexing of the answer'], a: 1, e: 'The element is a disclosure widget with the interaction already built. Reimplementing it with a `div` and a click handler means rebuilding the keyboard behaviour and the announced state, usually incompletely.', tags: ['HTML', 'Interaction'] },

  // ── Level 3 — Forms ────────────────────────────────────────────────────
  { q: 'What does a correctly associated `label` give an input, besides a caption?', opts: ['Validation', 'A larger click target and a name the screen reader announces on focus', 'A default value', 'Automatic submission'], a: 1, e: 'Clicking the label focuses the field, which matters on a phone, and the label becomes the field’s accessible name. Placeholder text does neither, and disappears the moment typing starts.', tags: ['HTML', 'Forms'] },
  { q: 'Which pairing correctly associates a label with its input?', opts: ['`<label class="email">` and `<input class="email">`', '`<label for="email">` and `<input id="email">`', '`<label name="email">` and `<input name="email">`', 'Placing them adjacent in the markup'], a: 1, e: '`for` points at the input’s `id`. Wrapping the input inside the label also works; adjacency and matching classes do not.', tags: ['HTML', 'Forms'] },
  { q: 'A form field is submitted but arrives empty on the server. Which attribute is most likely missing?', opts: ['`id`', '`name`', '`class`', '`placeholder`'], a: 1, e: 'The form serialises by `name`. An input without one is not submitted at all — a bug that looks like a backend problem and is not.', tags: ['HTML', 'Forms'] },
  { q: 'Why use `type="email"` rather than `type="text"` for an email field on a phone?', opts: ['It guarantees the address exists', 'It brings the right keyboard and a basic format check', 'It encrypts the value', 'It is required for autofill'], a: 1, e: 'The input type shapes the on-screen keyboard, which is most of the benefit on mobile. The format check is shallow — it is not verification that anyone reads that address.', tags: ['HTML', 'Forms'] },
  { q: 'What does `required` actually do?', opts: ['Prevents the form from being submitted empty in the browser, and marks the field as required to assistive technology', 'Guarantees the server receives a value', 'Styles the field red when empty', 'Blocks the user from leaving the field'], a: 0, e: 'It is a client-side constraint and an announced state. It is not a guarantee: a request can be made without a browser at all, so the server validates too.', tags: ['HTML', 'Forms'] },
  { q: 'Several radio buttons should be mutually exclusive. What makes them one group?', opts: ['Wrapping them in a `div`', 'Giving them the same `name`', 'Giving them the same `class`', 'Placing them in the same `fieldset`'], a: 1, e: 'The shared `name` is what makes the browser treat them as one choice. A `fieldset` and its `legend` label the group for a screen reader, which is worth adding — but it is not what creates the exclusivity.', tags: ['HTML', 'Forms'] },
  { q: 'A validation message appears above a field after submission. How does a screen-reader user learn about it?', opts: ['Automatically — errors are always announced', 'By associating it with the field, for example through `aria-describedby`, and marking the field invalid', 'By the message being red', 'By the message appearing first in the DOM'], a: 1, e: 'Position and colour communicate nothing to a reader. Tying the message to the field means it is read when the field is focused, which is when it is needed.', tags: ['HTML', 'Forms'] },
  { q: 'What does `autocomplete="new-password"` tell the browser?', opts: ['To generate a password automatically', 'That this field is for setting a password, so it should not offer the saved one and may offer to generate a new one', 'To disable autocomplete entirely', 'That the password must be new'], a: 1, e: 'Autocomplete tokens describe what the field is for. Getting them right is the difference between a password manager helping and getting in the way, and it costs one attribute.', tags: ['HTML', 'Forms'] },

  // ── Level 4 — Images, media and responsive delivery ────────────────────
  { q: 'A photograph carries information the surrounding text does not. What should its `alt` say?', opts: ['"image"', 'What the image conveys, in the context it appears in', 'The filename', 'Nothing — leave `alt` off'], a: 1, e: 'Alt text replaces the image for someone who cannot see it, so it depends on why the image is there. The same photo has different alt text in a news story and in a gallery of camera samples.', tags: ['HTML', 'Media'] },
  { q: 'An icon sits beside a label that already says the same thing. What should its `alt` be?', opts: ['A description of the icon', 'Empty: `alt=""`', 'The label repeated', 'Omitted entirely'], a: 1, e: 'An empty `alt` says "decorative, skip me". Omitting the attribute is different: some readers then announce the filename, which is worse than either.', tags: ['HTML', 'Media'] },
  { q: 'Why set `width` and `height` on an image even when CSS sizes it?', opts: ['To stop CSS overriding the size', 'To give the browser the aspect ratio so it can reserve the space before the file arrives', 'To improve image quality', 'To enable lazy loading'], a: 1, e: 'Without the ratio the browser reserves nothing, and the page jumps when the image lands. That jump is the commonest cause of a bad layout-shift score, and the fix is two attributes.', tags: ['HTML', 'Media'] },
  { q: 'What does `srcset` let the browser do?', opts: ['Choose a file appropriate to the screen’s size and pixel density', 'Load several images at once', 'Convert the image format', 'Defer loading until scrolled into view'], a: 0, e: 'You describe the candidates and the browser picks, because only it knows the viewport, the density and the network. That is a better decision than any media query you could write for it.', tags: ['HTML', 'Media'] },
  { q: 'When is `picture` the right element rather than `srcset` alone?', opts: ['When the image is decorative', 'When the image itself should differ — a different crop or format — rather than only its size', 'When the image is a logo', 'When lazy loading is wanted'], a: 1, e: '`srcset` picks between renditions of the same picture; `picture` picks between different pictures. A wide banner cropped to a square on a phone is art direction, and that is what `picture` is for.', tags: ['HTML', 'Media'] },
  { q: 'What does `loading="lazy"` on an image below the fold buy you?', opts: ['A smaller file', 'The browser defers the request until the image is near the viewport', 'Higher image quality', 'Automatic format selection'], a: 1, e: 'It removes competition for bandwidth with things the user can actually see. Applied to the hero image it does the opposite, delaying the one image that matters most.', tags: ['HTML', 'Media'] },
  { q: 'A video carries spoken information. What does it need beyond a play button?', opts: ['A higher bitrate', 'Captions, and a transcript where the content matters outside the video', 'Autoplay', 'A poster image'], a: 1, e: 'Captions serve deaf users, people in a noisy room and people who prefer reading. A transcript additionally makes the content searchable and skimmable, which no video is.', tags: ['HTML', 'Media'] },
  { q: 'What does `figure` with `figcaption` express that a `div` with a paragraph does not?', opts: ['Better styling defaults', 'That the caption belongs to that content, as one unit', 'Faster rendering', 'That the content is an image'], a: 1, e: 'It is an association, announced as one. A `figure` can hold a code sample, a table or a diagram — the element is about the caption relationship, not about images.', tags: ['HTML', 'Media'] },

  // ── Level 5 — Real content structures ──────────────────────────────────
  { q: 'A list of navigation links is marked up as `div`s. What does a `ul` add?', opts: ['Bullet points', 'The count — a screen reader announces "list, 5 items" and can navigate between them', 'Better SEO', 'Automatic spacing'], a: 1, e: 'The count and the position within it are what the list element is actually for. The bullets are CSS, and usually removed anyway.', tags: ['HTML', 'Content'] },
  { q: 'When is `ol` right rather than `ul`?', opts: ['When the list is long', 'When the order carries meaning — steps, rankings, sequence', 'When the list is nested', 'When numbers are wanted visually'], a: 1, e: 'The element states whether order matters. If you would be equally happy with the items shuffled, it is a `ul`, however the numbers look.', tags: ['HTML', 'Content'] },
  { q: 'A term-and-definition pairing — a glossary, a specification list — is best expressed by which element?', opts: ['`ul` with a bold word in each item', '`dl` with `dt` and `dd`', '`table` with two columns', 'Two parallel `ul` elements'], a: 1, e: 'A description list encodes the pairing itself, so a reader can move between term and description. A bold word inside a list item looks the same and says nothing.', tags: ['HTML', 'Content'] },
  { q: 'A data table has a header row. Which markup makes it navigable?', opts: ['Bold text in the first row', '`th` cells with `scope="col"` inside `thead`', 'A CSS class on the first row', 'An `aria-label` on the table'], a: 1, e: 'A screen reader announces the column header with each cell — but only if the header is a `th` with a scope. Without it, a table of twenty rows is twenty rows of unlabelled numbers.', tags: ['HTML', 'Content'] },
  { q: 'What does `caption` do for a table?', opts: ['Styles it', 'Names it, announced as part of the table', 'Adds a footer row', 'Enables sorting'], a: 1, e: 'It is the table’s title, tied to it structurally. A heading above the table looks identical and is not associated with it, so a user landing on the table has no idea what it holds.', tags: ['HTML', 'Content'] },
  { q: 'A layout is built with `table` elements. What is the cost?', opts: ['Slower rendering only', 'Assistive technology announces rows and columns that mean nothing', 'The page cannot be responsive', 'CSS cannot target it'], a: 1, e: 'Table semantics are a promise that the grid carries meaning. Used for layout they announce a structure the content does not have, which is more confusing than no structure at all.', tags: ['HTML', 'Content'] },
  { q: 'What does `blockquote` with `cite` express?', opts: ['Italic text', 'That the content is quoted, with a reference to where it came from', 'A pull quote style', 'An inline citation'], a: 1, e: 'The element marks the quotation; the `cite` attribute points at the source document. Styling it to look like a quotation without the element leaves the relationship unmarked.', tags: ['HTML', 'Content'] },
  { q: 'Some inline text is code. Which element says so?', opts: ['`pre`', '`code`', '`samp`', '`kbd`'], a: 1, e: '`code` marks a fragment of code, `pre` preserves whitespace, `samp` is program output and `kbd` is user input. Wrapping a snippet in `pre` alone preserves the layout and says nothing about what it is.', tags: ['HTML', 'Content'] },

  // ── Level 6 — Inspect and repair markup ────────────────────────────────
  { q: 'A component uses `role="button"` on a `div` and adds `tabindex="0"`. What is still missing?', opts: ['Nothing — that is a complete button', 'Key handling: Enter and Space must be wired up by hand', 'A `type` attribute', 'A `name` attribute'], a: 1, e: 'The role announces it and the tabindex reaches it; neither activates it. This is the standard argument for the native element — every attribute you add is a behaviour you now own.', tags: ['HTML', 'Repair'] },
  { q: 'A `nav` contains two menus. How should each be distinguished?', opts: ['By their position in the DOM', 'By giving each an accessible name, for example with `aria-label`', 'By a CSS class', 'By using `role="navigation"` on both'], a: 1, e: 'Two unnamed navigation landmarks are announced identically, so the user cannot tell "main" from "footer". A one-word label on each fixes it.', tags: ['HTML', 'Repair'] },
  { q: 'A heading is styled small so it fits the design, so `h4` is used for the page title. What is the correct fix?', opts: ['Keep the `h4`; the design decides', 'Use `h1` and style it small', 'Use a `div` with the right class', 'Add `aria-level="1"` to the `h4`'], a: 1, e: 'The level is structure and the size is presentation, and CSS separates them. Reaching for `aria-level` to correct a deliberately wrong element is treating the symptom.', tags: ['HTML', 'Repair'] },
  { q: 'When is ARIA the right tool rather than a native element?', opts: ['Whenever accessibility is a concern', 'When no native element expresses the pattern, and its behaviour is then implemented in full', 'On every interactive element, for safety', 'Never'], a: 1, e: 'ARIA changes what is announced and nothing else. A native element that already behaves correctly beats a described `div` every time; ARIA is for the patterns HTML has no element for.', tags: ['HTML', 'Repair'] },
  { q: 'An `iframe` embeds a third-party widget. What does it need for a screen-reader user?', opts: ['A `name` attribute', 'A `title` describing what it contains', 'An `alt` attribute', 'Nothing — iframes are announced automatically'], a: 1, e: 'The iframe is announced as a frame, and its `title` is the only clue about what is inside. Without one it is an unlabelled region the user has to enter to identify.', tags: ['HTML', 'Repair'] },
  { q: 'Markup arrives from a content editor and is inserted into the page. What is the risk?', opts: ['Slower rendering', 'Any script or event handler in it runs with the page’s privileges', 'The layout may break', 'The page may fail validation'], a: 1, e: 'Inserting HTML you did not author is executing content you did not author. Escape it, or sanitise it against an allow-list — and prefer inserting it as text when the markup is not needed.', tags: ['HTML', 'Repair'] },
  { q: 'The browser’s element inspector shows a `tbody` nobody wrote. Why is it there?', opts: ['A build step added it', 'The parser inserts it: the DOM the browser built is not always the markup you sent', 'The page is invalid', 'A browser extension added it'], a: 1, e: 'HTML parsing has recovery rules, and the tree can differ from the source. This is why inspecting the DOM rather than reading the template is the way to diagnose a structural bug.', tags: ['HTML', 'Repair'] },
  { q: 'A form works with a mouse and does nothing on Enter. Which cause is most likely?', opts: ['The submit control is not a `button` or `input` of type submit inside the form', 'The form is missing a `method`', 'The inputs are missing `id` attributes', 'The page has no `main`'], a: 0, e: 'Implicit submission needs a submit control in the form. Replace it with a `div` and a click handler and the mouse path keeps working while the keyboard path silently disappears — which is why a keyboard pass finds bugs a mouse pass cannot.', tags: ['HTML', 'Repair'] },
];
