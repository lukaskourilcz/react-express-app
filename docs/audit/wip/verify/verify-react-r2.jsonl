{"id":"rm-react-71","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["node (React 19.2.8, jsdom): setUser(prev => { prev.name = \"b\"; return prev; }) -> renders stayed 1, DOM stayed <p>a</p>, nothing thrown; setUser(prev => ({...prev, name: \"c\"})) -> renders 2, DOM <p>c</p>","https://react.dev/reference/react/useState: \"If the new value you provide is identical to the current state, as determined by an Object.is comparison, React will skip re-rendering the component and its children.\""],"notes":"The hint asks what React can observe rather than stating the rule, and the explanation's claim that mutation never throws holds in the React 19.2.8 development build."}
{"id":"rm-react-72","verdict":"amend","fields":{"hint":"Write out the index ranges each slice covers for a four-item array, then compare them with the original."},"rescored":null,"problems":["hint tells the learner to look for the index neither slice includes, which is the answer"],"evidence":["node: arr = [\"a\",\"b\",\"c\",\"d\"], i = 1 -> [\"a\",\"c\",\"d\"], original unchanged, sameRef false; with i = 0 -> [\"b\",\"c\"]"],"notes":"Key and explanation verified; only the hint gave the removal away."}
{"id":"rm-react-73","verdict":"reject","fields":null,"rescored":null,"problems":["duplicate: same objective, same four choices (compute in render / useState / useRef / effect-plus-state) and a near-identical hint and explanation as rm-react-77 in this batch, which is the better-built of the pair","option 3 calls a setCount the snippet never declares, and option 2 (const count = useRef(items.length)) is a line nobody writes, so two of four options are incoherent as code"],"evidence":["node (React 19.2.8, jsdom): useRef(items.length) inside the component evaluates to {\"current\":3}, not a number","node (React 19.2.8, jsdom): an effect that sets state commits the old value first (the DOM read 0 inside the effect, final <p>1</p>), which is the failure the explanation describes"],"notes":"Correct as keyed, but it teaches exactly what rm-react-77 teaches, and its useState/useRef/useEffect options are code lines rather than choices."}
{"id":"rm-react-74","verdict":"amend","fields":{"hint":"Notice where full is declared: in the component body, not with useState. Then ask what setFirst does to that body."},"rescored":null,"problems":["hint states the rule the item tests (\"an ordinary const rebuilt whenever the component body runs\")"],"evidence":["node (React 19.2.8, jsdom): full was \"A B\" on the first render and \"Z B\" after setFirst(\"Z\"); DOM ended <p>Z B</p>","node: `${first}${last}` -> \"AB\", so the space in the key comes from the literal"],"notes":"Explanation keeps literal backticks, which render as text here, but that reads as code marking and is not a defect."}
{"id":"rm-react-76","verdict":"amend","fields":{"hint":"Run the callback in your head for each of the four numbers, then ask which values in this component React is actually tracking."},"rescored":null,"problems":["hint asserts both halves of the key: what filter keeps and that evens is rebuilt on every render"],"evidence":["node: [1,2,3,4].filter(n => n % 2 === 0) -> [2,4]; the same call with a thisArg second argument still returns [2,4], so option 3's premise is false"],"notes":"Key and explanation verified; the hint left nothing to work out."}
{"id":"rm-react-77","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["https://react.dev/reference/react/useEffect: \"Reactive values include props, state, and all the variables and functions declared directly inside of your component body.\"","node (React 19.2.8, jsdom): an effect that sets state commits the stale value first (DOM read 0 inside the effect, final <p>1</p>), and useRef(...) evaluates to {\"current\":3}","knowledge: writing to a ref does not schedule a render, so the ref option cannot update the list on screen"],"notes":"All four options are real choices and the explanation gives each one's failure mode; rm-react-73 in this batch duplicates it."}
{"id":"rm-react-78","verdict":"amend","fields":{"explanation":"The component body runs on every render, so total is rebuilt from the current items each time and cannot fall behind them. Holding it in state would mean updating it everywhere items changes, and no effect is involved — the value is already correct by the time the render is committed. React does not cache a plain const between renders either; useMemo is what does that, and only as an optimization."},"rescored":null,"problems":["British spelling \"optimisation\" in a bank that uses American spelling"],"evidence":["node: [{price:1.5},{price:2}].reduce((s,i) => s + i.price, 0) -> 3.5","knowledge: a plain const in the component body is recomputed on every render; only useMemo caches a value between renders"],"notes":"Shares the derived-state rule with rm-react-76 and rm-react-77 but the \"cached value React reuses\" distractor keeps it distinct."}
{"id":"rm-react-80","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["node: items = [3,1,2]; [...items].sort((a,b) => a - b) -> [1,2,3], items still [3,1,2], sameRef false","node: [10,9,1].sort() -> [1,10,9], so the comparator the rewrite added was needed"],"notes":"The comparator fix removes the incidental bug the original blessed as fine."}
{"id":"rm-react-81","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["node (React 19.2.8, jsdom): useEffect(fn, []) -> setup once at mount, nothing on later renders, cleanup at unmount; the same effect inside <StrictMode> -> setup, cleanup, setup at mount","https://react.dev/reference/react/useEffect: \"When Strict Mode is on, React will run one extra development-only setup+cleanup cycle before the first real setup.\""],"notes":"This is the item that most needed the development caveat and the rewrite carries it."}
{"id":"rm-react-82","verdict":"amend","fields":{"explanation":"With no dependency array React has nothing to compare against the previous commit, so it runs the effect after every one — the first render and every update. It does not read the body and infer the dependencies, which is why \"only when a value inside changes\" is wrong; you get that behavior only by listing the values yourself."},"rescored":null,"problems":["British spelling \"behaviour\""],"evidence":["node (React 19.2.8, jsdom): an effect with no dependency array ran setup after mount and after every later render, including one caused by unrelated state","https://react.dev/reference/react/useEffect: \"If you pass no dependency array at all, your Effect runs after every single commit of your component.\""],"notes":"StrictMode doubles the mount run here too, but the key (\"after every render\") is not contradicted by it, so no caveat was added."}
{"id":"rm-react-83","verdict":"amend","fields":{"options":["Only after the first render, then never again","After the first render and after any render where count changed","After every render, whether or not count changed","Only when the component is removed from the page"],"correctAnswer":1,"explanation":"React compares each dependency with its value at the previous commit, so the effect runs after the first render and again whenever count differs. A render caused by other state leaves count equal, so the effect is skipped — that is exactly what an omitted array gives up, since it re-runs after every commit. In development inside <StrictMode> React adds one extra setup and cleanup pass, so the mount run shows up twice."},"rescored":null,"problems":["the key ran 63 characters against distractors of 18 to 34, so it could be picked on length alone","explanation described only the production run, while a StrictMode development build runs the mount effect twice"],"evidence":["node (React 19.2.8, jsdom): [count] effect -> setup at mount, nothing after an unrelated setOther(1), cleanup + setup when count changed; the same effect inside <StrictMode> -> setup, cleanup, setup at mount"],"notes":"Key stays at index 1; the distractors were lengthened rather than reordered."}
{"id":"rm-react-84","verdict":"amend","fields":{"explanation":"React renders with n = 0 and commits <p>0</p>, then runs the effect, which sets n to 1 and triggers a second render showing <p>1</p>. So the final DOM is <p>1</p> but 0 was on screen first — that extra pass is why an initial value that is really derived belongs in useState's initializer rather than in an effect. The empty array does not cancel the update; it only stops the effect from running again."},"rescored":null,"problems":["British spelling \"initialiser\""],"evidence":["node (React 19.2.8, jsdom): inside the effect the DOM already read <p id=\"t84\">0</p>; final DOM <p id=\"t84\">1</p>"],"notes":"The two-pass answer is right and StrictMode does not change what reaches the DOM, so no caveat was needed."}
{"id":"rm-react-86","verdict":"amend","fields":{"explanation":"React calls the component function to work out the markup, commits it to the DOM, and only then runs the effects — which is why an effect can measure or subscribe to the node that render produced. During render that node is not in the DOM yet, and React gives the effect no way to cancel a render that is already committed. Effects also run after updates, not just the first render, unless the dependency array rules them out."},"rescored":null,"problems":["\"During render the DOM still holds the previous output\" is false on a first render, when the node is not in the DOM at all"],"evidence":["node (React 19.2.8, jsdom): during the first render document.getElementById(\"t86\") was null (\"not-in-dom\"); inside the effect the same lookup read \"committed\""],"notes":"Options are now comparable in length, so the key can no longer be picked on shape."}
{"id":"rm-react-87","verdict":"amend","fields":{"explanation":"React compares the array entry by entry against the previous commit and re-runs the effect if any entry differs, so a changed roomId is enough on its own. Waiting for both would leave the component connected to the old room. The array is also what prevents the every-render behavior you get when it is omitted."},"rescored":null,"problems":["British spelling \"behaviour\""],"evidence":["node (React 19.2.8, jsdom): [serverUrl, roomId] with roomId r1 -> r2 logged \"connect s1/r2\"; setting roomId to the same value logged nothing","https://react.dev/reference/react/useEffect: \"React will compare each dependency with its previous value using the Object.is comparison.\""],"notes":"The concrete roomId/serverUrl pair makes this a genuinely different question from rm-react-83."}
{"id":"rm-react-89","verdict":"amend","fields":{"explanation":"With [] the setup runs once, so the only function React holds is the closure from the first render: it logs that render's value and is never called again. React does not re-read value in between — the closure is the whole story. An empty array does not silence the effect, and a missing dependency is a lint warning, not a build failure. In development inside <StrictMode> that first log appears twice, still with the first render's value."},"rescored":null,"problems":["explanation said the setup \"runs once\" and the key says it \"never logs again\", which a StrictMode development build contradicts"],"evidence":["node (React 19.2.8, jsdom): with [] and the prop going a -> b -> c the effect logged only \"log a\" while the DOM ended <p>c</p>","node (React 19.2.8, jsdom): the same empty-dependency effect inside <StrictMode> ran setup, cleanup, setup at mount"],"notes":"The stale-closure answer stands; only the run count needed the development caveat."}
{"id":"rm-react-90","verdict":"amend","fields":{"explanation":"The effect runs after the first commit, sets count, which changes its only dependency, so it runs again — and again. In development React logs \"Maximum update depth exceeded\", but it does not stop the component, so the loop is yours to break: compute the value during render instead, or depend on something the effect does not itself change."},"rescored":null,"problems":["the \"Maximum update depth exceeded\" log is development-only: the production react-dom build does not contain that message"],"evidence":["node (React 19.2.8, jsdom): 301 renders and 301 effect runs, stopped only by my own cap; React logged \"Maximum update depth exceeded. This can happen when a component calls setState inside useEffect...\" and never threw","run: grep -c \"Maximum update depth exceeded\" node_modules/react-dom/cjs/react-dom-client.production.js -> 0","run: grep -n \"Too many re-renders\" react-dom-client.development.js -> line 7748 only, inside renderWithHooks (render-phase updates), so that error does not apply to this effect loop"],"notes":"Checked the \"repeats forever\" worry: React 19 throws \"Too many re-renders\" only for setState during render, so the key and the console-error claim are both right for this code."}
{"id":"rm-react-91","verdict":"amend","fields":{"explanation":"Each render evaluates `{ id }` afresh and produces a different object; React compares dependencies with Object.is, which is false for two distinct objects even when their fields match, so every render looks like a change and fetchUser runs again. The comparison is by reference, never field by field — depend on the primitive ([id]) or memoize the object with useMemo."},"rescored":null,"problems":["British spelling \"memoise\""],"evidence":["node (React 19.2.8, jsdom): useEffect(fn, [{ id }]) ran on all three renders (fetchUser 0, 1, 2)","node: Object.is({id:1},{id:1}) -> false"],"notes":"Hint and explanation show literal backticks because both fields render as plain text, which is unusual for this bank but reads as code marking."}
{"id":"rm-react-94","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["node (React 19.2.8, jsdom): an effect depending on a function declared in the component body ran on all three renders with identical props","node (React 19.2.8, jsdom): a component with no return statement rendered nothing and threw nothing, so the snippet's missing return does not break the premise"],"notes":"Distractor 1 (\"only when id changes, because load closes over id\") is the real misconception and the explanation names both fixes."}
{"id":"rm-react-95","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["https://react.dev/reference/react/useEffect: \"Reactive values include props, state, and all the variables and functions declared directly inside of your component body.\"","https://react.dev/reference/react/useState: \"The set function has a stable identity, so you will often see it omitted from Effect dependencies.\""],"notes":"The key is slightly narrower than the documentation (which counts every variable declared in the body), but it is still the only defensible option."}
{"id":"rm-react-97","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["node (React 19.2.8, jsdom): with [] the log was setup at mount, nothing on a re-render, cleanup at unmount","node (React 19.2.8, jsdom): an effect returning 42 logged \"must not return anything besides a function, which is used for clean-up.\" and then threw \"destroy is not a function\""],"notes":"The throw lands at cleanup time rather than immediately, which the explanation does not claim either way."}
{"id":"rm-react-98","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["node (React 19.2.8, jsdom): id 1 -> 2 logged setup 1, cleanup 1, setup 2, then cleanup 2 at unmount; inside <StrictMode> the mount logged setup 1, cleanup 1, setup 1 first","https://react.dev/reference/react/useEffect: \"First, your cleanup code runs with the old props and state. Then, your setup code runs with the new props and state.\""],"notes":"The StrictMode note is needed here because the observed dev order is setup, cleanup, setup, cleanup, setup, and the rewrite carries it."}
{"id":"rm-react-99","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["node (React 19.2.8, jsdom): with the cleanup, 0 handler calls after unmount; two mount/unmount cycles without it -> 2 calls per dispatched resize","node (jsdom): the same function registered twice fired once per event, so \"each remount adds another\" holds only because each mount creates a new handler, which the explanation's wording covers"],"notes":"Key and all three distractors verified against the DOM, not just reasoned about."}
{"id":"rm-react-100","verdict":"amend","fields":{"explanation":"setInterval keeps firing until something calls clearInterval, and nothing here ever does: after unmount the callback still runs for a component that no longer exists, and every remount adds another timer. Returning () => clearInterval(id) fixes it. The rest of the snippet is right — the empty array keeps the effect from re-running on later renders, and setN(n => n + 1) is the correct form for a callback that cannot see the current n. In development inside <StrictMode> the effect runs twice at mount, so the missing cleanup already leaves two timers going."},"rescored":null,"problems":["\"the empty array is what makes the interval start once\" describes only the production run: in a StrictMode development build the effect runs twice at mount and starts two intervals"],"evidence":["node (React 19.2.8, jsdom): 5 ticks before unmount and 9 more in the 90 ms after unmount, with the container emptied","node (React 19.2.8, jsdom): an empty-dependency effect inside <StrictMode> ran setup, cleanup, setup at mount"],"notes":"The diagnosis is right; only the sentence blessing the empty array needed the development caveat."}
