{"id": "rm-git-61", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["run: git 2.43.0 — after `git fetch origin`, local main stayed d501d89 while origin/main moved to 428b2f5, f.txt still read 'one', and `git status -sb` printed `## main...origin/main [behind 1]`", "https://git-scm.com/docs/git-pull — pull is documented as fetch followed by an integration step, so 'merging them in is a separate step' is accurate"], "notes": "Hint poses the origin/* versus checked-out-branch choice without answering it, so options 0 and 1 both stay live."}
{"id": "rm-git-62", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["run: git 2.43.0 — after `git remote remove origin`, `git remote` printed nothing while `git log --oneline -1` still showed `d501d89 c1` and the bare server repo was untouched"], "notes": "Key echoes the command's own verb so the item leans on vocabulary, but the 'deletes the repository' misconception still discriminates; left as is."}
{"id": "rm-git-63", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["run: git 2.43.0 — `git clone srv.git c2` then `git remote -v` printed `origin\t.../srv.git (fetch)`; in a second clone `git remote rename origin upstream` succeeded and `git branch -r` listed `upstream/HEAD -> upstream/main` and `upstream/main`"], "notes": "Each option pairs a name with a claim, so lengths and shapes stay parallel."}
{"id": "rm-git-64", "verdict": "amend", "fields": {"hint": "Count your remotes before and after this runs. Ask whether the number goes up, down, or stays the same."}, "rescored": null, "problems": ["hint paraphrases three of the four options ('a name change, an extra entry, or a deletion') and its 'only your own .git/config is touched' echoes the word 'locally' that appears in the key alone"], "evidence": ["run: git 2.43.0 — one remote before (`origin`) and one after (`upstream`); `git branch -r` then listed upstream/main, and no objects were transferred"], "notes": "New hint points at a countable observable that separates rename (same count) from add and remove."}
{"id": "rm-git-65", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["run: git 2.43.0 — `git push origin main` printed `55cd4e3..94231f5  main -> main` and the bare remote advanced to that commit", "knowledge: deleting a remote branch is `git push origin --delete main`, as the explanation states"], "notes": "Hint names the two arguments and asks for the direction of travel, which leaves the download/upload pair open."}
{"id": "rm-git-66", "verdict": "amend", "fields": {"hint": "Your working tree usually changes after this command. Ask what must have happened to your branch for that to be true.", "explanation": "git pull runs git fetch and then integrates the upstream branch into the branch you are on. If your branch has not diverged the integration is a fast-forward; if it has, Git 2.34 and later stop with 'Need to specify how to reconcile divergent branches' until you choose merge (pull.rebase false), rebase (pull.rebase true) or pull.ff only. It never pushes, and it rebases only when you configure or ask for it."}, "rescored": null, "problems": ["explanation dated the divergent-pull failure to Git 2.27, which only warned and then merged; the fatal error was added in 2.34", "hint stated the tested rule ('It is two operations in one') and asked only for the name of the second"], "evidence": ["run: git 2.43.0, pull.rebase unset — a fast-forward pull printed `Updating 428b2f5..75eb6f3`, while a diverged pull ended `fatal: Need to specify how to reconcile divergent branches.`", "run: curl raw git sources — builtin/pull.c at v2.27.0 has `warning(_(\"Pulling without specifying how to reconcile divergent branches is...\"))` and v2.33.0 has the same text as advice(); the `die(_(\"Need to specify how to reconcile divergent branches.\"))` first appears at v2.34.0", "https://git-scm.com/docs/git-pull — --ff-only 'is the default when no method for reconciling divergent histories is provided'"], "notes": "Only the version number changed in the explanation; the rest of the sentence already described 2.43 behavior correctly."}
{"id": "rm-git-67", "verdict": "amend", "fields": {"hint": "Picture the next push from this branch with no arguments. Ask what would have to be true already for it to work."}, "rescored": null, "problems": ["hint states the key's distinguishing content — that the flag writes something persistent into .git/config — which leaves only the 'records origin/main as its upstream' option standing"], "evidence": ["run: git 2.43.0 — `git push -u origin feature` printed \"branch 'feature' set up to track 'origin/feature'.\" and left branch.feature.remote=origin, branch.feature.merge=refs/heads/feature", "run: git 2.43.0 — on a branch with no upstream, plain `git push` printed `fatal: The current branch brandnew has no upstream branch.` with push.autoSetupRemote unset"], "notes": "New hint points at the later bare push as the observable and leaves 'without recording anything' plausible."}
{"id": "rm-git-68", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["run: git 2.43.0 — with 2 local commits (localA af9afe8, localB 6c7c587) and 3 upstream commits, `git pull --rebase` printed 'Successfully rebased and updated refs/heads/main'; the log then read localB 9a084c8, localA d1e4be1 on top of remote3/remote2/remote1 and `git log --merges` counted 0"], "notes": "Hint rules out the merge and discard options but leaves the which-side-is-rewritten choice, which is the real discrimination."}
{"id": "rm-git-69", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["run: git 2.43.0 — after a teammate's push, `git push origin main` printed `! [rejected]        main -> main (fetch first)` and 'hint: Updates were rejected because the remote contains work that you do not'"], "notes": "Hint supplies the fast-forward definition and asks the learner to apply it rather than naming the condition in the key."}
{"id": "rm-git-70", "verdict": "amend", "fields": {"hint": "Picture a teammate pushing between your last fetch and this command. Ask whether Git has any way to notice."}, "rescored": null, "problems": ["hint asserts that Git compares a recorded value with the remote before overwriting, which is the key's content and rules out the other three options"], "evidence": ["run: git 2.43.0 — with a stale origin/main, `git push --force-with-lease origin main` printed `! [rejected]        main -> main (stale info)`; after `git fetch origin` the same command printed `+ 4efccf7...128e315 main -> main (forced update)`", "https://git-scm.com/docs/git-push — --force-with-lease without an expected value uses the remote-tracking ref, and a background fetch can refresh the lease"], "notes": "New hint points at the teammate's push as the thing to notice and leaves 'overwrites unconditionally' live."}
{"id": "rm-git-72", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["run: git 2.43.0 — `git push -u origin feature` on a branch the server had never seen printed `* [new branch]      feature -> feature` and \"branch 'feature' set up to track 'origin/feature'.\"", "run: git 2.43.0 — `git config --get push.autoSetupRemote` was unset and a plain push on a fresh branch printed `fatal: The current branch brandnew has no upstream branch.`"], "notes": "Overlaps rm-git-67 on the -u flag, but the tested misconception differs (the branch need not exist on the server first), so both are kept."}
{"id": "rm-git-73", "verdict": "amend", "fields": {"hint": "`git restore` and `git restore --staged` rewrite different copies of the file. Ask which copy the bare form touches."}, "rescored": null, "problems": ["hint asserts that the command reads a replacement and that edits therefore disappear, which is the key's content and rules out the stage, delete and commit options"], "evidence": ["run: git 2.43.0 — index held 'v2-staged', working tree 'v3-unstaged', HEAD 'v1'; after `git restore file.txt` the working tree read 'v2-staged', so the source is the index, not HEAD"], "notes": "New hint points at which copy is rewritten and leaves the delete misreading live."}
{"id": "rm-git-74", "verdict": "amend", "fields": {"hint": "Ask what `git log` shows afterwards: fewer commits than before, the same number, or one more."}, "rescored": null, "problems": ["hint states the answer's mechanism ('history is append-only', 'a brand-new commit'), which leaves only the 'creates a new commit' option standing"], "evidence": ["run: git 2.43.0 — `git revert --no-edit 3288003` produced `f90208c Revert \"second\"`, the log kept both commits, and `git cat-file -t 3288003` still returned `commit`"], "notes": "New hint offers three outcomes for the log without indicating which one holds."}
{"id": "rm-git-75", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["run: git 2.43.0 — after `git reset --soft HEAD~1` the log lost the 'second' commit, `git status --short` reported `A  x.txt`, and x.txt was still on disk", "knowledge: --mixed is git reset's default mode, so the explanation's contrast is right"], "notes": "Hint names the general reset rule but leaves the three modes open, which is the discrimination the options turn on."}
{"id": "rm-git-76", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["run: git 2.43.0 — `git reset --hard HEAD~1` printed `HEAD is now at cdb955c base`, the 'third' commit left the log and y.txt was no longer on disk", "run: git 2.43.0 — a commit dropped by `git reset --hard` was still listed by `git reflog` and restorable, matching the explanation's reflog caveat"], "notes": "Hint asks how far the mode reaches and offers three depths without choosing."}
{"id": "rm-git-77", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["run: git 2.43.0 — after `git restore --staged file.txt` the index read 'v1' (HEAD's copy) while the working tree still read 'v2' and status showed ` M file.txt`"], "notes": "Hint leaves the 'discards your edits as well' distractor live, which is the one a learner actually confuses."}
{"id": "rm-git-78", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["run: git 2.43.0 — `git revert --no-edit` added an inverse commit and left the original intact, so an ordinary push stays a fast-forward", "run: git 2.43.0 — after a history rewrite the remote refused the ordinary push (`! [rejected] topic -> topic (non-fast-forward)`) and only `--force-with-lease` went through"], "notes": "The closing reference to the golden rule of rebasing is framed as shared reasoning, not as something Git enforces, which matches rm-git-92."}
{"id": "rm-git-79", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["run: git 2.43.0 — file.txt held 'edited'; `git checkout -- file.txt` restored it to 'v1', the index and HEAD content", "knowledge: git switch and git restore were added in Git 2.23 to split checkout's two jobs"], "notes": "Hint points at the `--` separator, which is the feature of this form, and leaves three options live."}
{"id": "rm-git-80", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["run: git 2.43.0 — `git clean -fd` printed 'Removing loose.txt' and 'Removing untracked_dir/' while the .gitignore-matched ignored.txt survived; `git clean -fdx` then removed it too"], "notes": "Hint narrows to untracked paths but leaves the .gitignore option live, since ignored files are untracked too."}
{"id": "rm-git-81", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["run: git 2.43.0 — before stash the tree had a modified tracked file, a staged new file and an untracked u.txt; after `git stash` the tracked file was back to 'v1', the staged file was gone and u.txt was still on disk"], "notes": "Hint asks where the changes go and what the tree looks like, without asserting either."}
{"id": "rm-git-83", "verdict": "amend", "fields": {"hint": "Ask how many lines you would expect after stashing twice, and what each line has to tell you."}, "rescored": null, "problems": ["hint asks 'how each entry has to be named for other commands to reach it', and the stash@{0} label it points at appears in the key alone"], "evidence": ["run: git 2.43.0 — after two stashes `git stash list` printed `stash@{0}: WIP on main: cdb955c base` and `stash@{1}: WIP on main: cdb955c base`; `git for-each-ref` showed refs/stash is local only"], "notes": "New hint points at the shape of the output without naming the label."}
{"id": "rm-git-84", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["run: git 2.43.0 — `git stash apply` left the list at 2 entries; `git stash pop` printed `Dropped refs/stash@{0} (ab1bef9...)` and left 1", "knowledge: pop keeps the entry when the application conflicts, as the explanation says"], "notes": "Hint asks what the list looks like afterwards and leaves the 'identical' and reversed options live."}
{"id": "rm-git-85", "verdict": "amend", "fields": {"hint": "Plain `git stash` leaves one category of file exactly where it is. Ask which category, and what `-u` does about it."}, "rescored": null, "problems": ["hint says 'which category the flag adds', which settles the additive-versus-exclusive choice the options turn on"], "evidence": ["run: git 2.43.0 — after a plain `git stash` the untracked u.txt was still on disk; after `git stash -u` it was gone, while tracked changes were stashed either way"], "notes": "Only the give-away verb changed; the useful pointer at the untracked category is kept."}
{"id": "rm-git-86", "verdict": "amend", "fields": {"hint": "Two things have to hold here: the other branch has to check out, and your edits have to still be there when you come back.", "explanation": "Stash moves the unfinished changes off the working tree onto a local stack, so a checkout that would otherwise refuse to overwrite them can proceed, and git stash pop restores them when you come back. It does not commit them to the branch, discard them, or send them anywhere. A work-in-progress commit or a second worktree solve the same problem differently; stash is the option that leaves the branch history untouched."}, "rescored": null, "problems": ["hint's 'where the unfinished work can wait meanwhile' paraphrases the key", "explanation implied any uncommitted change blocks a checkout; Git refuses only when the switch would overwrite the modified file"], "evidence": ["run: git 2.43.0 — switching to a branch whose copy of the edited file differs printed 'error: Your local changes to the following files would be overwritten by checkout'; when the file matched, the switch carried the edit across", "run: git 2.43.0 — `git stash` cleaned the tracked modifications and `git stash pop` restored them with the branch history unchanged"], "notes": "Hint now states the two constraints and leaves the learner to test each option against them."}
{"id": "rm-git-87", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["run: git 2.43.0 — `git stash drop` printed `Dropped refs/stash@{0} (1fb61a4...)`, the list fell from 1 entry to 0 and nothing was applied to the working tree", "knowledge: the dropped stash commit survives until gc, so `git stash apply <hash>` can still recover it"], "notes": "Hint frames apply and remove as two independent effects without saying how many drop performs."}
{"id": "rm-git-88", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["run: git 2.43.0 — `git for-each-ref` listed only `refs/heads/main` and `refs/stash`, and `git cat-file -t $(git rev-parse refs/stash)` returned `commit`; refs/stash is outside the refs/heads/* and refs/tags/* a push sends"], "notes": "Hint asks which refs a push sends, which leaves the 'hidden branch that clone copies' distractor live."}
{"id": "rm-git-89", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["run: git 2.43.0 — from feature, `git rebase main` moved feature from cd6d774 to 6c6e289 on top of main1 (e26a56b) while main stayed at e26a56b and `git log --merges` counted 0"], "notes": "Hint asks whose commits get new parents, which is exactly the feature-versus-main confusion the distractors test."}
{"id": "rm-git-90", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["run: git 2.43.0 — rebase left a linear branch with new hashes (cd6d774 -> 6c6e289) and no merge commit; on a copy, `git merge --no-ff` produced a commit with two parents and left the originals untouched"], "notes": "Hint asks which command leaves the original commit objects alone, and the reversed claim in option 3 keeps that open."}
{"id": "rm-git-91", "verdict": "amend", "fields": {"hint": "Ask what `HEAD~3` selects here, and whether selecting commits is the same as removing them."}, "rescored": null, "problems": ["hint's 'the file Git opens' points at the todo list, and the key is the only option that mentions anything being opened"], "evidence": ["run: git 2.43.0 — `GIT_SEQUENCE_EDITOR=\"sed -i '2,3s/^pick/squash/'\" git rebase -i HEAD~3` collapsed sq1, sq2 and sq3 into one commit f707b16 whose message read 'sq1/sq2/sq3' and which held s1.txt, s2.txt and s3.txt"], "notes": "New hint aims at the 'deletes the last 3 commits' misconception instead of the editor."}
{"id": "rm-git-92", "verdict": "amend", "fields": {"hint": "Rebasing gives the commits new hashes. Ask what that means for a teammate whose clone already stores the old ones."}, "rescored": null, "problems": ["hint's 'what arrives alongside them when you publish the rewritten ones' paraphrases the key's 'collaborators end up holding both'"], "evidence": ["run: git 2.43.0 — after rebasing an already-pushed branch the remote rejected the push as non-fast-forward and `--force-with-lease` replaced it, so nothing in Git blocks the rewrite", "https://git-scm.com/book/en/v2/Git-Branching-Rebasing — 'Do not rebase commits that exist outside your repository and that people may have based work on'"], "notes": "Explanation already separates the convention from the tooling by saying Git does not prevent it, which this batch's other workflow items should match."}
{"id": "rm-git-93", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["run: git 2.43.0 — a conflicted rebase showed `UU c.txt` and printed `git rebase (--continue | --abort | --skip)`; staging the fix and running `git rebase --continue` printed 'Successfully rebased and updated refs/heads/feature'"], "notes": "The key is easy to spot from the option list itself, but the hint adds no further leak and the explanation earns its place by covering the git commit detour."}
{"id": "rm-git-94", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["run: git 2.43.0 — feature was 306b8d9 before the rebase; mid-conflict status showed `UU c.txt`, and after `git rebase --abort` feature was 306b8d9 again with c.txt back to 'feature-line'"], "notes": "Hint asks about the already-replayed commits, which is what separates the key from the 'keeps them and stops' distractor."}
{"id": "rm-git-95", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["run: git 2.43.0 — marking lines 2 and 3 of a three-commit todo as squash produced one commit f707b16 holding s1.txt, s2.txt and s3.txt with the combined message 'sq1/sq2/sq3'"], "notes": "Hint mentions two messages, which hints at a combination, but the 'drops it' option stays live since a dropped commit also shortens the log."}
{"id": "rm-git-96", "verdict": "amend", "fields": {"explanation": "Rebase re-creates the commits, so the remote's tip is no longer an ancestor of your branch and an ordinary push is rejected as non-fast-forward. --force-with-lease replaces the remote branch while still refusing if someone else pushed in the meantime. Pulling instead brings the old copies back: Git first asks how to reconcile the diverged branches, and merging leaves every commit duplicated."}, "rescored": null, "problems": ["explanation said 'pulling instead merges the old copies back in'; on git 2.43 with pull.rebase unset that pull does not merge, it stops with 'Need to specify how to reconcile divergent branches'"], "evidence": ["run: git 2.43.0 — after squashing an already-pushed topic branch, `git push origin topic` gave `! [rejected]        topic -> topic (non-fast-forward)`, plain `git pull` in that state ended `fatal: Need to specify how to reconcile divergent branches.`, and `git push --force-with-lease` then succeeded"], "notes": "The rebuttal of the pull option is kept but now names the prompt the learner will actually see first."}
{"id": "rm-git-97", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["run: git 2.43.0 — after `git tag v1.0`, `git cat-file -t v1.0` printed `commit` and .git/refs/tags/v1.0 held HEAD's sha (8f5344a3)", "run: git 2.43.0 — `git tag -m 'implied' v3.0` with no -a produced `git cat-file -t v3.0` = `tag`, so the explanation's '-a or -m' is right"], "notes": "Hint points at the absent flags, an observable in the command itself."}
{"id": "rm-git-98", "verdict": "amend", "fields": {"options": ["A lightweight tag", "An annotated tag", "A branch", "A commit"], "correctAnswer": 1}, "rescored": null, "problems": ["key was the only option carrying an explanatory parenthetical ('a full object with message and author'), and the hint's 'a message and an author' echoed it word for word"], "evidence": ["run: git 2.43.0 — after `git tag -a v2.0 -m 'Release 2.0'`, `git cat-file -t v2.0` printed `tag` and `git cat-file -p v2.0` showed object, type, tag and tagger lines plus the message"], "notes": "Trimming the parenthetical evens the four options and removes the hint echo in one change; the explanation still carries the detail."}
{"id": "rm-git-99", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["run: git 2.43.0 — after `git push origin main`, `git ls-remote --tags origin` was empty; `git push origin v1.0` added refs/tags/v1.0 and `git push --tags` then added v2.0 and v3.0", "https://git-scm.com/docs/git-push — --follow-tags pushes annotated tags reachable from the refs being pushed"], "notes": "Hint asks whether refs/tags travels with a branch push, an open yes-or-no."}
{"id": "rm-git-100", "verdict": "amend", "fields": {"hint": "Ask what Git does differently with a matching file: does anything vanish from disk, or only from a list?"}, "rescored": null, "problems": ["hint framed the question in the key's own vocabulary ('the ones Git is not already following') and asked about a tracked-versus-untracked distinction none of these options turn on"], "evidence": ["run: git 2.43.0 — an untracked 'other.env' listed in .gitignore never appeared in `git status --short` and `git add other.env` was refused with 'The following paths are ignored by one of your .gitignore files'; the file stayed on disk"], "notes": "New hint aims at the 'listing files to delete' misconception, which is the distractor this item exists for."}
{"id": "rm-git-101", "verdict": "amend", "fields": {"hint": "The file is already in the index. Ask what adding a line to `.gitignore` can do to something that is already there."}, "rescored": null, "problems": ["hint states the tested rule outright ('the rules decide what Git picks up, not what it is already holding'), which rules out all three distractors"], "evidence": ["run: git 2.43.0 — with 'secrets.env' added to .gitignore after the file was committed, `git status --short` still reported ` M secrets.env` and `git ls-files secrets.env` still listed it; the file stayed on disk"], "notes": "New hint restates only the question's premise and leaves all four outcomes open."}
{"id": "rm-git-102", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["run: git 2.43.0 — `git rm --cached secrets.env` left `git ls-files secrets.env` empty, the file still on disk, and status showing the staged deletion `D  secrets.env`"], "notes": "Hint contrasts the plain form with --cached and asks which side is spared, which is the one thing a learner has to know here."}
{"id": "rm-git-103", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["run: git 2.43.0 — v1.0 stayed at 8f5344a while main advanced to d7753ae over later commits, so the tag keeps naming one commit"], "notes": "Explanation says a tag 'is not expected to move' rather than claiming Git forbids it, and the hint leaves the raw-hash option live since a hash is stable too."}
{"id": "rm-git-104", "verdict": "amend", "fields": {"question": "You have just tagged the current commit as v1.0. What does this do?\n\n```bash\n$ git push origin v1.0\n```"}, "rescored": null, "problems": ["multiple-defensible: with no assumption stated, v1.0 could be a branch name (release branches are often named that way) and option 0 would then be correct"], "evidence": ["run: git 2.43.0 — with a tag v9.0 and a branch v9.0 both present, `git push origin v9.0` failed with `error: src refspec v9.0 matches more than one`; with only the tag, `git push origin v1.0` printed `* [new tag]         v1.0 -> v1.0`"], "notes": "One clause in the question fixes the ambiguity without touching the options or the key."}
{"id": "rm-git-105", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["run: git 2.43.0 — `git fork` printed `git: 'fork' is not a git command. See 'git --help'.`, confirming the Git-versus-platform boundary the explanation draws", "knowledge: GitLab calls the same object a merge request, and the merge a PR performs is an ordinary Git merge"], "notes": "Hint asks which system tracks the request rather than asserting that it is the hosting platform."}
{"id": "rm-git-106", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["run: git 2.43.0 — `git fork` printed `git: 'fork' is not a git command. See 'git --help'.`"], "notes": "Hint asks which machine copies, and option 0 also says 'on the server', so the hint does not settle the item on its own."}
{"id": "rm-git-107", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["knowledge: a pull request is opened against a branch that exists on the hosting platform, so the commits must be pushed before one can reference them", "run: git 2.43.0 — a branch created locally is absent from `git ls-remote --heads origin` until it is pushed"], "notes": "The key is a workflow order, but the explanation grounds it in the technical requirement rather than in team preference."}
{"id": "rm-git-108", "verdict": "amend", "fields": {"hint": "You already have a remote for your own fork. Ask what a second remote would be for."}, "rescored": null, "problems": ["hint ('which repository you can read from but cannot push to') identifies the key directly, since only the original repository fits that description"], "evidence": ["run: git 2.43.0 — `git remote rename origin upstream` succeeded and the tracking refs became upstream/main, showing the names are arbitrary local labels", "knowledge: origin for your fork and upstream for the source repository is the naming used by GitHub and GitLab contributing guides, not a Git rule"], "notes": "Explanation already says these are conventional names Git does not enforce, which is the right framing for this item."}
{"id": "rm-git-109", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["run: git 2.43.0 — fetching from a second remote moved only that remote's tracking refs; integrating took a separate merge or rebase before the result could be pushed to the first remote"], "notes": "Hint states the read-write asymmetry but the learner still has to assemble the three-step route, so it stops short of handing over the key."}
{"id": "rm-git-110", "verdict": "amend", "fields": {"hint": "A reviewer is a person reading a diff. Ask what such a person can and cannot promise about the merged change.", "options": ["A recorded second opinion on the change before the branch merges", "A guarantee that the merged change contains no bugs", "An automatic rollback if the change breaks production", "A merge that skips the repository's status checks"], "correctAnswer": 0}, "rescored": null, "problems": ["key was markedly longer than the three distractors and the only option with an em-dash aside", "hint ('be careful about what it cannot promise') is a test-taking cue rather than a pointer at the subject"], "evidence": ["knowledge: pull request review records comment, approve and request-changes verdicts on the pull request; required status checks are a separate mechanism that review does not bypass"], "notes": "Detail dropped from the key survives in the explanation, so nothing is lost for the learner reading afterwards."}
{"id": "rm-git-111", "verdict": "amend", "fields": {"hint": "Ask whether 'draft' changes who can see the pull request, or only what can be done with it."}, "rescored": null, "problems": ["hint's 'what it would be premature to do to the branch' points straight at the merge block, which is the key's distinguishing content"], "evidence": ["https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/proposing-changes-to-your-work-with-pull-requests/changing-the-stage-of-a-pull-request — 'No one can merge the pull request until you mark the pull request as ready for review again'", "knowledge: a draft pull request stays visible to everyone who can see the repository; it is not hidden or closed"], "notes": "New hint sets visibility against capability, which keeps the 'hides it from everyone but you' distractor live."}
{"id": "rm-git-112", "verdict": "amend", "fields": {"question": "Your pull request was merged with a merge commit. What happens to the commits if you now delete the feature branch?"}, "rescored": null, "problems": ["multiple-defensible: after a squash or rebase merge the original commits are not in the base branch's history, and option 2 becomes the better answer, so the merge type has to be stated"], "evidence": ["run: git 2.43.0 — after `git merge --no-ff` and `git branch -d feature`, the feature commit 5cc7e72 was still listed by `git log --oneline main` and `git fsck --unreachable` reported nothing", "run: git 2.43.0 — after `git merge --squash` plus a commit, the original 5cc7e72 appeared 0 times in `git log --oneline main`, so deleting the branch leaves it reachable only through the reflog"], "notes": "Stating the merge type keeps the objective and the key intact; hosting platforms offer all three merge buttons, so the assumption has to be explicit."}
{"id": "rm-git-113", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["run: git 2.43.0 — cherry-picking 2f163b8 onto a diverged main created a new commit a8b76fe with the same one-file change; fix.txt arrived, the unrelated extra.txt did not, and the original stayed on side", "run: git 2.43.0 — merging side into main afterwards left both 'fix' commits (a8b76fe and 2f163b8) in `git log`, which is the duplication the explanation warns about"], "notes": "Hint asks what is left on the source branch, which is exactly the copy-versus-move confusion in option 2."}
{"id": "rm-git-114", "verdict": "amend", "fields": {"hint": "`git log` answers what is in this branch now. Ask what the extra `ref` in `reflog` is a log of."}, "rescored": null, "problems": ["hint ('what would have recorded where you used to be') restates the key almost word for word"], "evidence": ["run: git 2.43.0 — after `git reset --hard HEAD~1`, `git reflog` showed `176fcce HEAD@{0}: reset: moving to HEAD~1` and `6fe233f HEAD@{1}: commit: important work`, a commit no branch pointed at any more", "https://git-scm.com/docs/git-reflog — gc.reflogExpire 'defaults to 90 days' and gc.reflogExpireUnreachable to 30 days"], "notes": "New hint points at the command's name and its contrast with git log without saying what the entries hold."}
{"id": "rm-git-115", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["run: git 2.43.0 — `git bisect start HEAD HEAD~5` then `git bisect run ./t.sh` reported `41112e46... is the first bad commit` after two automatic steps, and `git bisect reset` restored the branch"], "notes": "Hint gives the good/bad pair and asks what to do with it, leaving the 'finds the line' distractor live."}
{"id": "rm-git-116", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["run: git 2.43.0 — after `git checkout HEAD~1`, `git status` reported 'HEAD detached at 176fcce' and `git symbolic-ref -q HEAD` returned nothing; a commit made there was contained by 0 branches afterwards but still listed in the reflog"], "notes": "Explanation separates the definition from its consequence, which is what makes the 'commit no branch can reach' distractor wrong rather than partly right."}
{"id": "rm-git-117", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["run: git 2.43.0 — `git submodule add` wrote .gitmodules with path and url and produced the index entry `160000 339e5208... 0\tvendor/lib`; a plain `git clone` of the parent left vendor/lib empty"], "notes": "Hint offers a binary (the files, or something smaller) and leaves the 'second checkout of your own repository' distractor untouched."}
{"id": "rm-git-118", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["run: git 2.43.0 — a commit-msg hook exiting non-zero blocked `git commit -m 'bad message'` (log stayed at 1 commit) while 'feat: good message' succeeded; the pre-commit hook ran on both attempts and nothing under .git/hooks was tracked"], "notes": "Option 0 also describes something that runs commands, so the hint does not collapse the item to one answer."}
{"id": "rm-git-119", "verdict": "amend", "fields": {"hint": "Ask what `git reset --hard` actually destroyed: the commit object, or only the branch's claim on it."}, "rescored": null, "problems": ["hint's 'what recorded the pointer's earlier positions' names the reflog in all but the word, and the reflog is the key"], "evidence": ["run: git 2.43.0 — after `git reset --hard HEAD~1` the log lost 'important work'; `git reflog` showed `6fe233f HEAD@{1}: commit: important work` and `git reset --hard 6fe233f` restored the commit and important.txt"], "notes": "New hint attacks the 'gone for good' misconception and leaves the three recovery routes to be judged on knowledge."}
{"id": "rm-git-120", "verdict": "accept", "fields": null, "rescored": null, "problems": [], "evidence": ["run: git 2.43.0 — cherry-picking one commit from a diverged branch brought fix.txt across as a new commit a8b76fe and left the unrelated extra.txt behind; a later merge showed the same change under two hashes"], "notes": "Hint compares how much of main each option drags along, which is the axis the four options differ on."}
