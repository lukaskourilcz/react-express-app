{"id":"rm-dsa-1","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["knowledge: quadratic growth gives (2n)^2 = 4n^2, so four times the work per doubling is the n-squared signature"],"notes":"Hint points at the ratio in the stem without naming a growth class, and the explanation disposes of the memory distractor."}
{"id":"rm-dsa-2","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["knowledge: space complexity counts auxiliary memory as a function of n; an in-place sort uses O(1) extra space, a copying one O(n)"],"notes":"The key says 'extra memory', so the auxiliary-versus-total ambiguity the reviewer flagged is settled in the option itself."}
{"id":"rm-dsa-3","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["knowledge: n denotes input size by convention, and core count and recursion limit are fixed per run rather than functions of the input"],"notes":"All three distractors name machine or language properties that cannot be the variable a growth function is stated against."}
{"id":"rm-dsa-4","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["knowledge: hash-map lookup by key is average-case O(1) under a bounded load factor, while scanning an unsorted array is O(n)"],"notes":"Explanation correctly says average O(1) rather than plain O(1), and no distractor is defensible once the stem rules out ordering."}
{"id":"rm-dsa-5","verdict":"amend","fields":{"hint":"Ask what you would need to tell a caller who asks how slow this can get."},"rescored":null,"problems":["hint paraphrases the key: 'a guarantee has to cover every input' is the option 'an upper bound that holds for every input of that size' in other words"],"evidence":["knowledge: Big-O is notation for bounding a growth function and applies to best, average, or worst case alike, so option 3 stays false"],"notes":"Key and distractors are sound; only the hint hands over the answer."}
{"id":"rm-dsa-7","verdict":"amend","fields":{"hint":"Count the checks each loop performs when n is 10, then again when n is 1000."},"rescored":null,"problems":["hint states the key outright: 'one loop's step count depends on n and the other's does not' is the correct option restated"],"evidence":["knowledge: a fixed 5 checks is O(1) and a pass over every element is O(n); 5 > 1 does not change the growth class"],"notes":"Replacement hint asks the learner to count rather than handing over the comparison."}
{"id":"rm-dsa-8","verdict":"amend","fields":{"hint":"Compare the step count at n = 10 with the step count at n = 10 million before naming the class.","explanation":"Constant time, written O(1), means the number of steps is bounded by a fixed amount however large n is - reading a[i] is the standard example, since it costs the same at n = 10 and at n = 10 million. Linear time would rise in step with n, and quadratic and exponential rise faster still; all three change when the input changes, which the stem rules out."},"rescored":null,"problems":["hint calls the cost curve 'flat in n', which is the key 'Constant time' in other words","explanation offered 'pushing onto a stack' as a strictly bounded operation; push on an array-backed stack is amortized O(1), not bounded by a fixed number of steps"],"evidence":["knowledge: a dynamic array's push is amortized O(1) because a full backing buffer forces an O(n) copy on that one call","node: pop/push counts on a proxied 40-element array gave push idx gets=0 idx sets=1"],"notes":"Amended explanation keeps the indexed-read example and drops the amortized one."}
{"id":"rm-dsa-9","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["knowledge: Big-O bounds a growth function from above while dropping constant factors and lower-order terms, so seconds and exact operation counts are what it discards"],"notes":"Option 3 says the amount of memory allocated rather than how memory grows, so it is not a second defensible reading."}
{"id":"rm-dsa-10","verdict":"amend","fields":{"hint":"Work out what each notation predicts as n goes from 10 to 10 million, then compare that with the stem."},"rescored":null,"problems":["hint says 'pick the notation for a cost that never depends on n', which is the definition of the key O(1) and eliminates the other three"],"evidence":["node --allow-natives-syntax: [1,2,3] HasFastPackedElements=true, HasDictionaryElements=false, so indexed access is the packed contiguous case the stem assumes"],"notes":"Notation is written 'O(n squared)' here while rm-dsa-13 and rm-dsa-14 use O(n²); inconsistent but not a defect worth moving options for."}
{"id":"rm-dsa-12","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["node: counted 2n+5 loop at n=10,100,1000 -> 25,205,2005, matching the figures quoted in the explanation"],"notes":"The explanation correctly calls O(2n) and O(2n+5) unsimplified rather than wrong, and the stem says 'simplified' so the key is unique."}
{"id":"rm-dsa-13","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["node: at n=1e6, log2=19.93, n=1.0e6, n log n=1.99e7, n^2=1.00e12; n^2 / (n log n) = 50172 and n^2 / n = 1.0e6, so the quoted 'fifty thousand times' and 'a million times' both reproduce"],"notes":"Every distractor is strictly smaller at the stated n, so there is no second defensible reading."}
{"id":"rm-dsa-14","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["node: at n=1e6 the four values are 1, 19.93, 1.0e6 and 1.00e12, matching the 1, 20, 1e6, 1e12 quoted in the explanation"],"notes":"Hint gives a procedure rather than the ordering, and the explanation names the single swap in each near-miss option."}
{"id":"rm-dsa-15","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["node: counted inner-body executions for n=8,16,32 -> 64,256,1024, a fourfold rise per doubling exactly as quoted"],"notes":"Hint points at the counting method without naming a class."}
{"id":"rm-dsa-16","verdict":"amend","fields":{"question":"When we add two sequential steps that are O(n) and O(n²), the overall complexity in simplest Big-O form is:"},"rescored":null,"problems":["multiple-defensible: without a simplification qualifier, O(2n²) is a true upper bound for n + n² (the explanation itself concedes it is merely unsimplified), so option 3 matches a second reading of the stem"],"evidence":["node: counted operations for n + n^2 at n=10,100,1000 -> 110,10100,1001000, matching the explanation","knowledge: O is an upper bound, so n + n² is in O(2n²) and in O(n³) as well; only the tightest simplified form singles out O(n²)"],"notes":"Sibling item rm-dsa-12 already uses the word 'simplified' for exactly this reason, so the added qualifier matches house style."}
{"id":"rm-dsa-17","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["node --allow-natives-syntax: [1,2,3] gives HasFastPackedElements=true; after a[10000]=1 it gives HasDictionaryElements=true, confirming the V8 claim in the explanation"],"notes":"Stem says 'by its index', and the explanation separates indexed access from search and states the packed-representation caveat."}
{"id":"rm-dsa-18","verdict":"amend","fields":{"hint":"List the valid indexes for five elements, then check what each expression actually evaluates to."},"rescored":null,"problems":["hint states the rule the key encodes: 'the last valid index is one below the count' is a[a.length - 1] in words"],"evidence":["node: with a=[1,2,3,4,5], a[5]=undefined, a[a.length]=undefined, a[a.length-1]=5, a[-1]=undefined, a.at(-1)=5"],"notes":"Explanation is accurate including the a.at(-1) aside; only the hint gives the answer away."}
{"id":"rm-dsa-19","verdict":"amend","fields":{"hint":"Check how many slots the array has, then what `at` does with a negative argument."},"rescored":null,"problems":["hint supplies both halves of the answer: that index 3 is past the end and that at counts backward is the whole derivation of 'undefined 30'"],"evidence":["node: const a=[10,20,30]; console.log(a[3], a.at(-1)) printed 'undefined 30', and reading past the end threw nothing"],"notes":"Replacement hint names what to look up without stating either result."}
{"id":"rm-dsa-20","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["node: counted comparisons in linear search over an 8-element array -> 8 for a missing value, 1 for a value in first position, matching the explanation"],"notes":"Stem names the case (worst), the explanation names best case and says why O(log n) needs sorted data."}
{"id":"rm-dsa-21","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["node: counting Proxy on a 40-element array, unshift produced 40 indexed reads and 41 indexed writes (40 moves plus the new slot 0) while push produced 0 reads and 1 write"],"notes":"The quoted 'touched all 40 indexes' reproduces, and the explanation correctly says reallocation is not the reason the cost is linear."}
{"id":"rm-dsa-22","verdict":"amend","fields":{"hint":"Apply the two calls in order, and be sure which end each method touches."},"rescored":null,"problems":["hint says one call adds at the end and the other at the front, which is the entire trace the item asks the learner to perform"],"evidence":["node: [1,2,3] then push(4) then unshift(0) gives [0,1,2,3,4]","node: counting Proxy at n=40 - unshift made 40 indexed writes to move existing elements, push made 1, so the O(n²) remark holds"],"notes":"Quoted 40-versus-1 write figure reproduces exactly under an operation counter."}
{"id":"rm-dsa-23","verdict":"amend","fields":{"question":"Compared with linked lists, what is a key property of arrays?"},"rescored":null,"problems":["stem and options do not compose: 'is that arrays:' followed by 'They store elements in one contiguous block of memory' doubles the subject in all four options"],"evidence":["node: const a=[1,2,3]; delete a[1] leaves length=3, (1 in a)=false, JSON [1,null,3], confirming the hole claim","node --allow-natives-syntax: after a[10000]=1 on a 3-element array, HasDictionaryElements=true, confirming the sparse-array claim"],"notes":"Every factual claim in the explanation reproduces; only the stem grammar needed fixing."}
{"id":"rm-dsa-24","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["node: counting Proxy - pop touched 1 index (1 read, 0 writes, 1 delete) at n=10, 20 and 40; shift at n=40 made exactly 40 indexed reads and 39 indexed writes, matching the quoted figures"],"notes":"The explanation gets the shift contrast right, which is the fact learners most often have backwards."}
{"id":"rm-dsa-25","verdict":"amend","fields":{"hint":"Ask which operations the scan actually performs, then keep the smallest model that supports them."},"rescored":null,"problems":["hint eliminates every alternative but one: 'ask which model even has positions' leaves only the ordered sequence, since one distractor says it has no positions"],"evidence":["knowledge: a left-to-right scan needs an ordering and a successor relation, which a scalar, an unordered map, and a value-ordered tree do not provide"],"notes":"Explanation's separation of sequence from unit choice (code units versus grapheme clusters) is correct and worth keeping."}
{"id":"rm-dsa-26","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["node: console.log('hello'.length, '\\u{1F600}'.length) printed 5 2, and [...'\\u{1F600}'].length is 1 with codePointAt(0)=0x1f600"],"notes":"Hint states the unit rule but leaves three options live, since a byte-count reading would give a different number."}
{"id":"rm-dsa-27","verdict":"amend","fields":{"hint":"Index 3 is past the end of a three-character string. Look up what each of the two reads returns there."},"rescored":null,"problems":["hint says the two APIs disagree, which rules out both same-value options and leaves only the key and an implausible throw"],"evidence":["node: const s='cat'; s.charAt(3) is '' (typeof string) and s[3] is undefined; neither read threw","node: s.charAt(3)==='' is true while s[0]==='' is false, confirming the detection claim in the explanation"],"notes":"Key and explanation are exact; the hint was doing the discrimination for the learner."}
{"id":"rm-dsa-28","verdict":"amend","fields":{"hint":"Ask what happens to the original string when a method like replace appears to change it."},"rescored":null,"problems":["hint names a distractor to negate it: 'not about the variable that names it' is option 1 restated, and option 1 is the only real competitor"],"evidence":["node: in sloppy mode s[0]='z' left 'abc' unchanged; inside a 'use strict' function the same assignment threw TypeError: Cannot assign to read only property '0' of string 'abc'","node: 'abc'.replace('a','z') returned 'zbc' with the original still 'abc'"],"notes":"Both halves of the sloppy/strict claim in the explanation reproduce on Node 22."}
{"id":"rm-dsa-29","verdict":"amend","fields":{"hint":"Work out the time and the extra space each approach needs before comparing them with the two budgets."},"rescored":null,"problems":["hint eliminates every alternative but one: 'ask which approach allocates nothing' rules out sorting, the set, and the permutations in a single clause"],"evidence":["knowledge: two indexes moving inward do at most n/2 comparisons and hold two integers, so O(n) time and O(1) extra space; a character set is O(n) space and permutation generation is factorial time"],"notes":"Stem states both budgets, which is what makes the key unique; only the hint gave the discrimination away."}
{"id":"rm-dsa-30","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["node: ['ab','cd'].join('').split('').reverse().join('') returned 'dcba', and dropping the final join returns ['d','c','b','a']","node: 'ab😀'.split('').reverse().join('') produced code units de00 d83d 62 61 (a broken surrogate pair) while [...s].reverse().join('') kept 1f600 intact"],"notes":"Both the trace and the surrogate-pair warning in the explanation reproduce exactly."}
{"id":"rm-dsa-31","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["node: counted comparisons - 10 for two identical 10-character strings, 1 when they differ at the first position, matching the quoted figures"],"notes":"Stem names the case and the explanation supplies the best case and the timing-leak consequence."}
{"id":"rm-dsa-32","verdict":"amend","fields":{"hint":"Try each test on 'listen' and 'silent', then on 'aab' and 'abb'."},"rescored":null,"problems":["hint points straight at the key: 'ask what a set forgets' names the set distractor and leaves counting, which only the frequency-map option mentions"],"evidence":["node: on listen/silent (anagrams) the set test says true, reverse false, first-and-last false; on aab/abb (not anagrams) the set test says true and first-and-last says true, while the frequency-map test is correct on both"],"notes":"The two probes in the replacement hint defeat all three distractors without naming the property that fixes them."}
{"id":"rm-dsa-33","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["node: 100 string keys over 64 buckets gave a deepest bucket of 5 with djb2 and 5 with a Java-style hash (4 with FNV-1a); over 2000 random 100-key tables the modal deepest bucket was 5, so the quoted figure reproduces"],"notes":"Stem names the average case and the explanation states both preconditions (spreading hash, bounded load factor) plus the O(n) degenerate case."}
{"id":"rm-dsa-34","verdict":"amend","fields":{"hint":"Follow one insert: you have a key and an array of buckets. Ask what has to happen between the two."},"rescored":null,"problems":["hint paraphrases the key: 'the table needs a number to index its bucket array' is the option 'map a key to an index/bucket' in other words"],"evidence":["knowledge: a hash function reduces a key of any type to an integer bucket index; it is lossy and one-way, so it is neither encryption nor an ordering"],"notes":"Explanation correctly refutes the encryption distractor and adds the no-range-queries consequence."}
{"id":"rm-dsa-35","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["knowledge: the space of possible keys exceeds the bucket count, so by the pigeonhole principle collisions are unavoidable and must be handled rather than prevented"],"notes":"The hint does not single out the key, since a learner could just as easily read 'more keys than buckets' as the running-out-of-buckets option."}
{"id":"rm-dsa-36","verdict":"amend","fields":{"hint":"Ask what a table does when the bucket it computed is already occupied."},"rescored":null,"problems":["hint paraphrases the key: 'ask what can live in one bucket' points straight at storing colliding entries in a list at the same bucket"],"evidence":["knowledge: separate chaining stores colliding entries in a per-bucket list and open addressing probes for another slot; neither rejecting keys nor sorting is a collision strategy"],"notes":"Replacement hint states the situation and leaves both standard families open."}
{"id":"rm-dsa-37","verdict":"amend","fields":{"explanation":"If the hash sends every key to the same bucket the lookup walks that whole chain: with 400 colliding keys, finding the last one took 400 probes, against 3 when the same 400 keys were spread across 512 buckets. That is O(n), no better than scanning an array, and it is why a hash function an attacker can predict turns into a denial-of-service vector. O(log n) would require an ordered structure, which a hash table does not maintain."},"rescored":null,"problems":["quoted figure does not reproduce: 400 keys spread by a hash took 3 probes over 512 buckets and 1 with FNV-1a, not 13; 13 only appears at load factors around 6 to 12 per bucket, which a resizing table does not reach"],"evidence":["node: 400 keys, all colliding -> 400 probes to find the last; djb2 over 512 buckets -> 3 probes (max chain 3); FNV-1a over 512 -> 1 probe; djb2 over 64 buckets -> 14; djb2 over 32 buckets -> 29"],"notes":"Replaced the unreproducible 13 with a measured figure and stated the bucket count it was measured at."}
{"id":"rm-dsa-38","verdict":"amend","fields":{"hint":"Think about why you would reach for a Map instead of an array of key-value pairs."},"rescored":null,"problems":["hint paraphrases the key: 'both are reached by naming a key' plus 'which cost that buys' is the option 'key-value lookups in average O(1)' split in two"],"evidence":["node: o[1]='a'; o['1']='b' leaves one entry with key '1'; new Map().set(1,..).set('1',..) has size 2","node: Object.keys({b:1,2:1,a:1,10:1,1:1}) is ['1','2','10','b','a'] while the same Map iterates ['b',2,'a',10,1], confirming both iteration-order claims"],"notes":"Every Map-versus-object claim in the explanation reproduces on Node 22, including the string coercion and the integer-key ordering."}
{"id":"rm-dsa-39","verdict":"amend","fields":{"hint":"Follow one key through an insert and then a lookup, and ask what would make the lookup miss."},"rescored":null,"problems":["hint asks what must hold between the insert hash and the lookup hash, which is the key 'must map to a consistent hash value' restated"],"evidence":["node: new Map().set(NaN,'n').get(NaN) returns 'n', confirming the SameValueZero claim; a Map keyed by an object still finds the entry after the object is mutated, which is why the explanation limits the failure to tables hashed by contents"],"notes":"The explanation's mutation caveat is correctly scoped to content-hashed tables rather than to JavaScript Map."}
{"id":"rm-dsa-40","verdict":"amend","fields":{"hint":"For each option, ask whether hashing the value alone could answer it."},"rescored":null,"problems":["hint restates the key: 'the only question is have I seen this before' is 'test membership quickly' in other words"],"evidence":["node: scanning a 1000-element array for an absent value took 1000 comparisons; the same 1000 values in a Set answer has(-1)=false and has(999)=true without a scan"],"notes":"Explanation now names the trade-offs the three distractors are built from."}
{"id":"rm-dsa-41","verdict":"amend","fields":{"hint":"Picture a stack of plates: which one can you take without moving the others?"},"rescored":null,"problems":["hint expands the key acronym: 'the last thing placed on a pile and which item you can reach' is last in, first out spelled out"],"evidence":["knowledge: a stack exposes only its top, so the most recently pushed element is the first popped; pushing 3, 1, 2 pops 2, 1, 3"],"notes":"Replacement keeps the plate analogy but leaves the learner to supply which plate was added last."}
{"id":"rm-dsa-42","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["knowledge: push/pop are the stack pair, enqueue/dequeue the queue pair, insert/remove-min the priority-queue pair, and append/remove-first a queue under other names"],"notes":"Hint restates the scenario rather than the key, and never names the operation pair the learner has to supply."}
{"id":"rm-dsa-43","verdict":"amend","fields":{"question":"What is the amortized time complexity of push and pop on a stack?"},"rescored":null,"problems":["multiple-defensible: the stem states neither case nor model, and the explanation itself concedes that an array-backed push is amortized O(1) with an occasional resize copying every element - under a worst-case-per-call reading option 1 (O(n)) is defensible"],"evidence":["node: counting Proxy - push touched 1 index and pop touched 1 index at n=10, 20 and 40, which is the amortized behavior, not a bound on the resizing call","knowledge: a dynamic array doubling its capacity copies all n elements on that one push, so only the amortized claim holds for every implementation"],"notes":"The reviewer flagged the missing case and model but fixed only the explanation; one word in the stem closes the second reading."}
{"id":"rm-dsa-44","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["node: const stack=[]; push 1,2,3 then pop() returned 3 and left [1,2]; [].pop() returns undefined"],"notes":"The rewrite made the snippet runnable by declaring stack, and the hint points at the top without naming the value."}
{"id":"rm-dsa-45","verdict":"amend","fields":{"hint":"For each option, ask which item you need next: the newest, the oldest, or the smallest."},"rescored":null,"problems":["hint names the key option: describing closing and opening brackets points straight at 'matching balanced parentheses' without the learner doing anything"],"evidence":["knowledge: bracket matching needs the most recent unmatched opener, which is LIFO; print scheduling is FIFO, shortest paths use a queue or priority queue, and sorting needs comparisons"],"notes":"Replacement hint supplies the discriminating question without naming the domain of any option."}
{"id":"rm-dsa-46","verdict":"amend","fields":{"hint":"Trace where each frame sits while c() is running, then ask which one is on top."},"rescored":null,"problems":["hint states the rule the item tests: 'the most recently entered function is the one that can finish first' is the c-then-b-then-a key in words"],"evidence":["node: instrumenting a() calling b() calling c() logged a enter, b enter, c enter, c exit, b exit, a exit, exactly as the explanation claims"],"notes":"Key, options and explanation are sound; only the hint did the reasoning for the learner."}
{"id":"rm-dsa-47","verdict":"amend","fields":{"hint":"Compare the four on two points: which end they act on, and whether they remove anything."},"rescored":null,"problems":["hint eliminates every alternative but one: 'which name means read-only' leaves peek, since the other three all remove"],"evidence":["knowledge: peek (top) returns the most recent item without removing it; pop removes from the top, and dequeue and shift both remove from the front"],"notes":"Replacement gives the two axes to compare rather than the property only the key has."}
{"id":"rm-dsa-49","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["knowledge: a queue removes from the opposite end from where it adds, so the longest-waiting element leaves first; enqueuing 3, 1, 2 dequeues 3, 1, 2"],"notes":"The counter-line analogy asks a question about the model rather than spelling out first in, first out, so it stays on the right side of the hint rule."}
{"id":"rm-dsa-50","verdict":"amend","fields":{"hint":"Arrival order is the constraint. Check which pairs would reorder the jobs by something else."},"rescored":null,"problems":["hint eliminates every alternative but one: 'two different ends are involved, which pair says which end each acts on' selects the only option naming a back and a front"],"evidence":["knowledge: enqueue at the back with dequeue at the front preserves arrival order; push/pop at one end is a stack, peek with remove-min is a priority queue, and sorted insertion abandons arrival order"],"notes":"Replacement leaves the stack-versus-queue decision, which is the one the item is about, to the learner."}
{"id":"rm-dsa-51","verdict":"amend","fields":{"question":"For a queue backed by a circular buffer or a linked list, enqueue and dequeue are each:"},"rescored":null,"problems":["the stem's model is only 'well-implemented', which carries the whole claim; the implementation most JavaScript learners reach for (an array with shift) makes option 1 (O(n)) defensible, as the explanation itself sets out"],"evidence":["node: counting Proxy - shift on a 40-element array made 40 indexed reads and 39 indexed writes, so an array-backed dequeue is linear, not constant","node: a circular buffer draining 1000 items did 1000 writes and 1000 reads in total, against 500500 indexed writes for the shifting array queue"],"notes":"Naming the two implementations keeps the reviewer's teaching point and removes the second reading the hedge left open."}
{"id":"rm-dsa-52","verdict":"amend","fields":{"hint":"The two ends behave differently. Decide which end dequeue acts on before picking a value."},"rescored":null,"problems":["hint asks which value has been waiting longest, which is the FIFO rule under test and identifies the key directly"],"evidence":["knowledge: a queue dequeues from the front, so the first of a, b, c enqueued is the first returned; the stem now states the queue starts empty and the block is fenced as text rather than js"],"notes":"Replacement points at the end each operation acts on and leaves the discipline to the learner."}
{"id":"rm-dsa-53","verdict":"amend","fields":{"hint":"Ask what each traversal does with a node's neighbors: all of them next, or one of them next?","explanation":"BFS holds its frontier in a FIFO queue: dequeue a node, enqueue its unvisited neighbors, so everything at distance k is processed before anything at distance k+1 - on A to {B,C}, B to {D,E}, C to {F} the visit order is A, B, C, D, E, F. Swap the queue for a stack and the same code becomes depth-first, visiting A, C, F, B, E, D - one branch to the end before the other; in-order traversal and backtracking are likewise stack- or recursion-driven, so they dive deep before going wide."},"rescored":null,"problems":["quoted traversal order does not reproduce: swapping the queue for a stack in the same code visits A, C, F, B, E, D, not A, B, D, E, C, F, which is the recursive order (or a stack fed reversed neighbor lists)","hint asks which structure preserves arrival order, but every option is a traversal, not a structure, and the stem already names the queue"],"evidence":["node: on A to {B,C}, B to {D,E}, C to {F} - BFS gave A,B,C,D,E,F; the same loop with a stack gave A,C,F,B,E,D; recursive DFS gave A,B,D,E,C,F"],"notes":"Corrected the DFS order to the measured one and pointed the hint at the neighbor-expansion difference instead."}
{"id":"rm-dsa-54","verdict":"amend","fields":{"hint":"For each option, work out what it costs to reach and remove at the far end."},"rescored":null,"problems":["hint eliminates every alternative but one: 'which structure keeps a handle on each end' is the deque's defining property, so no comparison is left to make"],"evidence":["knowledge: a deque holds head and tail handles for O(1) work at both ends; a head-only singly linked list needs O(n) to reach the tail, a balanced tree costs O(log n), and a hash set has no ends"],"notes":"Stem states the O(1) requirement itself, so the hint only needed to name the cost to compare."}
{"id":"rm-dsa-55","verdict":"amend","fields":{"hint":"For each scenario, ask whether the item you need next is the most recent one."},"rescored":null,"problems":["hint uses the key option's own words: 'which scenario cares about arrival order' matches 'processing tasks in arrival order' directly"],"evidence":["knowledge: bracket matching, string reversal and recursion all need the most recent item, which is LIFO; only arrival-order processing is FIFO"],"notes":"Replacement asks the newest-item question, which sorts all four scenarios without quoting any option."}
{"id":"rm-dsa-56","verdict":"amend","fields":{"hint":"Draw the buffer after a few enqueues and dequeues, and see which slots are now unused."},"rescored":null,"problems":["hint states the key's mechanism: stranded front slots reached by wrapping the index is 'it reuses freed front slots instead of shifting' in other words"],"evidence":["node: counting Proxy - 1000 enqueues then 1000 dequeues on a shifting array queue made 500500 indexed writes (1000 enqueue writes plus 499500 moves), against 1000 writes and 1000 reads for a circular buffer, matching the quoted 500,500 and 2,000"],"notes":"Both quoted figures reproduce, and the explanation correctly denies that wrapping adds capacity."}
{"id":"rm-dsa-57","verdict":"amend","fields":{"explanation":"Nodes sit wherever they were allocated, so position k is reached only by following k next-pointers from the head: on a 100-node list, index 50 cost 50 hops and index 99 cost 99. That is O(n) in the worst case, against O(1) for an array's address computation. What the list buys in exchange is O(1) insertion after a node you already hold, with no shifting - removing that node is not free in a singly linked list, since its predecessor has to be found by walking from the head."},"rescored":null,"problems":["explanation claims O(1) removal once you hold the node, which is false for the singly linked list in the stem and contradicts rm-dsa-60 in this same batch, where finding the predecessor is what a doubly linked list exists to avoid"],"evidence":["node: on a 100-node list built from next-pointers, index 50 took 50 hops and index 99 took 99, matching the quoted figures","node: removing a held node at index 50 needed 50 steps from the head to find its predecessor"],"notes":"Hint is sound - it points at the absence of address arithmetic without naming a complexity class."}
{"id":"rm-dsa-58","verdict":"amend","fields":{"hint":"Picture the nodes scattered in memory in no particular order, and ask what still connects them."},"rescored":null,"problems":["hint paraphrases the key: 'each node has to say where to go next' is 'a value and a reference to the next node' restated"],"evidence":["knowledge: a singly linked node holds a value and a next reference, the last being null; storing an index instead would need the contiguous array the structure avoids and would renumber every later node on insertion"],"notes":"Replacement keeps the memory-layout cue but leaves the learner to name what does the connecting."}
{"id":"rm-dsa-59","verdict":"amend","fields":{"hint":"Count the references that have to change, and ask whether the rest of the list notices."},"rescored":null,"problems":["hint says only one link has to change, which contradicts its own explanation and the measurement: the new node's next and the head reference are two writes"],"evidence":["node: inserting at the head recorded 2 pointer writes (node.next, then head) on a 10-node list and the same 2 on a 100,000-node list, matching the explanation"],"notes":"Key is right and the array contrast in the explanation is accurate; only the hint's count was wrong."}
{"id":"rm-dsa-60","verdict":"amend","fields":{"hint":"Ask which operations a singly linked list cannot do without walking from the head again."},"rescored":null,"problems":["hint paraphrases the key: 'what a node needs in order to step backward' is 'a reference to the previous node' in other words"],"evidence":["node: in a singly linked 100-node list, removing a node you already hold needed 50 steps from the head to find its predecessor, which is the cost the prev pointer removes"],"notes":"Explanation names both the benefit and the price (one extra pointer per node, two links to keep consistent), which the reviewer's fix added correctly."}
