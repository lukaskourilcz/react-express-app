{"id":"rm-ai-109","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["https://platform.claude.com/docs/en/api/messages - \"there is no \\\"system\\\" role for input messages in the Messages API\"; top-level `system` parameter","https://developers.openai.com/api/docs/api-reference/chat/create - \"With o1 models and newer, `developer` messages replace the previous `system` messages\"","knowledge: the stem now says 'system instructions' and hedges with 'In most chat model APIs', so it no longer keys a role Anthropic does not expose"],"notes":"Rewrite drops the stale 'system message' role framing and the explanation names both vendor shapes, both of which I confirmed against the primary docs."}
{"id":"rm-ai-110","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["https://platform.claude.com/docs/en/api/rate-limits - \"429 error describing which rate limit was exceeded, along with a retry-after header indicating how long to wait\"; \"Earlier retries will fail\"","knowledge: jitter decorrelates retries across clients; a fixed 100 ms retry and parallel fan-out both keep the per-minute budget saturated"],"notes":"Key and both quoted retry-after claims verified; the word 'robust' in the stem is pre-existing and untouched by the rewrite, so I left it rather than desyncing the stem."}
{"id":"rm-ai-111","verdict":"amend","fields":{"hint":"Ask whether characters and tokens convert at a fixed rate, and whether the prompt is counted differently from the reply."},"rescored":null,"problems":["hint paraphrases the key: 'which unit the provider's limit and the provider's invoice are both written in' is the key's 'context windows and pricing are both measured in tokens'"],"evidence":["knowledge: BPE-style tokenizers give no fixed characters-per-token ratio, and providers meter both context length and billing in tokens","knowledge: a learner reading the old hint could pick option 2 without knowing anything, since it is the only option naming both the limit and the price"],"notes":"New hint points at the two tokenizer misconceptions in options 0 and 1 instead of restating the key."}
{"id":"rm-ai-112","verdict":"amend","fields":{"options":["Raise max_tokens so there is room for a longer reply","Trim, summarize or chunk the input so the prompt plus the expected reply fit the limit","Stream the response, since streamed tokens are not counted against the window","Remove the system instructions and retry, accepting whatever behavior results"],"correctAnswer":1,"hint":"Ask what each of these actually changes about the request you send.","explanation":"The context window caps input plus output for one request, so the only real fix is to send less: trim, summarize, or split the work into chunks and leave room for the reply. Raising max_tokens reserves more of the same budget for output and makes the overflow worse. Streaming only changes how the reply is delivered, not how many tokens it uses, and dropping the system instructions frees few tokens while giving up control of format and behavior."},"rescored":null,"problems":["the reviewer's own defect list called 'send the request twice' a non-mistake and the rewrite kept it","hint asserted the tested premise ('the limit counts the prompt and the reply together'), which is the clause that makes the max_tokens distractor wrong"],"evidence":["knowledge: the context window bounds input plus generated output for one request; max_tokens reserves part of that same budget","knowledge: streaming is a transport concern and changes neither the token count nor the window check"],"notes":"Replaced the leftover joke distractor with the streaming misconception, kept the key at index 1, and rewrote the hint so it no longer hands over the shared-budget rule."}
{"id":"rm-ai-114","verdict":"amend","fields":{"hint":"Ask what the word 'stateless' in the stem is actually promising about the server."},"rescored":null,"problems":["hint's second sentence ('what has to be inside the second one for turn one to exist') is the key: the client resends the prior messages"],"evidence":["https://developers.openai.com/api/docs/guides/conversation-state - manual history management alongside previous_response_id and the Conversations API","knowledge: model weights are frozen after training and hold nothing about a given conversation"],"notes":"Explanation's server-side-threading caveat checks out against OpenAI's conversation-state guide; only the hint needed fixing."}
{"id":"rm-ai-115","verdict":"amend","fields":{"options":["Replies get faster as the history grows, so nothing needs handling","The history outgrows the context window and every turn costs more, so older turns are summarized","Latency is the only thing that grows, so you stream the reply to hide it","The system instructions are discarded first, with no effect on behavior"],"correctAnswer":1,"hint":"Ask what a request looks like on turn fifty compared with turn one.","explanation":"Because each request carries the whole history, both the input bill and the prompt size grow with every turn until the conversation hits the context window. The usual fix is to summarize or drop older turns while keeping the system instructions and the recent ones. Latency does grow too, but streaming only changes when text appears, not whether the request still fits, and dropping the system instructions changes behavior rather than solving the growth."},"rescored":null,"problems":["'The API raises the temperature automatically to fit more in' is a joke distractor the rewrite reworded rather than replaced","key was 117 chars against a 71-char longest distractor (1.65x), so it was pickable by length","hint named both halves of the key (what grows without bound, what it runs into)"],"evidence":["knowledge: temperature is a caller-supplied parameter; no chat API adjusts it on its own","knowledge: streaming changes delivery timing only, so it does not address prompt growth or the window check"],"notes":"Swapped the temperature joke for the latency misconception, trimmed the key to 95 chars, and replaced the hint and matching explanation clause."}
{"id":"rm-ai-116","verdict":"amend","fields":{"hint":"The user's message is the most recent thing the model read. Ask whether recency is what sets priority."},"rescored":null,"problems":["hint states the tested rule ('which instruction the provider trains the model to weigh more heavily'), so the key is pickable from the hint alone"],"evidence":["knowledge: provider instruction hierarchies rank system-level rules above user turns as trained behavior, not as an enforced boundary","knowledge: jailbreak and prompt-injection results show prompt-level precedence is defeatable, which the rewritten explanation now says"],"notes":"The rewrite does fix the flagged 'stated as settled' defect - the explanation now calls the priority a learned tendency and says anything that must not happen needs a check outside the prompt - so only the hint is amended."}
{"id":"rm-ai-117","verdict":"amend","fields":{"hint":"Ask which of these constrains what the assistant may say, rather than how fast or how randomly it says it.","explanation":"Guardrails is an umbrella for everything holding an assistant inside intended behavior: safety training baked into the model, rules in the system prompt, and filters, allowlists and confirmation steps in the application around it. Because those layers are independent, you can add your own even on a hosted model - and should, since the first two can be talked around. Safety training is learned behavior rather than a word list compiled into the weights, rate limits protect the service rather than the content, and temperature only changes how the next token is sampled."},"rescored":null,"problems":["hint ('the term covers more than one layer... who besides the model provider can add a constraint') selects the only multi-layer option","explanation answered two distractors but never said why a banned-word list compiled into the weights is wrong"],"evidence":["knowledge: safety behavior is learned during training rather than stored as a word list in the parameters","knowledge: production assistants combine model-level training, system-prompt rules and application-side checks"],"notes":"Item sits at the quality floor (3) but clears both gates; hint and the missing distractor rebuttal are the only fixes needed."}
{"id":"rm-ai-118","verdict":"amend","fields":{"hint":"Temperature 0 makes sampling deterministic. Ask whether deterministic and schema-valid are the same property."},"rescored":null,"problems":["hint ('one of these changes what the model is allowed to emit; the others only ask for it more clearly') maps one-to-one onto the key's 'constrain the reply'"],"evidence":["https://platform.claude.com/docs/en/build-with-claude/structured-outputs - \"Structured outputs constrain Claude's responses to follow a specific schema\"; output_config.format + strict:true; \"beta headers are no longer required\"","https://developers.openai.com/api/docs/guides/structured-outputs - \"always generate responses that adhere to your supplied [JSON Schema]\"; refusal and max-token truncation named as exceptions"],"notes":"Both vendor claims in the explanation verified, including that Anthropic's structured outputs are now GA with output_config.format and that refusals and truncation remain exceptions."}
{"id":"rm-ai-119","verdict":"amend","fields":{"hint":"Ask which of these an attacker could carry out using only text the assistant will read."},"rescored":null,"problems":["hint states the key's mechanism (attacker text landing inside the model's one token sequence), so the key is pickable without reading the options"],"evidence":["knowledge: instructions, user text, retrieved documents and tool output share one token sequence with no trust boundary","knowledge: OWASP ranks prompt injection first among LLM application risks"],"notes":"Options and explanation are sound - flooding, key guessing and window overflow are real neighboring attacks - so only the hint is amended."}
{"id":"rm-ai-120","verdict":"amend","fields":{"hint":"The stem's word is 'untrusted'. Ask what you would have to assume about the page's author before acting on its text."},"rescored":null,"problems":["hint ('what an attacker could publish on a page your app will fetch and paste into the prompt') hands over the attack model, and only one option involves an attacker"],"evidence":["knowledge: indirect prompt injection places the payload in content the application retrieves, so the victim never types the attack","knowledge: latency and citation formatting are real costs but carry no trust consequence, which is what makes the other three options wrong"],"notes":"Options and explanation are sound; the hint did all the discrimination for the learner."}
{"id":"rm-ai-121","verdict":"amend","fields":{"options":["It is served from several data centers for redundancy","It handles more than one kind of input or output, such as text and images","It has been trained on text in more than one human language","It accepts the prompt in several formats, such as JSON, XML or plain text"],"correctAnswer":1,"hint":"Both halves of the word matter. Ask what counts as a different mode, and what is only a different encoding of the same one.","explanation":"Modalities are kinds of data - text, images, audio, video - so a multimodal model accepts or produces more than one of them, for instance answering questions about a screenshot you upload. Supporting many human languages is still a text-only model, and JSON, XML and plain text are all encodings of text rather than separate modalities. Where the model runs says nothing about which inputs it accepts."},"rescored":null,"problems":["hint states the definition being tested ('a modality is a kind of data'), which is the key","'It exposes several sampling presets for different kinds of task' is not a confusion anyone holds, so only one distractor was doing work"],"evidence":["knowledge: multimodal refers to the data types a model consumes or produces, independent of language coverage, serialization format or deployment","knowledge: JSON, XML and plain text are all text, so treating them as modalities is the encoding-versus-modality confusion"],"notes":"Replaced the dead sampling-presets option with the format-versus-modality confusion; key stays at index 1 and the explanation now answers all three distractors."}
{"id":"rm-ai-122","verdict":"amend","fields":{"hint":"Decide what the photo supplies and what a transcript must contain, then check which option runs in that direction."},"rescored":null,"problems":["hint hands over the key's exact input-output pair ('pixels in, characters out'), and every option is labeled by its own input and output, so matching is mechanical"],"evidence":["knowledge: reading text out of an image is optical character recognition regardless of whether a dedicated engine or a vision-language model does it","knowledge: vision-language models predict a likely transcription and do not emit per-character confidence, so a misread can look plausible"],"notes":"Stem, options and the silent-misread caveat in the explanation are sound; only the hint gave the answer away."}
{"id":"rm-ai-124","verdict":"amend","fields":{"hint":"The transcript has to be in the language people spoke. Ask what each of these produces, and in which language."},"rescored":null,"problems":["hint ('pick the one whose output is the words themselves, in the language spoken') eliminates all three distractors, leaving only the key"],"evidence":["knowledge: ASR, speaker diarization, speech translation and speech synthesis are distinct capabilities, commonly combined in one audio pipeline","knowledge: diarization labels who spoke when and is run alongside ASR rather than instead of it"],"notes":"Retaining this item over retirement is right - it is the only audio content in the level and the neighboring capabilities are genuinely confused when wiring a pipeline."}
{"id":"rm-ai-125","verdict":"amend","fields":{"hint":"A vision model costs far more than an image library. Ask what you would be paying it to do."},"rescored":null,"problems":["hint ('three of these can be done without knowing anything about what the image shows') eliminates every alternative but the key"],"evidence":["knowledge: compression, hash-based deduplication and cropping operate on bytes or pixel geometry and need no model","knowledge: only field extraction from a scanned invoice depends on what the image shows"],"notes":"New hint frames the same cost decision without pre-counting the options for the learner."}
{"id":"rm-ai-126","verdict":"amend","fields":{"hint":"Whatever happens, the model must still answer follow-up questions about parts of the picture that are not text."},"rescored":null,"problems":["hint states the mechanism the key describes ('a transformer consumes vectors; what has to happen to pixels'), so the encoder option is pickable from the hint alone"],"evidence":["https://platform.claude.com/docs/en/build-with-claude/vision.md - \"Claude views images in patches instead of pixels... an image costs ceil(width/28) x ceil(height/28) visual tokens\"","knowledge: a vision encoder maps image patches to vectors projected into the text embedding space, so image and text positions are attended over together"],"notes":"Verified that images are billed as visual tokens, which is the explanation's 'images consume part of your token budget' claim."}
{"id":"rm-ai-127","verdict":"amend","fields":{"hint":"Ask which of these claims you could test by generating the same diagram ten times."},"rescored":null,"problems":["hint ('what happens to the labels and the small details in a generated diagram') is the key restated: wrong details and garbled text"],"evidence":["knowledge: diffusion-based generators sample, so the same prompt produces different images and in-image text is frequently malformed","knowledge: no internal factual check exists that could trigger an accuracy-based refusal, and copyright status of generated output differs by jurisdiction"],"notes":"Scenario stem and the honest 'rights are not settled everywhere' distractor are good; only the hint needed replacing."}
{"id":"rm-ai-129","verdict":"amend","fields":{"hint":"Ask what would still be unsolved even if the model's output parsed perfectly and its scores were high."},"rescored":null,"problems":["hint names the category of all three distractors ('not about formats, scores or versions'), which leaves only the key"],"evidence":["knowledge: alignment is an open research area - value specification and verifying a model's internal goals are both unresolved, which the rewritten explanation now states","knowledge: the three distractors are genuine engineering senses of 'align' rather than puns, so the option set is a real improvement"],"notes":"The rewrite fixes the flagged 'contested definition stated as closed' defect in the explanation; the hint was the remaining problem."}
{"id":"rm-ai-130","verdict":"amend","fields":{"hint":"Reinforcement learning needs a reward number. Ask where that number comes from when the task has no score."},"rescored":null,"problems":["hint ('the signal is a comparison, not a correct answer; ask who produces it') is the key verbatim in substance: human preference comparisons"],"evidence":["knowledge: RLHF trains a reward model on ranked human comparisons and optimizes the policy against it","knowledge: direct preference optimization uses the same preference pairs without a separate RL loop, which the explanation correctly adds"],"notes":"Stem already expands the acronym, so the item leans on the stem; the new hint at least points at the reward-model mechanism rather than restating the key."}
{"id":"rm-ai-132","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["knowledge: jailbreaks are adversarial inputs against trained refusal behavior, distinct from authentication bypass or server-side exploitation","knowledge: the hint eliminates the self-hosting and credential options but leaves two prompt-shaped options, so it does not select the key"],"notes":"Reviewer's cross-batch overlap note with rm-ai-42, rm-ai-57 and rm-ai-89 stands and should be resolved before all four ship together."}
{"id":"rm-ai-134","verdict":"amend","fields":{"hint":"Ask what a guardrail is actually implemented as, and what that kind of thing is never perfect at."},"rescored":null,"problems":["hint names one of the two failure modes and asks for the other ('name the one besides letting something through'), and the key is the only two-part option"],"evidence":["knowledge: safety filtering is a classification problem with both false negatives (jailbreaks) and false positives (over-refusal), and the threshold trades one against the other","knowledge: guardrails do not alter sampling, do not check facts, and apply to the whole exchange rather than the system prompt alone"],"notes":"Best item in the level because it states a limitation rather than a reassurance; only the hint needed work."}
{"id":"rm-ai-135","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["knowledge: the standard post-training pipeline is supervised instruction fine-tuning followed by preference optimization (RLHF or DPO) on top of pretrained weights","knowledge: the hint's premise (a base model continues text) is background, and it leaves both the fine-tuning and the retrain-from-scratch options in play"],"notes":"Hint points at what to notice without selecting the key, and the explanation answers all three distractors including the pruning one."}
{"id":"rm-ai-136","verdict":"amend","fields":{"hint":"All four of these happen before a release. Ask what each one is trying to find out."},"rescored":null,"problems":["hint states the key's defining property ('the goal is to make the system fail, not to score how well it does'), and the key is the only option about attacking"],"evidence":["knowledge: red-teaming is adversarial evaluation aimed at eliciting failures, distinct from benchmarking, shadow-traffic performance testing and preference annotation"],"notes":"The rewrite replaced the 'red' pun set with three real pre-release activities, which is the right fix; the hint then gave the distinction away."}
{"id":"rm-ai-137","verdict":"amend","fields":{"hint":"The stem says 'most fundamentally'. Ask which of these creates an association and which only makes one visible."},"rescored":null,"problems":["hint ('what the model was fitted to before anyone typed a prompt') is the key's first half - the training data - and rules out the prompt distractor at the same time"],"evidence":["knowledge: model bias originates in the training distribution and in collection, filtering, labeling and alignment decisions; prompt and sampling settings modulate its expression","knowledge: uneven tokenization of names costs affected users tokens and latency but does not create the associations"],"notes":"New hint carries the origin-versus-symptom distinction as a question rather than as a conclusion."}
{"id":"rm-ai-138","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["knowledge: hosted inference involves provider-side logging and retention windows, and memorization of training data is a documented phenomenon","knowledge: the hint kills only the TLS distractor and leaves all three retention claims in play, so it does not select the key"],"notes":"Explanation is properly hedged - retention and training-use terms differ by provider, plan and region and have to be read - which is the right posture for a claim that changes."}
{"id":"rm-ai-139","verdict":"amend","fields":{"hint":"Copyright is national law. Ask which of these statements would have to be true everywhere at once."},"rescored":null,"problems":["hint ('which of these you could point to a final ruling for') telegraphs that the answer is the unsettled one"],"evidence":["https://www.copyright.gov/ai/ - Office still issuing its Report on \"the scope of copyright in AI-generated works and the use of copyrighted materials in AI training\"; Part 3 (training) only in pre-publication form","knowledge: training-data copyright cases remain in litigation across jurisdictions and the protectability of AI output differs by national law, so 'still being litigated' is the accurate framing"],"notes":"Confirmed with the US Copyright Office that its own report on training-data use is still only in pre-publication form, which supports the key's 'unsettled' claim."}
{"id":"rm-ai-141","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["knowledge: a systematic score disparity between equally qualified groups is bias, distinguishable from sampling noise (which is inconsistent) and hallucination (which is fabricated content)","knowledge: the hint restates the stem's 'consistently' and rules out only the temperature option, leaving three candidates"],"notes":"Overfitting, hallucination and high temperature are the misdiagnoses a reviewer would actually consider, so the differential is real."}
{"id":"rm-ai-142","verdict":"amend","fields":{"hint":"Ask which of these changes what the user knows, and which only changes what you store."},"rescored":null,"problems":["hint ('transparency is about what the user is told, and it still has to pass a privacy review') supplies both criteria the item tests"],"evidence":["knowledge: disclosure of AI interaction, stated limitations and data-use notice are the standard transparency practices; publishing the system prompt and unbounded retention address neither"],"notes":"The three-part key is still longer than the distractors, but each part is required by the objective and the distractors are now real practices, so I left the option set alone."}
{"id":"rm-ai-143","verdict":"amend","fields":{"hint":"Ask what a hash of an email address still lets you do."},"rescored":null,"problems":["hint ('the safest data is the data you never send') is a maxim that points straight at the minimization key, which is the only option about sending less"],"evidence":["knowledge: deterministic hashing of identifiers preserves linkability and re-identification from a small candidate set, so it is not anonymization","knowledge: a vendor zero-retention setting still depends on the vendor honoring it and on your configuration being correct"],"notes":"New hint targets the hashing misconception, which is the distractor that actually sends personal data to vendors."}
{"id":"rm-ai-144","verdict":"amend","fields":{"options":["The training data was balanced, so any skew must come from the prompt","Fitting frequent associations and then decoding the likely continuation concentrates output on the majority","Alignment training introduces skew that pretraining did not contain","Larger context windows give older, more stereotyped text more influence"],"correctAnswer":1,"hint":"Amplify means the output is more skewed than the data. Ask what could make that happen."},"rescored":null,"problems":["key was 119 chars against a 71-char longest distractor (1.68x) and the only technical-register option","hint stated the mechanism ('a model emits what is likely, not what is representative') and handed over the explanation's 60/40 example"],"evidence":["knowledge: likelihood-maximizing decoding over a skewed distribution yields output more concentrated than the training distribution, the standard account of bias amplification","knowledge: alignment training is applied to reduce such outputs rather than to create them, and window size changes what is in view rather than which continuation is likely"],"notes":"Trimmed the key to 101 chars and replaced the hint; the explanation's 60/40 arithmetic is the item's real content and stays where the learner sees it after answering."}
{"id":"rm-ai-145","verdict":"amend","fields":{"hint":"The word 'calling' is doing damage here. Ask what the model actually sends back in its response."},"rescored":null,"problems":["hint ('nothing runs on its own') asserts the exact fact the item tests, which is that the model emits a request and your code runs it"],"evidence":["https://platform.claude.com/docs/en/api/messages - tool definition = name, description, input_schema, sent in the request; \"return the following back to the model in a subsequent `user` message\" as tool_result","knowledge: provider-hosted code execution is a separate tool feature, and schema-constrained arguments replaced parsing function names out of prose"],"notes":"The flagged anthropomorphism is fixed: the key now reads 'the model emits a structured request... which your code then runs' and the explanation says the model executes nothing, matching rm-ai-148 in the same level."}
{"id":"rm-ai-146","verdict":"amend","fields":{"options":["The agent runs a larger model with a bigger context window","The agent runs a loop: the model's output triggers a tool, the result goes back in, and it repeats","The agent is fine-tuned on your data, while a one-shot call uses the base model","The agent runs on the provider's servers, while a one-shot call runs locally"],"correctAnswer":1,"hint":"Ask which of these describes a different model and which describes different code around the same model."},"rescored":null,"problems":["key was 121 chars against a 79-char longest distractor (1.53x) and the only option with three clauses","hint ('count the model calls, and ask what sits between them') selects the only option describing repetition"],"evidence":["knowledge: agent frameworks implement a caller-side loop over an ordinary stateless model endpoint, with no model-side state between iterations","knowledge: the stopping rule belongs to the caller's code, which the explanation states"],"notes":"Trimmed the trailing 'until a stop condition' clause, which the explanation already covers, and replaced the hint."}
{"id":"rm-ai-147","verdict":"amend","fields":{"hint":"A tool returns one exact number. Ask what is still left for the model to get wrong.","explanation":"Without a tool the model produces digits the same way it produces words, by predicting a likely next token, which is unreliable for arithmetic it has not effectively memorized; running the computation in code makes that step exact. It does not make the answer correct overall - the model still picks which computation to request and how to read the result - and tool definitions and results are ordinary input tokens, so a tool call costs more, not less."},"rescored":null,"problems":["explanation said 'the model still decides which computation to run', which gives the model the agency rm-ai-145 was corrected for and implies the model executes it","hint ('arithmetic produced by predicting the next digit is a guess') states the key's mechanism"],"evidence":["https://platform.claude.com/docs/en/api/messages - POST /v1/messages/count_tokens accepts `tools`, so tool definitions count as input tokens","knowledge: token-by-token generation approximates arithmetic the model has not memorized, so delegating the step to code is what makes it exact"],"notes":"Changed 'decides which computation to run' to 'picks which computation to request and how to read the result', keeping the batch consistent on who executes tools."}
{"id":"rm-ai-148","verdict":"amend","fields":{"hint":"The HTTP response has already been delivered. Ask what that rules out."},"rescored":null,"problems":["hint ('the request came back to you; who is holding the turn now') states the key: your application runs the tool"],"evidence":["https://platform.claude.com/docs/en/api/messages - tool definition = name, description, input_schema, sent in the request; \"return the following back to the model in a subsequent `user` message\" as tool_result","knowledge: each iteration resends the whole conversation because the endpoint is stateless, which is why long tool loops get expensive"],"notes":"Verified the round trip against the Messages API reference, including that the result goes back as a tool_result block in a subsequent user message."}
{"id":"rm-ai-149","verdict":"amend","fields":{"options":["Translating a fixed set of release notes into three languages","Finding which dependency introduced a regression, then reading that diff","Classifying ten thousand support tickets into a fixed topic list","Rewriting a page of documentation in a friendlier tone"],"correctAnswer":1,"hint":"Ask whether the size of the job, or the shape of it, is what an agent loop is for."},"rescored":null,"problems":["key was 91 chars against a 49-char longest distractor (1.86x), the worst length tell in the batch","hint ('whether step two depends on what step one finds') states the exact criterion the key embodies"],"evidence":["knowledge: agent loops are warranted by step dependency, while independent work parallelizes as separate calls with no loop","knowledge: the explanation's contrast between one call, a loop and a batch still matches the rebalanced option set"],"notes":"Evened the four workloads to 54-72 chars so the key is no longer identifiable by length alone."}
{"id":"rm-ai-150","verdict":"amend","fields":{"hint":"Ask what the provider stores about you between requests, and what it does not."},"rescored":null,"problems":["hint ('the call is stateless; where the tool list has to be for the model to see it') points straight at the key"],"evidence":["https://platform.claude.com/docs/en/api/messages - tool definition = name, description, input_schema, sent in the request; \"return the following back to the model in a subsequent `user` message\" as tool_result","https://platform.claude.com/docs/en/api/messages - POST /v1/messages/count_tokens accepts `tools`, so tool definitions count as input tokens"],"notes":"Confirmed from the Messages reference that a tool definition is name, description and input_schema sent in the request, and from count_tokens that tools are billed as input; the 'registered with the provider' distractor is defensible only under 'typically', which the stem says."}
{"id":"rm-ai-151","verdict":"amend","fields":{"hint":"Ask what an agent can do that a single model call cannot."},"rescored":null,"problems":["hint ('the loop can act before anyone reads the answer') is the argument that defeats the strongest distractor and points at the key"],"evidence":["knowledge: agent safeguards are permission scoping, confirmation before irreversible actions, and step and spend limits; output review is post hoc and cannot undo a completed action"],"notes":"The 'reviewing the output is enough' distractor is the belief that makes agent incidents possible and is worth keeping."}
{"id":"rm-ai-152","verdict":"amend","fields":{"hint":"A schema is checked before your code sees the call. Ask what it could not possibly check."},"rescored":null,"problems":["hint ('a schema constrains the shape of the arguments; what that does and does not buy you') hands over the shape-versus-meaning lesson the item tests"],"evidence":["https://platform.claude.com/docs/en/build-with-claude/structured-outputs - \"Structured outputs constrain Claude's responses to follow a specific schema\"; output_config.format + strict:true; \"beta headers are no longer required\"","knowledge: tool arguments are generated tokens and are billed as such, so the 'not billed as tokens' distractor is wrong"],"notes":"Verified that strict tool use guarantees schema validation on tool names and inputs, which is structure rather than semantics - exactly the distinction the explanation draws."}
{"id":"rm-ai-153","verdict":"amend","fields":{"hint":"Ask what 'open source' would additionally require, and whether the term promises it."},"rescored":null,"problems":["hint ('the term names exactly one released artifact; decide which one') eliminates every option but the key, since only the key names a single artifact"],"evidence":["knowledge: open-weight releases publish parameters while usually withholding training data and code, and several such licenses impose use restrictions that are not OSI-approved","knowledge: downloading weights shifts cost from tokens to GPUs rather than making inference free"],"notes":"The new hint carries the open-weight versus open-source contrast the stem's 'often loosely called' promises, without naming the artifact."}
{"id":"rm-ai-156","verdict":"amend","fields":{"hint":"A benchmark produces one number per model. Ask how much of what you care about could fit inside it."},"rescored":null,"problems":["hint ('what a leaderboard number actually holds constant between two models') is the key: shared, repeatable tasks so capabilities can be compared"],"evidence":["knowledge: benchmarks standardize task and scoring for cross-model comparison and certify nothing about safety, cost or data licensing"],"notes":"Pairs correctly with rm-ai-158, which teaches why the same number need not transfer."}
{"id":"rm-ai-158","verdict":"amend","fields":{"hint":"The problem is not whether the score can be computed. Ask whether it still means what it did."},"rescored":null,"problems":["hint ('what happens to a public test set after it has been on the web for a year') states contamination, which is the key's first half"],"evidence":["knowledge: benchmark contamination through web-scraped training data and optimization against a published metric are the documented reasons leaderboard scores fail to transfer","knowledge: many benchmarks are open and reproducible and cover far more than coding, which is what makes the two remaining distractors wrong"],"notes":"New hint separates 'can the score be computed' from 'does the score still mean anything', which is the item's actual distinction."}
{"id":"rm-ai-159","verdict":"amend","fields":{"hint":"An idle GPU still bills. Ask which of these claims that fact contradicts."},"rescored":null,"problems":["hint ('what you gain, and what lands on your on-call rotation') signals that the answer is the only two-sided option"],"evidence":["knowledge: self-hosting shifts serving, scaling and upgrade work to the operator; hosted fine-tuning is available from major providers, so customization is not the deciding factor","knowledge: per-token economics favor self-hosting only at high sustained utilization"],"notes":"New hint kills the 'always cheaper' distractor and teaches the utilization point without revealing the trade-off shape of the key."}
