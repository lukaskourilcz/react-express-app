{"id":"rm-css-1","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["knowledge: .card p = 0-1-1 (class .card 0-1-0 + type p 0-0-1); p = 0-0-1; class column decides","https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_cascade/Cascade — 'Order of appearance: ... the last declaration in the style order is applied' comes after specificity"],"notes":"Hint now asks for id-class-type counts and the explanation covers the not-dropped distractor; all four checks hold."}
{"id":"rm-css-2","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_cascade/Cascade — 'Order of appearance: In the origin with precedence, if there are competing values ... of equal specificity and scoping proximity, the last declaration in the style order is applied'","https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_cascade/Specificity — 'If scope proximity is also the same for both selectors, source order then comes into play. When all else is equal, the last selector wins.'"],"notes":"Tie conditions are now stated in the question and both new distractors (selector length, match count) are genuinely outside the cascade."}
{"id":"rm-css-3","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/important — 'While !important is not part of determining specificity, it is related. Important declarations override all other declarations from the same origin and cascade layer.'","https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_cascade/Cascade — precedence list places author !important above every normal declaration (user-agent, user and author normal)"],"notes":"Key is true for author !important against all normal declarations; options are now comparable in length."}
{"id":"rm-css-4","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_cascade/Inheritance — 'A typical example of an inherited property is the color property'; 'A typical example of a non-inherited property is the border property'","knowledge: padding and display are both defined as non-inherited; inherit/initial can force either behaviour on any property"],"notes":"Hint reframes the rule as a subtree question without naming an option; explanation's inherit/initial addition is correct."}
{"id":"rm-css-5","verdict":"amend","fields":{"options":["Faster matching for the selectors inside the layer","A whole group of rules that loses to a later group whatever their selectors","Styles that are scoped to a single component subtree","Styles that apply only while a condition holds"]},"rescored":null,"problems":["key option says a layered group 'always loses to a later group', which the item's own explanation contradicts (!important reverses layer order)"],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/@layer — 'The first declared layer gets the lowest priority and the last declared layer gets the highest priority. However, the priority is reversed when the !important flag is used.'","https://developer.mozilla.org/en-US/docs/Web/CSS/@layer — 'once the layer order has been established, specificity and order of appearance are ignored'"],"notes":"Dropping 'always' for 'whatever their selectors' keeps the key unique and matches the question's 'that specificity alone cannot'."}
{"id":"rm-css-6","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/:is — ':is() counts towards the specificity of the overall selector (it takes the specificity of its most specific argument), whereas :where() has a specificity value of 0'","knowledge: :is(h2, #hero) = 1-0-0, plus .badge 0-1-0 = 1-1-0; first-argument reading 0-1-1; sum reading 1-1-1; :where reading 0-1-0 — all four options give distinct values"],"notes":"The swapped example (id placed second) makes first-argument and most-specific differ, which the original could not."}
{"id":"rm-css-7","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/:has — 'represents an element if any of the relative selectors that are passed as an argument match at least one element when anchored against this element'","https://developer.mozilla.org/en-US/docs/Web/CSS/:has — Baseline widely available: 'It's been available across browsers since December 2023.'"],"notes":"Support sentence matches MDN's own dating, and the descendant-vs-child distractor is a strict superset that the selector does not match."}
{"id":"rm-css-8","verdict":"amend","fields":{"question":"A style applies to an element in one page and not another. The markup and the selectors are identical on both pages. What is worth checking first?"},"rescored":null,"problems":["multiple-defensible: with only the selectors declared identical, 'the element has a different tag name on each page' is a real cause of a non-matching rule"],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_cascade/Cascade — after specificity the criteria left are scoping proximity and order of appearance; layered styles rank below unlayered styles in the same origin","knowledge: a type selector stops matching if the element's tag name changes, which is a matching failure rather than a cascade outcome"],"notes":"Fixing the markup in the question closes the tag-name reading and leaves stylesheet order/layer as the only cascade explanation."}
{"id":"rm-css-9","verdict":"amend","fields":{"question":"An element has `width: 300px`, `padding: 20px` and `border: 5px solid`, under `box-sizing: content-box`. How wide is its border box?"},"rescored":null,"problems":["premise is unsound as written: `border: 5px` leaves border-style at none, so the used border width is 0 and the border box is 340px, which is not among the options"],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/border — 'border-style ... Defaults to none if absent'; 'any omitted sub-values will be set to their initial value'","https://www.w3.org/TR/css-backgrounds-3/ — border-width computed value: 'absolute length, snapped as a border width; zero if the border style is none or hidden'"],"notes":"Adding `solid` makes the stated 5px borders real; the arithmetic 300 + 2*20 + 2*5 = 350 and the 345/325 distractors are then exactly right."}
{"id":"rm-css-10","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/box-sizing — border-box: 'The width and height properties include the content, padding, and border, but do not include the margin'","knowledge: box-sizing does not affect margin collapsing, border painting position, or display"],"notes":"All three distractors are named and refuted in the explanation and the hint stays on the measurement question."}
{"id":"rm-css-11","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_box_model/Mastering_margin_collapsing — 'The top and bottom margins of blocks are sometimes combined (collapsed) into a single margin whose size is the largest of the individual margins'","https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_box_model/Mastering_margin_collapsing — 'Margins don't collapse in a container with display set to flex or grid'"],"notes":"max(20,30) = 30 is the only defensible option and the flex/grid exception in the explanation matches MDN."}
{"id":"rm-css-12","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/min-height — 'The percentage is calculated with respect to the height of the generated box's containing block. If the height of the containing block is not specified explicitly (i.e., it depends on content height), and this element is not absolutely positioned, the percentage value is treated as 0'"],"notes":"The old display distractor is gone and the flex/grid remark now says stretching rather than resolving, which is the accurate mechanism."}
{"id":"rm-css-13","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/overflow-wrap — anywhere: 'an otherwise unbreakable string of characters ... may be broken at any point if there are no otherwise-acceptable break points in the line'","https://developer.mozilla.org/en-US/docs/Web/CSS/text-overflow — 'The text-overflow property doesn't force an overflow to occur ... you have to set other CSS properties: overflow and white-space'"],"notes":"The added `break-word` alternative and the nowrap sentence are both accurate; the hint sorts the properties by function without naming one."}
{"id":"rm-css-14","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["knowledge: a document scrolls horizontally when some box's inline-end edge exceeds the viewport width; overflow-x: hidden on body clips that box without removing the cause and can put content out of reach","knowledge: outline does not participate in layout, so `* { outline: 1px solid red }` reveals box edges without shifting them"],"notes":"Judgement item, but the three alternatives are guesses that never locate the offending element, so the key stands alone."}
{"id":"rm-css-15","verdict":"amend","fields":{"hint":"Ask what each unit measures against, then follow a list nested three levels deep and watch which unit's computed size changes at each level."},"rescored":null,"problems":["hint eliminates every distractor: 'Both are relative' rules out the absolute option and 'which element's font size each one reads' rules out both identical and viewport"],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/length — em: 'If used on the font-size property itself, it represents the inherited font-size of the element'; rem: 'Represents the font-size of the root element'","node: 0.9**3 = 0.7290000000000001, so three levels of 0.9em give 0.729 of the start"],"notes":"The replacement hint still points at compounding but leaves the four readings open."}
{"id":"rm-css-16","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_flexible_box_layout/Controlling_ratios_of_flex_items_along_the_main_axis — 'Percentage values are relative to the flex container's inner main size. If width: 50% were set, the flex-basis for this item would be half of the container's content-box width'","https://developer.mozilla.org/en-US/docs/Web/CSS/flex-wrap — nowrap is the initial value, so the two items stay on one line and shrink or overflow"],"notes":"The rebuilt question asks a documented resolution fact instead of the original's unverifiable overflow claim, and the shrink/overflow sentence correctly hinges on flex-shrink's default of 1."}
{"id":"rm-css-17","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_flexible_box_layout/Basic_concepts_of_flexbox — flex-direction: row puts the main axis along the inline direction; justify-content aligns along the main axis, align-items along the cross axis","knowledge: no CSS property aligns on both axes at once, and free space never selects the axis"],"notes":"The horizontal-writing-mode qualifier sits in the explanation rather than the question, but no other option claims the main axis so the key is still unique."}
{"id":"rm-css-18","verdict":"amend","fields":{"hint":"A single unitless number sets `flex-grow`. Recall what the shorthand chooses for the other two longhands when you leave them out."},"rescored":null,"problems":["hint eliminates every distractor: 'fills in two values you did not write' rules out the one-longhand and basis-only options, and 'items end up equal even when their content is not' rules out the flex-basis: auto option"],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/flex — 'One value, unitless number: flex-grow ... flex-basis is then equal to 0%'; 'flex: auto; /* 1 1 auto */'; 'flex: initial; /* 0 1 auto */'","knowledge: the key and the flex-basis: auto distractor differ only in shrink and basis, which is the discrimination the old hint gave away"],"notes":"New hint keeps the three-longhand cue but leaves the basis 0% versus auto choice to the learner."}
{"id":"rm-css-19","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/flex-wrap — Initial value nowrap: 'The flex items are laid out in a single line which may cause the flex container to overflow'"],"notes":"Both defaults now carry the same option shape, and the explanation refutes justify-content, align-items and the missing width."}
{"id":"rm-css-20","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/min-width — auto: 'For flex items and grid items, the minimum width value is either the specified suggested size ... otherwise, the min-content size is used. If the flex or grid item is a scroll container ... the automatic minimum size is 0'"],"notes":"overflow: hidden would work on the item but the distractor puts it on the container, which the explanation now says explicitly."}
{"id":"rm-css-21","verdict":"amend","fields":{"options":["`align-items` spaces the lines; `align-content` aligns items within their line","`align-items` aligns items within their line; `align-content` spaces the lines","`align-content` only works inside grid containers","`align-items` only works when `flex-wrap` is `nowrap`"]},"rescored":null,"problems":["the question asks for the difference but only the key describes one, so the hint's framing ('two things to align: the items and the lines') hands over the answer by elimination"],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/align-content — 'sets the distribution of space between and around content items along a flexbox's cross axis, or a grid or block-level element's block axis'; 'This property has no effect on single line flex containers'","knowledge: align-items aligns each item inside its own line, so the reversed mapping is false and makes a legitimate strongest distractor"],"notes":"Replacing the 'two names for one behaviour' throwaway with the reversed mapping makes the item test the mapping the hint asks the learner to work out."}
{"id":"rm-css-22","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/gap — 'There is no gutter before the first track or after the last track', and gap applies to multi-column, flex and grid containers","knowledge: justify-content: space-between distributes leftover space, so the spacing varies with container width instead of being fixed"],"notes":"Hint turns on the wrap case, which is exactly where the margin hack and the spacer approach fail."}
{"id":"rm-css-23","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/flex-shrink — initial value 1; negative free space 'is distributed between the line's flex items that have a flex-shrink value greater than 0'"],"notes":"The explanation now refutes align-items, flex-wrap and the container min-width, and the hint names a mechanism rather than an option."}
{"id":"rm-css-24","verdict":"amend","fields":{"hint":"Both methods align items and both respond to width, so neither settles it. Ask what each one needs you to define before it lays anything out."},"rescored":null,"problems":["hint paraphrases the key: it names both of the key's clauses (how many dimensions, content versus a structure you define) and no distractor mentions either"],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_grid_layout/Relationship_of_grid_layout_with_other_layout_methods — 'Do we only need to control the layout by row or column? If yes, use flexbox'; 'Flexbox works from the content out'; 'Grid works from the layout in'","https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_grid_layout/Relationship_of_grid_layout_with_other_layout_methods — 'if you are setting a width on a flex item to make it line up with other items in a row above, a grid is likely a better choice'"],"notes":"New hint pushes back on the two throwaway distractors instead of restating the key's wording."}
{"id":"rm-css-25","verdict":"amend","fields":{"explanation":"`auto-fit` fits as many tracks as the width allows, `minmax(200px, 1fr)` makes each track at least 200px and lets it grow, and `1fr` shares the leftover space, so the column count follows the container width with no media query. It responds to the container rather than the viewport, which is usually the better answer. The width decides how many tracks fit rather than the item count, and nothing here is fixed at 200px; `auto-fit` only differs from `auto-fill` in collapsing tracks that end up empty."},"rescored":null,"problems":["explanation claims 'the number of items never sets the count', which is false for auto-fit: after placement any empty repeated tracks are collapsed, so the rendered column count follows the items when they are fewer than the tracks that fit"],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/repeat — auto-fit: 'Behaves as auto-fill, except that after placing grid items, any empty repeated tracks are collapsed'; auto-fill: 'Resolves to the largest number of repetitions that does not cause overflow'","https://developer.mozilla.org/en-US/docs/Web/CSS/flex_value — 'The fr unit represents a fraction of the leftover space in the grid container'"],"notes":"Key and hint are sound; only the sentence that overstates the item count needed correcting, and it now sets up rm-css-26."}
{"id":"rm-css-26","verdict":"amend","fields":{"question":"A grid uses `repeat(auto-fit, minmax(200px, 1fr))`, or the same with `auto-fill`. What is the difference when there are fewer items than tracks?"},"rescored":null,"problems":["key says the collapse makes items stretch, which holds only for a flexible track function: with `repeat(auto-fit, 250px)` the empty tracks still collapse but the items stay 250px"],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/repeat — 'The auto-fit value behaves the same as auto-fill, except that after placing the grid items any empty repeated tracks are collapsed. An empty track is one with no in-flow grid items placed into or spanning across it.'","https://developer.mozilla.org/en-US/docs/Web/CSS/flex_value — fr distributes leftover space, which is what lets the surviving tracks take the collapsed tracks' width"],"notes":"Naming the track function in the question makes the stretch claim in the key and the explanation true as written."}
{"id":"rm-css-27","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["https://www.w3.org/TR/css-grid-2/ — 'A flexible length or <flex> is a dimension with the fr unit, which represents a fraction of the leftover space in the grid container'","https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_grid_layout/Basic_concepts_of_grid_layout — 'Any space used by gaps will be accounted for before space is assigned to the flexible length fr tracks'"],"notes":"Hint sets a concrete test case (`200px 1fr`) rather than restating the rule, and the explanation refutes all three distractors."}
{"id":"rm-css-28","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["https://www.w3.org/TR/css-grid-2/ — <flex> 'When appearing outside a minmax() notation, implies an automatic minimum (i.e. minmax(auto, <flex>))'","https://developer.mozilla.org/en-US/docs/Web/CSS/min-width — for flex and grid items auto resolves to the specified or transferred size, 'otherwise, the min-content size is used'"],"notes":"Mechanism is spec-accurate, the unsupported superlative is gone, and it complements rather than duplicates the flex version in rm-css-20."}
{"id":"rm-css-29","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["https://developer.mozilla.org/en-US/docs/Learn_web_development/Core/CSS_layout/Responsive_Design — breakpoints are set where the content stops working, e.g. 'if a box becomes squashed with two words on each line as it narrows you can set a breakpoint'"],"notes":"Guidance item rather than a spec fact; it passes both gates at the reviewer's scores and the hint leaves the arbitrary-number option open."}
{"id":"rm-css-30","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/@container — Baseline widely available: 'It's been available across browsers since February 2023'","https://developer.mozilla.org/en-US/docs/Web/CSS/container-type — values inline-size, size and scroll-state; the default 'normal' is not a size query container"],"notes":"Checked the scroll distractor against scroll-state container queries: those report stuck/snapped/scrollable state, not scroll position, so it stays false."}
{"id":"rm-css-31","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["https://developer.mozilla.org/en-US/docs/Learn_web_development/Core/CSS_layout/Responsive_Design — mobile first design; the worked example uses the range syntax `@media screen and (width >= 600px)`","knowledge: a media query does not change the specificity of the declarations inside it, and min-width and max-width queries are evaluated the same way"],"notes":"Stating the mobile-first premise turns the old opinion into one defensible answer; the key is longer than the distractors but is a different kind of claim rather than a more detailed one."}
{"id":"rm-css-32","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/float — 'places an element on the left or right side of its container, allowing text and inline elements to wrap around it. The element is removed from the normal flow of the page, though still remaining a part of the flow'"],"notes":"Text wrap around a figure is the one job float still does that Flexbox and Grid do not, and the explanation names the modern replacement for each distractor."}
{"id":"rm-css-33","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/position — absolute: 'The element is positioned relative to its closest positioned ancestor (if any) or to the initial containing block'","https://developer.mozilla.org/en-US/docs/Web/CSS/position — fixed positioning containing block: an ancestor with transform, perspective or filter 'causes that ancestor to take the place of the elements containing block'"],"notes":"Padding-box detail and the transform caveat are both accurate, and the shared 'in every case' shape removes the length clue from the key."}
{"id":"rm-css-34","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/position — sticky: 'At least one inset property ... needs to be set to a non-auto value ... If both inset properties for an axis are set to auto, on that axis the sticky value will behave as relative'","https://developer.mozilla.org/en-US/docs/Web/CSS/position — a sticky element 'sticks' to its nearest ancestor with a 'scrolling mechanism' (overflow hidden, scroll, auto, or overlay), 'even if that ancestor isn't the nearest actually scrolling ancestor'"],"notes":"The explanation turns the overflow: hidden distractor into the real failure mode rather than leaving it unaddressed."}
{"id":"rm-css-35","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_positioned_layout/Stacking_context — 'the z-index values of its child stacking contexts only have meaning within its parent's stacking context. Stacking contexts are treated atomically as a single unit in the parent stacking context.'","https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_positioned_layout/Stacking_context — creators include opacity less than 1, transform, filter, will-change, and 'Element with a position value fixed or sticky'"],"notes":"Saying the modal is fixed with z-index 9999 in the question closes the old 'z-index needs positioning' reading, and all three new distractors are false as stated."}
{"id":"rm-css-36","verdict":"amend","fields":{"hint":"Being positioned is not enough on its own. Recall the short list of values that create a stacking context and check each of these four against it."},"rescored":null,"problems":["hint gives the answer away: 'paint the element and its descendants as one unit before blending them with the page' describes opacity specifically, so the key can be picked without knowing the stacking-context list the question tests"],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_positioned_layout/Stacking_context — list includes 'Element with an opacity value less than 1', transform, filter, will-change, 'Element with position absolute or relative and z-index value other than auto', flex item with z-index other than auto; overflow and display: flex are absent","knowledge: overflow: hidden establishes a block formatting context and clips, but does not create a stacking context"],"notes":"Replacement hint still rules out the positioned-without-z-index trap, which is the misconception, without pointing at opacity."}
{"id":"rm-css-37","verdict":"amend","fields":{"explanation":"In the ordinary case the containing block is the viewport, which is why a fixed header stays put while the document scrolls. The exception is an ancestor with a `transform`, `filter`, `perspective`, `will-change` naming one of those, or `contain: paint`: it becomes the containing block, and a fixed header that scrolls away inside a transformed section is exactly this. The nearest positioned ancestor is the rule for `absolute` rather than `fixed`, and neither the parent nor the document is a containing block on its own."},"rescored":null,"problems":["closing sentence claims the parent and the document are 'the rules for absolute', which is false: absolute resolves against the nearest positioned ancestor, else the initial containing block, and never against the document"],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/position — fixed: 'positioned relative to its initial containing block, which is the viewport in the case of visual media'","https://developer.mozilla.org/en-US/docs/Web/CSS/position — absolute: 'positioned relative to its closest positioned ancestor (if any) or to the initial containing block'"],"notes":"Key and the transform exception were already right; only the sentence that mislabelled the distractors needed correcting."}
{"id":"rm-css-38","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/overflow — hidden: 'overflowing content is clipped at the element's padding box. There are no scroll bars and the clipped content is not visible'","knowledge: z-index orders painting within a stacking context and cannot lift content out of an ancestor's clip"],"notes":"Hint reframes the question's own word (clipped) rather than pointing at overflow, and the explanation refutes all three distractors."}
{"id":"rm-css-39","verdict":"amend","fields":{"hint":"Name the four longhands `inset` stands for. Then ask what an absolutely positioned box with `auto` width does when both of its horizontal edges are set."},"rescored":null,"problems":["hint gives the answer away: calling `inset` 'a shorthand for four positioning properties' rules out both the padding and the margin distractors, which are the misconceptions the item tests"],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/inset — 'corresponds to the top, right, bottom, and/or left properties'; inset properties 'have no effect on non-positioned elements'","https://www.w3.org/TR/CSS22/visudet.html — 10.3.7: with 'width' auto and 'left'/'right' not auto, solve for 'width'; 10.6.4: with 'height' auto and 'top'/'bottom' not auto, solve for 'height'"],"notes":"Asking the learner to name the longhands keeps the padding and margin readings live while still setting up the stretch behaviour."}
{"id":"rm-css-40","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["https://developer.mozilla.org/en-US/docs/Web/API/Popover_API — Baseline 2025 Newly available: 'Since January 2025, this feature works across the latest devices and browser versions'","https://developer.mozilla.org/en-US/docs/Glossary/Top_layer — a layer that 'sits on top of all other layers displayed in a web document', holding fullscreen elements, modal dialogs and shown popovers"],"notes":"Support sentence matches MDN's own January 2025 date, and z-index, child overflow and padding are all genuinely unable to escape an ancestor's clip."}
{"id":"rm-css-41","verdict":"amend","fields":{"explanation":"A preprocessor variable is substituted at build time and is gone before the browser sees the file. A custom property is live: it participates in the cascade, inherits down the tree, can be redefined on any element and read or set from JavaScript — which is what makes theming by scope possible at all. It does not shrink files, older browsers handle preprocessor output better, and neither kind is type checked at build time; `@property` can constrain a custom property's value, but only in the browser."},"rescored":null,"problems":["explanation claims 'no CSS variable is type checked', which is false: @property defines a custom property's syntax and type checks its value"],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/@property — 'used to explicitly define CSS custom properties, allowing for property type checking and constraining, setting default values'; Baseline newly available since July 2024","https://developer.mozilla.org/en-US/docs/Web/CSS/--* — custom properties are 'scoped to the element(s) they are declared on, and participate in the cascade'; Inherited: yes"],"notes":"The distractor is about build-time checking, so narrowing the claim to build time keeps it false while making the explanation true."}
{"id":"rm-css-42","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/--* — Inherited: yes; a custom property redefined on an element applies to that element's subtree","knowledge: !important, duplicated rules and a second stylesheet all override declaration by declaration, which is what the token indirection exists to avoid"],"notes":"Hint points at where the value must be declared without naming the option, and the explanation now says why each of the three alternatives costs more."}
{"id":"rm-css-44","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["node: rm-css-44 spec(.p-4) = 0-1-0, spec(.mt-2) = 0-1-0, spec(.text-sm) = 0-1-0 — equal, so order decides","https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_cascade/Specificity — a class selector contributes 0-1-0 and at equal specificity 'the last declared selector wins'"],"notes":"The explanation's 'later one in the generated stylesheet wins' is the precise rule; the 'every rule' wording is a fair abstraction of plain utilities even though framework variants like `.hover\\:x:hover` sit at 0-2-0."}
{"id":"rm-css-45","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/:focus-visible — 'Removing focus styles makes keyboard navigation inaccessible for sighted users'; :focus-visible matches when the browser decides a focus indicator should be shown, which is keyboard rather than pointer focus"],"notes":"The explanation names the replacement (:focus-visible) and refutes the layout, handler and validity distractors."}
{"id":"rm-css-46","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["https://www.w3.org/WAI/WCAG22/Understanding/use-of-color.html — SC 1.4.1: 'Color is not used as the only visual means of conveying information, indicating an action, prompting a response, or distinguishing a visual element'"],"notes":"Key is longer than the three throwaway distractors but not more detailed about the mechanism, and the greyscale/read-aloud hint tests the criterion rather than naming it."}
{"id":"rm-css-47","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["https://developer.mozilla.org/en-US/docs/Web/CSS/@media/prefers-reduced-motion — 'minimize the amount of non-essential motion'; 'Animations such as scaling or panning large objects can be vestibular motion triggers'; the worked example replaces a transform: scale() pulse with an opacity dissolve","knowledge: an opacity change does not move anything on screen, so it is not a vestibular trigger"],"notes":"Adding 'without losing the feedback' to the question is what makes 'remove both' indefensible, and tying each effect to transform/opacity makes the distinction testable."}
{"id":"rm-css-48","verdict":"accept","fields":null,"rescored":null,"problems":[],"evidence":["https://www.w3.org/WAI/WCAG22/Understanding/resize-text.html — sufficient technique 'Ensuring that text containers resize when the text resizes'; failure F69 'when resizing visually rendered text up to 200 percent causes the text, image or controls to be clipped, truncated or obscured'; full-page zoom counts as the resizing mechanism","https://www.w3.org/WAI/WCAG22/Understanding/reflow.html — SC 1.4.10 requires presentation without loss of information and without two-dimensional scrolling"],"notes":"Checked the mechanism against WCAG's own framing, which treats browser zoom as text resize, so the explanation's 'the box does not follow' is the documented failure rather than a conflation."}
