# devShark redistribution candidates

Generated by `docs/audit/wip/tools/redistribution-report.py`. These are
items in a retired section whose review judged the underlying objective
worth keeping, and named a live level that does not already teach it.
Nothing here is applied: the rewrite patcher skips a retired section, so
every one of these needs a decision before it reaches a learner.

43 candidates across 3 retired sections.

| Candidate | From | Proposed destination | Objective |
| --- | --- | --- | --- |
| `rm-testing-55` | testing | databases level 11 | Use a per-test transaction that is rolled back to isolate database tests without a full reset. |
| `rm-abbr-72` | abbreviations | databases level 15 | Separate analytical from transactional database workloads (OLAP versus OLTP). |
| `rm-testing-110` | testing | devops level 3 | Place a local pre-commit hook correctly: fast feedback for the author, not an enforcement point. |
| `rm-testing-15` | testing | devops level 3 | Place static analysis in a check strategy: types and lint reject a class of defects before any test executes. |
| `rm-testing-52` | testing | devops level 11 | Point an integration suite at a disposable database created for the run rather than at production or a shared instance. |
| `rm-abbr-58` | abbreviations | general level 2 | Choose UDP when a fresh update matters more than retransmitting a lost one. |
| `rm-abbr-38` | abbreviations | general level 7 | Identify the abstract syntax tree as the parsed, structural representation a tool analyzes instead of raw text. |
| `rm-abbr-92` | abbreviations | general level 15 | Connect images that load without reserved space to Cumulative Layout Shift. |
| `rm-testing-3` | testing | general level 16 | Recognize that tests written against observable behavior, not internals, are what make an implementation swap safe. |
| `rm-testing-56` | testing | general level 16 | Choose the test that catches a provider and a consumer disagreeing about an API response, without deploying both. |
| `rm-testing-80` | testing | general level 16 | State what a snapshot assertion establishes: that output is unchanged since it was recorded, not that the output is right. |
| `rm-testing-83` | testing | general level 16 | Read a coverage report: full line coverage can hide an untaken branch, which is what branch coverage counts. |
| `rm-testing-24` | testing | general level 17 | Scope a unit test to one behavior so a failure names what broke. |
| `rm-testing-18` | testing | general level 18 | Pick the test shape that fits a suite running on every save: fast, deterministic, with controlled dependencies. |
| `rm-testing-4` | testing | general level 19 | Choose which checks belong in an automated per-pull-request gate: deterministic ones with an agreed pass condition. |
| `538` | code-snippets | javascript level 2 | Read negative slice indices, first-match indexOf and split length on one string. |
| `547` | code-snippets | javascript level 4 | Show that flat() defaults to depth 1 while flat(Infinity) flattens every level. |
| `531` | code-snippets | javascript level 9 | Show that a destructuring default fires only for undefined, not for null or other falsy values. |
| `533` | code-snippets | javascript level 10 | Show that object spread copies one level, so a nested object stays shared between original and copy. |
| `550` | code-snippets | javascript level 10 | Separate rest parameters from arguments: rest collects what is left over, arguments counts everything passed. |
| `526` | code-snippets | javascript level 12 | Explain why a var loop counter gives every deferred callback the same final value. |
| `529` | code-snippets | javascript level 13 | Recognize that an inner let declaration shadows an outer binding for the whole block, so the earlier read hits the temporal dead zone. |
| `551` | code-snippets | javascript level 13 | Show that each function's var creates its own binding, so the increment before the declaration touches only the local one. |
| `766` | code-snippets | javascript level 14 | Show that a method detached into a variable loses its receiver, and that strict mode turns that into a TypeError. |
| `763` | code-snippets | javascript level 15 | Show that map passes the index as a second argument, which parseInt reads as a radix. |
| `525` | code-snippets | javascript level 17 | Apply ToPrimitive under +: an empty array becomes "" and a plain object becomes "[object Object]". |
| `539` | code-snippets | javascript level 19 | Filter an object by value and project the keys using Object.entries with destructured pairs. |
| `780` | code-snippets | javascript level 19 | Show that JSON.stringify omits object properties whose values are functions or undefined. |
| `770` | code-snippets | javascript level 20 | Show that ?. short-circuits the whole property chain, not just the step it guards. |
| `546` | code-snippets | javascript level 21 | Show that Promise.allSettled reports a status for every input instead of short-circuiting on the first rejection. |
| `527` | code-snippets | javascript level 22 | Order synchronous statements, a microtask and a timer callback. |
| `761` | code-snippets | javascript level 22 | Show that a chained .then queued during the microtask drain still runs before the pending timer. |
| `773` | code-snippets | javascript level 22 | Show that calling an async function runs its body synchronously up to its first await, before the caller suspends. |
| `548` | code-snippets | javascript level 23 | Show that a Map keyed by an object matches on reference identity, so an equal-looking literal misses. |
| `537` | code-snippets | javascript level 24 | Separate loose equality from relational comparison for null, including the null > 0 / null >= 0 asymmetry. |
| `549` | code-snippets | javascript level 24 | Show that delete on an array clears the element but leaves a hole and the original length. |
| `555` | code-snippets | javascript level 24 | Show that a return inside finally replaces the value the try block was already returning. |
| `774` | code-snippets | javascript level 24 | Show that map skips array holes: the callback never runs for them and the hole survives in the result. |
| `775` | code-snippets | javascript level 24 | Show that a plain object converts numeric subscripts to string keys, so o[1] and o["1"] are one property. |
| `rm-testing-27` | testing | nodejs level 24 | Compare objects and arrays by structure rather than identity, so a separately built expected value still matches. |
| `rm-testing-28` | testing | nodejs level 24 | Assert that a call throws by handing the matcher a function instead of the call's result. |
| `rm-testing-48` | testing | nodejs level 24 | Clear a double's recorded calls between tests so a later assertion does not depend on run order. |
| `rm-testing-67` | testing | nodejs level 24 | Assert that a promise rejects by unwrapping the rejection reason with an awaited matcher, not by inspecting the promise object. |

## Why each one, in the reviewer's words

**`rm-testing-55` → databases level 11.** Worth redistributing to databases, level 11 'Transactions & ACID': the rollback-per-test pattern is a real use of transaction semantics and is distinct from rm-general-143, whose fix is giving each test its own data. The rewrite supplies the missing caveats — self-committing code and in-process state — as the distractors.

**`rm-abbr-72` → databases level 15.** The objective is a real capacity decision rather than a label, and no live item teaches it: databases L15 covers read replicas, pooling, caching, sharding and indexes but never workload separation, so the rewritten item belongs in databases, level 15 (Performance & Scaling). Rewritten, it tests the contention that makes the distinction matter instead of the two expansions.

**`rm-testing-110` → devops level 3.** Worth redistributing to devops, level 3 'CI Basics': the level keys what starts a pipeline and why a clean run matters (rm-devops-18, rm-devops-23) but nothing says why the local checks a team installs are not a substitute for it, and rm-git-118 only defines what a hook is. The skippable-and-untracked pair is the part teams get wrong.

**`rm-testing-15` → devops level 3.** Worth redistributing to devops, level 3 'CI Basics': rm-devops-19 already lists lint and typecheck as pipeline stages but nothing explains why that stage comes before the tests or what it cannot decide. The objective is sound; the distractors and hint are the defects.

**`rm-testing-52` → devops level 11.** Worth redistributing to devops, level 11 'Environments & Config': which environment a writing test suite is allowed to touch is an operational rule with real consequences, and no General item covers it — rm-general-143 is about two tests colliding inside one suite, not about the instance the suite connects to. The rewrite replaces two throwaway options with the shared-staging answer teams actually choose.

**`rm-abbr-58` → general level 2.** Destination: general L2 'Clients & Servers', which teaches clients, servers, reverse proxies and load balancers and mentions the TCP connection in rm-general-12 without ever saying what reliability costs; no active item in the bank mentions UDP. Reformulated with reasoning-bearing options, it teaches the latency-versus-reliability trade that also explains why HTTP/3 moved onto QUIC.

**`rm-abbr-38` → general level 7.** Destination: general L7 'How Code Runs', which already teaches compiler, interpreter, JIT, transpilation and bundling but never says what those tools operate on, and no active item anywhere in the bank mentions a syntax tree. Reformulated around a lint rule, the objective explains why structural tools succeed where find-and-replace over text corrupts code.

**`rm-abbr-92` → general level 15.** Unlike the rest of this section the stem already names a real cause, and preventing layout shift is a concrete change a learner makes, so the objective belongs in general, level 15 (Performance & Optimization), where rm-general-113 names CLS but no item teaches the fix. rm-next-99 teaches the same fix for the Next.js <Image> component only, so the framework-agnostic version is not a duplicate.

**`rm-testing-3` → general level 16.** Worth redistributing to general, level 16 'What Tests Can Prove': the behavior-versus-internals distinction is the prerequisite for rm-general-131 and rm-general-149 and is not itself taught there. Only the hint, one dead distractor and the explanation's silence on the strongest wrong answer need repair.

**`rm-testing-56` → general level 16.** Worth redistributing to general, level 16 'What Tests Can Prove': it is a which-test-proves-what question in the same shape as rm-general-123 and rm-general-125, and no General item covers the boundary between two deployed services. Only the filler hint and the distractor-blind explanation need repair; the scenario and the four options are already strong.

**`rm-testing-80` → general level 16.** Worth redistributing to general, level 16 'What Tests Can Prove': a snapshot is the clearest case of a green check that establishes less than it appears to, and no item in that level covers it — rm-general-127 is about coverage without assertions, rm-general-121 about a green pipeline. Snapshots are current in Jest, Vitest and Node's own runner, so learners meet them in real repositories.

**`rm-testing-83` → general level 16.** Worth redistributing to general, level 16 'What Tests Can Prove': it is a second, distinct way a coverage number overstates what ran, and rm-general-127 keys the missing assertion rather than the untaken branch, so the two do not collide. Learners read these reports in CI, and the untaken-else case is the one that misleads them.

**`rm-testing-24` → general level 17.** Worth redistributing to general, level 17 'Read a Meaningful Test': the three distractors are genuine misconceptions (coverage maximizing, asserting internals, wrong scope) and no General item yet asks how wide one test should be — rm-general-136 judges failure messages, not test scope. Only the filler hint and the thin explanation need repair.

**`rm-testing-18` → general level 18.** Worth redistributing to general, level 18 'Make Checks Reliable': the distractors are the three real sources of unreliable feedback and the seed point is not covered by rm-general-138 to rm-general-143. Only the hint and the explanation's silence on the seed need repair.

**`rm-testing-4` → general level 19.** Worth redistributing to general, level 19 'Verify a Change': deciding what a per-change gate should contain is a review judgment and is not covered by rm-general-148, which is about where effort goes rather than what automates. The distractors are already good; only the hint and explanation fall short.

**`538` → javascript level 2.** Worth moving into javascript L2 (Strings) as a predict snippet, where negative slice indices are not yet taught, though two of its three lines overlap rm-js-11 and rm-js-14. The hint points at the wrong property of strings.

**`547` → javascript level 4.** Worth moving into javascript L4 (Arrays: Basics) as a predict snippet: no live item anywhere uses flat, so the default-depth rule is new, even though the third line repeats rm-js-32. The hint points at a method the code never calls.

**`531` → javascript level 9.** Belongs in javascript L9 (Destructuring) as a predict snippet: rm-js-68 covers a missing property but no live item separates undefined from null or 0. It also subsumes batch twin 768, which should be retired.

**`533` → javascript level 10.** Belongs in javascript L10 (Spread & Rest) as a predict snippet: rm-js-78 and rm-js-148 only show that the top level is independent, so the shared nested object is new material and a real bug class. Only the hint needs work.

**`550` → javascript level 10.** Worth moving into javascript L10 (Spread & Rest) as a predict snippet: rm-js-75 covers rest length, but nothing contrasts rest with the array-like arguments object that still turns up in older code. The hint names a feature the snippet does not use.

**`526` → javascript level 12.** Belongs in javascript L12 (Closures) as a predict snippet: L12 has the let variant (rm-js-92) but no var-with-setTimeout item. Keep this copy over the thinner duplicate 756.

**`529` → javascript level 13.** Belongs in javascript L13 (Hoisting & let/const) as a predict snippet: rm-js-98 covers a bare TDZ read and rm-js-101 covers var shadowing, but nothing combines shadowing with the TDZ. Only the hint and the stem verb need work.

**`551` → javascript level 13.** Belongs in javascript L13 (Hoisting & let/const) as a predict snippet: rm-js-101 stops at reading a hoisted var, while this traces three nested scopes and the increment on the hoisted binding. The English fields need no change; only the topic and level declaration and the stale Czech hint do.

**`766` → javascript level 14.** Belongs in javascript L14 (this & Context) as a predict snippet: rm-js-105 to rm-js-112 cover call, bind and arrows but never the detached method, which is the bug behind passing `obj.method` to a callback. The stem already states the strict-mode assumption.

**`763` → javascript level 15.** Belongs in javascript L15 (Callbacks & HOFs) as a predict snippet: rm-js-114 and rm-js-117 pass String and Number to map, which happen to be safe, so nothing shows what the extra arguments do to a multi-parameter callback. The hint is already question-specific; only option 3 needs reshaping.

**`525` → javascript level 17.** Worth moving into javascript L17 (Type Coercion) as a predict snippet: rm-js-132 there covers only [] + [], so the object half is new. The hint and the malformed fourth option are the only local defects.

**`539` → javascript level 19.** Worth moving into javascript L19 (JSON & Objects) as a predict snippet: the entries-filter-map pattern for filtering an object by value is not taught there, though rm-js-194 in L25 covers the simpler entries-then-map step. The hint is filler.

**`780` → javascript level 19.** Worth moving into javascript L19 (JSON & Objects) as a predict snippet: rm-js-150 covers only the undefined case, and the dropped function value is the half that bites when an object with methods is serialized. The hint must stop announcing the answer.

**`770` → javascript level 20.** Belongs in javascript L20 (Optional & Nullish) as a predict snippet: rm-js-153 to rm-js-160 all guard a single step, so the whole-chain short-circuit and the undefined-not-null result are new. Only the hint and the missing distractor note need work.

**`546` → javascript level 21.** Belongs in javascript L21 (Promises) as a predict snippet: L21 covers all, race, then and catch but never allSettled, and javascript is the only topic in all three learner plans (nodejs L14's rm-node-111 states the contrast but reaches backend learners only). The hint and the missing distractor note are local defects.

**`527` → javascript level 22.** The strongest of the four event-loop items here and the only one with a question-specific hint; it belongs in javascript L22 (Async / Await) as a predict snippet, where rm-js-171 and rm-js-174 stop short of comparing microtasks with timers. Duplicate 760 should go instead.

**`761` → javascript level 22.** Belongs in javascript L22 (Async / Await) as a predict snippet: it adds the part 527 does not show, that microtasks queued during the drain also run before the timer, which is the basis of microtask starvation. The hint is the one local defect.

**`773` → javascript level 22.** Belongs in javascript L22 (Async / Await) as a predict snippet: it is the only item in the batch showing that an async callee's body runs synchronously, which rm-js-174 and the other copies (536, 764) never reach. The hint is the one local defect.

**`548` → javascript level 23.** Belongs in javascript L23 (Sets & Maps) as a predict snippet: rm-js-179 to rm-js-184 all use string keys, so reference-identity keys are new and explain a real class of cache and memo bugs. Only the hint needs replacing.

**`537` → javascript level 24.** Belongs in javascript L24 (Edge Cases & Gotchas) as a predict snippet: L3 has only `null == undefined` (rm-js-22) and nothing anywhere covers the relational asymmetry that silently makes null behave like 0. Only the hint is a local defect.

**`549` → javascript level 24.** Belongs in javascript L24 (Edge Cases & Gotchas) as a predict snippet: rm-js-190 shrinks an array through length but nothing covers delete leaving a hole, and the explanation already points at splice as the fix. Only the hint is a local defect.

**`555` → javascript level 24.** Worth moving into javascript L24 (Edge Cases & Gotchas) as a predict snippet: no level teaches error-handling control flow, but this is a genuine gotcha that silently swallows both returns and exceptions, which fits that level's brief. The hint and the missing distractor note are local defects.

**`774` → javascript level 24.** Belongs in javascript L24 (Edge Cases & Gotchas) as a predict snippet: nothing live covers holes, and this is the rule behind the common surprise that `new Array(3).map(...)` does nothing. Its prose options are the best written in the batch; only the hint spoils it.

**`775` → javascript level 24.** Belongs in javascript L24 (Edge Cases & Gotchas) as a predict snippet: no live item shows key coercion, which explains why Object.keys hands back strings and why numeric ids compare oddly. The hint is the one local defect.

**`rm-testing-27` → nodejs level 24.** Worth redistributing to nodejs, level 24 'Testing & Debugging': the identity-versus-structure trap is the one assertion mistake beginners hit on their first object test, and it is the same lesson as assert.strictEqual versus assert.deepStrictEqual in node:test. Naming the runner and making the stem concrete removes the term-matching shortcut.

**`rm-testing-28` → nodejs level 24.** Worth redistributing to nodejs, level 24 'Testing & Debugging': the wrapper requirement is a real first-week mistake in Jest, Vitest and node:test alike, and the current options waste it on throwaway matchers. Rewritten, every distractor encodes the same live misconception at a different angle.

**`rm-testing-48` → nodejs level 24.** Worth redistributing to nodejs, level 24 'Testing & Debugging': leaked call history is a concrete order-dependence cause that rm-general-138 only diagnoses in the abstract, and the clear-versus-restore distinction is runner API knowledge that belongs with the runner. The rewrite turns the instruction into the symptom and gives every distractor a real misconception.

**`rm-testing-67` → nodejs level 24.** Worth redistributing to nodejs, level 24 'Testing & Debugging', beside rm-node-186 and rm-node-191 and the rm-testing-27 and rm-testing-28 rewrites already headed there: rejection assertions are the async half of that lesson and General 18 covers only the missing await (rm-general-137), not which assertion reports a rejection. Rewritten, the silent-pass catch replaces the two unrunnable options.

