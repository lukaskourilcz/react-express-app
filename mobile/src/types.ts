// Domain types are shared with the web client — see ../../shared/types.ts.
export type {
  CategoryType,
  DifficultyMode,
  Question,
  QuizResultItem,
  QuizResult,
  LeaderboardGlobalEntry,
} from '@shared/types';
