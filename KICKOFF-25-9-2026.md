# KICKOFF 2026-09-25 — devShark as marketingShark's only brand

Hub: `KICKOFF-25-9-2026.md` in `lukaskourilcz/quorum`. BoardlessAI's marketingShark now
markets devShark only; StudyShark stays retired; the repository is being renamed to
`devShark`. This file is the order for this repository. Each issue body is self-contained.

## What the research found

| Fact | Where |
| --- | --- |
| BoardlessAI consumes exactly one surface here: `lib/webdev-bank.ts`, as a pinned snapshot | `README.md:173-194`, quorum `orchestrator/src/ventures/marketingshark/react-express-app-adapter.ts` |
| marketingShark has never drafted a devShark package; the fix is upstream | quorum #556 |
| No workflow, `vercel.json` or runtime code depends on the repository name; about 40 documentation URLs do | quality.yml, `docs/*.md`, `NEEDED.md` |
| The quality workflow runs on push to `main` and on pull requests only; nothing to cancel | `.github/workflows/quality.yml` |

## Order of work

| Step | Issue | When |
| --- | --- | --- |
| R2 | #218 Pin the `lib/webdev-bank.ts` contract with a test and document it | now |
| R1 | #217 Repository rename to devShark: documentation links, Vercel and OwnDashboard notes | **after the owner renames the repository** |

## Rules

1. `npm run typecheck:api`, `npm run test:launch`, `npm run build`, `npm run check:responsive`,
   both dependency audits and `git diff --check` before any push; report only executed checks.
2. devShark ships English only, no AI feature, promotes no other product; nothing here posts.
3. Compatibility storage keys, package names and history stay.
4. Commit in small steps; merge to `main` at the end of the session per `CLAUDE.md`.

## Owner actions

- [ ] Rename `lukaskourilcz/react-express-app` to `lukaskourilcz/devShark` (first of the three
      renames; the order is in the quorum hub) and confirm the Vercel project still deploys.
