---
description: Research providers, generate, select, optimize, integrate, and document one production Shark-family media asset with a verified cheap or free service.
argument-hint: <asset-and-placement> [--commit]
allowed-tools: Read, Grep, Glob, Bash, Edit, Write, WebSearch, WebFetch
---

Produce `$ARGUMENTS` as a real StudyShark/devShark asset, not an isolated concept.

1. Inspect Git state, the target route/metadata/component, current media, product and subject registries, EN/CS copy, loading behavior, responsive crop, and existing reusable media hooks.
2. Read `docs/design/generated-media-opportunity-audit.md`, `docs/design/generated-media-art-direction.md`, `docs/design/generated-media-manifest.md`, `DESIGN_RULES.md`, and `.claude/skills/generated-media-production/SKILL.md`.
3. Reject unnecessary, harmful, factual, fake-UI, or filler generation. Write a production brief for an accepted opportunity: placement, user problem, dimensions/aspects, safe regions, themes, mobile behavior, alt classification, fallback, and performance budget.
4. Research at least three current cheap/free generators from official pricing, license, privacy, and model documentation and record a dated comparison before recommending or using one. Never infer capabilities, sign up, purchase, upload sensitive material, or alter billing without explicit authorization.
5. Generate three genuinely different directions using the owner-authorized workflow, inspect them, record rejection reasons, refine the strongest, and create the required responsive variants.
6. Download and optimize selected outputs only. Overlay real copy, code, labels, and authentic UI deterministically. Keep expressive media outside core learning/admin bundles and freeze motion for reduced motion.
7. Integrate into the real product, update `docs/design/generated-media-manifest.md` with exact provenance/regeneration data, and reconcile any changed guidance.
8. Build and render the placement across the relevant StudyShark/devShark, EN/CS, light/dark, reduced-motion, and 360–1440 width matrix. Verify crop, contrast, intrinsic sizing, bytes, lazy/preload behavior, alt text, fallback, and console state.
9. Run `npm run typecheck:api`, `npm run test:launch`, `npm run build`, relevant responsive checks, both production dependency audits, and `git diff --check`.
10. If `--commit` is present and generation plus validation succeeded, stage only the coherent selected implementation and create an imperative commit. If all verified providers require owner action, commit no fake asset; record request IDs, provider research and the precise manual/authorization continuation point.
